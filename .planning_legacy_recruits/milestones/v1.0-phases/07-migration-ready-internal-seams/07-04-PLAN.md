---
phase: 07-migration-ready-internal-seams
plan: 04
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main/java/com/talhanation/recruits/compat/ReflectiveCompatAccess.java
  - src/main/java/com/talhanation/recruits/compat/SmallShips.java
  - src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java
  - src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java
  - src/test/java/com/talhanation/recruits/compat/ReflectiveCompatAccessTest.java
  - src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "Maintainer can change reflective compat lookup behavior through one helper instead of editing repeated `Class.forName` and `getMethod` calls inline."
    - "Maintainer can change async-path runtime startup and handoff behavior through one runtime seam instead of the raw executor code only."
    - "Representative safe-degradation guarantees from Phase 6 still hold after the seam extraction."
  artifacts:
    - path: "src/main/java/com/talhanation/recruits/compat/ReflectiveCompatAccess.java"
      provides: "Shared reflection seam for optional compat lookups"
      contains: "findClass"
    - path: "src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java"
      provides: "Shared runtime seam for async-path executor and handoff behavior"
      contains: "createExecutor"
    - path: "src/test/java/com/talhanation/recruits/compat/ReflectiveCompatAccessTest.java"
      provides: "Regression coverage for compat lookup degradation"
      contains: "missingLookupReturnsEmpty"
  key_links:
    - from: "src/main/java/com/talhanation/recruits/compat/SmallShips.java"
      to: "src/main/java/com/talhanation/recruits/compat/ReflectiveCompatAccess.java"
      via: "shared optional-mod reflection lookups"
      pattern: "ReflectiveCompatAccess"
    - from: "src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java"
      to: "src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java"
      via: "executor creation and delivery handoff delegation"
      pattern: "PathProcessingRuntime"
---

<objective>
Extract compat-reflection and async-path runtime seams so future migration work can adjust those risky areas through focused adapters.

Purpose: Advance MIG-02 while preserving the safe-degradation behavior established in Phase 6.
Output: A shared compat lookup helper, a shared path-processing runtime helper, and updated callers/tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-migration-ready-internal-seams/07-CONTEXT.md
@.planning/phases/07-migration-ready-internal-seams/07-SEAM-INVENTORY.md
@.planning/phases/06-full-surface-verification-and-safe-degradation/06-02-SUMMARY.md
@src/main/java/com/talhanation/recruits/compat/SmallShips.java
@src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java

<interfaces>
From src/main/java/com/talhanation/recruits/compat/SmallShips.java:
```java
static boolean isSmallShipEntity(@Nullable Object entity, ReflectiveClassResolver classResolver)
public float getShipSpeed()
public void setSailState(int state)
```

From src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java:
```java
static boolean shouldProcessSynchronously(@Nullable ThreadPoolExecutor executor)
static void deliverProcessedPath(@Nullable Path path, @Nullable Executor handoffExecutor, Consumer<@Nullable Path> afterProcessing)
public static void awaitProcessing(@Nullable Path path, @Nullable MinecraftServer server, Consumer<@Nullable Path> afterProcessing)
```

From src/main/java/com/talhanation/recruits/migration/CompatPathingSeams.java (created by 07-01):
```java
public interface ReflectiveLookup {
    Optional<Class<?>> findClass(String className);
}
public interface PathRuntime {
    ThreadPoolExecutor createExecutor(int workersCount);
    void deliver(@Nullable Path path, @Nullable Executor handoffExecutor, Consumer<@Nullable Path> afterProcessing);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock down compat lookup and path runtime seam behavior in JVM tests</name>
  <files>src/test/java/com/talhanation/recruits/compat/ReflectiveCompatAccessTest.java, src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java, src/main/java/com/talhanation/recruits/compat/ReflectiveCompatAccess.java, src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java</files>
  <behavior>
    - Test 1: missing compat classes, methods, or fields degrade to empty/false/no-op lookup results instead of throwing.
    - Test 2: compat lookup helpers can resolve present classes through injectable resolvers without hard-wiring `Class.forName` into every call site.
    - Test 3: path runtime helper preserves the existing direct-fallback and executor-handoff behavior already covered in Phase 6.
  </behavior>
  <action>First add failing JVM tests for a shared `ReflectiveCompatAccess` helper and the extracted `PathProcessingRuntime` helper. Then implement those helpers so they centralize lookup failure handling and async-path runtime behaviors already validated in Phase 6. Keep the helpers narrow and representative: do not widen scope into full optional-mod certification or a pathfinding redesign.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.compat.ReflectiveCompatAccessTest" --tests "com.talhanation.recruits.pathfinding.AsyncPathProcessorTest" --continue</automated>
  </verify>
  <done>Compat lookup and path runtime behavior are captured in JVM tests and implemented in shared helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire SmallShips and AsyncPathProcessor through the new helpers</name>
  <files>src/main/java/com/talhanation/recruits/compat/ReflectiveCompatAccess.java, src/main/java/com/talhanation/recruits/compat/SmallShips.java, src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java, src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java, src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java</files>
  <action>Update `SmallShips` to route repeated class/method/field discovery through `ReflectiveCompatAccess` instead of inlining raw reflection everywhere. Update `AsyncPathProcessor` to delegate executor creation and delivery handoff to `PathProcessingRuntime` while preserving the current queue-fallback and callback semantics established in 06-02. Keep all degraded behavior bounded and unchanged: missing compat classes should still collapse to safe fallbacks, and no new threading behavior should be introduced beyond the extracted seam.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.compat.ReflectiveCompatAccessTest" --tests "com.talhanation.recruits.pathfinding.AsyncPathProcessorTest" --tests "com.talhanation.recruits.compat.CompatSafeDegradationTest" --continue</automated>
  </verify>
  <done>Compat reflection and async-path runtime now flow through focused helpers, and the existing safe-degradation tests still pass.</done>
</task>

</tasks>

<verification>
1. Confirm missing compat lookups still degrade safely without uncaught reflection failures.
2. Confirm async-path fallback and callback handoff tests remain green after the helper extraction.
3. Confirm the new helpers stay representative and do not redesign compat support or pathfinding behavior.
</verification>

<success_criteria>
- Optional compat lookup behavior can be changed through one shared helper.
- Async-path runtime behavior can be changed through one shared helper.
- Phase 6 safe-degradation guarantees remain encoded in automated tests after the refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/07-migration-ready-internal-seams/07-04-SUMMARY.md`
</output>
