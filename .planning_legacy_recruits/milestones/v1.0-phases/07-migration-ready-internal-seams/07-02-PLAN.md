---
phase: 07-migration-ready-internal-seams
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/main/java/com/talhanation/recruits/Main.java
  - src/main/java/com/talhanation/recruits/network/RecruitsNetworkRegistrar.java
  - src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java
  - src/test/java/com/talhanation/recruits/network/RecruitsNetworkRegistrarTest.java
autonomous: true
requirements:
  - MIG-02
must_haves:
  truths:
    - "Maintainer can update packet registration order or common lifecycle bindings by editing dedicated helpers instead of a giant inline block in Main."
    - "The networking wire contract stays stable because packet ids and registration order remain unchanged."
    - "Main becomes a thinner bootstrap shell that delegates network and registration glue through explicit seams."
  artifacts:
    - path: "src/main/java/com/talhanation/recruits/network/RecruitsNetworkRegistrar.java"
      provides: "Ordered packet-registration seam outside Main"
      contains: "orderedMessageTypes"
    - path: "src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java"
      provides: "Lifecycle and deferred-register binding seam outside Main"
      contains: "registerCommon"
    - path: "src/test/java/com/talhanation/recruits/network/RecruitsNetworkRegistrarTest.java"
      provides: "Regression coverage for stable message ordering and ids"
      contains: "packetRegistrationOrderRemainsStable"
  key_links:
    - from: "src/main/java/com/talhanation/recruits/Main.java"
      to: "src/main/java/com/talhanation/recruits/network/RecruitsNetworkRegistrar.java"
      via: "channel creation and register-all delegation"
      pattern: "registerAll"
    - from: "src/main/java/com/talhanation/recruits/Main.java"
      to: "src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java"
      via: "constructor/setup lifecycle delegation"
      pattern: "ModLifecycleRegistrar"
---

<objective>
Extract the networking and registration-glue seams from `Main` so future version work can change bootstrap behavior through dedicated adapters.

Purpose: Advance MIG-02 by shrinking the highest-risk Forge bootstrap and packet-registration surface into narrow helpers while preserving the current runtime contract.
Output: A tested packet registrar, a lifecycle registrar, and a thinner `Main` bootstrap path.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-migration-ready-internal-seams/07-CONTEXT.md
@.planning/phases/07-migration-ready-internal-seams/07-SEAM-INVENTORY.md
@.planning/phases/04-command-and-ai-state-stabilization/04-command-and-ai-state-stabilization-01-SUMMARY.md
@src/main/java/com/talhanation/recruits/Main.java

<interfaces>
From src/main/java/com/talhanation/recruits/Main.java:
```java
public static final String MOD_ID = "recruits";
public static SimpleChannel SIMPLE_CHANNEL;

private void setup(final FMLCommonSetupEvent event) {
    SIMPLE_CHANNEL = CommonRegistry.registerChannel(Main.MOD_ID, "default");
    for (int i = 0; i < messages.length; i++) {
        CommonRegistry.registerMessage(SIMPLE_CHANNEL, i, messages[i]);
    }
}
```

From src/main/java/com/talhanation/recruits/migration/NetworkBootstrapSeams.java (created by 07-01):
```java
public record MessageRegistration(int id, Class<?> messageClass) {}
public interface ChannelRegistrar {
    void registerAll(SimpleChannel channel);
}
public interface LifecycleBinder {
    void registerCommon(IEventBus modEventBus);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock down packet registration order behind a dedicated registrar</name>
  <files>src/test/java/com/talhanation/recruits/network/RecruitsNetworkRegistrarTest.java, src/main/java/com/talhanation/recruits/network/RecruitsNetworkRegistrar.java</files>
  <behavior>
    - Test 1: `orderedMessageTypes()` returns the current packet classes in the exact existing wire order, with contiguous ids starting at 0.
    - Test 2: No duplicate packet classes or duplicate ids are emitted by the registrar.
    - Test 3: Sentinel positions for early and late packet classes stay stable so accidental reorderings fail fast.
  </behavior>
  <action>First add `RecruitsNetworkRegistrarTest` that captures the current packet order from `Main` as the wire-compat contract. Then implement `RecruitsNetworkRegistrar` to own the ordered message list and expose a `registerAll(SimpleChannel)` method that loops over those registrations. Preserve packet ids exactly; do not rename packet classes, change channel name, or alter side-specific behavior. This is a seam extraction only, not a networking redesign.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.network.RecruitsNetworkRegistrarTest" --continue</automated>
  </verify>
  <done>A dedicated registrar owns packet ordering and ids, and tests fail if the wire contract drifts.</done>
</task>

<task type="auto">
  <name>Task 2: Delegate Main bootstrap wiring to lifecycle helpers</name>
  <files>src/main/java/com/talhanation/recruits/Main.java, src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java, src/main/java/com/talhanation/recruits/network/RecruitsNetworkRegistrar.java</files>
  <action>Implement `ModLifecycleRegistrar` to own the common deferred-register bindings and Forge listener registrations currently assembled inline in `Main`, then rewire `Main` to delegate both lifecycle binding and packet registration through `ModLifecycleRegistrar` and `RecruitsNetworkRegistrar` per the 07-01 contracts. Keep all existing listeners, deferred registers, packet classes, mod flags, and outward behavior intact. Do not change channel naming, packet ids, creative-tab behavior, or client-only gating. The goal is to make bootstrap changes happen through smaller helpers, not to change how the mod starts.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.network.RecruitsNetworkRegistrarTest" --continue</automated>
  </verify>
  <done>`Main` delegates registration glue through focused helpers, and packet/lifecycle behavior remains stable under the captured tests.</done>
</task>

</tasks>

<verification>
1. Confirm packet order and ids are still exactly the same after the refactor.
2. Confirm `Main` no longer owns the giant inline packet array or listener/deferred-register wiring directly.
3. Confirm the bootstrap helpers remain narrow and do not absorb unrelated gameplay logic.
</verification>

<success_criteria>
- Networking and registration glue can be changed through dedicated helpers instead of deep edits inside `Main`.
- Existing packet compatibility is preserved by regression tests.
- `Main` becomes a smaller orchestration shell for future migration work.
</success_criteria>

<output>
After completion, create `.planning/phases/07-migration-ready-internal-seams/07-02-SUMMARY.md`
</output>
