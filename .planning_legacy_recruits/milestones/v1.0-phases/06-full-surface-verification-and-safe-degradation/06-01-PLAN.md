---
phase: 06-full-surface-verification-and-safe-degradation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VERIFICATION_MATRIX.md
  - BUILDING.md
autonomous: true
requirements:
  - TEST-04
  - STAB-01
must_haves:
  truths:
    - "Maintainer can open one repo document and see which subsystems have deep automated coverage versus smoke/manual coverage."
    - "The verification matrix points maintainers back to the canonical Gradle commands instead of inventing a new runner."
    - "Accepted external gaps are visible in the matrix so later failures are not mistaken for new regressions."
  artifacts:
    - path: "VERIFICATION_MATRIX.md"
      provides: "Full-surface coverage matrix with deep, smoke/manual, and deferred sections"
      contains: "Deep automated coverage"
    - path: "BUILDING.md"
      provides: "Canonical command entrypoint linked to the verification matrix"
      contains: "VERIFICATION_MATRIX.md"
  key_links:
    - from: "BUILDING.md"
      to: "VERIFICATION_MATRIX.md"
      via: "repo documentation link"
      pattern: "VERIFICATION_MATRIX"
    - from: "VERIFICATION_MATRIX.md"
      to: "./gradlew check --continue"
      via: "canonical verification commands"
      pattern: "check --continue"
---

<objective>
Create the explicit full-surface verification matrix that Phase 6 requires and anchor it to the repo's canonical build/verification workflow.

Purpose: Satisfy TEST-04 and the documentation half of STAB-01 by making current automation depth, smoke coverage, and accepted external gaps reviewable in one place.
Output: `VERIFICATION_MATRIX.md` plus a `BUILDING.md` link/update that points maintainers to the matrix during normal verification work.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-full-surface-verification-and-safe-degradation/06-CONTEXT.md
@BUILDING.md
@.planning/phases/05-persistence-and-multiplayer-sync-hardening/05-VERIFICATION.md
@.planning/phases/04-command-and-ai-state-stabilization/deferred-items.md
@src/test/java/com/talhanation/recruits
@src/gametest/java/com/talhanation/recruits
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the full-surface verification matrix</name>
  <files>VERIFICATION_MATRIX.md</files>
  <action>Create `VERIFICATION_MATRIX.md` as the canonical Phase 6 matrix document. Per the Phase 6 locked decisions, classify the mod surface by explicit coverage depth: deep automated coverage, baseline smoke/manual coverage, and accepted deferred gaps. Include at minimum battles, formations, commands, AI, persistence, networking, recruit spawn/runtime basics, menus/UI/world map, configs/update-check behavior, optional compat, mixins/pathfinding fallbacks, and build/test harness health. For each row, list the concrete evidence (specific JVM tests, GameTests, or repo docs), the command a maintainer runs to exercise it, and the current status. Preserve accepted external attribution by carrying forward the known Phase 3 battle failures from `04-command-and-ai-state-stabilization/deferred-items.md` instead of hiding them. Do not invent a new verification runner or claim automation where only smoke/manual checks exist; label those honestly per the Phase 6 boundary.</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('VERIFICATION_MATRIX.md').read_text(); assert 'Deep automated coverage' in t and 'Smoke/manual coverage' in t and 'Accepted deferred gaps' in t and './gradlew check --continue' in t"</automated>
  </verify>
  <done>`VERIFICATION_MATRIX.md` exists and gives maintainers a truthful subsystem-by-subsystem map of deep automation, baseline smoke/manual coverage, and accepted gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the matrix into the canonical maintainer workflow</name>
  <files>BUILDING.md</files>
  <action>Update `BUILDING.md` so the existing canonical workflow points maintainers to `VERIFICATION_MATRIX.md` for full-surface interpretation. Keep Phase 1's locked Gradle workflow intact: `./gradlew build` remains the clean-checkout build command and `./gradlew check --continue` remains the canonical verification entrypoint. Add only the minimal Phase 6 guidance needed to explain that the matrix defines deep-vs-smoke coverage and accepted-gap attribution; do not turn `BUILDING.md` into a second matrix or start a broader doc-system rewrite, which is deferred.</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('BUILDING.md').read_text(); assert 'VERIFICATION_MATRIX.md' in t and './gradlew check --continue' in t and './gradlew build' in t"</automated>
  </verify>
  <done>`BUILDING.md` still documents the canonical Gradle commands and now explicitly routes maintainers to the Phase 6 matrix for full-surface coverage interpretation.</done>
</task>

</tasks>

<verification>
1. Open `VERIFICATION_MATRIX.md` and confirm every major subsystem is present with a truthful deep/smoke/deferred classification.
2. Confirm the matrix cites existing JVM tests, GameTests, and accepted external failures rather than vague coverage claims.
3. Confirm `BUILDING.md` links maintainers to the matrix without changing the canonical `build` / `check --continue` workflow.
</verification>

<success_criteria>
- TEST-04 has a visible repo-level matrix document instead of implicit test-tree knowledge.
- Maintainers can distinguish high-depth automated areas from lighter smoke/manual areas at a glance.
- Accepted external gaps stay explicit and attributable before the final Phase 6 verification pass runs.
</success_criteria>

<output>
After completion, create `.planning/phases/06-full-surface-verification-and-safe-degradation/06-01-SUMMARY.md`
</output>
