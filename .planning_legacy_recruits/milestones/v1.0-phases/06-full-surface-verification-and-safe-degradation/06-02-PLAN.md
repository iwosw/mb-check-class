---
phase: 06-full-surface-verification-and-safe-degradation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/test/java/com/talhanation/recruits/compat/CompatSafeDegradationTest.java
  - src/main/java/com/talhanation/recruits/compat/SmallShips.java
  - src/main/java/com/talhanation/recruits/compat/MusketWeapon.java
  - src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java
  - src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java
autonomous: true
requirements:
  - STAB-03
must_haves:
  truths:
    - "Optional compatibility seams do not throw or destabilize core logic when their dependent mod classes are absent."
    - "Async pathfinding fallback stays bounded and verifiable when the processor is missing, shut down, or forced onto its degradation path."
    - "Maintainers can run focused automated tests that prove safe-degradation behavior instead of relying on code inspection alone."
  artifacts:
    - path: "src/test/java/com/talhanation/recruits/compat/CompatSafeDegradationTest.java"
      provides: "Pure JVM regression coverage for optional compat absence behavior"
      contains: "CompatSafeDegradationTest"
    - path: "src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java"
      provides: "Pure JVM regression coverage for pathfinding fallback behavior"
      contains: "AsyncPathProcessorTest"
    - path: "src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java"
      provides: "Bounded, testable async-path degradation contract"
      contains: "awaitProcessing"
  key_links:
    - from: "src/test/java/com/talhanation/recruits/compat/CompatSafeDegradationTest.java"
      to: "src/main/java/com/talhanation/recruits/compat/SmallShips.java"
      via: "missing-dependency safe-fallback assertions"
      pattern: "SmallShips"
    - from: "src/test/java/com/talhanation/recruits/compat/CompatSafeDegradationTest.java"
      to: "src/main/java/com/talhanation/recruits/compat/MusketWeapon.java"
      via: "reflection failure safe-fallback assertions"
      pattern: "MusketWeapon"
    - from: "src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java"
      to: "src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java"
      via: "null/shutdown/fallback path assertions"
      pattern: "queue|awaitProcessing|shutdown"
---

<objective>
Add representative automated safe-degradation coverage for the highest-risk optional and fallback surfaces Phase 6 explicitly calls out.

Purpose: Satisfy STAB-03 by proving optional compat and async-path fallback behavior stay bounded when dependencies or runtime conditions are missing.
Output: Focused JVM safe-degradation tests plus any minimal production hardening needed in the compat and pathfinding seams they exercise.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-full-surface-verification-and-safe-degradation/06-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/main/java/com/talhanation/recruits/compat/SmallShips.java
@src/main/java/com/talhanation/recruits/compat/MusketWeapon.java
@src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java

<interfaces>
From `src/main/java/com/talhanation/recruits/compat/SmallShips.java`:
```java
public static boolean isSmallShip(Entity entity)
public float getShipSpeed()
public void setSailState(int state)
```

From `src/main/java/com/talhanation/recruits/compat/MusketWeapon.java`:
```java
@Nullable public Item getWeapon()
public boolean isLoaded(ItemStack stack)
public void setLoaded(ItemStack stack, boolean loaded)
public AbstractHurtingProjectile getProjectile(LivingEntity shooter)
@Nullable public SoundEvent getShootSound()
@Nullable public SoundEvent getLoadSound()
```

From `src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java`:
```java
public static void start()
public static void shutdown()
protected static void queue(@NotNull AsyncPath path)
public static void awaitProcessing(@Nullable Path path, MinecraftServer server, Consumer<@Nullable Path> afterProcessing)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock down optional compat absence behavior</name>
  <files>src/test/java/com/talhanation/recruits/compat/CompatSafeDegradationTest.java, src/main/java/com/talhanation/recruits/compat/SmallShips.java, src/main/java/com/talhanation/recruits/compat/MusketWeapon.java</files>
  <behavior>
    - Test 1: `SmallShips.isSmallShip(null)` and a non-SmallShips vanilla entity path return `false` instead of throwing when the optional mod classes are absent.
    - Test 2: `MusketWeapon` reflective lookups (`getWeapon`, `isLoaded`, `setLoaded`, projectile/sound accessors) degrade to `null`/`false`/no-throw behavior when Musket Mod classes are absent.
  </behavior>
  <action>Write the pure JVM absence tests first. Then make only the minimal production changes needed so the representative optional compat seams behave safely under missing-class conditions per the Phase 6 locked scope. Keep this representative rather than expansive: prove safe absence/failure handling for the Small Ships and musket reflection paths, not broad compatibility certification. Preserve current outward behavior when the optional dependencies are actually present, and do not add new compat integrations or feature polish because those are deferred by context.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.compat.CompatSafeDegradationTest"</automated>
  </verify>
  <done>Focused JVM tests prove the representative optional compat seams return safe fallback values or no-op cleanly when dependent mod classes are missing, without destabilizing the core mod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock down bounded async-path fallback behavior</name>
  <files>src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java, src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java</files>
  <behavior>
    - Test 1: When the processor is absent or shut down, queueing and post-processing fall back predictably instead of throwing or hanging.
    - Test 2: The degradation path used when async processing cannot proceed is observable and bounded enough to verify in a pure JVM test.
  </behavior>
  <action>Write the JVM tests first, then extract the smallest package-private seam or helper needed to make `AsyncPathProcessor` fallback behavior deterministic under test. Keep the existing runtime contract centered on safe completion, not a broad pathfinding rewrite: null/shutdown/rejection paths should stay bounded, avoid uncaught exceptions, and remain compatible with `awaitProcessing`'s server-thread handoff. If the current rejection path needs a tiny diagnostic or helper to be testable, add it here; do not expand into pathfinding optimization or async architecture cleanup, which is deferred outside this phase.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.pathfinding.AsyncPathProcessorTest"</automated>
  </verify>
  <done>`AsyncPathProcessor` has pure JVM coverage for its degradation paths, and the tested fallback behavior stays bounded and safe when async processing cannot proceed normally.</done>
</task>

</tasks>

<verification>
1. Run the compat safe-degradation JVM suite and confirm missing optional dependencies no longer throw through the representative reflection seams.
2. Run the async-path JVM suite and confirm null/shutdown/degradation paths complete predictably.
3. Confirm the work stays within representative safe-absence verification and does not widen into broad compat expansion or pathfinding redesign.
</verification>

<success_criteria>
- STAB-03 has explicit automated evidence for representative optional compat and fallback-risk surfaces.
- Missing optional mod classes are handled as safe degradation, not test-only assumptions.
- Async pathfinding fallback behavior is explicit enough to verify and bounded enough not to destabilize core flows.
</success_criteria>

<output>
After completion, create `.planning/phases/06-full-surface-verification-and-safe-degradation/06-02-SUMMARY.md`
</output>
