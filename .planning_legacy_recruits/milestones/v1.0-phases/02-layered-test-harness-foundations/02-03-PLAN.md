---
phase: 02-layered-test-harness-foundations
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsGameTestSupport.java
  - src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsEntityAsserts.java
  - src/gametest/java/com/talhanation/recruits/gametest/recruit/RecruitSpawnGameTests.java
  - src/gametest/resources/data/recruits/structures/recruit_spawn_pad.nbt
autonomous: true
requirements:
  - TEST-02
  - TEST-03
must_haves:
  truths:
    - "Maintainer can add recruit-focused runtime tests by reusing shared GameTest spawn/assert helpers instead of rewriting setup."
    - "The GameTest harness includes at least one gameplay-facing recruit scenario, not only an empty smoke test."
    - "Later battle and command phases have a reusable runtime fixture they can build on for recruit entity scenarios."
  artifacts:
    - path: "src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsGameTestSupport.java"
      provides: "Reusable recruit spawning and world setup helpers"
      contains: "ModEntityTypes.RECRUIT"
    - path: "src/gametest/java/com/talhanation/recruits/gametest/recruit/RecruitSpawnGameTests.java"
      provides: "Reference recruit runtime tests"
      contains: "@GameTest"
    - path: "src/gametest/resources/data/recruits/structures/recruit_spawn_pad.nbt"
      provides: "Dedicated recruit spawn template"
      contains: "recruit_spawn_pad"
  key_links:
    - from: "src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsGameTestSupport.java"
      to: "src/gametest/java/com/talhanation/recruits/gametest/recruit/RecruitSpawnGameTests.java"
      via: "shared recruit spawn helper"
      pattern: "RecruitsGameTestSupport"
    - from: "src/gametest/java/com/talhanation/recruits/gametest/recruit/RecruitSpawnGameTests.java"
      to: "src/gametest/resources/data/recruits/structures/recruit_spawn_pad.nbt"
      via: "@GameTest template"
      pattern: "recruit_spawn_pad"
---

<objective>
Add reusable runtime helpers for recruit-focused GameTests and prove them with the first gameplay-facing recruit scenario.

Purpose: Complete the layered harness by giving later stabilization phases a practical runtime fixture for recruit entity tests.
Output: Shared GameTest recruit helpers, recruit spawn assertions, and a passing recruit runtime test class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-layered-test-harness-foundations/02-CONTEXT.md
@build.gradle
@src/main/java/com/talhanation/recruits/Main.java
@src/main/java/com/talhanation/recruits/init/ModEntityTypes.java
@src/main/java/com/talhanation/recruits/entities/RecruitEntity.java

<interfaces>
From `src/main/java/com/talhanation/recruits/Main.java`:
```java
public static final String MOD_ID = "recruits";
```

From `src/main/java/com/talhanation/recruits/init/ModEntityTypes.java`:
```java
public static final RegistryObject<EntityType<RecruitEntity>> RECRUIT;
```

From `src/main/java/com/talhanation/recruits/entities/RecruitEntity.java`:
```java
public void initSpawn();
public static AttributeSupplier.Builder setAttributes();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable recruit GameTest support helpers</name>
  <files>src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsGameTestSupport.java, src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsEntityAsserts.java</files>
  <action>Create shared runtime helpers for later recruit-centric tests. `RecruitsGameTestSupport` should centralize common template-relative spawn positions, recruit creation through `ModEntityTypes.RECRUIT.get()`, safe insertion into the `ServerLevel`, and any one-time initialization needed so later battle/command plans can spawn baseline recruits without rebuilding that plumbing. `RecruitsEntityAsserts` should wrap the repeated runtime checks later tests will need: entity present at expected position, entity alive after scheduled ticks, and recruit custom name/state checks after initialization. Keep these helpers in the GameTest tree only; they are test harness code, not gameplay code.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The GameTest tree has reusable recruit spawn/assert helpers that later plans can import directly.</done>
</task>

<task type="auto">
  <name>Task 2: Add the first recruit runtime scenario and dedicated template</name>
  <files>src/gametest/java/com/talhanation/recruits/gametest/recruit/RecruitSpawnGameTests.java, src/gametest/resources/data/recruits/structures/recruit_spawn_pad.nbt</files>
  <action>Create `RecruitSpawnGameTests` as the reference gameplay-facing GameTest class using `@GameTestHolder(Main.MOD_ID)`. Add a simple but real recruit contract on a dedicated `recruit_spawn_pad` structure: spawn a recruit via the shared helper, initialize it, assert the entity is present/alive after the first few ticks, and assert its default custom name resolves to `Recruit` so the test exercises actual recruit initialization instead of only world boot. Add a second small case that spawns two recruits on distinct positions from the same helper to prove the fixture supports multi-entity setup for later battle/formation work. Keep both tests required and deterministic.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The runtime harness now includes reusable recruit helpers plus a passing recruit GameTest class that later gameplay phases can extend.</done>
</task>

</tasks>

<verification>
Run `./gradlew verifyGameTestStage` and confirm both the smoke GameTest from Plan 01 and the recruit runtime cases execute successfully through the same harness.
</verification>

<success_criteria>
- The GameTest harness has reusable recruit spawn/assert helpers.
- At least one gameplay-facing recruit scenario runs through `verifyGameTestStage`.
- Later phases can build recruit/battle/command runtime tests by extending shared helpers instead of rebuilding entity setup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layered-test-harness-foundations/02-03-SUMMARY.md`
</output>
