---
phase: 02-layered-test-harness-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/test/java/com/talhanation/recruits/testsupport/RecruitsFixtures.java
  - src/test/java/com/talhanation/recruits/testsupport/NbtRoundTripAssertions.java
  - src/test/java/com/talhanation/recruits/testsupport/MessageCodecAssertions.java
  - src/test/java/com/talhanation/recruits/world/RecruitsFactionSerializationTest.java
  - src/test/java/com/talhanation/recruits/world/RecruitsClaimSerializationTest.java
  - src/test/java/com/talhanation/recruits/network/MessageMovementCodecTest.java
autonomous: true
requirements:
  - TEST-01
  - TEST-03
must_haves:
  truths:
    - "Maintainer can add new pure JVM tests without booting a Forge runtime."
    - "Faction and claim persistence round trips are backed by reusable fixture builders instead of ad hoc setup in each test."
    - "Network message codec tests can be written against a shared buffer helper rather than hand-rolling byte-buffer setup per test."
  artifacts:
    - path: "src/test/java/com/talhanation/recruits/testsupport/RecruitsFixtures.java"
      provides: "Reusable JVM-side faction, claim, and message fixtures"
      contains: "class RecruitsFixtures"
    - path: "src/test/java/com/talhanation/recruits/testsupport/NbtRoundTripAssertions.java"
      provides: "NBT round-trip helper"
      contains: "assert"
    - path: "src/test/java/com/talhanation/recruits/network/MessageMovementCodecTest.java"
      provides: "Reference packet codec test"
      contains: "MessageMovement"
  key_links:
    - from: "src/test/java/com/talhanation/recruits/testsupport/RecruitsFixtures.java"
      to: "src/test/java/com/talhanation/recruits/world/RecruitsClaimSerializationTest.java"
      via: "shared claim/faction fixture builder"
      pattern: "RecruitsFixtures"
    - from: "src/test/java/com/talhanation/recruits/testsupport/MessageCodecAssertions.java"
      to: "src/test/java/com/talhanation/recruits/network/MessageMovementCodecTest.java"
      via: "FriendlyByteBuf round-trip helper"
      pattern: "MessageCodecAssertions"
---

<objective>
Give pure JVM tests a reusable helper layer so later stabilization phases can encode persistence and packet contracts without booting Minecraft.

Purpose: Satisfy the fast-feedback half of the split harness by making unit-test authoring cheap and consistent.
Output: Shared JVM test-support classes plus representative persistence and codec tests.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-layered-test-harness-foundations/02-CONTEXT.md
@build.gradle
@src/main/java/com/talhanation/recruits/world/RecruitsFaction.java
@src/main/java/com/talhanation/recruits/world/RecruitsClaim.java
@src/main/java/com/talhanation/recruits/world/RecruitsPlayerInfo.java
@src/main/java/com/talhanation/recruits/network/MessageMovement.java

<interfaces>
From `src/main/java/com/talhanation/recruits/world/RecruitsFaction.java`:
```java
public CompoundTag toNBT();
public static RecruitsFaction fromNBT(CompoundTag nbt);
```

From `src/main/java/com/talhanation/recruits/world/RecruitsClaim.java`:
```java
public CompoundTag toNBT();
public static RecruitsClaim fromNBT(CompoundTag nbt);
public void addChunk(ChunkPos chunkPos);
public void setCenter(ChunkPos center);
```

From `src/main/java/com/talhanation/recruits/network/MessageMovement.java`:
```java
public MessageMovement(UUID player_uuid, int state, UUID group, int formation, boolean tight);
public MessageMovement fromBytes(FriendlyByteBuf buf);
public void toBytes(FriendlyByteBuf buf);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create reusable JVM fixture and assertion helpers</name>
  <files>src/test/java/com/talhanation/recruits/testsupport/RecruitsFixtures.java, src/test/java/com/talhanation/recruits/testsupport/NbtRoundTripAssertions.java, src/test/java/com/talhanation/recruits/testsupport/MessageCodecAssertions.java</files>
  <behavior>
    - Test helper callers can create a consistent sample `RecruitsFaction`, `RecruitsClaim`, and `MessageMovement` without duplicating setup.
    - NBT round-trip assertions prove `toNBT` -> `fromNBT` returns the important observable fields intact.
    - Codec round-trip assertions prove `toBytes` -> `fromBytes` preserves packet payload values through `FriendlyByteBuf`.
  </behavior>
  <action>Create a pure-JVM support layer under `src/test/java/com/talhanation/recruits/testsupport` per the Phase 2 discretion to keep the harness small but durable. `RecruitsFixtures` should centralize canonical sample UUIDs, faction/player data, claim chunk/center setup, and a representative `MessageMovement` instance. `NbtRoundTripAssertions` should wrap serialize/deserialize assertions for domain objects using `CompoundTag` without starting Forge runtime code. `MessageCodecAssertions` should wrap `FriendlyByteBuf` / Netty buffer setup so packet tests stay one line instead of hand-rolled buffer plumbing. Keep these helpers test-only; do not move them into `src/main/java`.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.world.RecruitsFactionSerializationTest" --tests "com.talhanation.recruits.world.RecruitsClaimSerializationTest" --tests "com.talhanation.recruits.network.MessageMovementCodecTest"</automated>
  </verify>
  <done>The repo has reusable JVM helpers for faction, persistence, and networking tests, and they stay executable from the normal `test` task.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the helpers with representative persistence and codec tests</name>
  <files>src/test/java/com/talhanation/recruits/world/RecruitsFactionSerializationTest.java, src/test/java/com/talhanation/recruits/world/RecruitsClaimSerializationTest.java, src/test/java/com/talhanation/recruits/network/MessageMovementCodecTest.java</files>
  <behavior>
    - `RecruitsFactionSerializationTest` proves display name, leader UUID, join requests, and members survive an NBT round trip.
    - `RecruitsClaimSerializationTest` proves owner faction, claimed chunks, center, health, siege speed, and admin/siege flags survive an NBT round trip.
    - `MessageMovementCodecTest` proves player UUID, state, group UUID, formation, and tight-formation flag survive a codec round trip.
  </behavior>
  <action>Add three focused reference tests that use the new helper layer instead of bespoke setup. These are the templates future persistence and packet tests should copy. Keep them pure JVM tests per TEST-01: no Minecraft runtime boot, no `gameTestServer`, and no dependency on client-only state like `ClientManager`.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.world.RecruitsFactionSerializationTest" --tests "com.talhanation.recruits.world.RecruitsClaimSerializationTest" --tests "com.talhanation.recruits.network.MessageMovementCodecTest"</automated>
  </verify>
  <done>Maintainers now have copyable examples for persistence and packet coverage that run entirely through `./gradlew test`.</done>
</task>

</tasks>

<verification>
Run the targeted `./gradlew test --tests ...` command from each task, then optionally `./gradlew test` to confirm the helpers integrate cleanly with the full JVM suite.
</verification>

<success_criteria>
- New persistence and packet tests run under `./gradlew test` without any Forge runtime boot.
- Shared fixture helpers remove repeated setup for faction, claim, and message scenarios.
- Later phases can extend the JVM harness by copying these helper/test patterns instead of rediscovering buffer or NBT setup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layered-test-harness-foundations/02-02-SUMMARY.md`
</output>
