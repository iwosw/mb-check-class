---
phase: 02-layered-test-harness-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - BUILDING.md
  - src/gametest/java/com/talhanation/recruits/gametest/HarnessSmokeGameTests.java
  - src/gametest/resources/data/recruits/structures/harness_empty.nbt
autonomous: true
requirements:
  - TEST-01
  - TEST-02
must_haves:
  truths:
    - "Maintainer can add GameTests in one standard source tree instead of relying on a future placeholder stage."
    - "`./gradlew check --continue` still exposes GameTests as a distinct verification layer per the Phase 1 contract."
    - "A first Forge GameTest is discoverable and runnable through the canonical Gradle workflow."
  artifacts:
    - path: "build.gradle"
      provides: "Dedicated GameTest source-set and run wiring"
      contains: "src/gametest/java"
    - path: "src/gametest/java/com/talhanation/recruits/gametest/HarnessSmokeGameTests.java"
      provides: "Smoke GameTest baseline"
      contains: "@GameTestHolder"
    - path: "src/gametest/resources/data/recruits/structures/harness_empty.nbt"
      provides: "Reusable empty test template"
      contains: "harness_empty"
  key_links:
    - from: "build.gradle"
      to: "src/gametest/java/com/talhanation/recruits/gametest/HarnessSmokeGameTests.java"
      via: "gametest source-set added to gameTestServer run"
      pattern: "sourceSets\\.(gameTest|gametest)|runGameTestServer"
    - from: "src/gametest/java/com/talhanation/recruits/gametest/HarnessSmokeGameTests.java"
      to: "src/gametest/resources/data/recruits/structures/harness_empty.nbt"
      via: "@GameTest template"
      pattern: "harness_empty"
---

<objective>
Make Forge GameTests a real, standard part of the repo by wiring a dedicated source tree and shipping a runnable smoke baseline.

Purpose: Replace the Phase 1 `NO-SOURCE` placeholder with a durable runtime harness while keeping `check` as the canonical verification entrypoint.
Output: Dedicated `src/gametest` layout, updated build/docs wiring, and one passing smoke GameTest.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-layered-test-harness-foundations/02-CONTEXT.md
@.planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
@.planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
@build.gradle
@BUILDING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize the split JVM/GameTest source layout</name>
  <files>build.gradle, BUILDING.md</files>
  <action>Update `build.gradle` so Forge GameTests live in one exact location: `src/gametest/java` plus `src/gametest/resources`. Keep `./gradlew check` as the top-level verification entrypoint per Phase 1 D-02/D-03, but replace the loose source detection with explicit wiring for this source tree. Make the GameTest compile/runtime classpath inherit from `main`, add the GameTest source output to the `gameTestServer` run's `mods { recruits { ... } }` block, and keep the named `verifyGameTestStage` task under `check` rather than inventing a new wrapper workflow. Update `BUILDING.md` to document the exact unit-test path (`src/test/java`), GameTest path (`src/gametest/java` and `src/gametest/resources`), and the rule that later gameplay contracts belong in the GameTest tree while pure logic stays JVM-only.</action>
  <verify>
    <automated>./gradlew tasks --all</automated>
  </verify>
  <done>`build.gradle` and `BUILDING.md` define one durable split harness layout, and the canonical verification contract from Phase 1 remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Add a smoke GameTest and reusable empty template</name>
  <files>src/gametest/java/com/talhanation/recruits/gametest/HarnessSmokeGameTests.java, src/gametest/resources/data/recruits/structures/harness_empty.nbt</files>
  <action>Create `HarnessSmokeGameTests` as the first required Forge GameTest using `@GameTestHolder(Main.MOD_ID)` so discovery stays namespaced to `recruits`. Use a generic empty template named `harness_empty` that later phases can reuse instead of creating one-off bootstrap scenes. Keep the smoke case intentionally tiny: it should prove the `gameTestServer` path finds the class, loads the template, and reaches success without touching gameplay logic yet. Do not reintroduce the Phase 1 `NO-SOURCE` placeholder once this baseline exists.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>`verifyGameTestStage` now runs a real Forge GameTest from the standardized source tree and exits successfully.</done>
</task>

</tasks>

<verification>
Run `./gradlew tasks --all` to confirm the GameTest stage remains visible, then run `./gradlew verifyGameTestStage` and confirm the smoke GameTest executes instead of reporting `NO-SOURCE`.
</verification>

<success_criteria>
- `src/gametest/java` and `src/gametest/resources` are the documented standard GameTest locations.
- `verifyGameTestStage` executes at least one passing Forge GameTest.
- The Phase 1 `check` / build / unit / game-test stage split remains visible and canonical.
</success_criteria>

<output>
After completion, create `.planning/phases/02-layered-test-harness-foundations/02-01-SUMMARY.md`
</output>
