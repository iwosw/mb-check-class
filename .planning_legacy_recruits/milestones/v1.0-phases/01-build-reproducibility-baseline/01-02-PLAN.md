---
phase: 01-build-reproducibility-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - build.gradle
  - src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java
autonomous: true
requirements:
  - BLD-02
  - BLD-04
must_haves:
  truths:
    - "Maintainer can run a named unit-test stage from Gradle instead of seeing `test NO-SOURCE`."
    - "Maintainer can run a named game-test stage from Gradle, even if the stage is still an empty baseline in Phase 1."
    - "Standard verification output makes it obvious whether build, unit-test, or game-test work failed."
  artifacts:
    - path: "build.gradle"
      provides: "Verification stage wiring and game test entrypoint"
      contains: "gameTestServer"
    - path: "build.gradle"
      provides: "Named verification stages"
      contains: "verifyBuildStage"
    - path: "src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java"
      provides: "Minimal unit-test baseline"
      contains: "BuildBaselineTest"
  key_links:
    - from: "build.gradle"
      to: "src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java"
      via: "test task"
      pattern: "useJUnitPlatform|junit-jupiter"
    - from: "build.gradle"
      to: "runGameTestServer"
      via: "gameTest lifecycle alias"
      pattern: "runGameTestServer"
---

<objective>
Wire `check` into explicit build, unit-test, and game-test stages so failures are attributable and future harness work has a stable entrypoint.

Purpose: Make verification output actionable before deeper test infrastructure arrives in Phase 2.
Output: Real unit-test baseline, dedicated game-test stage, and named verification tasks under `check`.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
@.planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
@build.gradle
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a real unit-test baseline</name>
  <read_first>
    - build.gradle
    - gradle.properties
    - src/main/resources/META-INF/mods.toml
    - .planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
  </read_first>
  <files>build.gradle, src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java</files>
  <behavior>
    - Test 1: `BuildBaselineTest` can run from `./gradlew test` without booting Minecraft runtime.
    - Test 2: The smoke test proves build metadata is readable by asserting `mods.toml` contains the `recruits` mod id.
  </behavior>
  <action>Add a minimal JUnit 5 baseline per D-02 and D-03. In `build.gradle`, add `testImplementation platform('org.junit:junit-bom:5.10.2')`, `testImplementation 'org.junit.jupiter:junit-jupiter'`, and configure `test { useJUnitPlatform() }`. Create `src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java` as a pure JVM smoke test that reads `src/main/resources/META-INF/mods.toml` and asserts the file contains the `recruits` mod id. Keep this test free of Forge runtime bootstrapping so it stays Phase-1-lightweight.</action>
  <acceptance_criteria>
    - `build.gradle` contains `junit-bom:5.10.2`
    - `build.gradle` contains `useJUnitPlatform()`
    - `src/test/java/com/talhanation/recruits/build/BuildBaselineTest.java` exists
    - `BuildBaselineTest.java` contains `mods.toml`
    - `./gradlew test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>`test` is now a real Gradle verification stage with one reproducible smoke assertion instead of `NO-SOURCE`.</done>
</task>

<task type="auto">
  <name>Task 2: Expose build, unit-test, and game-test stages through `check`</name>
  <read_first>
    - build.gradle
    - .planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
    - .planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
  </read_first>
  <files>build.gradle</files>
  <action>In `build.gradle`, add a `gameTestServer` run configuration per Forge 1.20.x docs with `workingDirectory project.file('run')`, `forge.logging.markers=REGISTRIES`, `forge.logging.console.level=debug`, and `forge.enabledGameTestNamespaces='recruits'`. Register lifecycle helper tasks named exactly `verifyBuildStage`, `verifyUnitTestStage`, and `verifyGameTestStage`. Wire them so `verifyBuildStage` depends on `assemble`, `verifyUnitTestStage` depends on `test`, and `verifyGameTestStage` uses `runGameTestServer` when a future GameTest source tree exists, otherwise logs `NO-SOURCE` with an explicit Phase 2 note and succeeds. Make `check` depend on all three helper tasks so the lifecycle entrypoint stays `check` per D-02, and ensure the documented standard invocation is `./gradlew check --continue` so later stages still run when practical per D-04.</action>
  <acceptance_criteria>
    - `build.gradle` contains `gameTestServer`
    - `build.gradle` contains `verifyBuildStage`
    - `build.gradle` contains `verifyUnitTestStage`
    - `build.gradle` contains `verifyGameTestStage`
    - `build.gradle` contains `runGameTestServer`
    - `./gradlew tasks --all` lists `verifyBuildStage`
    - `./gradlew tasks --all` lists `verifyGameTestStage`
    - `./gradlew check --continue` exits 0 and emits the named stage tasks in output
  </acceptance_criteria>
  <verify>
    <automated>./gradlew check --continue</automated>
  </verify>
  <done>The canonical verification path now surfaces build, unit-test, and game-test stages distinctly under Gradle lifecycle commands.</done>
</task>

</tasks>

<verification>
Run `./gradlew tasks --all` to confirm the new stage tasks exist, then run `./gradlew check --continue` and confirm the output includes the build, unit-test, and game-test stage names.
</verification>

<success_criteria>
- `test` is a real stage with at least one automated assertion.
- `check` now exposes build/unit/game-test layers with distinct task names.
- Maintainers can tell which verification layer failed from standard Gradle output.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-reproducibility-baseline/01-02-SUMMARY.md`
</output>
