---
phase: 01-build-reproducibility-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - README.md
  - BUILDING.md
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03
  - BLD-04
must_haves:
  truths:
    - "A maintainer can open the repo and see the canonical clean-checkout build and verification commands immediately."
    - "A maintainer can tell when `allowLocalMaven` is allowed and when it is not part of the canonical path."
    - "A maintainer can interpret build vs unit-test vs game-test failures from the documented workflow."
  artifacts:
    - path: "README.md"
      provides: "Quick-start maintainer commands"
      contains: "./gradlew build"
    - path: "BUILDING.md"
      provides: "Dedicated reproducibility workflow doc"
      contains: "allowLocalMaven"
  key_links:
    - from: "README.md"
      to: "BUILDING.md"
      via: "cross-link to detailed build workflow"
      pattern: "BUILDING\.md"
---

<objective>
Document the canonical maintainer workflow so Phase 1's reproducible commands are discoverable and interpretable from a clean checkout.

Purpose: Turn the hardened Gradle setup into a maintainer-facing contract instead of tribal knowledge.
Output: README quick-start plus a dedicated build-focused workflow document.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
@.planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the detailed build and verification contract</name>
  <read_first>
    - README.md
    - build.gradle
    - gradle.properties
    - .planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
  </read_first>
  <files>BUILDING.md</files>
  <action>Create a root-level `BUILDING.md` per D-08. It must document: host prerequisites, the canonical clean-checkout build command `./gradlew build` per D-01, the canonical verification lifecycle entrypoint `check` with the practical invocation `./gradlew check --continue` per D-02 and D-04, the meaning of the named build/unit-test/game-test stages, and the explicit non-canonical local override path `-PallowLocalMaven=true` per D-06. Include a short troubleshooting table that maps each verification stage to the most likely next debugging step.</action>
  <acceptance_criteria>
    - `BUILDING.md` exists
    - `BUILDING.md` contains `./gradlew build`
    - `BUILDING.md` contains `./gradlew check --continue`
    - `BUILDING.md` contains `allowLocalMaven`
    - `BUILDING.md` contains `game test`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "./gradlew build" BUILDING.md && grep -q "./gradlew check --continue" BUILDING.md && grep -q "allowLocalMaven" BUILDING.md</automated>
  </verify>
  <done>The repo has a dedicated maintainer document for reproducible build and verification behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add a README quick-start that points to the detailed contract</name>
  <read_first>
    - README.md
    - BUILDING.md
    - .planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
  </read_first>
  <files>README.md, BUILDING.md</files>
  <action>Update `README.md` so a maintainer landing on the repo immediately sees the canonical commands and where to read more. Add a short “Build and verify” section that includes `./gradlew build`, `./gradlew check --continue`, and a direct link to `BUILDING.md`. Keep the README summary compact; place the longer explanation and troubleshooting details only in `BUILDING.md`.</action>
  <acceptance_criteria>
    - `README.md` contains `Build and verify`
    - `README.md` contains `./gradlew build`
    - `README.md` contains `./gradlew check --continue`
    - `README.md` contains `BUILDING.md`
  </acceptance_criteria>
  <verify>
    <automated>grep -q "Build and verify" README.md && grep -q "./gradlew build" README.md && grep -q "BUILDING.md" README.md</automated>
  </verify>
  <done>The README exposes the canonical commands up front and routes maintainers to the full reproducibility guide.</done>
</task>

</tasks>

<verification>
Read `README.md` and `BUILDING.md` together and confirm the commands, override path, and failure interpretation language all align with the Phase 1 Gradle tasks.
</verification>

<success_criteria>
- Maintainers can discover the canonical build and verify commands from the repo root.
- The dedicated build doc explains stage-specific failures and the local override path.
- Documentation matches the actual Gradle task names landed in Plans 01 and 02.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-reproducibility-baseline/01-03-SUMMARY.md`
</output>
