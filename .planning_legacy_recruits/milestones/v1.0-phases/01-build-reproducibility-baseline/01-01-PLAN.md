---
phase: 01-build-reproducibility-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - settings.gradle
  - gradle.properties
autonomous: true
requirements:
  - BLD-01
  - BLD-03
must_haves:
  truths:
    - "A clean checkout resolves pinned Gradle plugins and repositories instead of floating or local-machine inputs."
    - "The canonical build path does not silently consume `mavenLocal()` artifacts."
  artifacts:
    - path: "build.gradle"
      provides: "Pinned plugin and repository resolution"
      contains: "6.0.52"
    - path: "build.gradle"
      provides: "Pinned MixinGradle version"
      contains: "0.7.38"
    - path: "build.gradle"
      provides: "Opt-in local Maven override only"
      contains: "allowLocalMaven"
  key_links:
    - from: "build.gradle"
      to: "settings.gradle"
      via: "stable wrapper/toolchain/plugin alignment"
      pattern: "6\\.0\\.52|0\\.7\\.38"
---

<objective>
Lock the build graph onto stable, reproducible inputs before adding more verification behavior.

Purpose: Remove drifting plugin and repository inputs from the canonical Phase 1 path.
Output: Pinned Gradle/plugin resolution and an explicit opt-in local override path.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
@.planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
@build.gradle
@settings.gradle
@gradle.properties
@gradle/wrapper/gradle-wrapper.properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin canonical Gradle and plugin resolution</name>
  <read_first>
    - build.gradle
    - settings.gradle
    - gradle.properties
    - gradle/wrapper/gradle-wrapper.properties
    - .planning/phases/01-build-reproducibility-baseline/01-CONTEXT.md
    - .planning/phases/01-build-reproducibility-baseline/01-RESEARCH.md
  </read_first>
  <files>build.gradle, settings.gradle, gradle.properties</files>
  <action>Update the canonical build path per D-05, D-06, and D-07. In `build.gradle`, replace `net.minecraftforge.gradle:ForgeGradle:6.+` with `6.0.52` and replace `org.spongepowered:mixingradle:0.7-SNAPSHOT` with `0.7.38`. Keep Gradle wrapper `8.8` and the existing Foojay toolchain resolver unchanged. Remove unconditional `mavenLocal()` from the default repositories and only add it when a Gradle property named `allowLocalMaven` is present, with an inline comment that this is a non-canonical local override. Preserve the canonical lifecycle commands as `./gradlew build` and `./gradlew check`; do not introduce a new top-level wrapper task.</action>
  <acceptance_criteria>
    - `build.gradle` contains `version: '6.0.52'`
    - `build.gradle` contains `version: '0.7.38'`
    - `build.gradle` contains `allowLocalMaven`
    - `build.gradle` does not contain an unconditional standalone `mavenLocal()` call
    - `gradle/wrapper/gradle-wrapper.properties` still contains `gradle-8.8-bin.zip`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew buildEnvironment</automated>
  </verify>
  <done>Pinned plugin versions and gated repository behavior make the clean-checkout build path deterministic and explicit.</done>
</task>

</tasks>

<verification>
Run `./gradlew buildEnvironment` and confirm the resolved buildscript dependencies show ForgeGradle `6.0.52` and no canonical reliance on local Maven state.
</verification>

<success_criteria>
- Clean checkout build inputs are pinned.
- Local Maven artifacts are opt-in, not part of the default workflow.
- Phase 1 can build on a stable baseline instead of drifting buildscript resolution.
</success_criteria>

<output>
After completion, create `.planning/phases/01-build-reproducibility-baseline/01-01-SUMMARY.md`
</output>
