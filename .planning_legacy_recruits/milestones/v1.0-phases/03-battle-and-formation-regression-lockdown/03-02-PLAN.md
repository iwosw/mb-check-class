---
phase: 03-battle-and-formation-regression-lockdown
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDecider.java
  - src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java
  - src/test/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDeciderTest.java
autonomous: true
requirements:
  - BATL-01
  - BATL-03
must_haves:
  truths:
    - "Maintainer can verify key patrol-leader battle decisions in fast JVM tests without booting the Forge runtime."
    - "Leader attack-controller behavior uses an explicit tactic decision contract instead of burying all branching inside one large method."
    - "Battle regression expectations are encoded as outcome-level assertions rather than incidental tick-order details."
  artifacts:
    - path: "src/main/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDecider.java"
      provides: "Pure decision contract for leader battle tactics"
      contains: "decide"
    - path: "src/test/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDeciderTest.java"
      provides: "Fast regression tests for tactic selection"
      contains: "@Test"
    - path: "src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java"
      provides: "Runtime controller wired to the extracted tactic seam"
      contains: "BattleTacticDecider"
  key_links:
    - from: "src/test/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDeciderTest.java"
      to: "src/main/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDecider.java"
      via: "pure JVM assertions"
      pattern: "BattleTacticDecider"
    - from: "src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java"
      to: "src/main/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDecider.java"
      via: "shared tactic selection"
      pattern: "BattleTacticDecider"
---

<objective>
Extract and lock down patrol-leader battle decision logic with fast JVM coverage.

Purpose: Give Phase 3 a unit-style regression seam for battle outcomes so command heuristics can be stabilized without relying only on heavy runtime GameTests.
Output: A testable tactic-decider class, targeted JVM tests, and the patrol leader controller wired to that seam.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-battle-and-formation-regression-lockdown/03-CONTEXT.md
@.planning/phases/02-layered-test-harness-foundations/02-02-SUMMARY.md
@src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java
@src/main/java/com/talhanation/recruits/util/NPCArmy.java

<interfaces>
From `src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java`:
```java
public class PatrolLeaderAttackController implements IAttackController {
    public void commandArmy(NPCArmy playerArmy, NPCArmy enemyArmy);
    public void charge();
    public void defaultAttack();
    public void back();
    public void forwarding();
}
```

From `src/main/java/com/talhanation/recruits/util/NPCArmy.java`:
```java
public int getTotalUnits();
public double getAverageHealth();
public double getAverageMorale();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write fast JVM regression tests for tactic selection</name>
  <files>src/test/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDeciderTest.java</files>
  <behavior>
    - Test 1: overwhelming advantage chooses the aggressive tactic without requiring Forge runtime boot.
    - Test 2: disadvantaged or low-morale inputs choose retreat/support tactics instead of charge.
    - Test 3: ranged-superiority edge cases prefer the bounded fallback expected by current controller behavior.
  </behavior>
  <action>Create a focused JVM test class for the battle tactic seam. Per D-04 from `03-CONTEXT.md`, assert only the intended tactic outcome for representative army-stat inputs rather than brittle internal sequencing. Cover the current high-value branches from `PatrolLeaderAttackController.commandArmy()` so later runtime tests can trust the tactical core. Keep fixtures local and lightweight; do not boot Forge runtime or instantiate full entities.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.entities.ai.controller.BattleTacticDeciderTest"</automated>
  </verify>
  <done>A failing-then-passing JVM test suite defines the intended battle tactic decisions for representative leader combat states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract the tactic seam and wire the patrol controller to it</name>
  <files>src/main/java/com/talhanation/recruits/entities/ai/controller/BattleTacticDecider.java, src/main/java/com/talhanation/recruits/entities/ai/controller/PatrolLeaderAttackController.java</files>
  <behavior>
    - Extracted decision code returns a stable tactic enum/result for the same army-state inputs used in Task 1.
    - `PatrolLeaderAttackController` still triggers the same high-level actions unless tests expose a clear bug.
  </behavior>
  <action>Create `BattleTacticDecider` as a pure helper or enum-driven decision module and move the branching logic out of `PatrolLeaderAttackController.commandArmy()`. Keep the runtime controller responsible for issuing the actual charge/back/default commands, but have it consume the extracted result so Phase 3 gains a durable unit-test seam. Preserve current outward behavior unless Task 1 proves a logic fault, matching the project stabilization constraint and D-12's focus on deliberate bug fixes only.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.recruits.entities.ai.controller.BattleTacticDeciderTest"</automated>
  </verify>
  <done>Leader battle-tactic selection is testable in pure JVM tests and the runtime controller delegates to the extracted seam.</done>
</task>

</tasks>

<verification>
Run the targeted JVM test class and confirm no Forge runtime boot is required to verify leader tactic regression behavior.
</verification>

<success_criteria>
- Battle tactic selection is covered by fast JVM tests.
- `PatrolLeaderAttackController` delegates decision branching to an explicit seam.
- Outcome-level battle expectations are encoded without freezing incidental controller implementation details.
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-and-formation-regression-lockdown/03-02-SUMMARY.md`
</output>
