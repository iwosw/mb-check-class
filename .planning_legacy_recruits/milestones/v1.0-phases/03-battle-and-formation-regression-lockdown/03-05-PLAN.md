---
phase: 03-battle-and-formation-regression-lockdown
plan: 05
type: execute
wave: 3
depends_on:
  - 03-04
files_modified:
  - src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java
  - src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java
  - src/gametest/resources/data/recruits/structures/battle_density_field.nbt
autonomous: true
requirements:
  - BATL-01
  - BATL-04
gap_closure: true
must_haves:
  truths:
    - "Maintainer can run `./gradlew check --continue` and see both dense-battle GameTests pass instead of timing out at the Phase 3 stress alarms."
    - "Dense battle verification still detects bounded combat stalls through runtime GameTests without degrading into exact casualty choreography."
    - "Ground-only dense battle scenarios remain deterministic and representative of mixed-squad combat per locked Phase 3 decisions."
  artifacts:
    - path: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java"
      provides: "Scenario records for dense-battle squad layouts, health shaping, winner expectation, and bounded deadlines"
      contains: "StressScenario"
    - path: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java"
      provides: "Runtime dense-battle assertions that distinguish healthy attrition from a broken combat stall"
      contains: "baselineDenseBattleCompletesWithoutBrokenLoops"
    - path: "src/gametest/resources/data/recruits/structures/battle_density_field.nbt"
      provides: "Dedicated dense-battle arena tuned for bounded mixed-squad combat"
      contains: "battle_density_field"
  key_links:
    - from: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java"
      to: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java"
      via: "scenario metadata consumed by stress assertions"
      pattern: "scenario\(\)"
    - from: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java"
      to: "src/gametest/resources/data/recruits/structures/battle_density_field.nbt"
      via: "@GameTest template"
      pattern: "battle_density_field"
    - from: "src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java"
      to: "src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      via: "spawnConfiguredRecruit and mutual-target helper wiring"
      pattern: "RecruitsBattleGameTestSupport"
---

<objective>
Close the Phase 3 verification blocker by making the dense-battle stress scenarios pass under the canonical Forge verification flow without watering down the regression alarm.

Purpose: Restore BATL-01 while preserving BATL-04's requirement that battle-density issues fail loudly through automated runtime coverage.
Output: Rebalanced dense-battle fixture data, sharpened stress assertions, and a green `check --continue` run for the full Phase 3 battle suite.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-battle-and-formation-regression-lockdown/03-CONTEXT.md
@.planning/phases/03-battle-and-formation-regression-lockdown/03-VERIFICATION.md
@.planning/phases/03-battle-and-formation-regression-lockdown/03-04-SUMMARY.md
@src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java
@src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java
@src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java

<interfaces>
From `src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java`:
```java
public record StressScenario(
    String scenarioId,
    int timeoutTicks,
    int resolveDeadlineTicks,
    ArenaBounds arenaBounds,
    List<RecruitSpec> westRecruits,
    List<RecruitSpec> eastRecruits,
    float westHealthCap,
    float eastHealthCap
) {}

public static ScenarioState spawnBaselineDenseScenario(GameTestHelper helper)
public static ScenarioState spawnHeavyDenseScenario(GameTestHelper helper)
```

From `src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java`:
```java
public static <T extends AbstractRecruitEntity> T spawnConfiguredRecruit(
    GameTestHelper helper,
    EntityType<T> entityType,
    BlockPos relativePos,
    String customName,
    UUID ownerId
)

public static void setMutualTargets(
    List<? extends AbstractRecruitEntity> attackers,
    List<? extends AbstractRecruitEntity> defenders
)
```

Current runtime guard already present in `src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java`:
```java
LivingEntity currentTarget = this.getTarget();
if(currentTarget != null && (currentTarget.isDeadOrDying() || currentTarget.isRemoved())) this.setTarget(null);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebalance dense-battle fixtures from reproduced failure evidence</name>
  <files>src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java, src/gametest/resources/data/recruits/structures/battle_density_field.nbt</files>
  <action>Start by reproducing the current failure with `./gradlew verifyGameTestStage` and use the existing `03-VERIFICATION.md` blocker as the baseline. Then update the dense scenario records per the locked Phase 3 decisions in `03-CONTEXT.md`: keep the coverage ground-only and deterministic (per the battle baseline decisions), preserve mixed melee+ranged squads (per the edge-case scope decisions), and keep stress alarms focused on bounded stability rather than exact casualty scripts (per the stress decisions). Encode enough scenario metadata in `BattleStressFixtures` to support a deterministic winner and a justifiable deadline: add scenario-specific balance inputs such as expected winning side, adjusted health caps, widened lane spacing, or slightly longer resolve bounds only when reproduction shows healthy attrition is still progressing. If reproduction shows the current layout is causing preventable path congestion, widen `battle_density_field.nbt` lanes or stagger spawn pads instead of merely inflating the timeout. Do not introduce naval/ship coverage or exact tick choreography; those are deferred by context.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>Each dense scenario has fixture-owned balance/layout data that still represents dense mixed-squad combat but now has a concrete, evidence-backed path to bounded resolution.</done>
</task>

<task type="auto">
  <name>Task 2: Tighten stress assertions without masking real stalls</name>
  <files>src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java</files>
  <action>Update the stress GameTests so they consume the richer fixture metadata and distinguish "healthy but still resolving" from an actual broken combat loop. Per the Phase 3 locked decisions, keep assertions outcome-level and observable: at the deadline, require the configured winning side to have surviving owned recruits, require the losing side to be eliminated, keep survivors inside arena bounds, and fail if a survivor still retains a dead target. Add one earlier progress probe driven by fixture metadata so the test can fail fast when neither side is taking losses or moving toward resolution, but avoid exact casualty counts or tick-perfect scripts. If Task 1 showed only threshold drift, keep the production combat code untouched; if the reproduced failure persists after the rebalance, use this task to expose that with clearer diagnostics rather than weakening the alarm.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage && ./gradlew check --continue</automated>
  </verify>
  <done>`baselineDenseBattleCompletesWithoutBrokenLoops` and `heavierDenseBattleCompletesWithoutBrokenLoops` pass in the canonical verification flow while still failing loudly on real dense-combat stalls.</done>
</task>

</tasks>

<verification>
1. Run `./gradlew verifyGameTestStage` to confirm the dense-battle GameTests pass in isolation with the revised scenario metadata.
2. Run `./gradlew check --continue` to confirm the full Phase 3 JVM + GameTest verification path is green again.
3. Confirm the dense-battle tests still assert bounded resolution, arena containment, and stale-target cleanup rather than exact casualty timing.
</verification>

<success_criteria>
- Phase 3 verification no longer reports `baselinedensebattlecompleteswithoutbrokenloops` or `heavierdensebattlecompleteswithoutbrokenloops` as blockers.
- Dense battle scenarios remain deterministic mixed-squad ground battles, not watered-down micro-tests.
- The canonical `./gradlew check --continue` run restores BATL-01 while preserving BATL-04's automated stress alarm coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/03-battle-and-formation-regression-lockdown/03-05-SUMMARY.md`
</output>
