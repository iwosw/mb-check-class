---
phase: 04-persistence-binding-resume
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified: []
autonomous: false
requirements:
  - AREA-05
  - STOR-01
must_haves:
  truths:
    - "A player can reload the world and see the representative worker keep its intended area and storage affinity."
    - "A resumed worker continues with the expected loop or degrades to a clean controllable idle state."
    - "Phase 4 closes with proof on an existing authoring surface rather than a synthetic demo path."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      provides: "Persisted binding restore behavior under reload"
    - path: "src/main/java/com/talhanation/workers/entities/FarmerEntity.java"
      provides: "Representative crop-area binding resume path"
    - path: "src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java"
      provides: "Existing lastStorage priority path used after reload"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      to: "src/main/java/com/talhanation/workers/entities/FarmerEntity.java"
      via: "reloaded current area field"
      pattern: "currentCropArea"
    - from: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      to: "src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java"
      via: "persisted lastStorage preference"
      pattern: "lastStorage"
---

<objective>
Prove the persistence slice on one representative gameplay path before moving to deeper storage-loop work.

Purpose: The locked phase context requires one reload proof that exercises both authored area continuity and storage/home-like binding continuity, so this plan turns the implementation into an explicit acceptance check.
Output: Human-approved reload smoke verification for a representative worker path plus passing automated regression commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistence-binding-resume/04-CONTEXT.md
@.planning/phases/04-persistence-binding-resume/04-01-SUMMARY.md
@.planning/phases/04-persistence-binding-resume/04-02-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@src/main/java/com/talhanation/workers/entities/FarmerEntity.java
@src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify a reload path for area and storage binding continuity</name>
  <files>src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/main/java/com/talhanation/workers/entities/FarmerEntity.java, src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java</files>
  <action>Use the farmer path as the representative smoke target per the phase discretion note because it exercises an authored work area plus the existing storage-affinity path. Verify the new persistence behavior per D-05 while preserving the current authoring and command surfaces per D-01/D-03; do not invent a new explicit assignment flow while testing.</action>
  <what-built>UUID-based worker-area rebinding after load plus resumed `lastStorage` affinity on the existing worker loop</what-built>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerBindingResumeTest && ./gradlew compileJava -x test</automated>
  </verify>
  <how-to-verify>
    1. Run the automated command above and confirm it passes.
    2. Run `./gradlew runClient`, open a test world, and set up one farmer with an authored crop area and one nearby valid storage area/chest that the farmer already uses.
    3. Let the farmer begin or resume work so both the current crop-area binding and `lastStorage` preference have a live proof path.
    4. Save and quit to title, then reload the same world. If practical in this environment, also repeat once through a full game restart to cover restart instead of just relog.
    5. Confirm the farmer keeps the intended crop-area context after reload instead of selecting a different area or dropping into a corrupted state.
    6. Confirm the farmer still prefers and can resume against the intended storage path; if the saved binding is intentionally broken before reload, confirm the worker idles or falls back cleanly without crashing or becoming uncontrollable.
  </how-to-verify>
  <done>Human verification confirms one representative worker keeps its intended authored area and storage affinity through reload, or degrades to a clean controllable idle state when the saved binding is invalid.</done>
  <resume-signal>Type "approved" or describe the reload/resume issue you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 is only ready to close after the shared binding test passes, the code compiles, and the representative reload path is approved on the existing in-game flow.
</verification>

<success_criteria>
- A representative worker keeps its authored area after relog or restart.
- The same worker retains or safely clears its storage affinity after reload.
- Reloaded workers remain controllable even when a saved binding no longer resolves.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistence-binding-resume/04-03-SUMMARY.md`
</output>
