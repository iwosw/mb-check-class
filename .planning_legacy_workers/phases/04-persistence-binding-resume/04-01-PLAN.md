---
phase: 04-persistence-binding-resume
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java
  - src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java
autonomous: true
requirements:
  - AREA-05
  - STOR-01
must_haves:
  truths:
    - "Saved worker-area and storage UUIDs are restored by direct identifier rules instead of fresh proximity scans."
    - "A stale saved UUID clears safely instead of leaving a half-broken binding behind."
    - "The shared persistence seam can be regression-tested without launching the game client."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java"
      provides: "Shared UUID-based restore and stale-binding clearing rules"
    - path: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      provides: "Base worker save-data seam for persisted binding UUIDs"
    - path: "src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java"
      provides: "Automated coverage for resume and clear behavior"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      to: "src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java"
      via: "save/load helper calls"
      pattern: "WorkerBindingResume"
    - from: "src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java"
      to: "src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java"
      via: "JUnit assertions"
      pattern: "resolve|save|clear"
---

<objective>
Create the shared persistence seam for Phase 4 before touching every worker implementation.

Purpose: Phase 4 depends on UUID-first resume behavior per the locked context decisions, so this plan defines the reusable save/load rules and protects them with lightweight automated coverage.
Output: A tested helper plus base worker save-data hooks for persisted work-area and storage UUIDs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-persistence-binding-resume/04-CONTEXT.md
@.planning/phases/02-worker-control-recovery/02-04-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java

<interfaces>
From src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java:
```java
public UUID lastStorage;
public abstract AbstractWorkAreaEntity getCurrentWorkArea();
public void addAdditionalSaveData(@NotNull CompoundTag nbt)
public void readAdditionalSaveData(@NotNull CompoundTag nbt)
```

From src/main/java/com/talhanation/workers/entities/ai/AbstractChestGoal.java:
```java
public void scanAvailableStorageAreas() {
    if (this.worker.lastStorage != null && list.stream().anyMatch(area -> area.getUUID().equals(this.worker.lastStorage))) {
        list.removeIf(area -> !area.getUUID().equals(this.worker.lastStorage));
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared UUID resume helper with focused regression coverage</name>
  <files>src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java, src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java</files>
  <behavior>
    - Test 1: A saved UUID that resolves to the expected binding type is restored unchanged.
    - Test 2: A missing or wrong-type UUID resolves to no binding and is marked for clearing.
    - Test 3: Nullable UUID save/load helpers preserve present values and omit absent ones cleanly.
  </behavior>
  <action>Create a small pure-Java helper for persisted binding rules per D-02 and D-04: direct UUID resolution first, no proximity fallback inside the helper, and explicit stale-binding clearing when the entity no longer resolves or resolves to the wrong type. Keep the seam generic enough for both work-area and storage affinity restoration per D-01, and do not introduce any new explicit assignment system per the deferred v2 idea and D-03.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerBindingResumeTest</automated>
  </verify>
  <done>The helper and test file exist, the tests pass, and the helper expresses UUID-first restore behavior plus safe clearing semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Persist worker binding UUID tags through the shared base seam</name>
  <files>src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java</files>
  <action>Extend `AbstractWorkerEntity` to save/load the current work-area UUID and normalize nullable UUID tag handling through `WorkerBindingResume` per D-01, D-02, and D-04. Keep this task limited to the shared save-data seam: persist the IDs needed for later rebinding, preserve the existing `lastStorage` affinity tag, and avoid per-profession restoration logic or broader work-loop changes until the next plan per D-03.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerBindingResumeTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Base worker save/load code writes and reads the UUID data needed for resume without changing current area-selection heuristics yet.</done>
</task>

</tasks>

<verification>
The shared helper test must pass and the project must still compile after the base worker save-data seam stores persisted binding UUIDs.
</verification>

<success_criteria>
- UUID-based binding restore rules are defined in one shared seam.
- Stale binding data can be cleared safely by rule, not by ad-hoc nulling.
- AbstractWorkerEntity persists the identifiers Phase 4 needs for later rebinding.
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistence-binding-resume/04-01-SUMMARY.md`
</output>
