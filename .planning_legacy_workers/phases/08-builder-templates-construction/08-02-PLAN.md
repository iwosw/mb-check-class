---
phase: 08-builder-templates-construction
plan: 2
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
  - src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
  - src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java
autonomous: true
requirements:
  - PROF-03
must_haves:
  truths:
    - Starting a builder job through the existing build-area flow marks real construction progress instead of relying on transient stacks that disappear on reload.
    - A partially completed structure can resume from the remaining work after a normal save/reload interruption.
    - Builder tool/material waits keep the existing build area recoverable until the structure is truly finished.
  artifacts:
    - path: src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
      provides: Persisted builder-start/progress state and runtime stack reconstruction.
    - path: src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
      provides: Builder loop that resumes pending build work against the persisted progress seam.
    - path: src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java
      provides: Server-side build start/update flow that seeds persisted build progress.
  key_links:
    - from: src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java
      to: src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
      via: build button packet starting persisted build progress
      pattern: setStartBuild|build
    - from: src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
      to: src/main/java/com/talhanation/workers/entities/workarea/BuildAreaProgressState.java
      via: rebuild pending stacks from structure/world state
      pattern: BuildAreaProgressState
    - from: src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
      to: src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
      via: area selection and resume against reconstructed pending stacks
      pattern: currentBuildArea|setStartBuild|stackToPlace
---

<objective>
Wire persisted builder progress into the brownfield runtime so existing build/template flows start correctly, survive reloads, and resume the intended construction loop.

Purpose: Fulfill PROF-03 per D-01, D-02, and D-03 by preserving the current BuildArea UI while fixing the server-side progress continuity that currently depends on non-persistent in-memory stacks.
Output: A persisted build-start/progress path in `BuildArea`, runtime builder-loop wiring, and build-start packet handling that support resumable construction.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-builder-templates-construction/08-CONTEXT.md
@.planning/phases/05-storage-loop-integrity/05-01-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java
@src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
@src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java

<interfaces>
From `src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java`:
```java
public MessageUpdateBuildArea(UUID uuid, int xSize, int ySize, int zSize, CompoundTag structureNBT, boolean build, boolean isCreative)
public void update(BuildArea buildArea)
```

From `src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java`:
```java
public void setStructureNBT(CompoundTag tag)
public CompoundTag getStructureNBT()
public void setStartBuild(boolean isCreative)
public List<ItemStack> getRequiredMaterials()
public Stack<BuildBlock> stackToPlace
public Stack<BuildBlock> stackToPlaceMultiBlock
public Stack<BlockPos> stackToBreak
```

From `08-01` planned seam:
```java
public final class BuildAreaProgressState { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist build-start state and rebuild pending stacks from structure data</name>
  <files>src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java</files>
  <action>Add the smallest correct persisted flag/state needed to distinguish “template loaded but not started” from “builder job started and may need resume” per D-03. Serialize that state in `BuildArea`, and route `setStartBuild(...)` plus any resume helper through the new `BuildAreaProgressState` seam so pending `stackToBreak`, `stackToPlace`, and `stackToPlaceMultiBlock` can be reconstructed from `structureNBT` + current world state after reload. Preserve the current authoring/template surfaces per D-01: do not redesign `BuildAreaScreen`, add new job controls, or auto-start untouched templates. Clear the persisted in-progress state only when the structure is genuinely complete or explicitly invalidated.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.workarea.BuildAreaProgressStateTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>`BuildArea` can tell whether a build was actually started, persist that fact, and reconstruct the remaining break/place work after a reload without treating every loaded template as an active job.</done>
</task>

<task type="auto">
  <name>Task 2: Resume the builder loop against persisted progress without changing the UI flow</name>
  <files>src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java, src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java</files>
  <action>Update the build-start packet path so clicking the existing Build button seeds persisted build progress through `BuildArea` per D-01. Then harden `BuilderWorkGoal` so area selection and runtime transitions refresh/resume pending work from the persisted seam instead of depending on stale in-memory stacks, while keeping missing-tool and missing-material waits recoverable through the Phase 5 shared request path. Preserve the current imperative state machine, existing build-area/template UX, and one-worker-at-a-time semantics; do not broaden template scanning/loading features or richer builder UX from the deferred section.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.workarea.BuildAreaProgressStateTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Pressing Build starts a resumable job, partial construction survives reload closely enough to resume the intended remaining work, and builder interruptions keep the same build area recoverable until the area is truly finished.</done>
</task>

</tasks>

<verification>
Run `./gradlew test --tests com.talhanation.workers.entities.workarea.BuildAreaProgressStateTest && ./gradlew compileJava -x test`.
</verification>

<success_criteria>
- Existing build-area/template flow can start a real builder job without UI redesign.
- Reloading after partial progress reconstructs remaining work instead of resetting to an empty transient stack state.
- Builder completion still marks the area done and clears the in-progress flag exactly once.
</success_criteria>

<output>
After completion, create `.planning/phases/08-builder-templates-construction/08-02-SUMMARY.md`
</output>
