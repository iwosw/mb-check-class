---
phase: 03-work-area-authoring
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java
  - src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java
  - src/main/java/com/talhanation/workers/network/MessageUpdateOwner.java
  - src/main/java/com/talhanation/workers/network/MessageUpdateStorageArea.java
  - src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java
  - src/main/resources/assets/workers/lang/en_us.json
autonomous: true
requirements:
  - AREA-02
  - AREA-03
must_haves:
  truths:
    - "Player can move, rotate, rename, destroy, and reassign an existing work area and have the server apply only valid edits."
    - "Unauthorized or overlapping edits are rejected without corrupting the current work-area state."
    - "The edit screen no longer drifts away from server truth after a rejected rotation or update."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java"
      provides: "Validated move/rename/destroy mutations"
    - path: "src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java"
      provides: "Validated rotation mutations"
    - path: "src/main/java/com/talhanation/workers/network/MessageUpdateOwner.java"
      provides: "Validated owner reassignment"
    - path: "src/main/java/com/talhanation/workers/network/MessageUpdateStorageArea.java"
      provides: "Validated storage-specific edit updates"
    - path: "src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java"
      provides: "Client edit UI aligned with server-authoritative results"
  key_links:
    - from: "src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java"
      to: "src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java"
      via: "move/destroy buttons"
      pattern: "MessageUpdateWorkArea"
    - from: "src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java"
      to: "src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java"
      via: "rotate buttons"
      pattern: "MessageRotateWorkArea"
    - from: "src/main/java/com/talhanation/workers/client/gui/StorageAreaScreen.java"
      to: "src/main/java/com/talhanation/workers/network/MessageUpdateStorageArea.java"
      via: "storage-type toggles and rename"
      pattern: "MessageUpdateStorageArea"
---

<objective>
Make existing work-area edits trustworthy by validating every mutation on the server and removing client-side drift.

Purpose: Phase 3 is not complete if players can open a screen but invalid edits still silently fail, overwrite ownership, or leave the UI showing state the server rejected.
Output: Hardened edit packets plus a work-area screen that stays aligned with canonical server state.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-area-authoring/03-CONTEXT.md
@.planning/phases/03-work-area-authoring/03-01-PLAN.md
@src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java
@src/main/java/com/talhanation/workers/client/gui/StorageAreaScreen.java
@src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java
@src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java
@src/main/java/com/talhanation/workers/network/MessageUpdateOwner.java
@src/main/java/com/talhanation/workers/network/MessageUpdateStorageArea.java

<decisions>
- D-01: Preserve the existing work-area screen instead of redesigning the UI.
- D-02: Keep move/rotate/rename/owner-change/destroy server-authoritative.
- D-03: Reject invalid edits explicitly and near the packet-side mutation seam.
- D-04: Stay within authoring/editing trust work; do not pull persistence/resume into this plan.
</decisions>

<interfaces>
From src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java:
```java
public AbstractWorkAreaEntity workArea;
public void setButtons()
public void onAreaMoved() {}
```

From src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java:
```java
public MessageUpdateWorkArea(UUID uuid, String name, Vec3 vec3, boolean destroy)
public void updateWorkArea(AbstractWorkAreaEntity workArea)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate every edit packet before mutating a work area</name>
  <files>src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java, src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java, src/main/java/com/talhanation/workers/network/MessageUpdateOwner.java, src/main/java/com/talhanation/workers/network/MessageUpdateStorageArea.java, src/main/resources/assets/workers/lang/en_us.json</files>
  <action>Update all edit packets to resolve the target area through a stable server-side lookup and run the shared 03-01 authoring rules before mutating anything. Per D-02/D-03, only accepted requests may move, rotate, rename, destroy, change owner, or update storage types; rejected requests must leave the current area untouched and emit localized feedback for missing area, forbidden access, or overlap rejection. Preserve the current packet-per-action structure instead of introducing a new service layer per D-01.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest --tests com.talhanation.workers.network.WorkAreaRotationTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Each existing edit packet performs permission/validation checks first, and invalid edits cannot silently mutate or destroy a work area.</done>
</task>

<task type="auto">
  <name>Task 2: Remove optimistic client drift from the shared work-area screen</name>
  <files>src/main/java/com/talhanation/workers/client/gui/WorkAreaScreen.java, src/main/resources/assets/workers/lang/en_us.json</files>
  <action>Adjust `WorkAreaScreen` so the current UI still sends the same edit packets per D-01, but it no longer mutates local area state in ways the server can reject. Specifically, remove or neutralize the immediate local facing mutation on rotate, keep box preview behavior limited to the existing visualization, and rely on synced entity state plus explicit rejection messages to show the canonical outcome per D-02/D-03. Do not add a new status panel or alternate editing surface.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest --tests com.talhanation.workers.network.WorkAreaRotationTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>The edit screen still offers the same controls, but rejected server-side edits no longer leave the local screen showing a rotated or otherwise mutated state the server refused.</done>
</task>

</tasks>

<verification>
Edit trust is achieved only when the server rejects bad mutations cleanly and the client screen stays aligned with the server-authoritative state after those rejections.
</verification>

<success_criteria>
- Existing edit actions apply only when validated by the server.
- Overlap and authorization failures leave the current area unchanged.
- The shared work-area screen no longer shows stale optimistic state after rejected edits.
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-area-authoring/03-03-SUMMARY.md`
</output>
