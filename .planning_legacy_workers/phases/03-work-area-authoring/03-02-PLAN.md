---
phase: 03-work-area-authoring
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
  - src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java
  - src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java
  - src/main/resources/assets/workers/lang/en_us.json
autonomous: true
requirements:
  - AREA-01
  - AREA-04
must_haves:
  truths:
    - "Player can create a work area through the existing worker command flow and get an explicit result."
    - "Player can inspect an existing work area through the current interact-to-open-screen flow."
    - "Inspection lookup does not depend on a fragile nearby-entity scan that can miss the intended area."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java"
      provides: "Server-side work-area creation with explicit validation feedback"
    - path: "src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java"
      provides: "Stable client open-screen lookup"
    - path: "src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      provides: "Interact path that dispatches the stable open-screen reference"
    - path: "src/main/resources/assets/workers/lang/en_us.json"
      provides: "Player-facing rejection messages for create/inspect flows"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      to: "src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java"
      via: "server interact packet"
      pattern: "MessageToClientOpenWorkAreaScreen"
    - from: "src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java"
      to: "src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java"
      via: "existing add-area buttons"
      pattern: "MessageAddWorkArea"
---

<objective>
Make the existing creation and inspection surfaces trustworthy without redesigning them.

Purpose: Phase 3 must keep the current command/screen surfaces while hardening the server-authoritative create/open-screen path so players can actually trust authored areas.
Output: Reliable work-area creation plus stable inspect/open-screen behavior with explicit player feedback on rejection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-area-authoring/03-CONTEXT.md
@.planning/phases/03-work-area-authoring/03-01-PLAN.md
@src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java
@src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java
@src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java

<decisions>
- D-01: Preserve the existing command-screen create flow and current work-area screens.
- D-02: Keep creation and inspection server-authoritative.
- D-03: Reject invalid create/inspect requests explicitly in current chat/screen surfaces.
- D-05: Prefer lightweight automated coverage and compile checks over heavy new infrastructure.
</decisions>

<interfaces>
From src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java:
```java
public MessageAddWorkArea(BlockPos pos, int type)
public void executeServerSide(NetworkEvent.Context context)
```

From src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java:
```java
public MessageToClientOpenWorkAreaScreen(UUID uuid)
public void executeClientSide(NetworkEvent.Context context)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden server-side work-area creation on the existing command flow</name>
  <files>src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java, src/main/resources/assets/workers/lang/en_us.json</files>
  <action>Update `MessageAddWorkArea` to call the shared authoring-rule seam from 03-01 before spawning any area. Per D-01 keep the existing button-driven command surface untouched, but per D-02/D-03 enforce claim-gating, overlap rejection, and unsupported/invalid create requests on the server with explicit localized system messages instead of silent failure. Keep defaults, ownership, and area sizing aligned with the current entity-specific behavior.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Area creation still uses the existing command screen, but invalid server-side requests are rejected with player-visible feedback and no bad entity is spawned.</done>
</task>

<task type="auto">
  <name>Task 2: Replace fragile inspect lookup with a stable open-screen reference</name>
  <files>src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, src/main/java/com/talhanation/workers/network/MessageToClientOpenWorkAreaScreen.java, src/main/resources/assets/workers/lang/en_us.json</files>
  <action>Change the inspect/open-screen flow so `AbstractWorkAreaEntity.interact()` sends a stable entity reference (prefer entity id, optionally with UUID fallback) to `MessageToClientOpenWorkAreaScreen` instead of relying on a 16-block nearby scan on the client. Per D-01 preserve the current interact-to-open-screen UX and box visualization, but per D-03 send explicit feedback when inspection is denied or the client cannot resolve the area instead of silently doing nothing.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest --tests com.talhanation.workers.network.WorkAreaRotationTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Authorized players can reliably open the current work-area screen from the existing interact flow, and failed open attempts report a visible reason rather than a silent miss.</done>
</task>

</tasks>

<verification>
Creation and inspection are only complete when the existing surfaces still work, failures are explicit, and packet lookup is no longer dependent on proximity scans that can miss the intended area.
</verification>

<success_criteria>
- A player can create a work area from the existing command flow and get explicit success/failure behavior.
- A player can inspect an authored area via the current interact flow and reliably reach the right screen.
- Rejected create/inspect requests no longer fail silently.
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-area-authoring/03-02-SUMMARY.md`
</output>
