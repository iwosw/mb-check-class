---
phase: 03-work-area-authoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
  - src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java
  - src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java
autonomous: true
requirements:
  - AREA-03
must_haves:
  truths:
    - "Work-area authoring requests produce one shared allow/reject decision instead of scattered silent no-ops."
    - "Owner, same-team, and admin access are distinguished consistently for later create/edit/inspect flows."
    - "Overlap and claim-gating decisions can be tested without booting the full game runtime."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java"
      provides: "Shared authoring decision seam for packets"
    - path: "src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java"
      provides: "Regression coverage for authoring allow/reject rules"
    - path: "src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      provides: "Reusable owner/team/admin authoring permission helpers"
  key_links:
    - from: "src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java"
      to: "src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      via: "shared permission predicates"
      pattern: "canPlayer(See|Modify)|AuthoringRules"
---

<objective>
Create the shared rule seam that all Phase 3 packet fixes will use for permission and validation decisions.

Purpose: Phase 3 needs explicit, testable validation near the server mutation points, so this plan establishes one brownfield-friendly source of truth before packet wiring begins.
Output: A reusable authoring-rules helper plus focused tests for permission, overlap, and claim-gating outcomes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-work-area-authoring/03-CONTEXT.md
@.planning/phases/02-worker-control-recovery/02-04-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java

<decisions>
- D-01: Preserve the existing authoring surfaces (worker command screen + work-area screens).
- D-02: Keep create/edit/rotate/rename/owner-change/destroy server-authoritative.
- D-03: Keep validation close to mutation points and fail explicitly instead of silently corrupting state.
- D-04: Stay within creation/edit/validation/inspection scope; no persistence or profession redesign.
- D-05: Add lightweight automated coverage around authoring rules where practical.
</decisions>

<interfaces>
From src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java:
```java
public boolean canPlayerSee(Player player)
public static boolean isAreaOverlapping(Level level, AbstractWorkAreaEntity currentArea, AABB targetBox)
public UUID getPlayerUUID()
public String getTeamStringID()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared authoring rule contract with tests</name>
  <files>src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java, src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java</files>
  <behavior>
    - Test 1: owner, same-team member, and admin each receive ALLOW for inspect/mutate requests.
    - Test 2: unrelated players receive FORBIDDEN for inspect/mutate requests.
    - Test 3: create requests reject OUTSIDE_FACTION_CLAIM and OVERLAPPING with distinct outcomes.
  </behavior>
  <action>Create a small pure-Java helper that packets can call before any work-area mutation. Per D-02/D-03/D-05, return normalized outcomes such as ALLOW, FORBIDDEN, OUTSIDE_FACTION_CLAIM, OVERLAPPING, and AREA_NOT_FOUND instead of scattering ad-hoc booleans. Keep the seam dependency-light and focused on current brownfield authoring rules; do not redesign the broader work-area architecture or pull persistence concerns forward per D-04.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest</automated>
  </verify>
  <done>The new helper exists, its outcomes match the tested authoring cases, and the tests fail if permission or validation behavior regresses.</done>
</task>

<task type="auto">
  <name>Task 2: Expose reusable permission predicates on the base work-area entity</name>
  <files>src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java</files>
  <action>Add minimal reusable helpers on `AbstractWorkAreaEntity` for owner/team/admin authoring checks so later packet plans can call one consistent API. Per D-01/D-03, preserve the current visible access behavior while making mutate-vs-inspect intent explicit for downstream packet wiring. Keep the change narrowly scoped to helper methods and shared predicates; do not alter persistence or profession behavior per D-04.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaAuthoringRulesTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>`AbstractWorkAreaEntity` exposes explicit reusable authoring permission helpers, and the project still compiles against the new shared rule seam.</done>
</task>

</tasks>

<verification>
The phase can only trust later packet fixes if permission and validation outcomes are centralized and covered by targeted regression tests.
</verification>

<success_criteria>
- Later packet plans have one shared allow/reject seam to call.
- Permission logic distinguishes owner, team, admin, and stranger cases consistently.
- Overlap and claim-gating decisions are covered by focused automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-work-area-authoring/03-01-SUMMARY.md`
</output>
