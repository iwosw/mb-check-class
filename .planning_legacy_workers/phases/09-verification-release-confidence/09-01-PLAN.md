---
phase: 09-verification-release-confidence
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - .planning/phases/09-verification-release-confidence/09-RELEASE-CHECKLIST.md
autonomous: true
requirements:
  - QUAL-02
must_haves:
  truths:
    - One command runs the release-confidence regression suite for the recovered seams that matter most.
    - The final milestone smoke route is defined as one reproducible path instead of scattered phase-specific notes.
    - The release-confidence artifacts stay within the existing Gradle + JUnit pattern instead of introducing a new test harness.
  artifacts:
    - path: build.gradle
      provides: Named Gradle task that aggregates the release-confidence regression tests.
      contains: releaseConfidenceTest
    - path: .planning/phases/09-verification-release-confidence/09-RELEASE-CHECKLIST.md
      provides: Reproducible dedicated-server and final smoke instructions.
      contains: Final smoke path
  key_links:
    - from: build.gradle
      to: src/test/java/com/talhanation/workers/**
      via: releaseConfidenceTest filter includes
      pattern: releaseConfidenceTest
    - from: .planning/phases/09-verification-release-confidence/09-RELEASE-CHECKLIST.md
      to: .planning/phases/02-worker-control-recovery/02-04-SUMMARY.md
      via: representative farmer/control proof route
      pattern: farmer
---

<objective>
Create the reproducible automation and smoke-route artifacts for Phase 9.

Purpose: Satisfy QUAL-02 with the smallest high-value finish by consolidating the existing JUnit seams and defining one final release-confidence route without broad harness redesign.
Output: A named Gradle regression task plus a written checklist for the dedicated-server smoke gate.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-verification-release-confidence/09-CONTEXT.md
@build.gradle
@.planning/phases/01-recovery-baseline/01-03-SUMMARY.md
@.planning/phases/02-worker-control-recovery/02-04-SUMMARY.md
@.planning/phases/03-work-area-authoring/03-04-SUMMARY.md
@.planning/phases/05-storage-loop-integrity/05-03-SUMMARY.md
@.planning/phases/06-farming-husbandry/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one named Gradle release-confidence regression suite</name>
  <files>build.gradle</files>
  <action>Extend `build.gradle` with a `releaseConfidenceTest` task that reuses the existing JUnit Platform setup and aggregates the already recovered focused seams instead of inventing a new harness, per D-01 and D-02 from `09-CONTEXT.md`. Include the representative tests for mechanics, validation, persistence, and regression-prone logic already present in the repository: work-area rotation/authoring, worker control/recovery, binding resume, storage request/deposit rules, farmer continuity, animal-farmer continuity, mining selection, merchant authority/access control, and builder progress. Keep the task brownfield-friendly: no new plugins, no new source sets, no broad Gradle restructuring.</action>
  <verify>
    <automated>./gradlew releaseConfidenceTest</automated>
  </verify>
  <done>`./gradlew releaseConfidenceTest` exists, passes, and clearly represents the release-confidence seam coverage for the recovered mod.</done>
</task>

<task type="auto">
  <name>Task 2: Write the single final smoke checklist</name>
  <files>.planning/phases/09-verification-release-confidence/09-RELEASE-CHECKLIST.md</files>
  <action>Create a concise reproducible checklist that defines the one final smoke route per D-03, D-04, D-05, and D-06 from `09-CONTEXT.md`: dedicated-server startup is required, the route should use the already stabilized farmer/control/authoring/storage path, and it must avoid an exhaustive profession-by-profession matrix. Include exact commands or run targets for the automated prereqs, dedicated-server launch, client launch, the chosen smoke steps, and the expected outcomes for create/edit authoring, blocked-reason visibility, recovery/unblock, storage fetch/deposit behavior, and restart/rejoin continuity. Keep it short enough that a future executor will actually rerun it.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
text = Path('.planning/phases/09-verification-release-confidence/09-RELEASE-CHECKLIST.md').read_text()
required = ['releaseConfidenceTest', 'Dedicated server', 'Final smoke path', 'farmer', 'storage', 'restart']
missing = [item for item in required if item not in text]
assert not missing, f'Missing checklist content: {missing}'
PY</automated>
  </verify>
  <done>The phase has one durable checklist that another executor can use to rerun the release-confidence gate without rediscovering the path.</done>
</task>

</tasks>

<verification>
Automation is complete only when `./gradlew releaseConfidenceTest` passes and the written checklist captures the exact dedicated-server smoke route and expected outcomes.
</verification>

<success_criteria>
- A single Gradle task covers the repository's release-confidence regression seams.
- The final smoke path is documented as one focused dedicated-server route, not scattered across prior phases.
- No new harness, dependency stack, or speculative gameplay work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/09-verification-release-confidence/09-01-SUMMARY.md`
</output>
