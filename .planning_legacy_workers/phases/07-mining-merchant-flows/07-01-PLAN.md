---
phase: 07-mining-merchant-flows
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/workers/entities/ai/MinerLoopProgress.java
  - src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
  - src/test/java/com/talhanation/workers/entities/ai/MinerLoopProgressTest.java
autonomous: true
requirements:
  - PROF-02
must_haves:
  truths:
    - Miner keeps its assigned mining area through normal missing-tool or missing-material interruptions instead of silently abandoning the loop.
    - Miner surfaces missing pickaxe, torch, and floor-closing materials through the shared worker-control/storage path.
    - Miner only releases the mining area when the work is invalidated or truly finished.
  artifacts:
    - path: src/main/java/com/talhanation/workers/entities/ai/MinerLoopProgress.java
      provides: Pure miner continuity decisions for interruption and completion handling.
    - path: src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
      provides: Server-authoritative miner loop using the shared request/status seam.
    - path: src/test/java/com/talhanation/workers/entities/ai/MinerLoopProgressTest.java
      provides: Automated regression coverage for miner interruption decisions.
  key_links:
    - from: src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
      to: src/main/java/com/talhanation/workers/entities/ai/MinerLoopProgress.java
      via: interruption/finish decisions inside prepare states
      pattern: MinerLoopProgress
    - from: src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
      to: src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
      via: requestRequiredItem for pickaxe/torch/cobblestone complaints
      pattern: requestRequiredItem
---

<objective>
Stabilize the miner loop so missing inputs and normal interruptions do not strand the worker or hide why mining stopped.

Purpose: Fulfill PROF-02 by preserving mining-area continuity and routing miner failures through the shared recovery/status behavior established in Phases 2 and 5.
Output: A miner-specific continuity helper, regression tests, and a hardened `MinerWorkGoal` implementation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mining-merchant-flows/07-CONTEXT.md
@.planning/phases/05-storage-loop-integrity/05-01-SUMMARY.md
@.planning/phases/06-farming-husbandry/06-01-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java

<interfaces>
From `src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java`:
```java
public void requestRequiredItem(NeededItem neededItem, String reasonToken, Component message)
public void clearPendingStorageComplaint()
@Nullable public WorkerStorageRequestState.PendingComplaint releasePendingStorageComplaint()
public boolean hasPendingStorageComplaint()
```

From `src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java`:
```java
public enum State {
    SELECT_WORK_AREA,
    MOVE_TO_WORK_AREA,
    PREPARE_MINE_WALLS,
    MINE_WALLS,
    PREPARE_CLOSE_FLOOR,
    CLOSE_FLOOR,
    PREPARE_MINING,
    MINING,
    CHECK,
    DONE,
    ERROR
}

public static List<MiningArea> getAvailableWorkAreasByPriority(ServerLevel level, MinerEntity minerEntity, @Nullable MiningArea currentArea)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the miner continuity decision seam</name>
  <files>src/main/java/com/talhanation/workers/entities/ai/MinerLoopProgress.java, src/test/java/com/talhanation/workers/entities/ai/MinerLoopProgressTest.java</files>
  <behavior>
    - Test 1: waiting for a required mining input (pickaxe, torch, or cobblestone) keeps the current mining area active and resumes the same prepare state.
    - Test 2: a temporary deposit/request interruption does not mark the area finished or release ownership.
    - Test 3: true completion returns a decision that allows the area to be marked done and released.
  </behavior>
  <action>Create a small pure helper, matching the Phase 6 continuity-helper pattern, that expresses miner interruption vs completion decisions without rewriting the whole goal. Keep it focused on state transitions and area-retention semantics; do not move world-scanning or pathing logic out of `MinerWorkGoal`.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.ai.MinerLoopProgressTest</automated>
  </verify>
  <done>`MinerLoopProgressTest` proves miner wait/resume/completion decisions independent of world state.</done>
</task>

<task type="auto">
  <name>Task 2: Wire miner requests and completion through the shared seam</name>
  <files>src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java</files>
  <action>Update `MinerWorkGoal` to use the new helper when deciding whether to keep or release `currentMiningArea`, and route every required-input stall through `requestRequiredItem(...)` per the Phase 7 storage/recovery decision. Replace direct `addNeededItem(...)` miner stalls for torches and cobblestone so blocked feedback comes through the shared worker-control path instead of silent stalls. Preserve the existing imperative state machine, current control surface, and bound-area prioritization; do not redesign mining-area selection or work-area UX.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.ai.MinerLoopProgressTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Miner tool/material interruptions keep the same mining area alive, shared blocked feedback covers all required mining inputs, and the area is only marked done/released on real completion or invalidation.</done>
</task>

</tasks>

<verification>
Run `./gradlew test --tests com.talhanation.workers.entities.ai.MinerLoopProgressTest && ./gradlew compileJava -x test`.
</verification>

<success_criteria>
- Miner no longer silently stalls when pickaxe, torch, or cobblestone is missing.
- Normal storage/deposit interruptions preserve the active mining area for later resume.
- Completed mining areas are still marked done and released exactly once.
</success_criteria>

<output>
After completion, create `.planning/phases/07-mining-merchant-flows/07-01-SUMMARY.md`
</output>
