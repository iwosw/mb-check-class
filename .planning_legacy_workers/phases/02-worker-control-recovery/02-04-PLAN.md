---
phase: 02-worker-control-recovery
plan: 04
type: execute
wave: 3
depends_on:
  - 02-02
  - 02-03
files_modified: []
autonomous: false
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
must_haves:
  truths:
    - "Player can issue a worker command and see either an immediate worker reaction or an explicit failure reason."
    - "Player can trigger the new in-game recovery action and get control back over a stuck worker."
    - "Player can observe a blocked or idle reason on the chosen proof path without per-tick message spam."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java"
      provides: "Existing command UI with the new recovery action"
    - path: "src/main/java/com/talhanation/workers/network/MessageRecoverWorkerControl.java"
      provides: "Server-side recovery execution"
    - path: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      provides: "Shared recovery and blocked/idle status behavior"
    - path: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      provides: "Blocked-reason proof seam for storage/item failures"
  key_links:
    - from: "src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java"
      to: "src/main/java/com/talhanation/workers/network/MessageRecoverWorkerControl.java"
      via: "recovery button click"
      pattern: "MessageRecoverWorkerControl"
    - from: "src/main/java/com/talhanation/workers/network/MessageRecoverWorkerControl.java"
      to: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      via: "shared recovery call"
      pattern: "recoverControl"
    - from: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      to: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      via: "blocked reason emission"
      pattern: "reportBlockedReason"
---

<objective>
Prove the Phase 2 worker-control slice end-to-end on one concrete worker path before moving on to broader work-area and persistence phases.

Purpose: The phase boundary explicitly requires one end-to-end command/recovery smoke check plus targeted regression coverage, so this plan turns the new control seam into an approved gameplay proof.
Output: Human-approved smoke verification for command response, recovery, and blocked/idle feedback on the selected farmer proof path.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-worker-control-recovery/02-CONTEXT.md
@.planning/phases/02-worker-control-recovery/02-01-SUMMARY.md
@.planning/phases/02-worker-control-recovery/02-02-SUMMARY.md
@.planning/phases/02-worker-control-recovery/02-03-SUMMARY.md
@src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java
@src/main/java/com/talhanation/workers/network/MessageRecoverWorkerControl.java
@src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify farmer command response, blocked reason, and recovery smoke path</name>
  <files>src/main/java/com/talhanation/workers/client/gui/WorkerCommandScreen.java, src/main/java/com/talhanation/workers/network/MessageRecoverWorkerControl.java, src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java</files>
  <action>Use the farmer path as the concrete proof target per the phase discretion note. This checkpoint verifies the shared control seam, not full farming completion or a redesigned UI.</action>
  <what-built>Existing command screen recovery action plus shared blocked/idle reporting across worker control and storage/profession goals</what-built>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerControlStatusTest && ./gradlew compileJava -x test</automated>
  </verify>
  <how-to-verify>
    1. Run the automated command above and confirm it passes.
    2. Run `./gradlew runClient`, open a creative test world, and spawn a farmer worker.
    3. Use the existing Recruits/Workers command flow to select the farmer group and open the existing worker command screen; confirm the new recovery action is present on that screen and does not replace the existing area actions.
    4. Create a simple farmer proof path: place a crop area through the command screen, omit or break the expected storage/tool prerequisite, and confirm the owner receives one actionable blocked/idle message instead of silence or rapid spam.
    5. Trigger the new recovery action while the farmer is stalled or pathing; confirm the farmer stops the stuck attempt, clears the occupied assignment, and becomes controllable again through the normal command flow.
    6. Restore the missing prerequisite and confirm the farmer can re-enter normal behavior without the old blocked reason continuing to spam.
  </how-to-verify>
  <done>Human verification confirms the command flow responds visibly, the blocked/idle reason is actionable and non-spammy, and the new in-game recovery action restores control on the chosen proof path.</done>
  <resume-signal>Type "approved" or describe the command/recovery/status issue you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 2 is ready to close only after the automated control-state test still passes and the farmer proof path is approved for command response, blocked/idle feedback, and recovery behavior.
</verification>

<success_criteria>
- A player-visible command response exists for the proof-path worker.
- The in-game recovery action restores control over a stuck proof-path worker.
- Blocked/idle feedback appears once per transition and clears after recovery or resumed progress.
</success_criteria>

<output>
After completion, create `.planning/phases/02-worker-control-recovery/02-04-SUMMARY.md`
</output>
