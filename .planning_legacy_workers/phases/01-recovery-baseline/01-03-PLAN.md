---
phase: 01-recovery-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - build.gradle
  - src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java
  - src/main/java/com/talhanation/workers/network/WorkAreaRotation.java
  - src/test/java/com/talhanation/workers/network/WorkAreaRotationTest.java
autonomous: false
requirements:
  - QUAL-03
must_haves:
  truths:
    - "The recovered baseline has one lightweight automated regression check on a pure logic seam."
    - "The dedicated server can start, and the client can complete a spawn-focused smoke path without a critical crash."
    - "Phase 1 proof stays baseline-oriented instead of drifting into deeper profession or UI validation."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/network/WorkAreaRotation.java"
      provides: "Pure logic seam for work-area rotation decisions"
    - path: "src/test/java/com/talhanation/workers/network/WorkAreaRotationTest.java"
      provides: "Automated regression coverage for the extracted seam"
    - path: "src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java"
      provides: "Runtime caller that delegates rotation decisions to the tested seam"
    - path: "build.gradle"
      provides: "JUnit/test task wiring for the lightweight regression check"
  key_links:
    - from: "src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java"
      to: "src/main/java/com/talhanation/workers/network/WorkAreaRotation.java"
      via: "delegated clockwise/counter-clockwise rotation decision"
      pattern: "WorkAreaRotation"
    - from: "build.gradle"
      to: "src/test/java/com/talhanation/workers/network/WorkAreaRotationTest.java"
      via: "JUnit test execution"
      pattern: "testImplementation|useJUnitPlatform"
---

<objective>
Add the minimum regression protection and final baseline proof for Phase 1: one isolated automated test plus explicit client/server smoke verification.

Purpose: D-01, D-07, and D-08 require the phase to prove both runtime startup confidence and a small durable safety net right now, not only later in Phase 9.
Output: Lightweight test harness + seam, and a blocking human verification checkpoint for client/server startup and spawn-focused smoke validation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recovery-baseline/01-CONTEXT.md
@build.gradle
@src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java
@src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
@src/main/java/com/talhanation/workers/init/ModItems.java

<interfaces>
From src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java:
```java
public class MessageRotateWorkArea implements Message<MessageRotateWorkArea> {
    public UUID uuid;
    public boolean clockwise;
    public void rotate(AbstractWorkAreaEntity workArea) { ... }
    public MessageRotateWorkArea fromBytes(FriendlyByteBuf buf) { ... }
    public void toBytes(FriendlyByteBuf buf) { ... }
}
```

From src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java:
```java
public enum MiningMode {
    CUSTOM(0),
    MINE(1),
    TUNNEL(2);

    public int getIndex() { ... }
    public static MiningMode fromIndex(int index) { ... }
}
```

From src/main/java/com/talhanation/workers/init/ModItems.java:
```java
public static final RegistryObject<SpawnEggItem> FARMER_SPAWN_EGG = ...;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the lightweight regression seam and test harness</name>
  <files>build.gradle, src/main/java/com/talhanation/workers/network/WorkAreaRotation.java, src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java, src/test/java/com/talhanation/workers/network/WorkAreaRotationTest.java</files>
  <behavior>
    - Test 1: rotating NORTH clockwise returns EAST.
    - Test 2: rotating NORTH counter-clockwise returns WEST.
    - Test 3: vertical directions or other unsupported inputs preserve/guard behavior explicitly instead of silently breaking the caller contract.
  </behavior>
  <action>Per D-07 and D-08, introduce the smallest useful automated regression check now: add JUnit test wiring in `build.gradle`, extract the pure rotation decision from `MessageRotateWorkArea` into `WorkAreaRotation`, and cover that helper with focused unit tests before the implementation. Keep this seam isolated and baseline-oriented; do not add GameTests, full Forge bootstrap tests, or broad gameplay suites in Phase 1.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaRotationTest</automated>
  </verify>
  <done>A single isolated regression seam is under automated test, the runtime message delegates to it, and the new test runs from Gradle without requiring a game bootstrap.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify dedicated-server startup and spawn-focused smoke path</name>
  <action>Pause after the automated regression seam is in place and have the user verify the recovered baseline runtime per D-01 and D-02. This checkpoint must confirm dedicated-server startup plus one minimal spawn-focused worker interaction; do not expand it into broader gameplay/UIs.</action>
  <what-built>Recovered baseline build/startup path plus one automated regression check</what-built>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.network.WorkAreaRotationTest</automated>
  </verify>
  <how-to-verify>
    1. Run `./gradlew test --tests com.talhanation.workers.network.WorkAreaRotationTest` and confirm it passes.
    2. Run `./gradlew runServer`, wait for the dedicated server to finish startup without a critical crash, then stop it cleanly.
    3. Run `./gradlew runClient`, create or open a creative test world, and obtain a worker spawn egg via the Spawn Eggs tab or `/give @p workers:farmer_spawn_egg`.
    4. Spawn the worker and confirm the entity appears and remains in-world for at least 10 seconds without crashing the client or integrated server.
    5. If any non-core compatibility path was intentionally degraded per D-04/D-09, confirm the limitation is visible in code/comments/SUMMARY rather than being a hidden failure.
  </how-to-verify>
  <done>Human verification explicitly confirms passing dedicated-server startup and the spawn-focused smoke interaction, or returns actionable failure notes for follow-up.</done>
  <resume-signal>Type "approved" or describe the startup/smoke failure.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 proof is complete only after the automated seam test passes and the blocking human checkpoint confirms both dedicated-server startup and the spawn-focused smoke path.
</verification>

<success_criteria>
- One pure logic seam has automated regression coverage.
- Dedicated server startup succeeds without a critical crash.
- Client smoke verification can spawn at least one worker item/entity without crashing.
</success_criteria>

<output>
After completion, create `.planning/phases/01-recovery-baseline/01-03-SUMMARY.md`
</output>
