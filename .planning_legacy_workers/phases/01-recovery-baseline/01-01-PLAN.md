---
phase: 01-recovery-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - gradle.properties
  - settings.gradle
  - src/main/resources/META-INF/mods.toml
autonomous: true
requirements:
  - QUAL-03
must_haves:
  truths:
    - "A clean checkout resolves the baseline dependency/toolchain graph without blocking Gradle errors."
    - "Forge metadata matches the recovered 1.20.1 baseline and does not block startup on required dependencies."
    - "Non-core compatibility dependencies that break baseline startup are either fixed or temporarily gated without expanding scope."
  artifacts:
    - path: "build.gradle"
      provides: "Build, dependency, and test/runtime wiring for the 1.20.1 recovery baseline"
      contains: "minecraft 'net.minecraftforge:forge:1.20.1-47.4.1'"
    - path: "src/main/resources/META-INF/mods.toml"
      provides: "Mod loader and dependency metadata"
      contains: "[[dependencies.workers]]"
    - path: "gradle.properties"
      provides: "Version/property values kept consistent with the recovered baseline"
    - path: "settings.gradle"
      provides: "Plugin/toolchain repository wiring required for a clean baseline build"
  key_links:
    - from: "build.gradle"
      to: "src/main/resources/META-INF/mods.toml"
      via: "matching Forge/mod dependency versions"
      pattern: "1.20.1|47\\.4\\.1|recruits"
    - from: "settings.gradle"
      to: "build.gradle"
      via: "Gradle plugin resolution"
      pattern: "pluginManagement|foojay"
---

<objective>
Recover the build/dependency baseline so the mod can compile and resolve its required 1.20.1 startup metadata without pulling Phase 1 into broader feature work.

Purpose: Remove build-time and metadata blockers first so later startup verification is proving the real mod baseline, not fighting dependency drift.
Output: Corrected Gradle/dependency metadata files ready for compile and startup verification.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recovery-baseline/01-CONTEXT.md
@.planning/research/SUMMARY.md
@build.gradle
@gradle.properties
@settings.gradle
@src/main/resources/META-INF/mods.toml

<interfaces>
From build.gradle:
```groovy
minecraft {
    runs {
        client { property 'forge.enabledGameTestNamespaces', 'workers' }
        server { property 'forge.enabledGameTestNamespaces', 'workers' }
    }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.1-47.4.1'
    implementation fg.deobf("curse.maven:recruits-523860:7374573")
}
```

From src/main/resources/META-INF/mods.toml:
```toml
modId="workers"
loaderVersion="[47,)"

[[dependencies.workers]]
modId="recruits"
mandatory=true
versionRange="[1.14, 1.14.5)"
side="BOTH"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair baseline Gradle and dependency metadata</name>
  <files>build.gradle, gradle.properties, settings.gradle, src/main/resources/META-INF/mods.toml</files>
  <action>Audit and fix the 1.20.1 build/bootstrap path per D-01, D-03, D-04, and D-09. Keep the scope minimal: resolve broken repositories, plugin/toolchain properties, dependency coordinates, and metadata mismatches that stop clean compilation or startup. If a non-core compatibility dependency is the blocking point, gate or disable only that path temporarily per D-04 instead of redesigning the mod or touching gameplay logic. Preserve the 1.20.1 target for v1 and do not introduce 1.21.1 port work here.</action>
  <verify>
    <automated>./gradlew compileJava -x test</automated>
  </verify>
  <done>Gradle resolves from a clean checkout, compile-time dependency/metadata blockers are removed, and any tolerated degraded compatibility path is explicit in code/comments rather than accidental breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the baseline verification entry points to the recovered metadata</name>
  <files>build.gradle, src/main/resources/META-INF/mods.toml</files>
  <action>Align the build/run entry points with the recovered baseline per D-01 and D-05: ensure the existing client and dedicated-server run targets remain the canonical smoke entry points, and make the metadata/version/dependency declarations consistent with those runs. Do not add broader release automation or extra feature toggles in this phase.</action>
  <verify>
    <automated>./gradlew tasks --all</automated>
  </verify>
  <done>`client` and `server` dev runs remain available from Gradle, and mods.toml/build metadata no longer contradict the baseline startup path.</done>
</task>

</tasks>

<verification>
Build metadata verification for Phase 1 is complete when `compileJava` succeeds and the Gradle run tasks needed for later smoke tests are still available.
</verification>

<success_criteria>
- Clean-checkout dependency resolution no longer blocks `./gradlew compileJava -x test`.
- Forge/mod metadata is internally consistent with the recovered 1.20.1 baseline.
- Any temporary compatibility degradation is narrow, intentional, and documented for later phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-recovery-baseline/01-01-SUMMARY.md`
</output>
