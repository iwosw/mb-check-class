---
phase: 06-farming-husbandry
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified: []
autonomous: false
requirements:
  - PROF-01
  - PROF-04
must_haves:
  truths:
    - "A player can watch one farmer complete till/plant/harvest/replant work in an authored crop area without losing the field after a normal interruption."
    - "A player can watch one animal farmer complete the intended breed or cull loop in an authored pen and resume that pen after a normal interruption."
    - "Both agricultural loops still respect the shared storage-request rule before surfacing missing-input complaints."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java"
      provides: "Representative crop-loop proof path"
    - path: "src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java"
      provides: "Representative husbandry-loop proof path"
    - path: "src/test/java/com/talhanation/workers/entities/ai/FarmerLoopProgressTest.java"
      provides: "Automated proof seam for crop continuity"
    - path: "src/test/java/com/talhanation/workers/entities/ai/AnimalFarmerLoopProgressTest.java"
      provides: "Automated proof seam for husbandry continuity"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java"
      to: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      via: "shared item-request handoff"
      pattern: "requestRequiredItem"
    - from: "src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java"
      to: "src/main/java/com/talhanation/workers/entities/workarea/AnimalPenArea.java"
      via: "current-pen scans and busy-state release"
      pattern: "scanAnimalBreed|scanAnimalSlaughter|setBeingWorkedOn"
---

<objective>
Prove the farming and husbandry fixes on one farmer path and one animal-farmer path before advancing to later profession phases.

Purpose: D-04 requires runtime proof for both agricultural professions, so this plan turns the new automated seams into explicit in-game acceptance for crop and pen continuity.
Output: Passing automated verification plus human-approved farmer and animal-farmer smoke checks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-farming-husbandry/06-CONTEXT.md
@.planning/phases/06-farming-husbandry/06-01-SUMMARY.md
@.planning/phases/06-farming-husbandry/06-02-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/workarea/AnimalPenArea.java
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify representative farmer and animal-farmer loops end-to-end</name>
  <files>src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java, src/main/java/com/talhanation/workers/entities/workarea/AnimalPenArea.java</files>
  <action>Use one farmer path and one animal-farmer path as the required runtime proof per D-04. Verify only the intended existing agricultural loops per D-01: crop work in a crop area and breed/cull work in an animal pen. While testing, preserve the Phase 5 shared storage-request rule from D-02 and focus interruption checks on normal missing-item or forced-deposit cases per D-03, not on new feature scenarios.</action>
  <what-built>Farmer and animal-farmer continuity fixes with targeted automated seams</what-built>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.ai.FarmerLoopProgressTest --tests com.talhanation.workers.entities.ai.FarmerPlantingPreparationTest --tests com.talhanation.workers.entities.ai.FarmerAreaSelectionTimingTest --tests com.talhanation.workers.entities.ai.AnimalFarmerLoopProgressTest && ./gradlew compileJava -x test</automated>
  </verify>
  <how-to-verify>
    1. Run the automated command above and confirm it passes.
    2. Run `./gradlew runClient`, open a test world, and set up one farmer with an authored crop area plus valid storage containing a hoe, seeds, and any needed water bucket.
    3. Confirm the farmer tills, plants, harvests, and replants inside that same crop area. Then remove a required input from the farmer inventory while leaving it in configured storage; confirm the farmer uses the shared storage path first and resumes the same field instead of dropping the crop area.
    4. Set up one animal farmer with an authored animal pen configured for the most representative current loop in code (breed plus slaughter if both are enabled, otherwise the enabled path). Ensure storage holds the needed breed item and an axe.
    5. Confirm the animal farmer works only inside that pen, completes the intended breed or cull behavior, and after a normal interruption such as forced deposit or temporary missing feed/axe, resumes the same pen instead of drifting to another pen or leaving it permanently marked busy.
    6. If you intentionally remove the required husbandry or farming input from both inventory and storage, confirm the missing-item complaint appears only after the shared storage path fails.
  </how-to-verify>
  <done>Human verification confirms both representative agricultural professions operate in their intended area/pen, survive normal interruptions, and only complain after storage has been tried.</done>
  <resume-signal>Type "approved" or describe the farming/husbandry issue you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 6 is only ready to close after both new automated seams pass, the code compiles, and the representative farmer and animal-farmer smoke checks are approved.
</verification>

<success_criteria>
- A farmer completes the expected crop loop inside the assigned field and keeps that field through normal interruptions.
- An animal farmer completes the intended pen loop and resumes the same pen after normal interruptions.
- Missing-input complaints for both professions still obey the shared storage-first rule.
</success_criteria>

<output>
After completion, create `.planning/phases/06-farming-husbandry/06-03-SUMMARY.md`
</output>
