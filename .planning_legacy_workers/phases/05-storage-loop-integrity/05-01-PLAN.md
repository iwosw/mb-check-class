---
phase: 05-storage-loop-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/workers/entities/WorkerStorageRequestState.java
  - src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java
  - src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java
  - src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java
  - src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
  - src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
  - src/main/java/com/talhanation/workers/entities/ai/FishermanWorkGoal.java
  - src/main/java/com/talhanation/workers/entities/ai/LumberjackWorkGoal.java
  - src/test/java/com/talhanation/workers/entities/WorkerStorageRequestStateTest.java
autonomous: true
requirements:
  - STOR-02
must_haves:
  truths:
    - "A worker only complains about missing tools or materials after checking its inventory and exhausting the configured storage path."
    - "A successful storage fetch clears the pending missing-item stall instead of leaving stale blocked feedback behind."
    - "Profession goals still request the same tools/materials, but they now hand complaint timing to one shared rule."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/entities/WorkerStorageRequestState.java"
      provides: "Shared pending-request and complaint-gating state for missing-item flows"
    - path: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      provides: "Base worker helpers for inventory-first missing-item requests"
    - path: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      provides: "Shared storage-failure complaint handoff"
    - path: "src/test/java/com/talhanation/workers/entities/WorkerStorageRequestStateTest.java"
      provides: "Automated coverage for complaint gating behavior"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java"
      to: "src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      via: "shared missing-item request helper"
      pattern: "requestRequiredItem|queueRequiredItem"
    - from: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      to: "src/main/java/com/talhanation/workers/entities/WorkerStorageRequestState.java"
      via: "storage exhaustion and success transitions"
      pattern: "storage_missing|storage_missing_items|clear"
---

<objective>
Create the shared missing-item request seam so Phase 5 enforces inventory-first, storage-second complaint timing everywhere it matters.

Purpose: D-01 locks the complaint rule, so profession goals must stop shouting early and instead hand missing-item feedback to one shared storage-aware path.
Output: A tested worker request-state helper plus wiring from profession goals through the shared storage fetch goal.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-storage-loop-integrity/05-CONTEXT.md
@.planning/phases/04-persistence-binding-resume/04-CONTEXT.md
@.planning/phases/02-worker-control-recovery/02-03-SUMMARY.md
@.planning/phases/02-worker-control-recovery/02-04-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java
@src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/FishermanWorkGoal.java
@src/main/java/com/talhanation/workers/entities/ai/LumberjackWorkGoal.java

<interfaces>
From src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java:
```java
public List<NeededItem> neededItems = new ArrayList<>();
public boolean needsToGetItems() {
    return neededItems.stream().anyMatch(neededItem -> neededItem.required);
}
public void addNeededItem(NeededItem neededItem)
public void reportBlockedReason(String reasonToken, Component message)
public void clearWorkStatus()
```

From src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java:
```java
public boolean canUse() {
    return !worker.needsToDeposit() && worker.needsToGetItems() && super.canUse();
}
case ERROR_NO_STORAGE_FOUND -> { ... }
case ERROR_ITEM_NOT_IN_STORAGE -> { ... }
case DONE -> {
    worker.clearWorkStatus();
    worker.neededItems.clear();
    worker.lastStorage = this.storageArea.getUUID();
}
```
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a shared storage-request state helper with regression coverage</name>
  <files>src/main/java/com/talhanation/workers/entities/WorkerStorageRequestState.java, src/test/java/com/talhanation/workers/entities/WorkerStorageRequestStateTest.java</files>
  <behavior>
    - Test 1: Recording a required-item request keeps the complaint pending instead of immediately surfacing a blocked reason.
    - Test 2: Marking the storage path exhausted releases the stored complaint details exactly once.
    - Test 3: Clearing or satisfying the request removes the pending complaint so successful fetches do not leave stale blocked state behind.
  </behavior>
  <action>Create a small pure-Java helper that stores pending missing-item complaint metadata for a worker per D-01 and D-05. The helper must separate "item requested" from "complaint may be surfaced", support a clean success/reset path, and avoid inventing any new logistics system or profession-specific branching per D-04.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerStorageRequestStateTest</automated>
  </verify>
  <done>The helper and test exist, the tests pass, and the helper expresses deferred complaint timing for missing-item requests.</done>
</task>

<task type="auto">
  <name>Task 2: Route profession missing-item requests through the shared inventory-first gate</name>
  <files>src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java, src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/AnimalFarmerWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/BuilderWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/FishermanWorkGoal.java, src/main/java/com/talhanation/workers/entities/ai/LumberjackWorkGoal.java</files>
  <action>Add a shared worker helper that profession goals use when they discover a missing tool or material: it must first honor already-carried inventory, then queue the `NeededItem`, and only let `GetNeededItemsFromStorage` surface the stored blocked reason after the storage path truly fails per D-01. Update the listed profession goals to stop calling `reportBlockedReason(...)` at request time, preserve their existing `NeededItem` semantics and work-loop states per D-02/D-04, and make the shared storage goal clear the pending complaint on successful fetch or restart while preserving `lastStorage` affinity behavior.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerStorageRequestStateTest && ./gradlew compileJava -x test</automated>
  </verify>
  <done>Missing-item requests across the shared profession paths obey inventory-first and storage-second complaint timing without changing the brownfield job-loop shape.</done>
</task>

</tasks>

<verification>
The new complaint-gating test must pass, and the project must still compile after all representative profession goals delegate missing-item timing to the shared storage seam.
</verification>

<success_criteria>
- Shared worker logic now owns when missing-item complaints become visible.
- Profession goals still request the same tools/materials, but do not falsely block before storage has been tried.
- A successful fetch clears stale blocked state from the request path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-loop-integrity/05-01-SUMMARY.md`
</output>
