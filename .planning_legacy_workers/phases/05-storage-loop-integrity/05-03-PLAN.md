---
phase: 05-storage-loop-integrity
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
  - 05-02
files_modified: []
autonomous: false
requirements:
  - STOR-02
  - STOR-03
  - STOR-04
must_haves:
  truths:
    - "A player can watch one representative worker fetch required items, work, and deposit surplus without false missing-item complaints or reserve loss."
    - "The representative worker keeps the essentials needed for the next loop instead of depositing them away."
    - "Phase 5 closes with proof on an existing gameplay path instead of a synthetic storage demo."
  artifacts:
    - path: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      provides: "Shared withdraw complaint timing"
    - path: "src/main/java/com/talhanation/workers/entities/ai/DepositItemsToStorage.java"
      provides: "Shared reserve-aware deposit behavior"
    - path: "src/main/java/com/talhanation/workers/entities/FarmerEntity.java"
      provides: "Representative fetch/work/deposit proof path"
  key_links:
    - from: "src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java"
      to: "src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java"
      via: "missing seed/tool requests"
      pattern: "NeededItem|storage_missing"
    - from: "src/main/java/com/talhanation/workers/entities/ai/DepositItemsToStorage.java"
      to: "src/main/java/com/talhanation/workers/entities/FarmerEntity.java"
      via: "reserve-aware deposit filtering"
      pattern: "wantsToKeep"
---

<objective>
Prove the shared storage-loop fixes on one real worker path before moving into profession-completion phases.

Purpose: D-05 requires one representative runtime proof, and the farmer path exercises tool/material fetch, work, pickup, deposit, and reserve retention with minimal unrelated complexity.
Output: Human-approved farmer storage-loop verification plus passing automated regression commands.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-storage-loop-integrity/05-CONTEXT.md
@.planning/phases/05-storage-loop-integrity/05-01-SUMMARY.md
@.planning/phases/05-storage-loop-integrity/05-02-SUMMARY.md
@src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java
@src/main/java/com/talhanation/workers/entities/ai/DepositItemsToStorage.java
@src/main/java/com/talhanation/workers/entities/FarmerEntity.java
@src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify one farmer fetch-and-deposit loop end-to-end</name>
  <files>src/main/java/com/talhanation/workers/entities/ai/GetNeededItemsFromStorage.java, src/main/java/com/talhanation/workers/entities/ai/DepositItemsToStorage.java, src/main/java/com/talhanation/workers/entities/FarmerEntity.java, src/main/java/com/talhanation/workers/entities/ai/FarmerWorkGoal.java</files>
  <action>Use the farmer path as the representative smoke target per D-05 because it naturally exercises missing tools/seeds, storage fetch, harvesting pickup, surplus deposit, and reserve retention. Verify the shared rules exactly as locked in D-01 and D-03; do not broaden the scenario into profession redesign or logistics-system testing per D-04.</action>
  <what-built>Shared inventory-first missing-item gating plus reserve-aware deposit behavior on a representative worker path</what-built>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.entities.WorkerStorageRequestStateTest --tests com.talhanation.workers.entities.ai.StorageDepositRulesTest && ./gradlew compileJava -x test</automated>
  </verify>
  <how-to-verify>
    1. Run the automated command above and confirm it passes.
    2. Run `./gradlew runClient`, open a test world, and set up one farmer with a crop area and one valid storage area containing seeds/tools the farmer may need.
    3. Force a fetch case by removing the farmer's hoe or seeds from its inventory while leaving them in configured storage; confirm the farmer does not immediately complain and instead walks the shared storage path first.
    4. Confirm the missing-item complaint only appears if both the carried inventory and configured storage path fail to satisfy the need.
    5. Let the farmer harvest enough items to trigger deposit behavior, then confirm surplus goes into storage without disappearing or duplicating.
    6. After deposit, confirm the farmer still keeps its essential reserve items (for example its working hoe and the seed reserve needed for the next planting pass) instead of depositing them all away.
  </how-to-verify>
  <done>Human verification confirms one representative worker now fetches before complaining, deposits surplus safely, and keeps loop-critical reserve items.</done>
  <resume-signal>Type "approved" or describe the storage-loop issue you observed.</resume-signal>
</task>

</tasks>

<verification>
Phase 5 is only ready to close after both automated seams pass, the project compiles, and the representative farmer storage loop is approved on the existing in-game flow.
</verification>

<success_criteria>
- A farmer fetches required items before surfacing a missing-item complaint.
- The same farmer deposits surplus without item loss or duplication.
- The farmer keeps loop-critical reserves after deposit.
</success_criteria>

<output>
After completion, create `.planning/phases/05-storage-loop-integrity/05-03-SUMMARY.md`
</output>
