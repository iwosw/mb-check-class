---
phase: 06-player-cycle-gametest-validation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settlement-side supply pressure and recruit upkeep readiness are validated as one live root GameTest slice."
    - "A same-owner settlement source can move recruit upkeep from blocked to ready in the merged runtime."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java"
      provides: "Dedicated upkeep flow slice validation"
      contains: "upkeep"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java"
      to: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      via: "owned build-area and template helpers"
      pattern: "createMinimalBuildTemplate|spawnOwnedBuildArea"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java"
      via: "shared supply-status assertions"
      pattern: "BannerModSupplyStatus"
---

<objective>
Add a dedicated root GameTest that validates settlement-side supply and recruit upkeep readiness as one player-facing transition.

Purpose: Prove that the merged runtime does more than expose passive supply vocabulary — it must validate that one player-owned settlement source can unblock military upkeep readiness in a live root GameTest slice.
Output: One dedicated upkeep-flow root GameTest class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/06-player-cycle-gametest-validation/06-CONTEXT.md
@.planning/phases/06-player-cycle-gametest-validation/06-RESEARCH.md
@.planning/phases/06-player-cycle-gametest-validation/06-VALIDATION.md
@src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
@src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java

<interfaces>
Use the helper signatures created in Plan 01:

```java
BannerModGameTestSupport.spawnOwnedBuildArea(GameTestHelper helper, Player player, BlockPos relativePos)
BannerModGameTestSupport.createMinimalBuildTemplate()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate same-owner settlement supply to recruit upkeep readiness in dedicated root GameTests</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java</files>
  <read_first>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, .planning/phases/06-player-cycle-gametest-validation/06-RESEARCH.md, src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java, recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java, workers/src/main/java/com/talhanation/workers/entities/workarea/BuildArea.java</read_first>
  <acceptance_criteria>
    - `src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java` contains `BannerModSupplyStatus.BuildState.NEEDS_MATERIALS`
    - `src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java` contains `BannerModSupplyStatus.RecruitSupplyState.NEEDS_FOOD_AND_PAYMENT`
    - `src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java` contains `BannerModSupplyStatus.RecruitSupplyState.READY`
    - `src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java` asserts the same player owns both the build area and the recruit upkeep source
    - `./gradlew verifyGameTestStage` exits 0
  </acceptance_criteria>
  <action>Create `BannerModUpkeepFlowGameTests.java` as a dedicated `harness_empty` GameTest class that reproduces and preserves the upkeep assertions documented in `06-RESEARCH.md`. Spawn one player-owned recruit plus one player-owned build area through `BannerModGameTestSupport.spawnOwnedBuildArea(...)`, apply `BannerModGameTestSupport.createMinimalBuildTemplate()`, push exactly four oak-plank `BuildBlock` entries, set `recruit.forcedUpkeep = true`, `recruit.paymentTimer = 0`, `recruit.setHunger(1.0F)`, and `recruit.setUpkeepPos(buildArea.blockPosition())`, then assert the blocked state through `BannerModSupplyStatus.BuildState.NEEDS_MATERIALS` and `BannerModSupplyStatus.RecruitSupplyState.NEEDS_FOOD_AND_PAYMENT`. After placing bread and 16 currency items into the upkeep container, assert the recruit status becomes `READY` and `blocked()` becomes false. Keep the explicit reason-token assertion for `recruit_upkeep_missing_food_and_payment` so the shared supply vocabulary stays pinned by a root GameTest. Do not modify production gameplay code in this plan unless the new dedicated GameTest exposes a real runtime defect.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest suite proves a same-owner settlement source can move recruit upkeep from blocked to ready through the shared BannerMod supply vocabulary.</done>
</task>

</tasks>

<verification>
Run the full root GameTest stage and confirm the dedicated upkeep-flow class passes with the ownership and runtime-smoke slices already established by earlier plans.
</verification>

<success_criteria>
- Upkeep readiness is validated in its own root GameTest artifact
- The shared BannerMod supply vocabulary stays pinned by explicit blocked and ready assertions
- The test remains additive and deterministic
</success_criteria>

<output>
After completion, create `.planning/phases/06-player-cycle-gametest-validation/06-player-cycle-gametest-validation-03-SUMMARY.md`
</output>
