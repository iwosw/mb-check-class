---
phase: 06-player-cycle-gametest-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
  - src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "Root GameTests still prove the merged `bannermod` runtime seam after Phase 06 refactoring starts."
    - "A dedicated root GameTest proves recruit-worker shared ownership and divergent-ownership boundaries for one player-facing slice."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      provides: "Reusable root BannerMod GameTest spawn/build helpers"
      contains: "spawnOwnedFarmer"
    - path: "src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java"
      provides: "Retained runtime smoke coverage"
      contains: "recruitAndWorkerSeamsCoexistInBannerModRuntime"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java"
      provides: "Dedicated ownership slice validation"
      contains: "sharedOwnership"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java"
      to: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      via: "shared spawn/build helpers"
      pattern: "BannerModGameTestSupport"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java"
      to: "recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      via: "recruit spawn positions and loadout helpers"
      pattern: "RecruitsBattleGameTestSupport"
---

<objective>
Establish the reusable root BannerMod GameTest helper contract, keep the existing runtime smoke green, and move the shared-ownership slice into its own dedicated root GameTest class.

Purpose: Give the remaining Phase 06 plans a stable helper surface and a small dedicated ownership artifact instead of forcing all later work through one growing monolithic test file.
Output: One shared helper file, one trimmed runtime-smoke file, and one ownership-focused root GameTest class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/06-player-cycle-gametest-validation/06-CONTEXT.md
@.planning/phases/06-player-cycle-gametest-validation/06-RESEARCH.md
@.planning/phases/06-player-cycle-gametest-validation/06-VALIDATION.md
@src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java

<interfaces>
Create these exact helper signatures in `BannerModGameTestSupport.java`:

```java
public static FarmerEntity spawnOwnedFarmer(GameTestHelper helper, Player player, BlockPos relativePos)
public static CropArea spawnOwnedCropArea(GameTestHelper helper, Player player, BlockPos relativePos)
public static StorageArea spawnOwnedStorageArea(GameTestHelper helper, Player player, BlockPos relativePos)
public static BuildArea spawnOwnedBuildArea(GameTestHelper helper, Player player, BlockPos relativePos)
public static CompoundTag createMinimalBuildTemplate()
public static <T extends Entity> T spawnEntity(GameTestHelper helper, EntityType<T> entityType, BlockPos relativePos)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the shared BannerMod root GameTest support contract</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java</files>
  <read_first>src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java</read_first>
  <acceptance_criteria>
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java` contains `spawnOwnedFarmer(GameTestHelper helper, Player player, BlockPos relativePos)`
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java` contains `spawnOwnedCropArea(GameTestHelper helper, Player player, BlockPos relativePos)`
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java` contains `spawnOwnedStorageArea(GameTestHelper helper, Player player, BlockPos relativePos)`
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java` contains `spawnOwnedBuildArea(GameTestHelper helper, Player player, BlockPos relativePos)`
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java` contains `createMinimalBuildTemplate()`
    - `./gradlew compileGameTestJava` exits 0
  </acceptance_criteria>
  <action>Create `BannerModGameTestSupport.java` in the root `src/gametest/java/com/talhanation/bannermod/` package and move the reusable spawn/build helper logic out of `IntegratedRuntimeGameTests.java`. Implement the exact helper methods listed in the interfaces block so later plans can call them without rediscovery. `spawnOwnedFarmer(...)` must create `com.talhanation.workers.init.ModEntityTypes.FARMER`, set custom name visibility, mark the worker owned, set the owner UUID to the mock player's UUID, set follow state `2`, and set hold position to the worker's current block center. Each owned work-area helper must set the player UUID, player name, and deterministic dimensions (`CropArea`: width 3, depth 3, height 2; `StorageArea`: width 3, depth 3, height 2; `BuildArea`: width 3, depth 3, height 4). `createMinimalBuildTemplate()` must build the same 4-plank vertical template currently used in `IntegratedRuntimeGameTests.java`. Do not change production gameplay code in this task.</action>
  <verify>
    <automated>./gradlew compileGameTestJava</automated>
  </verify>
  <done>The root GameTest source set exposes one stable BannerMod helper file that later slice plans can import directly.</done>
</task>

<task type="auto">
  <name>Task 2: Split ownership validation into a dedicated root GameTest while retaining runtime smoke</name>
  <files>src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java</files>
  <read_first>src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java, recruits/src/main/java/com/talhanation/recruits/RecruitEvents.java</read_first>
  <acceptance_criteria>
    - `src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java` still contains `recruitAndWorkerSeamsCoexistInBannerModRuntime`
    - `src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java` no longer contains the ownership slice method body now moved to a dedicated file
    - `src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java` contains a `@GameTest(template = "harness_empty")` method proving shared-owner recruit and worker non-hostility
    - `src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java` contains an assertion that divergent ownership restores hostility
    - `./gradlew verifyGameTestStage` exits 0
  </acceptance_criteria>
  <action>Trim `IntegratedRuntimeGameTests.java` down to the merged runtime smoke test only: keep `recruitAndWorkerSeamsCoexistInBannerModRuntime(...)` and delete the embedded ownership/supply slice methods that Phase 06 is breaking into dedicated files. Create `BannerModOwnershipCycleGameTests.java` with one `harness_empty` GameTest that recreates the current shared-owner slice using `BannerModGameTestSupport` and `RecruitsBattleGameTestSupport`: spawn one owned recruit, one worker, and one owned crop area; assert pre-sync hostility is allowed; then sync the worker owner to the player and assert `RecruitEvents.canAttack(recruit, worker)` and `RecruitEvents.canAttack(worker, recruit)` both become false, `cropArea.canWorkHere(worker)` becomes true, and `worker.recoverControl(player)` clears the claimed work area. End by changing the worker owner UUID to a random UUID and assert hostility and crop-area rejection return. Keep the failure messages explicit and player-cycle oriented.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest baseline keeps the original runtime seam smoke green and now exposes a dedicated ownership slice artifact for later execution summaries and regressions.</done>
</task>

</tasks>

<verification>
Compile the root GameTest source set after the helper extraction, then run the full `verifyGameTestStage` gate to prove the new ownership slice and retained runtime smoke both pass in the merged runtime.
</verification>

<success_criteria>
- Root runtime-smoke coverage still exists in `IntegratedRuntimeGameTests.java`
- Shared ownership and divergent-ownership behavior now live in a dedicated root GameTest file
- Later Phase 06 plans can import `BannerModGameTestSupport` instead of copying spawn/build helpers
</success_criteria>

<output>
After completion, create `.planning/phases/06-player-cycle-gametest-validation/06-player-cycle-gametest-validation-01-SUMMARY.md`
</output>
