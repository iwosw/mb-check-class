---
phase: 06-player-cycle-gametest-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "An owned worker can bind to settlement infrastructure and legally participate in an owned work area."
    - "Settlement labor validation distinguishes work-area authorization from owner-only control recovery."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java"
      provides: "Dedicated settlement labor slice validation"
      contains: "settlement"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java"
      to: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      via: "owned worker and work-area spawning"
      pattern: "BannerModGameTestSupport"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      via: "work-area authorization assertions"
      pattern: "canWorkHere"
---

<objective>
Add a dedicated root GameTest that proves owned worker participation in settlement infrastructure under the merged authority model.

Purpose: Move Phase 06 beyond raw coexistence by validating that the same player can establish legal labor participation in owned work areas without confusing same-team access with owner-only recovery authority.
Output: One dedicated settlement labor root GameTest class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-player-cycle-gametest-validation/06-CONTEXT.md
@.planning/phases/06-player-cycle-gametest-validation/06-RESEARCH.md
@.planning/phases/06-player-cycle-gametest-validation/06-VALIDATION.md
@src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java

<interfaces>
Use the helper signatures created in Plan 01:

```java
BannerModGameTestSupport.spawnOwnedFarmer(GameTestHelper helper, Player player, BlockPos relativePos)
BannerModGameTestSupport.spawnOwnedCropArea(GameTestHelper helper, Player player, BlockPos relativePos)
BannerModGameTestSupport.spawnOwnedStorageArea(GameTestHelper helper, Player player, BlockPos relativePos)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate owned worker settlement labor participation in dedicated root GameTests</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java</files>
  <read_first>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java</read_first>
  <acceptance_criteria>
    - `src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java` contains `@GameTest(template = "harness_empty")`
    - `src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java` asserts `cropArea.canWorkHere(worker)` for a same-owner worker
    - `src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java` asserts a second infrastructure node such as `StorageArea` remains owned by the same player as the worker
    - `src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java` asserts a non-owner requester cannot recover control of the worker
    - `./gradlew verifyGameTestStage` exits 0
  </acceptance_criteria>
  <action>Create `BannerModSettlementLaborGameTests.java` with one `harness_empty` GameTest that proves a player-owned worker can bind to owned settlement infrastructure. Use `BannerModGameTestSupport.spawnOwnedFarmer(...)`, `spawnOwnedCropArea(...)`, and `spawnOwnedStorageArea(...)`. In the test, create `ownerPlayer = helper.makeMockPlayer()` and `outsiderPlayer = helper.makeMockPlayer()`. Spawn the worker and both work areas for `ownerPlayer`, set the worker's current crop area, mark the crop area as being worked on, and assert `cropArea.canWorkHere(worker)` is true plus `storageArea.getPlayerUUID().equals(ownerPlayer.getUUID())` is true. Then call `worker.recoverControl(outsiderPlayer)` and assert it returns false, `cropArea.isBeingWorkedOn()` is still true, and `worker.getCurrentWorkArea()` still points at the crop area so the test proves labor authorization is separate from owner-only recovery control. Keep the slice additive-only; do not change production authority logic unless the GameTest exposes a real defect.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest suite proves settlement labor participation under the merged BannerMod authority model instead of only proving recruit-worker coexistence.</done>
</task>

</tasks>

<verification>
Run the full root GameTest stage and confirm the new settlement labor class passes alongside the runtime smoke and ownership slice from Plan 01.
</verification>

<success_criteria>
- One root GameTest explicitly covers worker participation in owned settlement infrastructure
- The test distinguishes work-area authorization from owner-only control recovery
- No production gameplay rewrite is introduced unless a failing test proves a bug
</success_criteria>

<output>
After completion, create `.planning/phases/06-player-cycle-gametest-validation/06-player-cycle-gametest-validation-02-SUMMARY.md`
</output>
