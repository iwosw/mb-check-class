---
phase: 06-player-cycle-gametest-validation
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "One root GameTest proves the stitched BannerMod player cycle from ownership to labor to upkeep to authority-safe recovery."
    - "The stitched cycle ends with explicit authority expectations instead of implied success."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java"
      provides: "Full player-cycle root GameTest"
      contains: "playerCycle"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java"
      to: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      via: "shared scenario setup"
      pattern: "BannerModGameTestSupport"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java"
      via: "recovery/ownership end-state expectations"
      pattern: "BannerModAuthorityRules|recoverControl"
---

<objective>
Add the stitched root GameTest that validates the BannerMod player cycle end to end: ownership, labor participation, upkeep transition, and authority-safe recovery in one runtime flow.

Purpose: Close Phase 06 with one player-facing BannerMod scenario that demonstrates the merged systems operate coherently together rather than only as separate slice tests.
Output: One stitched full-cycle root GameTest class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/06-player-cycle-gametest-validation/06-CONTEXT.md
@.planning/phases/06-player-cycle-gametest-validation/06-RESEARCH.md
@.planning/phases/06-player-cycle-gametest-validation/06-VALIDATION.md
@src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java
@src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java
@src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
@src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate the stitched BannerMod player cycle in one root GameTest flow</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java</files>
  <read_first>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java, src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java, src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java</read_first>
  <acceptance_criteria>
    - `src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java` contains `@GameTest(template = "harness_empty")`
    - `src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java` asserts shared-owner recruit and worker coexistence
    - `src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java` asserts worker participation in an owned work area
    - `src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java` asserts recruit upkeep transitions from blocked to ready
    - `src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java` asserts worker recovery clears the claimed work area or otherwise ends in an explicit authority-safe state
    - `./gradlew verifyGameTestStage` exits 0
  </acceptance_criteria>
  <action>Create `BannerModPlayerCycleGameTests.java` with one `harness_empty` GameTest that stitches the earlier slice behaviors into one ordered scenario. Use one mock player who owns: (1) a configured recruit from `RecruitsBattleGameTestSupport.spawnConfiguredRecruit(...)`, (2) a farmer from `BannerModGameTestSupport.spawnOwnedFarmer(...)`, (3) an owned crop area, and (4) an owned build area with the minimal plank template. First assert the shared-owner recruit and worker do not attack each other. Next bind the worker to the crop area, mark it being worked on, and assert `cropArea.canWorkHere(worker)` is true. Then configure recruit upkeep against the same build-area position, assert blocked upkeep before resupply, add bread plus 16 currency to the upkeep container, and assert the recruit reaches `BannerModSupplyStatus.RecruitSupplyState.READY`. Finish with an authority-safe recovery step: call `worker.recoverControl(player)`, assert the crop area is released and the worker current work area is null, then optionally diverge ownership and assert hostility or work-area rejection returns so the cycle ends with an explicit boundary check instead of a silent pass. Do not introduce deep persistence or runtime rewrites; this plan exists to prove the stitched loop under the current merged runtime.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest suite contains one end-to-end BannerMod player-cycle scenario covering ownership, labor, upkeep, and recovery in one deterministic runtime flow.</done>
</task>

</tasks>

<verification>
Run the full root GameTest gate and confirm the stitched player-cycle class passes together with the dedicated runtime, ownership, settlement-labor, and upkeep-flow classes.
</verification>

<success_criteria>
- One root GameTest now demonstrates the full BannerMod player cycle in sequence
- The stitched flow reuses the earlier slice contracts rather than inventing a new parallel approach
- The scenario ends with explicit authority-safe recovery assertions
</success_criteria>

<output>
After completion, create `.planning/phases/06-player-cycle-gametest-validation/06-player-cycle-gametest-validation-04-SUMMARY.md`
</output>
