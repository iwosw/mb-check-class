---
phase: 07-dedicated-server-authority-edge-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
  - recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
  - workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
autonomous: true
requirements:
  - DSAUTH-02
must_haves:
  truths:
    - "A player reconnecting with the same UUID regains recruit command authority without manual rebinding."
    - "A player reconnecting with the same UUID regains worker recovery authority and can safely release the claimed work area."
    - "Recruit, worker, and work-area ownership survive an entity save/load round trip so reconnect behavior stays persistence-safe."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java"
      provides: "Dedicated-server reconnect and persistence-safe ownership recovery coverage"
      contains: "reconnectedOwner"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java"
      provides: "Shared save/load helpers reused for reconnect coverage"
      contains: "loadEntity"
    - path: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      provides: "Persisted work-area ownership and dimensions for reconnect tests"
      contains: "readAdditionalSaveData"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java"
      to: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java"
      via: "save/load roundtrip and fake reconnect player helpers"
      pattern: "loadEntity|createFakeServerPlayer"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java"
      to: "recruits/src/main/java/com/talhanation/recruits/network/MessageMovement.java"
      via: "reconnected owner command recovery"
      pattern: "MessageMovement\\.dispatchToServer"
---

<objective>
Validate the reconnect path for dedicated-server ownership by proving the same UUID regains recruit and worker authority after owner absence, including a persistence-safe save/load round trip.

Purpose: DSAUTH-02 closes the dedicated-server loop by proving authority returns only to the correct reconnecting owner and that entity serialization does not silently drop ownership needed for that recovery.
Output: One reconnect-focused GameTest class plus any minimal persistence fix needed for recruit, worker, or work-area ownership serialization.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/07-dedicated-server-authority-edge-validation/07-CONTEXT.md
@.planning/phases/07-dedicated-server-authority-edge-validation/07-01-PLAN.md
@.planning/phases/06-player-cycle-gametest-validation/06-player-cycle-gametest-validation-04-SUMMARY.md
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerAuthorityGameTests.java
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@recruits/src/main/java/com/talhanation/recruits/network/MessageMovement.java

<interfaces>
Reuse these helper contracts from Plan 07-01:

From `src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java`:
```java
public static Player createFakeServerPlayer(ServerLevel level, UUID playerId, String name)
public static CompoundTag saveEntity(Entity entity)
public static <T extends Entity> T loadEntity(GameTestHelper helper, EntityType<T> entityType, BlockPos relativePos, CompoundTag savedData)
```

From `recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java`:
```java
public UUID getOwnerUUID()
public void setOwnerUUID(Optional<UUID> id)
public void addAdditionalSaveData(CompoundTag nbt)
public void readAdditionalSaveData(CompoundTag nbt)
```

From `workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java`:
```java
public UUID getPlayerUUID()
public void setPlayerUUID(UUID playerUUID)
public void addAdditionalSaveData(CompoundTag tag)
public void readAdditionalSaveData(CompoundTag tag)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reconnect-path dedicated-server authority tests</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java, recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java, workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java</files>
  <behavior>
    - Test 1: Reconnecting with the same UUID restores recruit command authority and worker recovery authority without changing ownership records.
    - Test 2: After a representative save/load round trip, recruit, worker, and work-area ownership still match the reconnecting UUID and the worker can legally recover from the owned work area.
  </behavior>
  <action>Create `BannerModDedicatedServerReconnectGameTests.java` with two `harness_empty` GameTests. First, model a reconnect without persistence reload: spawn offline-owned recruits, a worker, and a crop area using the detached-owner helpers from Plan 07-01, then create a fake reconnect player with the exact same UUID. Dispatch `MessageMovement.dispatchToServer(reconnectedPlayer, reconnectedPlayer.getUUID(), TARGET_GROUP_UUID, 1, 0, false)` and assert the targeted recruits now change follow state while the same player can also call `worker.recoverControl(reconnectedPlayer)` and release the crop area. Second, model a persistence-safe reconnect: save the recruit, worker, and work area with `saveEntity(...)`, spawn fresh instances via `loadEntity(...)`, assert all three still carry the original owner UUID, then create a reconnect player with that UUID and prove `cropArea.canWorkHere(worker)` plus `worker.recoverControl(reconnectedPlayer)` both succeed. If these tests expose serialization or reconnect defects, patch only the smallest relevant file in `<files>` so DSAUTH-02 holds without inventing a new persistence manager.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest suite proves the correct reconnecting UUID regains recruit and worker authority, and ownership survives the representative save/load round trip.</done>
</task>

</tasks>

<verification>
Run the full `verifyGameTestStage` gate after the reconnect tests land so the new dedicated-server reconnect slice is proven alongside the previously added offline-owner denial slice.
</verification>

<success_criteria>
- Reconnecting with the same UUID restores both recruit command control and worker recovery authority
- Recruit, worker, and work-area ownership all survive the save/load round trip used by the new GameTests
- Any runtime fix remains a minimal serialization or authority correction rather than a new manager or packet rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/07-dedicated-server-authority-edge-validation/07-dedicated-server-authority-edge-validation-02-SUMMARY.md`
</output>
