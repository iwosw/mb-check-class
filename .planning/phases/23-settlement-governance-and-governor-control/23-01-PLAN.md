---
phase: 23-settlement-governance-and-governor-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorSnapshot.java
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java
  - src/test/java/com/talhanation/bannermod/governance/BannerModGovernorRulesTest.java
autonomous: true
requirements:
  - GOV-01
must_haves:
  truths:
    - "Settlement governance state exists as an explicit persisted seam instead of living as ad hoc fields on claims or recruits."
    - "A governor assignment can be described against a friendly claim without creating a second settlement world model."
    - "Downstream plans can call one pure governance rule layer before mutating runtime state."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorSnapshot.java"
      provides: "Claim-keyed persisted governor state and report snapshot"
      exports: ["BannerModGovernorSnapshot"]
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java"
      provides: "Pure allow/deny and binding rules for governance flows"
      exports: ["BannerModGovernorRules"]
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java"
      provides: "World-level load/save access for governor snapshots"
      exports: ["BannerModGovernorManager"]
    - path: "src/test/java/com/talhanation/bannermod/governance/BannerModGovernorRulesTest.java"
      provides: "Automated rule coverage for claim-friendly and degraded cases"
      contains: "@Test"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "friendly/degraded settlement legality checks"
      pattern: "BannerModSettlementBinding"
    - from: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java"
      to: "recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java"
      via: "claim UUID keyed persistence"
      pattern: "UUID|claim"
---

<objective>
Create the governance contracts and persistence boundary before any live governor assignment, UI, or heartbeat logic lands.

Purpose: this gives Phase 23 one explicit claim-keyed governance seam instead of scattering governor state through recruit entities or claim objects.
Output: governance snapshot types, pure rules, a persistence manager, and focused JUnit coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settlement-governance-and-governor-control/23-RESEARCH.md
@.planning/phases/23-settlement-governance-and-governor-control/23-VALIDATION.md
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java`:
```java
public enum Status { FRIENDLY_CLAIM, HOSTILE_CLAIM, UNCLAIMED, DEGRADED_MISMATCH }
public record Binding(Status status, @Nullable String settlementFactionId, @Nullable String claimFactionId)
public static Binding resolveSettlementStatus(@Nullable RecruitsClaimManager claimManager, BlockPos pos, @Nullable String settlementFactionId)
public static boolean allowsSettlementOperation(Binding binding)
```

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java`:
```java
public UUID getUUID()
public ChunkPos getCenter()
public String getOwnerFactionStringID()
public RecruitsFaction getOwnerFaction()
public boolean containsChunk(ChunkPos chunkPos)
```
Executor note: keep governance state keyed to claim UUID / claim center rather than creating a second settlement identity object.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the claim-keyed governor snapshot and pure rules contract</name>
  <files>src/main/java/com/talhanation/bannermod/governance/BannerModGovernorSnapshot.java, src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java</files>
  <action>Create a minimal governance contract that keeps settlement identity claim-derived: one snapshot type keyed by claim UUID/anchor chunk and one pure rules helper for assignment/control legality. Include only fields Phase 23 needs now: governor recruit UUID, governor owner UUID, settlement faction id, last-heartbeat metadata, local tax summary fields, and latest recommendation/incident collections. Do not mutate `RecruitsClaim`, add a new settlement manager, or introduce treasury/upkeep accounting in this plan.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
required = [
    Path('src/main/java/com/talhanation/bannermod/governance/BannerModGovernorSnapshot.java'),
    Path('src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java'),
]
missing = [str(path) for path in required if not path.exists()]
if missing:
    raise SystemExit('Missing governance contract files: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The repository has one explicit snapshot and one pure governance rule contract downstream plans can program against directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement persistence access and rule coverage for governance foundations</name>
  <files>src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java, src/test/java/com/talhanation/bannermod/governance/BannerModGovernorRulesTest.java</files>
  <behavior>
    - Test 1: friendly-claim settlement binding allows governor assignment when the target recruit and owner metadata are present.
    - Test 2: hostile, unclaimed, or degraded settlement states deny assignment/control even if ownership would otherwise pass.
    - Test 3: manager round-trips snapshots by claim UUID without changing claim identity or requiring a new settlement object.
  </behavior>
  <action>Implement a claim-keyed governance manager that loads/saves `BannerModGovernorSnapshot` records for the world and add focused JUnit coverage for the pure rules and persistence boundary. Keep the manager narrow and SavedData-style; it may store governance snapshots, but it must not become a general settlement simulation engine.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.bannermod.governance.BannerModGovernorRulesTest --console=plain</automated>
  </verify>
  <done>Governance state persists independently of claims while legality stays proven by automated tests before live runtime hooks use it.</done>
</task>

</tasks>

<verification>
- `BannerModGovernorSnapshot`, `BannerModGovernorRules`, and `BannerModGovernorManager` exist under one shared `bannermod.governance` package.
- JUnit coverage proves friendly allow plus hostile/unclaimed/degraded denial before runtime assignment code lands.
- No new settlement world model or treasury system is introduced in this foundation slice.
</verification>

<success_criteria>
- Downstream plans can read and update one claim-keyed governance snapshot instead of rediscovering fields across claims and recruits.
- Governance legality is explicit and testable before UI or server heartbeat hooks are added.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settlement-governance-and-governor-control/23-settlement-governance-and-governor-control-01-SUMMARY.md`
</output>
