---
phase: 23-settlement-governance-and-governor-control
plan: 06
type: execute
wave: 5
gap_closure: true
depends_on:
  - 23-05
files_modified:
  # Vendored gametest support classes (NEW files)
  - src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsBattleGameTestSupport.java
  - src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsCommandGameTestSupport.java
  # Sibling gametests with stale FQN references (16 files)
  - src/gametest/java/com/talhanation/bannermod/BannerModCitizenRecruitGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModCitizenWorkerGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModClaimProtectionGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModClaimWorkerGrowthGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerAuthorityGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModMultiplayerAuthorityConflictGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModMultiplayerCooperationGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionEnforcementGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java
  - src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java
  # Root test tree with Phase-21 consolidation debt (6 files)
  - src/test/java/com/talhanation/bannermod/BannerModIntegratedRuntimeSmokeTest.java
  - src/test/java/com/talhanation/workers/MergedRuntimeCleanupPolicyTest.java
  - src/test/java/com/talhanation/workers/WorkersRuntimeLegacyIdMigrationTest.java
  - src/test/java/com/talhanation/workers/WorkersRuntimeSmokeTest.java
  - src/test/java/com/talhanation/workers/MiningClaimExcavationRulesTest.java
  - src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java
autonomous: true
requirements:
  - GOV-04
must_haves:
  truths:
    - "`./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain` exits 0 from the repo root."
    - "`compileTestJava` compiles every file under `src/test/java/com/talhanation/{bannermod,workers}/**` without missing-symbol or type-mismatch errors."
    - "`compileGameTestJava` compiles every file under `src/gametest/java/com/talhanation/bannermod/**` without `package does not exist` or `cannot find symbol` errors."
    - "`BannerModGovernorControlGameTests` actually runs during `verifyGameTestStage` (its three @GameTest methods appear in the run output), closing the GOV-04 gate."
    - "No production code under `src/main/java/com/talhanation/bannermod/governance/**` is modified."
    - "None of the three plan-23-05 gametest files (`BannerModGovernorControlGameTests.java`, `BannerModGameTestSupport.java`, `BannerModDedicatedServerGameTestSupport.java`) is modified."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsBattleGameTestSupport.java"
      provides: "Vendored battle gametest support helpers (squad anchors, spawn scenarios) in the bannermod namespace"
      contains: "package com.talhanation.bannermod.gametest.support"
    - path: "src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsCommandGameTestSupport.java"
      provides: "Vendored command gametest support helpers (command scenarios, group UUIDs) in the bannermod namespace"
      contains: "package com.talhanation.bannermod.gametest.support"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/*GameTests.java"
      to: "com.talhanation.bannermod.bootstrap.BannerModMain.MOD_ID"
      via: "@GameTestHolder annotation value"
      pattern: "@GameTestHolder\\(BannerModMain\\.MOD_ID\\)"
    - from: "src/gametest/java/com/talhanation/bannermod/*GameTests.java"
      to: "com.talhanation.bannermod.gametest.support.RecruitsBattleGameTestSupport / RecruitsCommandGameTestSupport"
      via: "import statement in sibling gametest files"
      pattern: "import com\\.talhanation\\.bannermod\\.gametest\\.support\\."
    - from: "sibling gametests referencing worker APIs"
      to: "com.talhanation.bannermod.events.WorkersVillagerEvents / com.talhanation.bannermod.bootstrap.WorkersSubsystem / com.talhanation.bannermod.events.ClaimEvents"
      via: "rewritten imports"
      pattern: "import com\\.talhanation\\.bannermod\\.(events|bootstrap)\\."
---

<objective>
Close the single Phase 23 gap (truth 15): `./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain` currently fails with ~39 `compileTestJava` errors and ~34 `compileGameTestJava` errors in files **outside** Phase 23's plan-scoped governance work. These are Phase-21 source-tree consolidation carryovers: stale `com.talhanation.recruits.*` / `com.talhanation.workers.*` FQNs, stale `Main.MOD_ID` references, two never-vendored gametest support classes (`RecruitsBattleGameTestSupport`, `RecruitsCommandGameTestSupport`), and `shared.settlement` vs `settlement` type mismatches in worker/mining tests.

Until this cleanup lands, `BannerModGovernorControlGameTests` cannot be compiled or executed as part of `verifyGameTestStage`, so GOV-04 has source integrity but no runtime evidence.

This plan is a **mechanical FQN sweep + vendoring exercise**, not a redesign. It touches test-tree and gametest-tree files only. It does NOT touch `src/main/java/com/talhanation/bannermod/governance/**` or the three plan-23-05 gametest files.

Purpose: unblock the phase-level validation gate so Phase 23 can close with real root GameTest evidence (GOV-04).
Output: a green `./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain` run with `BannerModGovernorControlGameTests` actually executing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-settlement-governance-and-governor-control/23-VERIFICATION.md
@.planning/phases/23-settlement-governance-and-governor-control/deferred-items.md
@./CLAUDE.md

<interfaces>
<!-- Key FQN rewrites the executor must apply verbatim. -->
<!-- Source: grep of actual error output from `./gradlew compileGameTestJava --console=plain` -->
<!-- and `./gradlew compileTestJava --console=plain` as of 2026-04-15. -->

**Symbol rewrites for sibling gametests (`src/gametest/java/com/talhanation/bannermod/*GameTests.java`):**

| Old reference | New reference |
| --- | --- |
| `com.talhanation.recruits.gametest.support.RecruitsBattleGameTestSupport` | `com.talhanation.bannermod.gametest.support.RecruitsBattleGameTestSupport` (vendored by this plan) |
| `com.talhanation.recruits.gametest.support.RecruitsCommandGameTestSupport` | `com.talhanation.bannermod.gametest.support.RecruitsCommandGameTestSupport` (vendored by this plan) |
| `com.talhanation.recruits.ClaimEvents` | `com.talhanation.bannermod.events.ClaimEvents` |
| `com.talhanation.workers.VillagerEvents` | `com.talhanation.bannermod.events.WorkersVillagerEvents` |
| `com.talhanation.workers.WorkersSubsystem` | `com.talhanation.bannermod.bootstrap.WorkersSubsystem` |
| `@GameTestHolder(Main.MOD_ID)` | `@GameTestHolder(BannerModMain.MOD_ID)` (and drop any `import com.talhanation.recruits.Main;` / `import com.talhanation.bannermod.Main;`) |
| bare `Main.MOD_ID` inside method bodies | `BannerModMain.MOD_ID` |

Note: `com.talhanation.bannermod.bootstrap.BannerModMain` is already imported in most of these files; where it is not, add `import com.talhanation.bannermod.bootstrap.BannerModMain;`.

**Symbol rewrites for root test tree (`src/test/java/com/talhanation/{bannermod,workers}/**`):**

| File | Rewrite |
| --- | --- |
| `src/test/java/com/talhanation/bannermod/BannerModIntegratedRuntimeSmokeTest.java` | `Main.MOD_ID` → `BannerModMain.MOD_ID` (both call sites, lines 19-20); `com.talhanation.workers.WorkersSubsystem` → `com.talhanation.bannermod.bootstrap.WorkersSubsystem` |
| `src/test/java/com/talhanation/workers/MergedRuntimeCleanupPolicyTest.java` | Add `import com.talhanation.bannermod.bootstrap.MergedRuntimeCleanupPolicy;` |
| `src/test/java/com/talhanation/workers/WorkersRuntimeSmokeTest.java` | Add `import com.talhanation.bannermod.bootstrap.WorkersRuntime;` |
| `src/test/java/com/talhanation/workers/WorkersRuntimeLegacyIdMigrationTest.java` | Add `import com.talhanation.bannermod.bootstrap.WorkersRuntime;` |
| `src/test/java/com/talhanation/workers/MiningClaimExcavationRulesTest.java` | `import com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding;` → `import com.talhanation.bannermod.settlement.BannerModSettlementBinding;` (match the production class `com.talhanation.bannermod.ai.civilian.MiningClaimExcavationRules`, which uses the legacy settlement variant) |
| `src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java` | `import com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding;` → `import com.talhanation.bannermod.settlement.BannerModSettlementBinding;` (match `com.talhanation.bannermod.settlement.civilian.WorkerSettlementSpawnRules`) |

**Vendoring targets:**

| Source | Destination |
| --- | --- |
| `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java` | `src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsBattleGameTestSupport.java` (rewrite `package com.talhanation.recruits.gametest.support;` → `package com.talhanation.bannermod.gametest.support;`, rewrite `import com.talhanation.recruits.RecruitEvents;` → `import com.talhanation.bannermod.events.RecruitEvents;`, rewrite `import com.talhanation.recruits.init.ModEntityTypes;` → `import com.talhanation.bannermod.registry.military.ModEntityTypes;`, rewrite `import com.talhanation.recruits.world.RecruitsGroup;` → `import com.talhanation.bannermod.persistence.military.RecruitsGroup;`) |
| `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsCommandGameTestSupport.java` | `src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsCommandGameTestSupport.java` (rewrite package declaration as above) |

**Files that MUST NOT be modified:**

- `src/main/java/com/talhanation/bannermod/governance/**` (any file)
- `src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java`
- `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java`
- `src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java`
- `src/main/java/com/talhanation/bannermod/events/ClaimEvents.java` (already correct as of plan 23-03)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor the two missing gametest support classes into the bannermod namespace</name>
  <files>
    src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsBattleGameTestSupport.java,
    src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsCommandGameTestSupport.java
  </files>
  <read_first>
    - `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java` (full legacy source, ~several hundred lines — this is the canonical content to vendor)
    - `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsCommandGameTestSupport.java` (full legacy source)
    - `src/main/java/com/talhanation/bannermod/events/RecruitEvents.java` (confirm FQN)
    - `src/main/java/com/talhanation/bannermod/registry/military/ModEntityTypes.java` (confirm FQN)
    - `src/main/java/com/talhanation/bannermod/persistence/military/RecruitsGroup.java` (confirm FQN)
    - `.planning/phases/23-settlement-governance-and-governor-control/deferred-items.md` (explicit scope note: these two classes were never vendored)
  </read_first>
  <action>
    Create `src/gametest/java/com/talhanation/bannermod/gametest/support/` (new directory). Copy the two legacy files from `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/` into this new directory with these EXACT rewrites applied:

    1. Rewrite the package declaration on line 1: `package com.talhanation.recruits.gametest.support;` → `package com.talhanation.bannermod.gametest.support;`
    2. Rewrite the internal import `import com.talhanation.recruits.RecruitEvents;` → `import com.talhanation.bannermod.events.RecruitEvents;` (RecruitsBattleGameTestSupport.java only)
    3. Rewrite the internal import `import com.talhanation.recruits.init.ModEntityTypes;` → `import com.talhanation.bannermod.registry.military.ModEntityTypes;` (RecruitsBattleGameTestSupport.java only)
    4. Rewrite the internal import `import com.talhanation.recruits.world.RecruitsGroup;` → `import com.talhanation.bannermod.persistence.military.RecruitsGroup;` (RecruitsBattleGameTestSupport.java only)
    5. Leave all other imports unchanged — the `com.talhanation.bannermod.*` imports (AbstractRecruitEntity, BowmanEntity, formation controllers, etc.) are already correct.
    6. Do NOT invent new helper methods. Do NOT trim methods. The two classes are referenced by ~10 sibling gametests (listed in `files_modified`), and dropping methods breaks them.

    After copying, verify: `grep -rn "com.talhanation.recruits" src/gametest/java/com/talhanation/bannermod/gametest/support/` returns ZERO matches.
  </action>
  <verify>
    <automated>test -f src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsBattleGameTestSupport.java && test -f src/gametest/java/com/talhanation/bannermod/gametest/support/RecruitsCommandGameTestSupport.java && ! grep -rn "com\.talhanation\.recruits" src/gametest/java/com/talhanation/bannermod/gametest/support/</automated>
  </verify>
  <acceptance_criteria>
    - Two new files exist under `src/gametest/java/com/talhanation/bannermod/gametest/support/`.
    - `grep -rn "package com.talhanation.bannermod.gametest.support" src/gametest/java/com/talhanation/bannermod/gametest/support/ | wc -l` returns `2`.
    - `grep -rn "com\.talhanation\.recruits" src/gametest/java/com/talhanation/bannermod/gametest/support/ | wc -l` returns `0`.
    - `./gradlew compileGameTestJava --console=plain` error count for `RecruitsBattleGameTestSupport` / `RecruitsCommandGameTestSupport` missing-symbol errors drops to 0 (other errors in sibling gametests remain — they are fixed in Task 2).
  </acceptance_criteria>
  <done>Both support classes exist in the bannermod namespace; zero remaining `com.talhanation.recruits.*` references inside them.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep stale FQNs across the 16 sibling gametest files</name>
  <files>
    src/gametest/java/com/talhanation/bannermod/BannerModCitizenRecruitGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModCitizenWorkerGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModClaimProtectionGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModClaimWorkerGrowthGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerAuthorityGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModMultiplayerAuthorityConflictGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModMultiplayerCooperationGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModOwnershipCycleGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionEnforcementGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModUpkeepFlowGameTests.java,
    src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java,
    src/gametest/java/com/talhanation/bannermod/IntegratedRuntimeGameTests.java
  </files>
  <read_first>
    - Each of the 16 listed files (read the import block — lines 1-25 — and scan for every `Main.MOD_ID` / `Main.` / `com.talhanation.recruits.` / `com.talhanation.workers.` usage).
    - `src/main/java/com/talhanation/bannermod/events/WorkersVillagerEvents.java` (confirm it exposes `attemptClaimWorkerGrowth`, `attemptBirthWorkerSpawn`, `attemptSettlementWorkerSpawn` — it does, at lines 221/240/278/308).
    - `src/main/java/com/talhanation/bannermod/bootstrap/WorkersSubsystem.java` (confirm class location).
    - `src/main/java/com/talhanation/bannermod/events/ClaimEvents.java` (confirm class location).
    - `src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java` (confirm `MOD_ID` constant exists).
    - `src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java` (reference pattern for correct imports — do NOT modify this file).
  </read_first>
  <action>
    Apply this exact set of literal substitutions across all 16 files listed in `<files>`. Do NOT perform any other refactor (no reformatting, no extracting helpers, no renaming locals).

    **Import rewrites (line-by-line):**
    - `import com.talhanation.recruits.gametest.support.RecruitsBattleGameTestSupport;` → `import com.talhanation.bannermod.gametest.support.RecruitsBattleGameTestSupport;`
    - `import com.talhanation.recruits.gametest.support.RecruitsCommandGameTestSupport;` → `import com.talhanation.bannermod.gametest.support.RecruitsCommandGameTestSupport;`
    - `import com.talhanation.recruits.ClaimEvents;` → `import com.talhanation.bannermod.events.ClaimEvents;`
    - `import com.talhanation.workers.VillagerEvents;` → `import com.talhanation.bannermod.events.WorkersVillagerEvents;`
    - `import com.talhanation.workers.WorkersSubsystem;` → `import com.talhanation.bannermod.bootstrap.WorkersSubsystem;`
    - If any file has `import com.talhanation.recruits.Main;` or `import com.talhanation.bannermod.Main;` — DELETE that import line.
    - If any file references `BannerModMain.MOD_ID` but lacks `import com.talhanation.bannermod.bootstrap.BannerModMain;` — ADD that import (most files already have it; verify per-file).

    **Body rewrites:**
    - `@GameTestHolder(Main.MOD_ID)` → `@GameTestHolder(BannerModMain.MOD_ID)` (this is the ONLY `@GameTestHolder` occurrence per file; usually near line 20-25).
    - Any bare `Main.MOD_ID` reference inside method bodies → `BannerModMain.MOD_ID`.
    - Any `VillagerEvents.attemptClaimWorkerGrowth(...)` / `VillagerEvents.attemptBirthWorkerSpawn(...)` / `VillagerEvents.attemptSettlementWorkerSpawn(...)` method call → rewrite the call receiver to `WorkersVillagerEvents.attemptClaimWorkerGrowth(...)` / `WorkersVillagerEvents.attemptBirthWorkerSpawn(...)` / `WorkersVillagerEvents.attemptSettlementWorkerSpawn(...)`. (The method signatures are identical — only the class name changes.)
    - `ClaimEvents.recruitsClaimManager` references remain as-is; only the import changed.
    - `WorkersSubsystem` type references remain as-is; only the import changed.

    **Known examples to cross-check after editing:**
    - `BannerModClaimWorkerGrowthGameTests.java:6` import line and `:25` holder + `:45,72,73,74,101,107,137` call sites.
    - `BannerModWorkerBirthAndSettlementSpawnGameTests.java:4-7` import block and `:21` holder.
    - `IntegratedRuntimeGameTests.java:5-6` imports and `:12,21,22` body references to `Main.MOD_ID`.
    - `BannerModDedicatedServerReconnectGameTests.java:5-6` support-class imports.

    Do NOT touch:
    - `src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java`
    - `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java`
    - `src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java`

    After editing all 16 files, confirm with grep that no stale references remain in the entire gametest tree.
  </action>
  <verify>
    <automated>./gradlew compileGameTestJava --console=plain 2>&1 | tee /tmp/compile-gametest.log | grep -E "^BUILD SUCCESSFUL" && ! grep -rn "com\.talhanation\.recruits\.\(gametest\|ClaimEvents\|Main\)\|com\.talhanation\.workers\.\(VillagerEvents\|WorkersSubsystem\)\|@GameTestHolder(Main\." src/gametest/java/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "com\.talhanation\.recruits\.gametest\.support" src/gametest/java/ | wc -l` returns `0`.
    - `grep -rn "com\.talhanation\.recruits\.ClaimEvents" src/gametest/java/ | wc -l` returns `0`.
    - `grep -rn "com\.talhanation\.workers\.VillagerEvents" src/gametest/java/ | wc -l` returns `0`.
    - `grep -rn "com\.talhanation\.workers\.WorkersSubsystem" src/gametest/java/ | wc -l` returns `0`.
    - `grep -rn "@GameTestHolder(Main\.MOD_ID)" src/gametest/java/ | wc -l` returns `0`.
    - `grep -rn "@GameTestHolder(BannerModMain\.MOD_ID)" src/gametest/java/ | wc -l` returns `>= 17` (16 sibling files + existing plan-23-05 files already correct).
    - `./gradlew compileGameTestJava --console=plain` exits 0 with `BUILD SUCCESSFUL`.
    - Git diff is limited to: the 16 sibling gametest files + the 2 vendored support files from Task 1. No production code diff. No plan-23-05 file diff.
  </acceptance_criteria>
  <done>`compileGameTestJava` is green; the three plan-23-05 files are byte-identical to their pre-Task-2 state; the governance source tree is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the 6 root-test-tree files for FQN and settlement-binding type mismatches</name>
  <files>
    src/test/java/com/talhanation/bannermod/BannerModIntegratedRuntimeSmokeTest.java,
    src/test/java/com/talhanation/workers/MergedRuntimeCleanupPolicyTest.java,
    src/test/java/com/talhanation/workers/WorkersRuntimeSmokeTest.java,
    src/test/java/com/talhanation/workers/WorkersRuntimeLegacyIdMigrationTest.java,
    src/test/java/com/talhanation/workers/MiningClaimExcavationRulesTest.java,
    src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java
  </files>
  <read_first>
    - Each of the 6 test files (full contents — they are all <260 lines).
    - `src/main/java/com/talhanation/bannermod/bootstrap/MergedRuntimeCleanupPolicy.java` (confirm class exists with `enableLegacyUpdateCheckers()`).
    - `src/main/java/com/talhanation/bannermod/bootstrap/WorkersRuntime.java` (confirm class exists with `modId()`, `id(String)`, `legacyId(String)`, `mergedGuiTexture(String)`, `mergedStructureRoot()`, `networkIdOffset()`, `migrateLegacyId(String)`, `migrateStructureNbt(CompoundTag)`).
    - `src/main/java/com/talhanation/bannermod/bootstrap/WorkersSubsystem.java` (confirm class exists with `networkMessageCount()`).
    - `src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java` (confirm `MOD_ID = "bannermod"`).
    - `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java` (the legacy variant used by `MiningClaimExcavationRules` and `WorkerSettlementSpawnRules`; confirm it has `Status` and `Binding` nested types).
    - `src/main/java/com/talhanation/bannermod/ai/civilian/MiningClaimExcavationRules.java` line 3 (confirms production class imports `com.talhanation.bannermod.settlement.BannerModSettlementBinding`, the legacy variant — that is why the test import must match).
    - `src/main/java/com/talhanation/bannermod/settlement/civilian/WorkerSettlementSpawnRules.java` line 3 (same — legacy variant).
  </read_first>
  <action>
    Apply these EXACT file-by-file rewrites. Do not reformat. Do not rename tests. Do not delete tests.

    **File: `src/test/java/com/talhanation/bannermod/BannerModIntegratedRuntimeSmokeTest.java`**
    - Line 6: `import com.talhanation.workers.WorkersSubsystem;` → `import com.talhanation.bannermod.bootstrap.WorkersSubsystem;`
    - Line 19: `assertEquals("bannermod", Main.MOD_ID);` → `assertEquals("bannermod", BannerModMain.MOD_ID);`
    - Line 20: `assertEquals(Main.MOD_ID, WorkersRuntime.modId());` → `assertEquals(BannerModMain.MOD_ID, WorkersRuntime.modId());`
    - No `import com.talhanation.bannermod.Main;` line exists to delete; `BannerModMain` import at line 3 is already present.

    **File: `src/test/java/com/talhanation/workers/MergedRuntimeCleanupPolicyTest.java`**
    - Add an import line between line 3 and line 5 (after the junit import, before the static import): `import com.talhanation.bannermod.bootstrap.MergedRuntimeCleanupPolicy;`
    - Leave the body reference `MergedRuntimeCleanupPolicy.enableLegacyUpdateCheckers()` as-is — it now resolves via the new import.
    - Do NOT change the `package com.talhanation.workers;` declaration — test-tree packaging stays stable.

    **File: `src/test/java/com/talhanation/workers/WorkersRuntimeSmokeTest.java`**
    - Add an import line (after line 3, the ResourceLocation import): `import com.talhanation.bannermod.bootstrap.WorkersRuntime;`
    - Leave all body references to `WorkersRuntime.modId()` / `WorkersRuntime.id(...)` / `WorkersRuntime.legacyId(...)` / `WorkersRuntime.mergedGuiTexture(...)` / `WorkersRuntime.mergedStructureRoot()` / `WorkersRuntime.networkIdOffset()` as-is — they now resolve via the new import.

    **File: `src/test/java/com/talhanation/workers/WorkersRuntimeLegacyIdMigrationTest.java`**
    - Add an import line (after line 5, the junit Test import): `import com.talhanation.bannermod.bootstrap.WorkersRuntime;`
    - Leave body references to `WorkersRuntime.migrateLegacyId(...)` / `WorkersRuntime.migrateStructureNbt(...)` as-is — they now resolve.

    **File: `src/test/java/com/talhanation/workers/MiningClaimExcavationRulesTest.java`**
    - Line 3: `import com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding;` → `import com.talhanation.bannermod.settlement.BannerModSettlementBinding;`
    - Rationale: the production class `com.talhanation.bannermod.ai.civilian.MiningClaimExcavationRules` still imports the legacy variant (`com.talhanation.bannermod.settlement.BannerModSettlementBinding`). The test was updated to the shared variant prematurely; fixing the test's import restores the matching type. The governance code's reliance on `shared.settlement` is unaffected because the governance tree never calls `MiningClaimExcavationRules`.

    **File: `src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java`**
    - Line 3: `import com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding;` → `import com.talhanation.bannermod.settlement.BannerModSettlementBinding;`
    - Rationale: identical — `com.talhanation.bannermod.settlement.civilian.WorkerSettlementSpawnRules` still imports the legacy variant.
    - All ~14 body references to `BannerModSettlementBinding.Status.*` and `BannerModSettlementBinding.Binding` will now resolve against the correct legacy type.

    **DO NOT:**
    - Modify any production file under `src/main/java/**`.
    - Delete or merge `com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding` and `com.talhanation.bannermod.settlement.BannerModSettlementBinding` into one class — that is a Rule 4 consolidation for a future phase, explicitly out of scope here.
    - Move the test files out of `com.talhanation.workers` / `com.talhanation.bannermod` packages.

    After editing, confirm the full compile+test gate passes.
  </action>
  <verify>
    <automated>./gradlew compileJava test --console=plain 2>&1 | tee /tmp/compile-test.log | grep -E "^BUILD SUCCESSFUL" && ! grep -rn "shared\.settlement\.BannerModSettlementBinding" src/test/java/ && ! grep -rn "com\.talhanation\.workers\.WorkersSubsystem\|com\.talhanation\.workers\.WorkersRuntime\|com\.talhanation\.workers\.MergedRuntimeCleanupPolicy" src/test/java/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "shared\.settlement\.BannerModSettlementBinding" src/test/java/ | wc -l` returns `0`.
    - `grep -rn "com\.talhanation\.bannermod\.bootstrap\.MergedRuntimeCleanupPolicy" src/test/java/ | wc -l` returns `>= 1`.
    - `grep -rn "com\.talhanation\.bannermod\.bootstrap\.WorkersRuntime" src/test/java/ | wc -l` returns `>= 2`.
    - `grep -rn "com\.talhanation\.bannermod\.bootstrap\.WorkersSubsystem" src/test/java/ | wc -l` returns `>= 1`.
    - `grep -rn "\\bMain\\.MOD_ID" src/test/java/ | wc -l` returns `0`.
    - `./gradlew compileJava test --console=plain` exits 0 with `BUILD SUCCESSFUL` and reports `BannerModGovernorServiceTest`, `BannerModGovernorHeartbeatTest`, `BannerModGovernorRulesTest` in the run output (these were previously blocked by the test-tree compile failure).
    - No production code diff vs. HEAD at task start (`git diff --stat src/main/java/` returns empty).
  </acceptance_criteria>
  <done>`compileJava` + `test` is green; all 6 test files compile; governance unit tests now actually run; no production code touched.</done>
</task>

<task type="auto">
  <name>Task 4: Run the full Phase 23 validation gate and confirm BannerModGovernorControlGameTests executes</name>
  <files>
    (verification task — produces no file changes; only runs and records output)
  </files>
  <read_first>
    - `.planning/phases/23-settlement-governance-and-governor-control/23-VERIFICATION.md` (the phase-level gate command and the three plan-23-05 @GameTest methods that must appear in run output).
    - `.planning/phases/23-settlement-governance-and-governor-control/23-VALIDATION.md` (for context on what "evidence" looks like for GOV-04).
    - The Tasks 1-3 acceptance criteria output (confirm all three previous tasks reported BUILD SUCCESSFUL before running Task 4).
  </read_first>
  <action>
    Run the phase-level validation gate:

    ```
    ./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain
    ```

    Capture the full output to `/tmp/phase23-gate.log`. Confirm:
    1. The command exits with status code 0.
    2. The final line is `BUILD SUCCESSFUL`.
    3. The `verifyGameTestStage` output mentions the three plan-23-05 `@GameTest` method names (from `BannerModGovernorControlGameTests.java`): the friendly-designation test, the hostile-swap degradation test, and the live heartbeat reporting test. (Grep the log for `BannerModGovernorControlGameTests` — at least one match is required to prove the gametest class was picked up by the runner.)
    4. No `cannot find symbol` or `package does not exist` errors appear anywhere in the log.

    Document the result in the plan summary (written later by the execute-phase flow):
    - Record `BUILD SUCCESSFUL` line.
    - Record the elapsed time.
    - Record which governance gametest methods appeared in the `verifyGameTestStage` section.
    - Record any unrelated deferred-test failures that appeared (there should be none now, but if any surface that are genuinely orthogonal to Phase-21 consolidation, log them to `deferred-items.md` — do NOT fix them here).

    If the gate fails, triage:
    - If the failure is in governance code → STOP — governance was not supposed to be modified; revert.
    - If the failure is in a file listed in `files_modified` → refine the Task 2 or Task 3 sweep; the FQN rewrite table above is the ground truth.
    - If the failure is in a file NOT listed in `files_modified` (genuinely new deferred debt) → log to `deferred-items.md`, note it in the summary, and escalate rather than widening scope.
  </action>
  <verify>
    <automated>./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain 2>&1 | tee /tmp/phase23-gate.log | grep -E "^BUILD SUCCESSFUL" && grep -q "BannerModGovernorControlGameTests" /tmp/phase23-gate.log</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain` exits with status code 0.
    - `/tmp/phase23-gate.log` contains the literal string `BUILD SUCCESSFUL`.
    - `/tmp/phase23-gate.log` contains at least one reference to `BannerModGovernorControlGameTests` in the gametest stage output (proving the class was compiled and picked up by `verifyGameTestStage`).
    - `/tmp/phase23-gate.log` contains ZERO occurrences of `cannot find symbol`.
    - `/tmp/phase23-gate.log` contains ZERO occurrences of `package does not exist`.
    - `/tmp/phase23-gate.log` contains ZERO occurrences of `incompatible types: com.talhanation.bannermod.shared.settlement.BannerModSettlementBinding`.
    - `git diff --stat src/main/java/com/talhanation/bannermod/governance/` is empty (no governance modifications).
    - `git diff src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java` is empty (no plan-23-05 modifications).
  </acceptance_criteria>
  <done>Phase 23 validation gate is GREEN; `BannerModGovernorControlGameTests` runs as part of `verifyGameTestStage`; GOV-04 is now satisfied with runtime evidence.</done>
</task>

</tasks>

<verification>
Run the phase-level gate one final time after all tasks complete:

```
./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain
```

Expected: `BUILD SUCCESSFUL`.

Cross-check the VERIFICATION.md truth #15 by re-running verification:
- `compileJava` green (was already green pre-plan).
- `compileTestJava` green (was 39 errors pre-plan, 0 after Task 3).
- `compileGameTestJava` green (was 34 errors pre-plan, 0 after Task 2).
- `verifyGameTestStage` green with `BannerModGovernorControlGameTests` appearing in the output.

Re-check scope-preservation:
- `git diff --stat src/main/java/com/talhanation/bannermod/governance/` is empty.
- `git diff --stat src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java` is empty.
</verification>

<success_criteria>
- `./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain` exits 0.
- `BannerModGovernorControlGameTests` actually executes during `verifyGameTestStage` (appears in run output).
- All 22 files listed in `files_modified` are the ONLY files changed by this plan. Zero production-code diff. Zero plan-23-05 gametest-file diff.
- REQUIREMENTS.md GOV-04 can be flipped from `[ ]` to `[x]` by the next verifier pass.
- VERIFICATION.md truth #15 flips from `FAILED` to `VERIFIED` on re-verification.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settlement-governance-and-governor-control/23-settlement-governance-and-governor-control-06-SUMMARY.md` following the standard summary template. Include:
- The exact FQN rewrite count per file (as a table).
- The `BUILD SUCCESSFUL` line from `/tmp/phase23-gate.log` and the elapsed time.
- The specific `BannerModGovernorControlGameTests` methods that appeared in the `verifyGameTestStage` output.
- A note that governance code and plan-23-05 gametest files were untouched (with `git diff --stat` proof).
- Any residual deferred items that surfaced (expected: none).
</output>
