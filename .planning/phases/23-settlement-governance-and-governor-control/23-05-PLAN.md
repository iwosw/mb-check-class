---
phase: 23-settlement-governance-and-governor-control
plan: 05
type: execute
wave: 4
depends_on:
  - 23-02
  - 23-03
  - 23-04
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
autonomous: true
requirements:
  - GOV-04
must_haves:
  truths:
    - "Automated validation proves a governor can be designated in a friendly claim and survives live runtime execution."
    - "Automated validation proves degraded or hostile settlement states deny or degrade governor control correctly."
    - "Phase 23 closes with root GameTest evidence, not only unit tests or UI compilation."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java"
      provides: "Root GameTests for governor designation, persistence, and reporting"
      contains: "@GameTest"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      provides: "Reusable helpers for governor-capable recruit and settlement setup"
      contains: "spawn"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java"
      provides: "Reusable claim/faction helpers for governance GameTests"
      contains: "seedClaim"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java"
      via: "live designation path assertions"
      pattern: "Governor"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorHeartbeat.java"
      via: "live report and local-tax assertions"
      pattern: "Governor"
---

<objective>
Close the phase with executable validation for governor designation, persistence, and reporting behavior.

Purpose: settlement governance must be proven in the same root GameTest discipline used for earlier settlement and authority phases.
Output: reusable GameTest helpers and a dedicated governor validation class.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settlement-governance-and-governor-control/23-VALIDATION.md
@.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-03-PLAN.md
@src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java

<interfaces>
From `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java`:
```java
public static <T extends Entity> T spawnEntity(GameTestHelper helper, EntityType<T> entityType, BlockPos relativePos)
public static Villager spawnVillagerWithMemories(GameTestHelper helper, BlockPos relativePos, String name)
```

From `src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java`:
```java
public static Player createFakeServerPlayer(ServerLevel level, UUID playerId, String name)
public static RecruitsClaim seedClaim(ServerLevel level, BlockPos pos, String factionId, UUID leaderId, String leaderName)
public static RecruitsClaim swapClaimFaction(ServerLevel level, RecruitsClaim claim, String factionId, UUID leaderId, String leaderName)
```
Executor note: reuse the existing claim/faction support helpers; do not build a second settlement-governance-only harness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GameTest support with governor-ready settlement fixtures</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java</files>
  <action>Add only the helper seams needed to stand up Phase 23 scenarios: spawn a recruit that can be designated as governor, seed a friendly claim with stable faction/leader ownership, and create any minimal villager or worker presence needed to drive local governance reports. Keep these helpers additive and reusable for later logistics/treasury phases; do not build a parallel governance test subsystem.</action>
  <verify>
    <automated>./gradlew compileGameTestJava --console=plain</automated>
  </verify>
  <done>GameTest support can seed governed settlement scenarios through reusable helpers rather than bespoke setup inside each test.</done>
</task>

<task type="auto">
  <name>Task 2: Add root GameTests for governor designation, degraded denial, and live reporting</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModGovernorControlGameTests.java</files>
  <action>Implement a dedicated `BannerModGovernorControlGameTests` class that proves three live behaviors: (1) a recruit in a friendly claim can be designated as governor and the assignment persists through the live runtime seam, (2) claim loss or hostile swap degrades or denies governor control rather than silently continuing normal governance, and (3) the governed settlement publishes local tax/report output through the real heartbeat path. Reuse the real claim manager, real governance service, and real heartbeat rather than mocking outcomes in the test.</action>
  <verify>
    <automated>./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain</automated>
  </verify>
  <done>Phase 23 closes with root GameTests proving governor assignment, degraded denial, and live governance reporting.</done>
</task>

</tasks>

<verification>
- Run `./gradlew compileJava test compileGameTestJava verifyGameTestStage --console=plain`.
- Confirm the new GameTests assert both authority/settlement status and governance output fields, not just button reachability or entity counts.
</verification>

<success_criteria>
- Root GameTests prove the governor role is live, claim-aware, and persistence-safe.
- Phase 23 ends with executable evidence future treasury/logistics phases can build on.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settlement-governance-and-governor-control/23-settlement-governance-and-governor-control-05-SUMMARY.md`
</output>
