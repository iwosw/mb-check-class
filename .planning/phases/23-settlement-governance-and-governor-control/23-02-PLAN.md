---
phase: 23-settlement-governance-and-governor-control
plan: 02
type: execute
wave: 2
depends_on:
  - 23-01
files_modified:
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorAuthority.java
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java
  - src/test/java/com/talhanation/bannermod/governance/BannerModGovernorServiceTest.java
autonomous: true
requirements:
  - GOV-02
must_haves:
  truths:
    - "One recruit can be designated as a settlement governor without spawning a new entity type."
    - "Governor assignment and revocation respect existing owner/admin/friendly-claim authority boundaries."
    - "Governor designation persists independently of transient entity state."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorAuthority.java"
      provides: "Authority decisions for governor assignment and control"
      exports: ["BannerModGovernorAuthority"]
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java"
      provides: "Runtime service for assigning, revoking, and loading governors"
      exports: ["BannerModGovernorService"]
    - path: "src/test/java/com/talhanation/bannermod/governance/BannerModGovernorServiceTest.java"
      provides: "Automated service-level coverage for governor designation"
      contains: "@Test"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorAuthority.java"
      to: "src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java"
      via: "owner/admin relationship mapping"
      pattern: "BannerModAuthorityRules"
    - from: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java"
      to: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java"
      via: "claim-keyed snapshot mutation"
      pattern: "BannerModGovernorManager"
---

<objective>
Implement the runtime governor designation service and authority checks before wiring any UI or heartbeat reporting to it.

Purpose: Phase 23 needs one low-risk assignment path that turns the governor slot into a real designation without introducing a new recruit subclass or breaking ownership rules.
Output: governor authority helper, assignment/revocation service, and JUnit coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-settlement-governance-and-governor-control/23-RESEARCH.md
@.planning/phases/23-settlement-governance-and-governor-control/23-VALIDATION.md
@.planning/phases/23-settlement-governance-and-governor-control/23-01-PLAN.md
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@src/main/java/com/talhanation/bannermod/persistence/military/RecruitsClaim.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java`:
```java
public enum Relationship { OWNER, SAME_TEAM, ADMIN, FORBIDDEN }
public static Relationship resolveRelationship(boolean owner, boolean sameTeam, boolean admin)
public static Decision recoverControlDecision(boolean targetExists, Relationship relationship)
public static boolean isAllowed(Decision decision)
```

From the Phase 23 foundation:
```java
BannerModGovernorManager
BannerModGovernorRules
BannerModGovernorSnapshot
```
Executor note: governor must remain a designation over an existing recruit/citizen UUID, not a new entity type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add governor authority and assignment service coverage first</name>
  <read_first>src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java, src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java, src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java, src/main/java/com/talhanation/bannermod/governance/BannerModGovernorRules.java, .planning/phases/23-settlement-governance-and-governor-control/23-01-PLAN.md</read_first>
  <files>src/main/java/com/talhanation/bannermod/governance/BannerModGovernorAuthority.java, src/test/java/com/talhanation/bannermod/governance/BannerModGovernorServiceTest.java</files>
  <behavior>
    - Test 1: owner or admin can designate a recruit as governor for a friendly claim.
    - Test 2: same-team or outsider callers cannot assign or revoke a governor when the authority policy forbids it.
    - Test 3: degraded, hostile, or unclaimed settlement states are denied even if the recruit target exists.
  </behavior>
  <action>Create an authority helper that combines existing owner/admin relationships with friendly-claim settlement legality for governance actions, then pin it with service-level JUnit tests. Keep the policy explicit and additive; do not fork a second permission model just for governors.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.bannermod.governance.BannerModGovernorServiceTest --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `BannerModGovernorAuthority` combines existing relationship checks with friendly-claim legality for governor assignment and revocation.
    - `BannerModGovernorServiceTest` covers allowed owner/admin designation and denied same-team, outsider, hostile, degraded, and unclaimed cases.
    - `./gradlew test --tests com.talhanation.bannermod.governance.BannerModGovernorServiceTest --console=plain` passes.
  </acceptance_criteria>
  <done>Automated tests define the legal governor assignment boundary before runtime mutation code lands.</done>
</task>

<task type="auto">
  <name>Task 2: Implement designation and revocation as snapshot-backed runtime operations</name>
  <read_first>src/main/java/com/talhanation/bannermod/governance/BannerModGovernorAuthority.java, src/main/java/com/talhanation/bannermod/governance/BannerModGovernorManager.java, src/main/java/com/talhanation/bannermod/governance/BannerModGovernorSnapshot.java, src/test/java/com/talhanation/bannermod/governance/BannerModGovernorServiceTest.java</read_first>
  <files>src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java</files>
  <action>Implement the runtime service that designates, revokes, and resolves the current governor for a claim using the persisted snapshots from Plan 23-01. Keep the governor attached to an existing recruit UUID and owner UUID; do not spawn or register a `GovernorEntity`, and do not mutate the claim object to hold governor state directly. Expose methods downstream message or screen code can call later without exploring the codebase to discover the assignment contract.</action>
  <verify>
    <automated>./gradlew compileJava --console=plain</automated>
  </verify>
  <acceptance_criteria>
    - `BannerModGovernorService` exposes server-side designation, revocation, and current-governor lookup over persisted snapshots.
    - The implementation keeps governor identity attached to existing recruit and owner UUIDs instead of a new entity type.
    - `./gradlew compileJava --console=plain` passes after the service is wired.
  </acceptance_criteria>
  <done>Downstream UI and heartbeat plans can call one server-side service to assign or revoke governors without touching claim persistence directly.</done>
</task>

</tasks>

<verification>
- Governor assignment uses existing relationship vocabulary and friendly-claim checks together.
- No new recruit entity type or recruit promotion entity map is introduced for governor designation.
- The runtime service updates persisted snapshots instead of hiding state inside a live entity only.
</verification>

<success_criteria>
- The governor slot has a real backend assignment path ready for later UI wiring.
- Authority and degraded/hostile denial remain explicit and automated.
</success_criteria>

<output>
After completion, create `.planning/phases/23-settlement-governance-and-governor-control/23-settlement-governance-and-governor-control-02-SUMMARY.md`
</output>
