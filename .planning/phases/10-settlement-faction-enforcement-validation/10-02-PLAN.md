---
phase: 10-settlement-faction-enforcement-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
  - workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
  - recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "Settlement claim loss or faction mismatch degrades civilian throughput by making the settlement footprint non-operational before any ownership transfer occurs."
    - "Degradation preserves explicit owner and team identity; worker or work-area ownership does not silently move to the new claim holder or to nobody."
    - "The degraded behavior is validated through the explicit `DEGRADED_MISMATCH` or `UNCLAIMED` runtime vocabulary rather than by hidden side effects alone."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java"
      provides: "Root GameTest coverage for claim-loss and faction-mismatch degradation behavior"
      contains: "claimLossDegrades"
    - path: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      provides: "Live settlement-operation gate whose degraded behavior is exercised by the tests"
      contains: "canWorkHere|getSettlementBinding"
    - path: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      provides: "Explicit degraded or unclaimed settlement status vocabulary used by the degradation tests"
      contains: "DEGRADED_MISMATCH"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "claim-loss and mismatch validation should assert the explicit degraded or unclaimed status rather than infer it only from worker failure"
      pattern: "DEGRADED_MISMATCH|UNCLAIMED|resolveSettlementStatus"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      via: "degraded settlements must stop allowing civilian work without changing area ownership"
      pattern: "canWorkHere|getPlayerUUID|getTeamStringID"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java"
      to: "recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java"
      via: "the tests should trigger degradation by changing or removing the live claim entry rather than faking a detached state"
      pattern: "addOrUpdateClaim|removeClaim|getClaim"
---

<objective>
Validate settlement degradation on claim loss or faction mismatch without silent ownership transfer.

Purpose: The architecture and Phase 09 contract both say settlement throughput should degrade before ownership silently transfers, but that rule is still only trustworthy if the merged runtime demonstrates it under live claim mutation rather than only in pure status-resolution tests.
Output: One focused degradation GameTest class plus any minimal runtime fix needed to keep claim loss or mismatch deterministic and ownership-safe.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-CONTEXT.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-01-PLAN.md
@.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModPlayerCycleGameTests.java
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerReconnectGameTests.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsFactionManager.java

<interfaces>
Reuse these existing contracts directly:

From `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java`:
```java
public record Binding(Status status, @Nullable String settlementFactionId, @Nullable String claimFactionId)
public static Binding resolveSettlementStatus(@Nullable RecruitsClaimManager claimManager, BlockPos pos, @Nullable String settlementFactionId)
public static boolean allowsSettlementOperation(Binding binding)
```

From `workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java`:
```java
public boolean canWorkHere(AbstractWorkerEntity worker)
public BannerModSettlementBinding.Binding getSettlementBinding()
public UUID getPlayerUUID()
public String getTeamStringID()
```

From `workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java`:
```java
public boolean recoverControl(Player requester)
public UUID getOwnerUUID()
```

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java`:
```java
public void addOrUpdateClaim(ServerLevel level, RecruitsClaim claim)
public void removeClaim(RecruitsClaim claim)
@Nullable
public RecruitsClaim getClaim(ChunkPos chunkPos)
```

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java`:
```java
public void addChunk(ChunkPos chunkPos)
public void removeChunk(ChunkPos chunkPos)
public void setOwnerFaction(RecruitsFaction faction)
public String getOwnerFactionStringID()
```

If the current GameTest helpers lack a small claim-mutation helper for removal or faction swap, add the smallest additive helper in `BannerModDedicatedServerGameTestSupport.java` instead of duplicating that setup in each test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add live degradation GameTests for claim loss and faction mismatch</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionDegradationGameTests.java, src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java, recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java</files>
  <behavior>
    - Test 1: A settlement that starts inside a friendly claim becomes non-operational after the claim is removed, and the work area reports `UNCLAIMED` or the equivalent non-friendly status while ownership fields stay unchanged.
    - Test 2: A settlement that starts friendly becomes degraded after the live claim owner faction changes to a different faction, and the work area reports `DEGRADED_MISMATCH` without silently rebinding the area or worker to the new faction.
    - Test 3: After degradation, explicit owner-controlled recovery still behaves according to the existing authority contract rather than any implicit transfer to the new claim holder.
  </behavior>
  <action>Create `BannerModSettlementFactionDegradationGameTests.java` with at least two `@GameTest(template = "harness_empty")` methods that begin from a valid friendly settlement setup, then mutate the live claim state in-place by removing the claim or swapping its owner faction. Assert that `getSettlementBinding()` changes to the expected explicit non-friendly status, `canWorkHere(...)` stops allowing civilian throughput, and the existing `playerUUID`, `teamStringID`, and worker owner UUID remain anchored to the original owner until an explicit authority action occurs. If a test exposes a real runtime defect, patch only the smallest listed production file so degradation stays live, deterministic, and ownership-safe without broad settlement persistence or military rewrites.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage</automated>
  </verify>
  <done>The root GameTest suite proves claim loss and faction mismatch degrade settlement throughput before any silent ownership transfer can occur.</done>
</task>

</tasks>

<verification>
Run `./gradlew verifyGameTestStage` after the degradation tests land, then confirm the claim-loss and mismatch scenarios both report non-friendly settlement status while original owner identity remains unchanged until a deliberate authority action happens.
</verification>

<success_criteria>
- One root GameTest class now proves claim loss and faction mismatch degrade settlement throughput in live runtime execution
- The tests assert explicit degraded or unclaimed settlement status and also verify the absence of silent ownership transfer
- Any runtime fix remains narrow and does not introduce a broad settlement manager, persistence rewrite, or faction-control redesign
</success_criteria>

<output>
After completion, create `.planning/phases/10-settlement-faction-enforcement-validation/10-settlement-faction-enforcement-validation-02-SUMMARY.md`
</output>
