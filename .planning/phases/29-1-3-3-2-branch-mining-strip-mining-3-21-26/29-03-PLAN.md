---
phase: 29-1-3-3-2-branch-mining-strip-mining-3-21-26
plan: 03
type: execute
wave: 3
depends_on:
  - 29-02
files_modified:
  - workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java
  - workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
  - src/test/java/com/talhanation/workers/MiningPatternPlannerBranchTest.java
autonomous: true
requirements:
  - MINING-03
must_haves:
  truths:
    - "A miner assigned to branch/strip mining follows a main corridor and side branches at the configured height instead of reusing tunnel mode or idling."
    - "Branch spacing and branch length are player-controlled and produce alternating left/right strip branches rather than random wall mining."
    - "Branch-mining completion checks cover the full branch layout so the area only marks done when the configured strip pattern has actually been mined."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      provides: "Branch-mining corridor and side-branch planning"
      contains: "planBranchCorridor|planBranchSegment"
    - path: "workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java"
      provides: "Live branch-mining execution over the planned strip layout"
      contains: "CHECK|DONE|PREPARE_MINE_WALLS"
    - path: "src/test/java/com/talhanation/workers/MiningPatternPlannerBranchTest.java"
      provides: "Regression proof for alternating strip branches"
      contains: "branchMining"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      via: "branch spacing/length settings must directly control the produced strip pattern"
      pattern: "branchSpacing|branchLength|planBranch"
    - from: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java"
      via: "the planner's alternating branches must drive live miner progress and done detection"
      pattern: "planBranch|CHECK|DONE"
---

<objective>
Implement the requested branch/strip-mining mode on top of the new tunnel foundation.

Purpose: after tunnel mode is working, miners still need a second high-value excavation pattern for controlled ore harvesting. This plan adds explicit branch-mining geometry and wires the miner state machine to execute it cleanly.
Output: one branch-mining planner extension plus live miner execution for strip branches at configured height/spacing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
@workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java
@workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
@.planning/phases/29-1-3-3-2-branch-mining-strip-mining-3-21-26/29-02-PLAN.md

<interfaces>
From `workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java`:
```java
// Plan 29-01 establishes the canonical settings contract.
// Use its mode, branchSpacing, branchLength, width, height, and heightOffset fields directly.
```

From `workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java`:
```java
// Plan 29-02 establishes the tunnel-planning seam.
// Extend that seam for branch mining instead of creating a second planner path.
```

From `workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java`:
```java
public boolean mineBlocks(Stack<BlockPos> positions)
public boolean closeHoles(Stack<BlockPos> positions)
```

Branch mode must stay a deterministic strip pattern per MINING-03: one main corridor at the configured height, alternating side branches at configured spacing/length, and no fallback to digging under the worker.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the planner with deterministic branch-mining geometry</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java, src/test/java/com/talhanation/workers/MiningPatternPlannerBranchTest.java</files>
  <behavior>
    - Test 1: the main strip corridor stays at the configured work height instead of descending like tunnel mode.
    - Test 2: branch segments alternate left/right from the main corridor at the configured spacing and stop at the configured branch length.
    - Test 3: planned strip targets still exclude self-undermine cells and preserve the configured cross-section.
  </behavior>
  <action>Extend `MiningPatternPlanner` for MINING-03 rather than creating a second unrelated mining algorithm. Add deterministic branch-mining planning based on the settings contract from Plan 29-01 and the tunnel foundation from Plan 29-02. The planner should emit the main corridor plus alternating side branches using the area facing as the primary axis. Keep this helper pure and fully covered by focused JUnit assertions.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MiningPatternPlannerBranchTest --console=plain</automated>
  </verify>
  <done>Branch-mining geometry is explicit, deterministic, and proven by tests before live worker wiring changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute branch mining through MinerWorkGoal and finish only when the strip layout is exhausted</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java, workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java, workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java, src/test/java/com/talhanation/workers/MiningPatternPlannerBranchTest.java</files>
  <behavior>
    - Test 1: branch mode progresses through corridor and branch segments without dropping back to idle between branches.
    - Test 2: ore-wall cleanup still applies only to the exposed walls of the active strip path instead of scanning an unrelated box.
    - Test 3: `DONE` is reached only after every configured corridor/branch segment has been mined.
  </behavior>
  <action>Wire `MinerWorkGoal` to run the new strip planner when the area is in branch mode per MINING-03. Reuse the existing tool/torch/floor maintenance loops, but replace the old `scanBreakArea()` completion logic with strip-layout progress tracking so the worker does not stop after one corridor slice. Keep modifications local to the mining flow; do not redesign storage, ownership, or unrelated worker AI.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MiningPatternPlannerBranchTest --console=plain && ./gradlew compileJava --console=plain</automated>
  </verify>
  <done>Branch-mining mode produces a real strip-mining loop with configurable height, spacing, and branch length instead of leaving miners stationary.</done>
</task>

</tasks>

<verification>
- Branch mode produces a corridor plus alternating side branches.
- Branch spacing/length come from the authored settings contract, not hardcoded magic numbers.
- Miner completion tracks the full strip layout rather than the old generic scan box.
</verification>

<success_criteria>
- Phase 29 delivers the second requested miner pattern, not only the diagonal tunnel fix.
- Branch mining is test-backed and configurable from the same MiningArea authoring surface.
- Miner AI no longer reverts to static behavior once tunnel mode is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/29-1-3-3-2-branch-mining-strip-mining-3-21-26/29-1-3-3-2-branch-mining-strip-mining-3-21-26-03-SUMMARY.md`
</output>
