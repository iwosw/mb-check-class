---
phase: 29-1-3-3-2-branch-mining-strip-mining-3-21-26
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java
  - workers/src/main/java/com/talhanation/workers/world/StructureManager.java
  - workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java
  - workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java
  - src/test/java/com/talhanation/workers/StructureTemplateLoaderTest.java
autonomous: true
requirements:
  - BUILD-01
must_haves:
  truths:
    - "Builders can load external schematic templates into a BuildArea instead of being limited to saved NBT scans."
    - "Loaded schematics feed the same preview, required-material, and build execution pipeline as existing scans rather than creating a second build path."
    - "The template chooser can browse supported structure files without hiding or breaking the current scan/save workflow."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java"
      provides: "Unified loader for `.nbt`, `.schem`, and `.schematic` templates"
      contains: "loadTemplate|supportedExtensions"
    - path: "workers/src/main/java/com/talhanation/workers/world/StructureManager.java"
      provides: "Conversion of loaded templates into the existing structure NBT shape"
      contains: "parseStructureFromNBT|loadScanNbt"
    - path: "workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java"
      provides: "BuildArea load-mode UI wired to the unified template loader"
      contains: "Mode.LOAD|setStructure"
    - path: "workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java"
      provides: "Template picker that lists supported schematic and scan files"
      contains: "scanDirectory|onSelect"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java"
      to: "workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java"
      via: "the picker must pass the real selected file path, including its extension, to the unified loader"
      pattern: "scanDirectory|supportedExtensions|loadTemplate"
    - from: "workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java"
      to: "workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java"
      via: "loaded templates must arrive as the same CompoundTag shape BuildAreaScreen already previews and sends to the server"
      pattern: "CompoundTag|setStructure|MessageUpdateBuildArea"
---

<objective>
Let builders load schematic templates through the existing BuildArea flow.

Purpose: the third Phase 29 request is to restore builder productivity by accepting external schematic files, not only locally scanned `.nbt` templates. The safest path is to import schematics into the same NBT structure the current preview/build pipeline already understands.
Output: one unified structure-template loader plus BuildArea UI support for browsing and loading supported schematic files.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@build.gradle
@workers/src/main/java/com/talhanation/workers/world/StructureManager.java
@workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java
@workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java
@workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java

<interfaces>
From `build.gradle`:
```groovy
implementation fg.deobf('curse.maven:worldedit-225608:4586218')
```

From `workers/src/main/java/com/talhanation/workers/world/StructureManager.java`:
```java
public static CompoundTag loadScanNbt(String scanRelativePath)
public static List<ScannedBlock> parseStructureFromNBT(CompoundTag root)
```

From `workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java`:
```java
private void setStructure(List<ScannedBlock> structure, CompoundTag structureNBT)
private void performClientScan()
```

From `workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java`:
```java
private List<Entry> scanDirectory(Path dir, Path relativePath)
```

Use the existing WorldEdit dependency already present in `build.gradle` for schematic parsing per BUILD-01; do not invent a second build-preview format or break `.nbt` scans that already work.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a unified structure-template loader that keeps BuildArea on one NBT contract</name>
  <files>workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java, workers/src/main/java/com/talhanation/workers/world/StructureManager.java, src/test/java/com/talhanation/workers/StructureTemplateLoaderTest.java</files>
  <behavior>
    - Test 1: `.nbt` templates still load through the current scan path without changing the returned CompoundTag shape.
    - Test 2: `.schem` and `.schematic` files are parsed with the existing WorldEdit dependency and converted into the same root tag fields (`name`, `width`, `height`, `depth`, `facing`, `blocks`, `entities`).
    - Test 3: imported tags still pass through `WorkersRuntime.migrateStructureNbt(...)` before BuildArea consumes them.
  </behavior>
  <action>Create `StructureTemplateLoader` per BUILD-01 as the single import seam for builder templates. Dispatch by extension: keep `.nbt` on the existing loader, and add `.schem` / `.schematic` import through the already-declared WorldEdit dependency from `build.gradle`. Convert imported schematics into the exact CompoundTag shape that `BuildAreaScreen`, `BuildArea`, and `MessageUpdateBuildArea` already use. Do not create a parallel structure-preview or build execution path.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.StructureTemplateLoaderTest --console=plain && ./gradlew compileJava --console=plain</automated>
  </verify>
  <done>Builders have one template-loading seam that supports both legacy scans and schematic imports while preserving the existing build contract.</done>
</task>

<task type="auto">
  <name>Task 2: Surface supported template files in the BuildArea load UI without breaking scan/save mode</name>
  <files>workers/src/main/java/com/talhanation/workers/client/gui/BuildAreaScreen.java, workers/src/main/java/com/talhanation/workers/client/gui/widgets/ScrollDropDownMenuWithFolders.java, workers/src/main/java/com/talhanation/workers/world/StructureTemplateLoader.java</files>
  <action>Update the BuildArea load flow per BUILD-01 so the picker lists supported template extensions and passes the real selected path (including extension) to the unified loader. Keep scan/save mode behavior intact: scan mode still captures and saves `.nbt` structures, while load mode can open `.nbt`, `.schem`, and `.schematic` templates and feed them into `setStructure(...)`, required-material calculation, and `MessageUpdateBuildArea`. Do not regress the current required-items dropdown, preview widget, or build/place buttons.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.StructureTemplateLoaderTest --console=plain && ./gradlew compileJava --console=plain</automated>
  </verify>
  <done>BuildArea load mode can browse supported schematic files and preview/build them through the same existing pipeline used for saved scans.</done>
</task>

</tasks>

<verification>
- `.nbt` scans still load.
- `.schem` / `.schematic` imports resolve through the unified loader.
- BuildArea preview, required materials, and build actions still operate on one CompoundTag contract.
</verification>

<success_criteria>
- Builders are no longer blocked on scan-only templates.
- Schematic import reuses the existing BuildArea preview/build path instead of introducing a second codepath.
- Template browsing in load mode supports the new formats without breaking current saved scans.
</success_criteria>

<output>
After completion, create `.planning/phases/29-1-3-3-2-branch-mining-strip-mining-3-21-26/29-1-3-3-2-branch-mining-strip-mining-3-21-26-04-SUMMARY.md`
</output>
