---
phase: 29-1-3-3-2-branch-mining-strip-mining-3-21-26
plan: 02
type: execute
wave: 2
depends_on:
  - 29-01
files_modified:
  - workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java
  - workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
  - src/test/java/com/talhanation/workers/MiningPatternPlannerTunnelTest.java
autonomous: true
requirements:
  - MINING-02
must_haves:
  truths:
    - "A miner assigned to tunnel mode advances diagonally down and forward relative to the area facing instead of standing still."
    - "The mined tunnel keeps the configured cross-section (3x3 or other allowed size) while avoiding any block directly under the miner's own standing cell."
    - "Tunnel execution still respects the existing torch, close-floor, and tool-request loops instead of bypassing worker survival/maintenance behavior."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      provides: "Pure tunnel-segment planning for diagonal excavation"
      contains: "planTunnelSegment|excludeSelfUndermine"
    - path: "workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java"
      provides: "Live miner execution that consumes planned tunnel segments instead of only generic area scans"
      contains: "PREPARE_MINING|MINING|CHECK"
    - path: "src/test/java/com/talhanation/workers/MiningPatternPlannerTunnelTest.java"
      provides: "Regression proof for the requested diagonal tunnel geometry"
      contains: "diagonalTunnel"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      via: "the stored mining settings must drive tunnel geometry rather than ad hoc constants"
      pattern: "MiningPatternSettings|planTunnelSegment"
    - from: "workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java"
      via: "planned tunnel segments must feed the live break/place stacks the worker already executes"
      pattern: "planTunnelSegment|stackToBreak|stackToPlace"
---

<objective>
Make miners actually excavate the requested diagonal tunnel.

Purpose: the highest-priority defect is that miners idle instead of progressing toward a mine. This plan replaces the box-scan-only behavior with an explicit diagonal tunnel pattern that moves away from the miner and never digs directly under them.
Output: one testable tunnel planner plus live MinerWorkGoal wiring for diagonal excavation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
@workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java
@workers/src/main/java/com/talhanation/workers/entities/ai/MiningPositionSelection.java

<interfaces>
From `workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java`:
```java
public boolean mineBlocks(Stack<BlockPos> positions)
public boolean closeHoles(Stack<BlockPos> positions)
public boolean moveToPosition(BlockPos pos, int threshold)
private BlockPos getNewMiningPosition(Stack<BlockPos> positions)
```

From `workers/src/main/java/com/talhanation/workers/entities/ai/MiningPositionSelection.java`:
```java
public static BlockPos popNearest(Stack<BlockPos> positions, Vec3 origin)
```

From `workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java`:
```java
public void scanBreakArea()
public void scanFloorArea()
public AABB createArea()
```

Tunnel mode must be implemented per MINING-02: descend diagonally away from the worker's placement/facing, keep the configured tunnel dimensions, and never schedule the block directly under the miner as a break target.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a pure diagonal tunnel planner with self-undermine guardrails</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java, src/test/java/com/talhanation/workers/MiningPatternPlannerTunnelTest.java</files>
  <behavior>
    - Test 1: a tunnel plan for facing north/south/east/west advances one step forward and one step down per segment instead of mining a vertical shaft.
    - Test 2: the generated break targets cover the requested tunnel width/height (including the 3x3 case) but exclude the block directly below the miner's current standing location.
    - Test 3: floor-close targets are emitted separately so the existing cobblestone patching loop can stabilize liquids/holes behind the miner.
  </behavior>
  <action>Create `MiningPatternPlanner` as a pure helper that consumes `MiningPatternSettings`, the work-area origin/facing, and a current segment index to produce deterministic break/place targets per MINING-02. Keep the planner side-effect free so JUnit can prove the geometry. Do not embed worker inventory checks or navigation here; this file only describes tunnel layout and the no-dig-under-self rule.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MiningPatternPlannerTunnelTest --console=plain</automated>
  </verify>
  <done>The repository has a test-backed diagonal tunnel planner that produces a descending corridor without self-undermining.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive MinerWorkGoal from the tunnel planner instead of the old rectangular scan</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java, workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java, workers/src/main/java/com/talhanation/workers/entities/ai/MiningPatternPlanner.java, src/test/java/com/talhanation/workers/MiningPatternPlannerTunnelTest.java</files>
  <behavior>
    - Test 1: tunnel mode reuses the planned break stack until the current diagonal segment is complete, then advances to the next segment instead of resetting to idle.
    - Test 2: torch placement, pickaxe requests, and close-floor cobblestone filling still run on the tunnel path.
    - Test 3: done-check logic finishes only after all planned tunnel segments are mined, not merely after the legacy box scan empties.
  </behavior>
  <action>Wire `MinerWorkGoal` to consume `MiningPatternPlanner` when the area is in tunnel mode per MINING-02. Replace the old `scanBreakArea()`-only behavior with planned tunnel segments, but keep the existing worker maintenance loops (tool requests, torch placement, close-floor fill, ore cleanup where still appropriate). Use `MiningArea` only as the source of persisted settings/progress; do not reintroduce vertical-shaft targets or any block directly under the miner.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MiningPatternPlannerTunnelTest --console=plain && ./gradlew compileJava --console=plain</automated>
  </verify>
  <done>A miner in tunnel mode now progresses through a diagonal tunnel layout and no longer freezes at the mine entrance.</done>
</task>

</tasks>

<verification>
- Tunnel targets descend diagonally away from the miner's facing.
- No planned break target is the block directly under the miner.
- `MinerWorkGoal` still preserves its maintenance loops while mining the new pattern.
</verification>

<success_criteria>
- The core “miner stands still” defect is replaced by a deterministic diagonal tunnel loop.
- The requested 3x3-or-configurable tunnel shape is explicit and test-backed.
- Tunnel mode can be extended later without reopening the UI/network contract work from Plan 29-01.
</success_criteria>

<output>
After completion, create `.planning/phases/29-1-3-3-2-branch-mining-strip-mining-3-21-26/29-1-3-3-2-branch-mining-strip-mining-3-21-26-02-SUMMARY.md`
</output>
