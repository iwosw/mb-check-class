---
phase: 29-1-3-3-2-branch-mining-strip-mining-3-21-26
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
  - workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java
  - workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java
  - src/test/java/com/talhanation/workers/MiningPatternSettingsTest.java
  - src/test/java/com/talhanation/workers/MessageUpdateMiningAreaCodecTest.java
autonomous: true
requirements:
  - MINING-01
must_haves:
  truths:
    - "Players can switch a mining area between diagonal tunnel mode and branch-mining mode instead of miners inferring behavior from the old generic box scan."
    - "Configured mining settings survive the full UI -> packet -> entity-state round-trip so miners do not revert to stale defaults after editing."
    - "Downstream miner logic has one explicit settings contract for width, height, descent, branch spacing, branch length, and close-floor behavior."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java"
      provides: "Shared mining-mode/settings contract for UI, packets, and miner AI"
      contains: "MiningMode|branchSpacing|branchLength|heightOffset"
    - path: "workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java"
      provides: "Persistent mining-area state that stores the selected excavation pattern"
      contains: "readAdditionalSaveData|addAdditionalSaveData|setHeightOffset"
    - path: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java"
      provides: "Network payload that mirrors the mining settings contract"
      contains: "toBytes|fromBytes|update"
    - path: "workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java"
      provides: "Authoring surface for selecting and editing mining pattern settings"
      contains: "setButtons|sendMessage"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java"
      to: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java"
      via: "screen controls must send the exact mining-mode fields the server applies"
      pattern: "sendMessage|new MessageUpdateMiningArea"
    - from: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java"
      via: "server packet handling must update the same stored settings the miner later reads"
      pattern: "update\(|setHeightOffset|setCloseFloor"
---

<objective>
Define the mining-area settings contract before changing miner behavior.

Purpose: Phase 29 starts by making the requested tunnel and branch-mining modes explicit and persistent so later execution work does not reverse-engineer intent from a generic rectangular area. This is the control surface for the high-priority miner fix.
Output: one shared mining settings contract plus synchronized MiningArea/UI/network support for editing and storing it.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/VERIFICATION.md
@workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java
@workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java
@workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java
@workers/src/main/java/com/talhanation/workers/entities/ai/MinerWorkGoal.java

<interfaces>
From `workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java`:
```java
public int getHeightOffset()
public void setHeightOffset(int offset)
public boolean getCloseFloor()
public void setCloseFloor(boolean close)

public enum MiningMode {
    CUSTOM(0),
    MINE(1),
    TUNNEL(2);
}
```

From `workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java`:
```java
public MessageUpdateMiningArea(UUID uuid, int xSize, int ySize, int zSize, int yOffset, boolean closeFloor)
public void update(MiningArea miningArea)
public MessageUpdateMiningArea fromBytes(FriendlyByteBuf buf)
public void toBytes(FriendlyByteBuf buf)
```

From `workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java`:
```java
protected void init()
public void setButtons()
public void sendMessage()
```

Preserve the existing size/height-offset/close-floor controls, but stop relying on them alone to imply the mining algorithm. This plan creates the contract later miner tasks will implement against.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the explicit mining pattern settings contract</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/workarea/MiningPatternSettings.java, src/test/java/com/talhanation/workers/MiningPatternSettingsTest.java</files>
  <behavior>
    - Test 1: tunnel-mode settings preserve width, height, descent/height offset, and close-floor choices in one immutable contract.
    - Test 2: branch-mode settings expose branch spacing and branch length with sane lower bounds so later AI logic never receives zero/negative branch geometry.
    - Test 3: the contract can convert to and from packet/NBT-friendly primitives without embedding world or entity references.
  </behavior>
  <action>Create `MiningPatternSettings` as the single source of truth for Phase 29 mining behavior per MINING-01. Include the excavation mode plus the minimum parameters later plans need (`width`, `height`, `heightOffset`, `closeFloor`, `branchSpacing`, `branchLength`, and any simple descent step field you need for diagonal tunneling). Keep this file pure and testable: no `Level`, `Entity`, or pathfinding calls. Use the current `MiningArea.MiningMode` vocabulary as the starting point, but expand it so downstream plans can distinguish the user-requested diagonal tunnel and branch-mining flows cleanly.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MiningPatternSettingsTest --console=plain</automated>
  </verify>
  <done>A reusable mining settings contract exists, is covered by focused JUnit assertions, and is ready for MiningArea/UI/network wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and author the mining settings through MiningArea, packet, and screen flow</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/workarea/MiningArea.java, workers/src/main/java/com/talhanation/workers/network/MessageUpdateMiningArea.java, workers/src/main/java/com/talhanation/workers/client/gui/MiningAreaScreen.java, src/test/java/com/talhanation/workers/MessageUpdateMiningAreaCodecTest.java</files>
  <behavior>
    - Test 1: `MessageUpdateMiningArea` round-trips every mining settings field instead of silently dropping the new mode/branch parameters.
    - Test 2: server-side `update(MiningArea)` applies the same settings contract the screen edited.
    - Test 3: mining-area save/load keeps the selected mode and branch parameters so workers resume the same job after reload.
  </behavior>
  <action>Wire the new contract into `MiningArea`, `MessageUpdateMiningArea`, and `MiningAreaScreen` per MINING-01. Add screen controls for mining mode and branch-specific values while preserving the existing size, y-offset, and close-floor controls. The packet must carry every new field, and `MiningArea` must persist them through synced entity state and NBT save/load. Do not change `MinerWorkGoal` execution in this plan; the only goal here is to make author intent explicit and durable.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.MessageUpdateMiningAreaCodecTest --tests com.talhanation.workers.MiningPatternSettingsTest --console=plain && ./gradlew compileJava --console=plain</automated>
  </verify>
  <done>Players can configure mining behavior explicitly, and the chosen settings survive UI edits, packet delivery, and MiningArea persistence.</done>
</task>

</tasks>

<verification>
- `MiningPatternSettings` exists as the explicit contract later miner plans consume.
- `MessageUpdateMiningArea` round-trips all mining settings fields.
- `MiningAreaScreen` can author mode-specific values without removing the existing size/close-floor controls.
</verification>

<success_criteria>
- Phase 29 no longer depends on a generic rectangular mining box to guess intent.
- The repository has one durable settings contract for diagonal tunneling and branch mining.
- Later miner execution plans can implement behavior without redefining UI/network/persistence fields.
</success_criteria>

<output>
After completion, create `.planning/phases/29-1-3-3-2-branch-mining-strip-mining-3-21-26/29-1-3-3-2-branch-mining-strip-mining-3-21-26-01-SUMMARY.md`
</output>
