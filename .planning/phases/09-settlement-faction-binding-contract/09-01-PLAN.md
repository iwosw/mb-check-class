---
phase: 09-settlement-faction-binding-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md
  - .planning/CODEBASE.md
  - .planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md
  - MERGE_NOTES.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "BannerMod settlement-faction binding is defined explicitly as a derived claim-plus-infrastructure contract rather than an implied architectural note."
    - "Friendly, hostile, unclaimed, and degraded settlement states are named in one active contract that later runtime and validation slices can reuse."
    - "The contract does not promise a new standalone settlement manager or broad persistence rewrite unless execution proves one is necessary."
  artifacts:
    - path: ".planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md"
      provides: "Active settlement-faction lifecycle and legality contract"
      contains: "friendly claim"
    - path: ".planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md"
      provides: "Integrated architecture wording aligned to the new contract"
      contains: "Claim loss should degrade"
    - path: ".planning/CODEBASE.md"
      provides: "Merged runtime vocabulary updated with the new settlement contract seam"
      contains: "Settlement layer"
  key_links:
    - from: ".planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md"
      to: "workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java"
      via: "current settlement-placement legality already depends on faction-claim ownership"
      pattern: "isInsideOwnFactionClaim"
    - from: ".planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md"
      to: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      via: "current settlement participation and authoring already depend on owner/team checks"
      pattern: "getAuthoringAccess|canWorkHere"
    - from: ".planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md"
      to: "recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java"
      via: "current claim lookup seam that grounds derived settlement binding"
      pattern: "getClaim"
---

<objective>
Publish the explicit settlement-faction binding rules and lifecycle vocabulary for the merged runtime.

Purpose: BannerMod already treats settlements as derived claim-plus-work-area hubs in architecture notes and scattered code paths, but later legality, degradation, and military/logistics follow-up will stay ambiguous until that gameplay contract is written down in one active, code-backed source.
Output: One settlement-faction contract doc plus aligned root planning vocabulary in the active codebase and merge notes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/phases/09-settlement-faction-binding-contract/09-CONTEXT.md
@.planning/phases/08-multiplayer-authority-conflict-validation/08-multiplayer-authority-conflict-validation-02-SUMMARY.md
@.planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md
@MERGE_NOTES.md
@workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java
@recruits/src/main/java/com/talhanation/recruits/FactionEvents.java

<interfaces>
Reuse these current code seams directly when writing the contract:

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java`:
```java
@Nullable
public RecruitsClaim getClaim(ChunkPos chunkPos)
public static RecruitsClaim getClaimAt(ChunkPos pos, List<RecruitsClaim> allClaims)
```

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java`:
```java
public boolean containsChunk(ChunkPos chunkPos)
public RecruitsFaction getOwnerFaction()
public String getOwnerFactionStringID()
```

From `workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java`:
```java
public WorkAreaAuthoringRules.AccessLevel getAuthoringAccess(Player player)
public boolean isSameTeamMember(Player player)
public boolean canWorkHere(AbstractWorkerEntity worker)
```

From `workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java`:
```java
public static Decision createDecision(boolean insideFactionClaim, boolean overlapping)
public static Decision modifyDecision(boolean areaExists, AccessLevel accessLevel)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the active settlement-faction contract</name>
  <files>.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md</files>
  <action>Create `.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-contract.md` as the active contract for settlement-faction binding in the merged runtime. Define settlement as a derived operational footprint formed by friendly claim coverage plus active worker infrastructure, not a required new saved-data manager. Name the minimum lifecycle and legality vocabulary explicitly: at least friendly-claim settlement, hostile-claim settlement, unclaimed footprint, and degraded settlement after claim loss or faction mismatch. State how these statuses should be interpreted by placement, worker participation, shared authoring, and later military/logistics readers without promising immediate broad behavior rewrites. Ground every rule in current code seams such as claim lookup, claim owner faction, work-area authoring access, and worker `canWorkHere(...)` behavior.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>The phase has one explicit contract document that downstream implementation and validation slices can cite instead of re-deriving settlement-faction rules from architecture notes.</done>
</task>

<task type="auto">
  <name>Task 2: Align active planning vocabulary to the settlement-faction contract</name>
  <files>.planning/CODEBASE.md, .planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md, MERGE_NOTES.md</files>
  <action>Update the active planning docs so the new contract is visible outside the phase folder. In `.planning/CODEBASE.md`, add the settlement-faction contract as an active shared seam beside authority and supply vocabulary. In `.planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md`, tighten the settlement/claim relationship and degradation wording so it matches the new contract rather than broad future-facing language alone. In `MERGE_NOTES.md`, record any conflict between older implied settlement wording and the now-explicit derived-binding contract, especially if legacy notes suggest a deeper manager before the current code justifies one. Keep all wording truthful to the current codebase and do not claim that Phase 09 already enforces the contract at runtime.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>The active root planning docs and merge notes all describe the same derived settlement-faction contract and do not force later readers to infer it from scattered phrases.</done>
</task>

</tasks>

<verification>
Run `./gradlew test` after the planning docs land, then confirm the new contract doc, `.planning/CODEBASE.md`, `.planning/codebase/INTEGRATED_SYSTEM_ARCHITECTURE.md`, and `MERGE_NOTES.md` all describe the same derived settlement-faction lifecycle and scope boundary.
</verification>

<success_criteria>
- The phase directory contains one explicit settlement-faction contract document grounded in current claim and work-area code seams
- Active planning docs use the same lifecycle vocabulary for friendly, hostile, unclaimed, and degraded settlement states
- No document overstates Phase 09 into a required new persistence manager or broad gameplay rewrite
</success_criteria>

<output>
After completion, create `.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-binding-contract-01-SUMMARY.md`
</output>
