---
phase: 09-settlement-faction-binding-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
  - workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java
  - workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
  - workers/src/main/java/com/talhanation/workers/client/WorkersClientManager.java
  - src/test/java/com/talhanation/bannermod/settlement/BannerModSettlementBindingTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "BannerMod exposes one shared settlement-faction query seam instead of scattering friendly-claim checks across worker creation, participation, and client helpers."
    - "The new seam reports faction-aware settlement status consistently for friendly, hostile, unclaimed, and mismatch/degraded situations without requiring a new settlement manager."
    - "Current worker and authoring behavior stays low-risk: callers are routed through the shared seam rather than rewritten into a broader settlement system."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      provides: "Shared faction-aware settlement legality and status vocabulary"
      contains: "Status"
    - path: "workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java"
      provides: "Work-area placement legality routed through the shared settlement seam"
      contains: "executeServerSide"
    - path: "src/test/java/com/talhanation/bannermod/settlement/BannerModSettlementBindingTest.java"
      provides: "Focused regression coverage for settlement status resolution"
      contains: "friendlyClaim"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      to: "recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java"
      via: "shared claim lookup used to resolve settlement binding status"
      pattern: "getClaim"
    - from: "workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "creation legality delegates to the shared settlement status seam"
      pattern: "BannerModSettlementBinding"
    - from: "workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "worker participation or visibility reads the same settlement status vocabulary"
      pattern: "BannerModSettlementBinding"
---

<objective>
Add the smallest runtime seam needed so faction-aware settlement legality and status can be queried consistently.

Purpose: Today the merged runtime already checks faction claims, owner/team membership, and work-area legality, but those decisions are split across worker packets, area entities, and client helpers. Phase 09 needs one shared settlement-binding seam before Phase 10 can validate legality and degradation behavior cleanly.
Output: One shared settlement-binding utility plus the smallest call-site routing and narrow regression coverage needed to prove the seam.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/phases/09-settlement-faction-binding-contract/09-CONTEXT.md
@.planning/phases/09-settlement-faction-binding-contract/09-01-PLAN.md
@workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java
@workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java
@workers/src/main/java/com/talhanation/workers/client/WorkersClientManager.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java
@recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java
@recruits/src/main/java/com/talhanation/recruits/FactionEvents.java

<interfaces>
Build the new shared seam around these current contracts:

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaimManager.java`:
```java
@Nullable
public RecruitsClaim getClaim(ChunkPos chunkPos)
```

From `recruits/src/main/java/com/talhanation/recruits/world/RecruitsClaim.java`:
```java
public boolean containsChunk(ChunkPos chunkPos)
public RecruitsFaction getOwnerFaction()
public String getOwnerFactionStringID()
```

From `workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java`:
```java
public boolean isOwnedBy(Player player)
public boolean isSameTeamMember(Player player)
public boolean canWorkHere(AbstractWorkerEntity worker)
```

From `workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java`:
```java
public static Decision createDecision(boolean insideFactionClaim, boolean overlapping)
public static boolean isAllowed(Decision decision)
```

If execution needs a shared status vocabulary, create it under `src/main/java/com/talhanation/bannermod/settlement/` and keep the API utility-shaped rather than introducing a manager or saved-data type.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared settlement-binding runtime seam</name>
  <files>src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java, workers/src/main/java/com/talhanation/workers/network/MessageAddWorkArea.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java, workers/src/main/java/com/talhanation/workers/client/WorkersClientManager.java</files>
  <behavior>
    - Friendly claim resolution returns an explicit status that current placement and settlement participation callers can treat as legal.
    - Hostile, unclaimed, or faction-mismatch resolution returns explicit non-friendly statuses instead of each caller recomputing ad hoc booleans.
    - Existing runtime behavior stays low-risk by routing present checks through the shared seam rather than inventing new gameplay permissions.
  </behavior>
  <action>Create `BannerModSettlementBinding.java` under `src/main/java/com/talhanation/bannermod/settlement/` as a small shared utility that resolves faction-aware settlement status from claim ownership plus caller faction/team context. Include the minimum explicit status vocabulary needed by the Phase 09 contract, such as `FRIENDLY_CLAIM`, `HOSTILE_CLAIM`, `UNCLAIMED`, and one degraded or mismatch status when claim ownership and settlement affiliation no longer align. Then route the current scattered callers through that seam: replace the private claim check inside `MessageAddWorkArea` with the shared resolver, update `AbstractWorkAreaEntity` and `WorkersClientManager` only where using the same seam materially removes duplicate settlement legality logic, and keep all changes additive/minimal. Do not create a persistent settlement registry, saved-data manager, or broad behavior rewrite in this plan.</action>
  <verify>
    <automated>./gradlew compileJava test</automated>
  </verify>
  <done>The merged runtime has one shared settlement-binding query seam, and the highest-value existing settlement legality callers now use it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add focused regression coverage for settlement-binding status resolution</name>
  <files>src/test/java/com/talhanation/bannermod/settlement/BannerModSettlementBindingTest.java</files>
  <behavior>
    - The shared resolver distinguishes friendly claim, hostile claim, and unclaimed territory deterministically.
    - A mismatch or degraded path is covered so Phase 10 validation does not have to discover the status vocabulary by accident.
  </behavior>
  <action>Create `BannerModSettlementBindingTest.java` as a narrow root regression test for the new shared settlement-binding utility. Prefer pure or near-pure status-resolution tests over GameTests in this phase: construct the minimum representative claim or faction inputs needed to assert friendly, hostile, unclaimed, and mismatch/degraded outcomes, and verify any helper methods used by `MessageAddWorkArea` or `WorkersClientManager` stay truthful. Keep the tests focused on the new shared seam, not on broad worker AI or multiplayer flows that belong to Phase 10.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>The new settlement-binding seam is defended by focused automated coverage before later GameTest enforcement slices depend on it.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileJava test` after the shared settlement-binding seam lands, then confirm `MessageAddWorkArea`, any touched work-area/client helpers, and the new regression test all use the same explicit settlement status vocabulary.
</verification>

<success_criteria>
- One shared BannerMod settlement-binding utility now resolves faction-aware legality/status without a new manager
- Current worker placement and any touched settlement participation helpers route through that seam instead of duplicate claim checks
- Focused regression coverage pins the friendly, hostile, unclaimed, and degraded/mismatch status vocabulary before Phase 10 GameTests consume it
</success_criteria>

<output>
After completion, create `.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-binding-contract-02-SUMMARY.md`
</output>
