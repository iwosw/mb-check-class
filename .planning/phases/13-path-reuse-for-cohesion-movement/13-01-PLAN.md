---
phase: 13-path-reuse-for-cohesion-movement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java
  - recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java
  - recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java
autonomous: true
requirements:
  - PATHREUSE-01
must_haves:
  truths:
    - "Nearby or formation-cohesive recruits can reuse an already computed compatible path instead of immediately recomputing one."
    - "Stale, incompatible, or finished paths are discarded instead of being forced onto a different recruit."
    - "Built-in profiling can show reuse hits, misses, and invalidation/drop reasons on the shared controller seam."
  artifacts:
    - path: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      provides: "Shared reuse cache/eligibility seam and profiling snapshot fields"
      contains: "reuse"
    - path: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java"
      provides: "Live recruit path issuance wired through controller reuse lookup before fresh computation"
      contains: "GlobalPathfindingController"
    - path: "recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java"
      provides: "Regression coverage for reuse eligibility, discard behavior, and profiling counters"
      contains: "reuse"
  key_links:
    - from: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java"
      to: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      via: "request wrapper that attempts compatible-path reuse before supplier execution"
      pattern: "GlobalPathfindingController\\.(request|reuse)"
    - from: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      to: "recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java"
      via: "unit assertions on reuse hit/miss/invalidation counters"
      pattern: "reuse"
---

<objective>
Implement the smallest safe runtime seam for path reuse on top of the Phase 12 global controller.

Purpose: reduce duplicate path creation for cohesive movement without widening correctness risk.
Output: controller-managed reuse logic, caller wiring, and unit coverage for the reuse rules.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-path-reuse-for-cohesion-movement/13-CONTEXT.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md
@.planning/phases/12-global-pathfinding-control/12-VALIDATION.md
@.planning/phases/12-global-pathfinding-control/12-global-pathfinding-control-01-SUMMARY.md
@.planning/phases/12-global-pathfinding-control/12-global-pathfinding-control-02-SUMMARY.md
@recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java
@recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java

<interfaces>
From recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java:
```java
public static <T> T requestPath(PathRequest request, Supplier<T> requestSupplier)
public static void resetProfiling()
public static ProfilingSnapshot profilingSnapshot()

public record PathRequest(RequestKind requestKind, boolean asyncPathfindingEnabled, int targetCount) {}
public record ProfilingSnapshot(
        long totalRequests,
        long blockTargetRequests,
        long entityTargetRequests,
        long asyncEnabledRequests,
        long asyncDisabledRequests,
        long targetPositionsObserved
) {}
```

From recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java:
```java
@Nullable
public Path createPath(Entity entityTarget, int reachRange)

@Nullable
protected Path createPath(Set<BlockPos> targets, int regionOffset, boolean offsetUpward, int reachRange, float followRange)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the global controller with explicit reuse contracts and counters</name>
  <files>recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java, recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java</files>
  <behavior>
    - Test 1: a compatible second request can receive the previously computed path result without invoking a fresh supplier.
    - Test 2: stale, null, done, or incompatible candidates are rejected and counted as misses/drops instead of being reused.
    - Test 3: profiling snapshots expose reuse hits, misses, invalidations, and retained Phase 12 request counters together.
  </behavior>
  <action>Create the narrowest controller-owned reuse contract inside `GlobalPathfindingController` instead of scattering cache logic into entity classes, per the Phase 12 controller-first decision. Add request metadata needed to decide compatibility for cohesion movement (same request kind plus bounded target compatibility and reuse-safe caller context), store only the minimum reusable path reference/state, and expose resettable profiling counters for reuse attempts, hits, misses, and invalidation/drop reasons. Do not add async queueing, throttling, or formation-targeting policy here; this seam must stay synchronous/pass-through when no safe reusable result exists.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest</automated>
  </verify>
  <done>`GlobalPathfindingController` can answer a reuse attempt deterministically, falls back cleanly when unsafe, and unit tests lock the compatibility/invalidation vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route recruit path creation through the reuse-aware controller seam</name>
  <files>recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java, recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java, recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java</files>
  <behavior>
    - Test 1: entity-target and block-target requests still record the original Phase 12 request-shape counters.
    - Test 2: compatible repeated requests from nearby/cohesive movement flow through the controller and can reuse instead of recomputing.
    - Test 3: incompatible targets or changed movement intent force a fresh supplier path rather than reusing a stale result.
  </behavior>
  <action>Update `AsyncPathNavigation` so both entity-target and block-target path creation provide the controller with enough metadata to attempt safe reuse before calling `doCreatePath(...)`. Keep the actual path-building logic in `doCreatePath(...)`; the controller should decide reuse, not reimplement path generation. Any reused path must still honor existing early-return safety in `doCreatePath(...)` (server thread, empty targets, unusable current path, processed-path checks). If safety is ambiguous, drop the candidate and recompute. Preserve Phase 12 counter behavior while expanding the snapshot with Phase 13 reuse metrics.</action>
  <verify>
    <automated>./gradlew compileJava test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest</automated>
  </verify>
  <done>Live recruit path issuance reaches one reuse-aware controller seam, duplicate cohesion movement can reuse only compatible paths, and unsafe cases still recompute through the existing path builder.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest`
- `./gradlew compileJava`
</verification>

<success_criteria>
- A single controller-owned reuse seam exists for recruit path issuance.
- The seam exposes built-in counters for reuse attempts, hits, misses, and invalidation/discard reasons.
- Unit coverage proves compatible reuse works and unsafe reuse is rejected.
</success_criteria>

<output>
After completion, create `.planning/phases/13-path-reuse-for-cohesion-movement/13-path-reuse-for-cohesion-movement-01-SUMMARY.md`
</output>
