---
phase: 11-large-battle-ai-profiling-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md
  - .planning/phases/11-large-battle-ai-profiling-baseline/11-VALIDATION.md
  - .planning/VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Large-battle baseline work stays evidence-first: it defines repeatable scenarios and counters before Phase 11-02 adds instrumentation or harness code."
    - "The baseline is anchored to real merged-runtime battle and pathfinding seams that already exist, especially recruit battle stress fixtures, recruit target acquisition, and async pathfinding execution."
    - "Profiling evidence records both runtime configuration and measurement outputs so later optimization slices can rerun the same matrix honestly."
  artifacts:
    - path: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      provides: "Canonical profiling scenario matrix, hotspot inventory, and counter definitions for large-battle AI work"
      contains: "Scenario Matrix|Counters|Evidence Package"
    - path: ".planning/phases/11-large-battle-ai-profiling-baseline/11-VALIDATION.md"
      provides: "Repeatable baseline capture and rerun procedure for the performance roadmap"
      contains: "Capture Procedure|Acceptance Rules"
    - path: ".planning/VERIFICATION.md"
      provides: "Root reference to the new large-battle profiling baseline entrypoint"
      contains: "Phase 11"
  key_links:
    - from: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      to: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java"
      via: "baseline scenarios should derive from the existing dense-battle fixtures instead of inventing unrelated performance maps"
      pattern: "BASELINE_DENSE|HEAVY_DENSE|spawnScenario"
    - from: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      via: "target-acquisition profiling needs to point at the current recruit search cadence and async target-finding seam"
      pattern: "searchForTargets|searchForTargetsAsync|searchForTargetsSync"
    - from: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      to: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java"
      via: "pathfinding profiling must capture whether async queueing, sync fallback, and worker-count configuration affect the baseline"
      pattern: "start|queue|awaitProcessing"
    - from: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java"
      via: "baseline documentation should call out the current recruit navigation entrypoint that switches between vanilla and async pathfinders"
      pattern: "UseAsyncPathfinding|createPathFinder|moveTo"
---

<objective>
Define the large-battle profiling scenarios, counters, and evidence format for AI and pathfinding work.

Purpose: The performance roadmap already commits BannerMod to pathfinding and combat-AI optimization, but no active artifact yet says exactly which battle setups, runtime toggles, counters, and evidence bundles count as the shared baseline. This plan makes the measurement contract explicit before instrumentation or optimization changes begin.
Output: One Phase 11 baseline specification, one matching validation/capture procedure, and one root verification reference that together define how large-battle profiling is collected and compared across later phases.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-CONTEXT.md
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/MixedSquadBattleGameTests.java
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathfinder.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java
@recruits/src/main/java/com/talhanation/recruits/config/RecruitsServerConfig.java

<interfaces>
Treat these existing seams as the baseline source of truth:

From `recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressFixtures.java`:
```java
public static final StressScenario BASELINE_DENSE
public static final StressScenario HEAVY_DENSE
public static ScenarioState spawnBaselineDenseScenario(GameTestHelper helper)
public static ScenarioState spawnHeavyDenseScenario(GameTestHelper helper)
```

From `recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java`:
```java
public void tick()
public void searchForTargets()
```

From `recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java`:
```java
protected PathFinder createPathFinder(int range)
public boolean moveTo(double x, double y, double z, double speed)
```

From `recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java`:
```java
public static void start()
protected static void queue(@NotNull AsyncPath path)
public static void awaitProcessing(@Nullable Path path, @Nullable MinecraftServer server, Consumer<@Nullable Path> afterProcessing)
```

From `recruits/src/main/java/com/talhanation/recruits/config/RecruitsServerConfig.java`:
```java
public static ForgeConfigSpec.BooleanValue UseAsyncPathfinding
public static ForgeConfigSpec.BooleanValue UseAsyncTargetFinding
public static ForgeConfigSpec.IntValue AsyncPathfindingThreadsCount
```

Do not add new runtime counters in this plan unless documenting an existing signal requires a tiny naming clarification in `VERIFICATION.md`; instrumentation belongs to Plan 11-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Publish the canonical large-battle profiling baseline specification</name>
  <files>.planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md</files>
  <behavior>
    - The baseline names the concrete battle scenarios that later phases must rerun, rooted in the current stress fixtures or other already-shipped battle harnesses.
    - The baseline names the hot-path counters to collect for AI and pathfinding, including at minimum server tick cost, pathfinding volume or completion behavior, and target-acquisition activity.
    - The baseline defines one evidence package format so later optimization slices can compare measurements without ad hoc notes.
  </behavior>
  <action>Create `11-PROFILING-BASELINE.md` under the Phase 11 folder as the canonical large-battle profiling spec. Anchor the scenario matrix to the current merged-runtime battle fixtures, especially `BASELINE_DENSE` and `HEAVY_DENSE`, and note any additional comparison scenario only if it already exists in the codebase and materially broadens the baseline. Document the current hotspot inventory with direct references to recruit target search, recruit navigation/path creation, and async path processing. Then define the counters and evidence package that future phases must preserve, including runtime config toggles, scenario id, entity counts, warmup or settle assumptions if any, measurement duration, raw capture location, and summary fields that make before/after comparisons possible. Keep the artifact doc-shaped and evidence-first; do not bundle instrumentation implementation or optimization proposals into this task.</action>
  <verify>
    <automated>./gradlew compileGameTestJava test</automated>
  </verify>
  <done>Phase 11 has one explicit profiling contract that says which large-battle scenarios, hotspots, counters, and evidence bundles define the baseline for the rest of the performance roadmap.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Publish the baseline capture procedure and root verification reference</name>
  <files>.planning/phases/11-large-battle-ai-profiling-baseline/11-VALIDATION.md, .planning/VERIFICATION.md</files>
  <behavior>
    - The repo now states how to capture and store Phase 11 profiling evidence reproducibly.
    - The verification docs distinguish doc-only baseline definition from the later instrumentation task so readers know what is ready now versus what Plan 11-02 must add.
    - Acceptance rules explain what makes a baseline capture complete enough to compare against later phases.
  </behavior>
  <action>Create `11-VALIDATION.md` with the exact capture procedure for the baseline: prerequisite build or test commands, expected config toggles to record, scenario execution order, minimum rerun count or sample expectation if the codebase supports it, and where raw profiler outputs or logs should be stored in the workspace when the baseline is actually collected. Update `.planning/VERIFICATION.md` with a concise Phase 11 entry that points readers at the new baseline spec and validation procedure, explicitly noting that Plan 11-01 defines the contract while Plan 11-02 will add any missing instrumentation or harness seams needed to gather the measurements. Keep the wording truthful to the current repo state.</action>
  <verify>
    <automated>./gradlew compileGameTestJava test</automated>
  </verify>
  <done>The root planning docs now tell contributors exactly how Phase 11 baseline evidence should be captured and where that protocol lives.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileGameTestJava test` after the new Phase 11 docs land, then confirm the scenario ids, hotspot references, and config toggles in the docs match the current code paths instead of speculative future implementation details.
</verification>

<success_criteria>
- Phase 11 now has one canonical baseline spec that names repeatable large-battle scenarios, hotspot seams, counters, and evidence fields
- Root verification docs point at the baseline spec and clearly separate baseline-definition work from later instrumentation work
- The new documents stay grounded in existing recruit battle fixtures and current async target/pathfinding seams rather than inventing a detached benchmark harness
</success_criteria>

<output>
After completion, create `.planning/phases/11-large-battle-ai-profiling-baseline/11-large-battle-ai-profiling-baseline-01-SUMMARY.md`
</output>
