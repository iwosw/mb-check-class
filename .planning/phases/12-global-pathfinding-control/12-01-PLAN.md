---
phase: 12-global-pathfinding-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java
  - recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java
  - recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java
  - recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java
autonomous: true
requirements: []
must_haves:
  truths:
    - "High-volume recruit path requests now pass through one explicit BannerMod-owned control seam instead of being issued ad hoc from each caller."
    - "The new control seam preserves current small-battle behavior by delegating to the existing synchronous or async path creation flow without adding throttles yet."
    - "Later performance phases can inspect or extend one shared request-issuance point instead of rediscovering path creation hooks across entity AI files."
  artifacts:
    - path: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      provides: "Shared path-request control and profiling seam for recruit navigation"
      contains: "requestPath|resetProfiling|profilingSnapshot"
    - path: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java"
      provides: "Routing of current navigation path creation through the shared controller"
      contains: "createPath|GlobalPathfindingController"
    - path: "recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java"
      provides: "Low-risk regression coverage for control-seam pass-through and resettable counters"
      contains: "requestPath|profilingSnapshot"
  key_links:
    - from: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java"
      to: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      via: "all shared createPath entrypoints should delegate request issuance through one controller-owned method"
      pattern: "createPath|requestPath"
    - from: "recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java"
      to: "recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java"
      via: "recruit-specific navigation should keep using the shared async navigation base rather than reintroducing a local bypass"
      pattern: "moveTo|createPath"
---

<objective>
Add the shared global pathfinding control seam and route the current high-volume callers through it.

Purpose: Phase 11 established how BannerMod measures dense-battle pathfinding cost. This plan creates the first real runtime control boundary so later reuse, throttling, and async safety work can act on one request-issuance seam instead of many scattered navigation calls.
Output: One shared pathfinding control class, routed recruit navigation entrypoints, and narrow JUnit coverage proving the seam remains pass-through and resettable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/12-global-pathfinding-control/12-CONTEXT.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-VALIDATION.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-large-battle-ai-profiling-baseline-02-SUMMARY.md
@recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/PathProcessingRuntime.java
@recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java
@recruits/src/test/java/com/talhanation/recruits/pathfinding/AsyncPathProcessorTest.java

<interfaces>
Reuse these existing seams directly:

From `recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java`:
```java
protected Path createPath(Set<BlockPos> targets, int additionalOffset, boolean targetBlockAbove, int accuracy, float followRange)
public boolean moveTo(double x, double y, double z, double speed)
public boolean moveTo(@NotNull Entity entity, double speed)
public boolean moveTo(@Nullable Path path, double speed)
```

From `recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathProcessor.java`:
```java
public static void start()
protected static void queue(@NotNull AsyncPath path)
public static void awaitProcessing(@Nullable Path path, @Nullable MinecraftServer server, Consumer<@Nullable Path> afterProcessing)
public static void resetProfiling()
public static ProfilingSnapshot profilingSnapshot()
```

From `recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java`:
```java
protected @NotNull PathFinder createPathFinder(int range)
public boolean moveTo(double x, double y, double z, double speed)
```

Keep this plan pass-through only: do not add queue budgets, request deferral, random slowdown, or caller-specific guards yet.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared global pathfinding control contract</name>
  <files>recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java, recruits/src/test/java/com/talhanation/recruits/pathfinding/GlobalPathfindingControllerTest.java</files>
  <behavior>
    - A single controller-owned API now represents path-request issuance for the recruit runtime.
    - The controller records minimal resettable request counters or source metadata so later phases can observe this seam without rediscovering every caller.
    - The controller delegates immediately to the current path-building lambda and returns the exact current result; no throttling, caching, or async policy changes are introduced in this phase.
  </behavior>
  <action>Create `GlobalPathfindingController` under `recruits.pathfinding` as the one BannerMod-owned request-issuance seam for recruit navigation. Model it as a narrow static utility with a small request descriptor and `requestPath(...)` API that accepts enough metadata to distinguish the current high-volume request shapes (at minimum block-position versus entity-targeted requests, plus whether async pathfinding is enabled) while still delegating to the existing path-creation logic immediately. Add `resetProfiling()` and `profilingSnapshot()` style helpers so the later validation plan can compare controller activity alongside the Phase 11 counters. Add `GlobalPathfindingControllerTest` first, covering pass-through result behavior, request counting, and reset semantics without introducing Minecraft-heavy mocks or runtime initialization.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest</automated>
  </verify>
  <done>The codebase has one explicit global controller seam for path request issuance, and its narrow contract is protected by JUnit coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route the current recruit navigation entrypoints through the shared controller</name>
  <files>recruits/src/main/java/com/talhanation/recruits/pathfinding/AsyncPathNavigation.java, recruits/src/main/java/com/talhanation/recruits/entities/ai/navigation/RecruitPathNavigation.java</files>
  <behavior>
    - Shared recruit path creation now flows through `GlobalPathfindingController` before the existing pathfinder work begins.
    - The public navigation behavior stays unchanged for small battles: same return types, same async handoff rules, same failure/null handling.
    - Recruit-specific navigation does not keep a hidden direct path-issuance bypass after the controller lands.
  </behavior>
  <action>Update `AsyncPathNavigation` so the common `createPath(...)` flow delegates actual request issuance through `GlobalPathfindingController.requestPath(...)` instead of instantiating or invoking the pathfinder directly inline. Pass enough request metadata for later profiling and budgeting work, but keep the implementation synchronous/pass-through in this phase. Adjust `RecruitPathNavigation` only as needed so its recruit-specific target setup still happens before the shared controller path is requested and it does not bypass the new seam. Do not add budgets, sleeps, random skipping, per-entity cooldowns, or any local feature flags here; the goal is one shared control point, not optimization yet.</action>
  <verify>
    <automated>./gradlew compileJava test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest</automated>
  </verify>
  <done>Current recruit path creation is now routed through one explicit shared control boundary, with existing navigation semantics preserved.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileJava test --tests com.talhanation.recruits.pathfinding.GlobalPathfindingControllerTest` and confirm the new controller stays pass-through only while `AsyncPathNavigation` now owns the routing into that controller for shared recruit path issuance.
</verification>

<success_criteria>
- BannerMod has one explicit runtime seam for recruit path-request issuance
- Shared recruit navigation routes through that seam without adding throttles or behavior drift yet
- Resettable controller counters exist so Phase 12-02 can validate and profile the seam against the Phase 11 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-pathfinding-control/12-global-pathfinding-control-01-SUMMARY.md`
</output>
