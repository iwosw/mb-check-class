---
phase: 12-global-pathfinding-control
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/MixedSquadBattleGameTests.java
  - .planning/phases/12-global-pathfinding-control/12-VALIDATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Small-battle navigation behavior still resolves correctly after routing requests through the shared global control seam."
    - "Dense-battle profiling now reports the new control-seam counters alongside the Phase 11 target-search and async pathfinding counters."
    - "Before/after evidence for the global control boundary can be compared against Phase 11 using the same scenario ids and async config snapshot rules."
  artifacts:
    - path: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/MixedSquadBattleGameTests.java"
      provides: "Small-battle correctness validation with the new global pathfinding control seam active"
      contains: "mixed"
    - path: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java"
      provides: "Dense-battle controller-aware profiling capture on the existing baseline scenarios"
      contains: "baselineDenseBattleCompletesWithoutBrokenLoops|heavierDenseBattleCompletesWithoutBrokenLoops"
    - path: ".planning/phases/12-global-pathfinding-control/12-VALIDATION.md"
      provides: "Phase 12 correctness and before/after profiling procedure tied back to the Phase 11 baseline"
      contains: "before/after|baseline_dense_battle|heavy_dense_battle"
  key_links:
    - from: "recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      to: "recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java"
      via: "profiling snapshots should include the new controller counters in the same capture path as Phase 11 battle evidence"
      pattern: "profilingSnapshot|captureProfilingSnapshot"
    - from: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java"
      to: ".planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md"
      via: "dense-battle comparison must stay on the same mandatory scenario ids and config vocabulary"
      pattern: "baseline_dense_battle|heavy_dense_battle"
---

<objective>
Validate correctness and capture before/after profiling for the new global control boundary.

Purpose: The shared control seam is only useful if it preserves live battle behavior and produces evidence that later optimization slices can compare honestly. This plan adds the validation hooks and Phase 12 evidence procedure without widening scope into throttling or reuse.
Output: GameTest coverage for small-battle correctness, dense-battle controller-aware profiling snapshots, and one Phase 12 validation procedure for before/after capture.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/12-global-pathfinding-control/12-CONTEXT.md
@.planning/phases/12-global-pathfinding-control/12-01-PLAN.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-VALIDATION.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-large-battle-ai-profiling-baseline-02-SUMMARY.md
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/MixedSquadBattleGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java

<interfaces>
Expect Plan 12-01 to provide a narrow shared control seam like:

```java
public final class GlobalPathfindingController {
    public static <T> T requestPath(...);
    public static void resetProfiling();
    public static ProfilingSnapshot profilingSnapshot();
}
```

Reuse these existing validation seams:

From `recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java`:
```java
public static void baselineDenseBattleCompletesWithoutBrokenLoops(GameTestHelper helper)
public static void heavierDenseBattleCompletesWithoutBrokenLoops(GameTestHelper helper)
```

From `recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java`:
```java
public static BattleProfilingSnapshot captureProfilingSnapshot(GameTestHelper helper, String scenarioId, int warmupTicks)
public static String formatProfilingSnapshot(BattleProfilingSnapshot snapshot)
public static BattleSquad spawnWestMixedSquad(GameTestHelper helper, UUID ownerId)
public static BattleSquad spawnEastMixedSquad(GameTestHelper helper, UUID ownerId)
```

Stay on the Phase 11 scenario ids and config vocabulary; do not invent a new benchmark map or a separate profiler harness in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend GameTests to prove the control seam preserves battle behavior and exposes controller activity</name>
  <files>recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/BattleStressGameTests.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/MixedSquadBattleGameTests.java</files>
  <behavior>
    - Small mixed-squad combat still resolves correctly with the global controller seam active.
    - Dense-battle snapshots now include the controller request counters beside the Phase 11 target-search and async path counters.
    - The validation path resets controller profiling between scenarios so repeated runs stay comparable.
  </behavior>
  <action>Update the existing GameTest harness so `GlobalPathfindingController.resetProfiling()` runs alongside the Phase 11 counter resets. Extend `RecruitsBattleGameTestSupport.BattleProfilingSnapshot` and formatting helpers to carry the controller snapshot fields. Then add the smallest correctness assertion to `MixedSquadBattleGameTests` proving a representative small battle still resolves and emits non-zero controller activity after Plan 12-01 routes path requests through the seam. In `BattleStressGameTests`, keep the current combat assertions intact but require the dense scenarios to emit non-zero controller request activity and log the controller fields in the same stable snapshot line used for evidence capture.</action>
  <verify>
    <automated>./gradlew compileGameTestJava verifyGameTestStage</automated>
  </verify>
  <done>The live GameTest harness now proves the global pathfinding controller is exercised in both small and dense battle flows without regressing the current correctness contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Publish the Phase 12 before/after validation procedure</name>
  <files>.planning/phases/12-global-pathfinding-control/12-VALIDATION.md</files>
  <behavior>
    - Phase 12 evidence capture tells operators how to compare controller-aware runs against the Phase 11 baseline.
    - The procedure preserves the same mandatory scenario ids and async config snapshot requirements.
    - The evidence contract clearly separates correctness verification from performance comparison so later phases can reuse it.
  </behavior>
  <action>Create `12-VALIDATION.md` as the execution-time procedure for this phase. Reference the Phase 11 baseline as the before-state source of truth, require the same mandatory dense scenarios (`baseline_dense_battle` and `heavy_dense_battle`) plus the optional `mixed_squad_battle` comparison when available, and specify that every Phase 12 evidence bundle must include the new controller counters next to the Phase 11 target-search and async path counters. Document the expected commands, reset behavior, config fields, evidence directory layout, and comparison rules needed to say whether the global control seam preserved correctness while changing the measured request-issuance picture.</action>
  <verify>
    <automated>./gradlew compileGameTestJava verifyGameTestStage</automated>
  </verify>
  <done>Phase 12 has a truthful, repeatable correctness-and-profiling procedure for comparing the new control seam against the Phase 11 baseline.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileGameTestJava verifyGameTestStage`, then confirm the mixed-squad and dense-battle flows still succeed while the logged profiling snapshot now includes non-zero controller-request activity and the Phase 12 validation doc points back to the Phase 11 baseline for before/after comparison.
</verification>

<success_criteria>
- Small-battle and dense-battle GameTests still pass with the new global control seam in place
- Profiling snapshots include controller activity in the same capture path as the Phase 11 counters
- Phase 12 has one explicit before/after validation procedure that later optimization slices can reuse without redefining the baseline
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-pathfinding-control/12-global-pathfinding-control-02-SUMMARY.md`
</output>
