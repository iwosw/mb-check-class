---
phase: 01-workspace-bootstrap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/VERIFICATION.md
  - MERGE_NOTES.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - BOOT-03
  - BOOT-04
must_haves:
  truths:
    - "The bootstrap phase documents the current root validation baseline."
    - "Merge conflicts and source-of-truth rules are recorded in one active note file."
    - "The roadmap shows explicit staged bootstrap work rather than hidden rewrites."
  artifacts:
    - path: ".planning/VERIFICATION.md"
      provides: "Root validation baseline"
      contains: "./gradlew compileJava"
    - path: "MERGE_NOTES.md"
      provides: "Recorded merge decisions and plan/code conflicts"
      contains: "real code wins over old plans"
    - path: ".planning/ROADMAP.md"
      provides: "Concrete phase goals and plan list"
      contains: "Phase 1: Workspace Bootstrap"
    - path: ".planning/STATE.md"
      provides: "Current milestone and latest verified progress"
      contains: "merged runtime baseline"
  key_links:
    - from: ".planning/VERIFICATION.md"
      to: "build.gradle"
      via: "documented root Gradle commands"
      pattern: "compileJava|processResources|test"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/01-workspace-bootstrap/01-01-PLAN.md"
      via: "phase plan list"
      pattern: "01-01-PLAN.md"
---

<objective>
Document the active root verification baseline, merge-truth rules, and the explicit bootstrap execution slices.

Purpose: Give future planning and execution work a stable source for how to validate the merged workspace and where conflicts are recorded.
Output: Updated verification/merge notes plus roadmap/state entries that make bootstrap work explicit and traceable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-workspace-bootstrap/01-RESEARCH.md
@.planning/VERIFICATION.md
@MERGE_NOTES.md
@build.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock the bootstrap verification baseline and merge conflict policy</name>
  <files>.planning/VERIFICATION.md, MERGE_NOTES.md</files>
  <read_first>.planning/VERIFICATION.md, MERGE_NOTES.md, build.gradle, .planning/phases/01-workspace-bootstrap/01-CONTEXT.md, .planning/CODEBASE.md</read_first>
  <action>Update the active verification and merge-truth docs to implement D-02 through D-07. In `.planning/VERIFICATION.md`, keep the current stabilization baseline as exactly `./gradlew compileJava`, `./gradlew processResources`, and `./gradlew test`, and state that `./gradlew runGameTestServer` is additive only when gameplay/runtime behavior changes while root GameTests remain sparse. In `MERGE_NOTES.md`, keep the bootstrap decision list explicit: runtime base is `recruits`, final runtime id is `bannermod`, one active root `.planning/` tree exists, legacy planning trees are archived, and real code plus active root docs beat legacy plans on conflict. If you uncover any mismatch between those active docs and the current root code, record the disagreement in `MERGE_NOTES.md` per D-03 instead of silently choosing the legacy wording.</action>
  <acceptance_criteria>
    - `.planning/VERIFICATION.md` contains `./gradlew compileJava`
    - `.planning/VERIFICATION.md` contains `./gradlew processResources`
    - `.planning/VERIFICATION.md` contains `./gradlew test`
    - `.planning/VERIFICATION.md` contains `Use \`./gradlew runGameTestServer\` only when a change requires runtime/gameplay validation`
    - `MERGE_NOTES.md` contains `Chosen root planning model: one active `.planning/` at repository root.`
    - `MERGE_NOTES.md` contains `Chosen truth policy: real code wins over old plans; disagreements must be recorded here.`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileJava processResources test</automated>
  </verify>
  <done>The root validation baseline is explicitly documented and the merge notes remain the active place to record conflicts between legacy planning and current root truth.</done>
</task>

<task type="auto">
  <name>Task 2: Publish explicit bootstrap plans in roadmap and state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md, .planning/STATE.md, .planning/phases/01-workspace-bootstrap/01-CONTEXT.md, .planning/phases/01-workspace-bootstrap/01-RESEARCH.md, .planning/REQUIREMENTS.md</read_first>
  <action>Update the active planning files so Phase 1 is represented as explicit staged work per D-11 and D-15. In `.planning/ROADMAP.md`, keep the Phase 1 goal outcome-focused around one root Gradle entrypoint, one active root planning context, preserved legacy archives, and merge-truth documentation; add a `**Plans:** 2 plans` line and list `01-01-PLAN.md` and `01-02-PLAN.md` with short objectives. In `.planning/STATE.md`, keep the current merged-runtime-baseline milestone accurate while making the latest planning activity mention that Phase 1 bootstrap planning artifacts now exist under `.planning/phases/01-workspace-bootstrap/`. Do not backfill fake execution progress; only record the new planning state truthfully.</action>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `**Plans:** 2 plans`
    - `.planning/ROADMAP.md` contains `01-01-PLAN.md`
    - `.planning/ROADMAP.md` contains `01-02-PLAN.md`
    - `.planning/STATE.md` contains `.planning/phases/01-workspace-bootstrap/`
    - `.planning/STATE.md` still contains `milestone: merged-runtime-baseline`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>The roadmap shows explicit bootstrap execution slices and the project state records that Phase 1 planning artifacts now exist without pretending Phase 1 was re-executed.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileJava processResources test` from the root, then read `.planning/VERIFICATION.md`, `MERGE_NOTES.md`, and `.planning/ROADMAP.md` to confirm the bootstrap truth, validation baseline, and plan list are all explicit.
</verification>

<success_criteria>
- Root verification docs specify compile/resources/test as the current default baseline
- `MERGE_NOTES.md` remains the explicit conflict log for legacy-vs-root truth
- `ROADMAP.md` lists the Phase 1 plan files and plan count
- `STATE.md` records the creation of Phase 1 planning artifacts without inflating execution status
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-bootstrap/01-workspace-bootstrap-02-SUMMARY.md`
</output>
