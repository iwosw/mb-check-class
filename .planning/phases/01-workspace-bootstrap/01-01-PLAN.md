---
phase: 01-workspace-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - settings.gradle
  - .planning/PROJECT.md
  - .planning/CODEBASE.md
autonomous: true
requirements:
  - BOOT-01
  - BOOT-02
must_haves:
  truths:
    - "The repository builds from one root Gradle entrypoint."
    - "The active planning source of truth is the root `.planning/` tree."
    - "Legacy planning trees stay preserved without becoming active requirement sources."
  artifacts:
    - path: "build.gradle"
      provides: "Single merged root build entrypoint"
      contains: "java.srcDirs = ['src/main/java', 'recruits/src/main/java', 'workers/src/main/java']"
    - path: "settings.gradle"
      provides: "Single root project identity"
      contains: "rootProject.name = 'bannermod'"
    - path: ".planning/PROJECT.md"
      provides: "Active project identity and merge stance"
      contains: "Legacy context archives"
    - path: ".planning/CODEBASE.md"
      provides: "Root source-of-truth paths and active runtime/build wiring"
      contains: "Planning source of truth: `.planning/`"
  key_links:
    - from: "build.gradle"
      to: "recruits/ and workers/ source trees"
      via: "root sourceSets configuration"
      pattern: "sourceSets"
    - from: ".planning/PROJECT.md"
      to: ".planning/CODEBASE.md"
      via: "shared root-truth wording"
      pattern: "source of truth"
---

<objective>
Lock the repository to one root workspace/build entrypoint and one active planning root.

Purpose: Make downstream work start from the merged root instead of treating legacy trees as peer workspaces.
Output: Hardened root build identity plus root project/codebase docs that explicitly describe the active workspace contract.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-workspace-bootstrap/01-CONTEXT.md
@.planning/phases/01-workspace-bootstrap/01-RESEARCH.md
@.planning/CODEBASE.md
@MERGE_NOTES.md
@build.gradle
@settings.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the single root build entrypoint</name>
  <files>build.gradle, settings.gradle</files>
  <read_first>build.gradle, settings.gradle, .planning/phases/01-workspace-bootstrap/01-CONTEXT.md, .planning/CODEBASE.md, .planning/VERIFICATION.md</read_first>
  <action>Update the root Gradle files so they explicitly preserve one active merged entrypoint per D-05 and D-12 through D-14. In `build.gradle`, keep one root `main` source set that composes exactly `src/main/java`, `recruits/src/main/java`, and `workers/src/main/java`; keep one root `test` source set that composes exactly `src/test/java` and `recruits/src/test/java`; keep one root `gametest` source set that composes exactly `src/gametest/java` and `recruits/src/gametest/java`; keep Forge run targets registered under the `bannermod` mod id; keep `processResources` as the single merged resource entrypoint; and keep the verification tasks `verifyBuildStage`, `verifyUnitTestStage`, `verifyGameTestStage`, and `mergeStatus` wired from the root file. In `settings.gradle`, keep the single root identity `rootProject.name = 'bannermod'`. Do not add secondary subproject build files or alternate active mod ids.</action>
  <acceptance_criteria>
    - `settings.gradle` contains `rootProject.name = 'bannermod'`
    - `build.gradle` contains `java.srcDirs = ['src/main/java', 'recruits/src/main/java', 'workers/src/main/java']`
    - `build.gradle` contains `property 'forge.enabledGameTestNamespaces', 'bannermod'`
    - `build.gradle` contains `tasks.register('mergeStatus')`
    - `build.gradle` contains `tasks.named('processResources')`
  </acceptance_criteria>
  <verify>
    <automated>./gradlew compileJava processResources</automated>
  </verify>
  <done>The root build file is the only active build entrypoint and it still composes the merged runtime from the repository root.</done>
</task>

<task type="auto">
  <name>Task 2: Make the active workspace contract explicit in root docs</name>
  <files>.planning/PROJECT.md, .planning/CODEBASE.md</files>
  <read_first>.planning/PROJECT.md, .planning/CODEBASE.md, .planning/phases/01-workspace-bootstrap/01-CONTEXT.md, MERGE_NOTES.md, MERGE_PLAN.md</read_first>
  <action>Refresh the active root docs so they implement D-01, D-02, D-03, D-08, D-09, and D-15 explicitly. In `.planning/PROJECT.md`, state that the merged workspace has one active root `.planning/` context, that `.planning_legacy_recruits/` and `.planning_legacy_workers/` are preserved archives, that root code and active root docs win on conflict, and that the workspace is staged around the `bannermod` root runtime. In `.planning/CODEBASE.md`, keep the source-of-truth paths section explicit about `.planning/`, `MERGE_NOTES.md`, `build.gradle`, `settings.gradle`, `src/main/java/`, `recruits/src/main/java/`, and `workers/src/main/java/`, and keep the legacy archive section clearly marked as historical-only so follow-up agents do not treat archived planning as active requirements.</action>
  <acceptance_criteria>
    - `.planning/PROJECT.md` contains `Legacy context archives: `.planning_legacy_recruits/`, `.planning_legacy_workers/``
    - `.planning/PROJECT.md` contains `Prefer real code over legacy planning artifacts when the two disagree.`
    - `.planning/CODEBASE.md` contains `Planning source of truth: `.planning/``
    - `.planning/CODEBASE.md` contains `Legacy archives are preserved for historical context only`
    - `.planning/CODEBASE.md` contains `Root build/runtime wiring: `build.gradle`, `settings.gradle`, `src/main/resources/``
  </acceptance_criteria>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>Active root docs tell downstream agents to use the root workspace/build/docs as canonical and treat legacy planning trees as archives.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileJava processResources test` from the repository root. Confirm the root docs still describe one active build/planning workspace and preserved legacy archives.
</verification>

<success_criteria>
- Root Gradle identity remains `bannermod`
- The merged source sets still compile from the root build
- Root project/codebase docs explicitly describe the root workspace as canonical
- Legacy planning trees remain preserved but non-canonical
</success_criteria>

<output>
After completion, create `.planning/phases/01-workspace-bootstrap/01-workspace-bootstrap-01-SUMMARY.md`
</output>
