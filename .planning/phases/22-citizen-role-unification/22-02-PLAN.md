---
phase: 22-citizen-role-unification
plan: 02
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - src/main/java/com/talhanation/bannermod/citizen/CitizenRole.java
  - src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java
  - src/main/java/com/talhanation/bannermod/citizen/CitizenRoleContext.java
  - recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
  - workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - src/test/java/com/talhanation/bannermod/citizen/CitizenRoleControllerTest.java
autonomous: true
requirements:
  - CITIZEN-01
  - CITIZEN-02
must_haves:
  truths:
    - "Role-specific behavior has an explicit citizen role/controller seam instead of pushing Phase 22 into a deeper inheritance tree."
    - "Recruit and worker base wrappers can expose a shared citizen core without losing their current class identities or entrypoints."
    - "Common ownership, team, inventory, persistence, navigation-handoff, and runtime-flag operations are delegatable through one citizen-owned seam."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenRole.java"
      provides: "Minimal role vocabulary for recruit and worker specializations"
      exports: ["CitizenRole"]
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java"
      provides: "Role-controller contract for shared citizen runtime hooks"
      exports: ["CitizenRoleController"]
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenRoleContext.java"
      provides: "Typed context object passed to role controllers"
      exports: ["CitizenRoleContext"]
    - path: "src/test/java/com/talhanation/bannermod/citizen/CitizenRoleControllerTest.java"
      provides: "Focused automated coverage for role/controller delegation"
      contains: "@Test"
  key_links:
    - from: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      to: "src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java"
      via: "wrapper-owned citizen accessor/delegation"
      pattern: "CitizenCore"
    - from: "workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      to: "src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java"
      via: "worker wrapper role/controller bridge"
      pattern: "CitizenRoleController"
---

<objective>
Install the citizen role/controller seam and wrapper adapters so recruit and worker bases can participate in one citizen runtime without changing public entrypoints.

Purpose: this is the compatibility layer that lets later live-path conversions stay narrow.
Output: citizen role contracts, recruit/worker wrapper accessors, and regression tests proving the shared seam is delegatable.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-citizen-role-unification/22-CONTEXT.md
@.planning/phases/22-citizen-role-unification/22-01-PLAN.md
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java` (from Plan 22-01):
```java
public interface CitizenCore
```

From `recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java`:
```java
public UUID getOwnerUUID()
public int getFollowState()
public void setOwnerUUID(Optional<UUID> id)
public void setFollowState(int state)
public void clearHoldPos()
public void clearMovePos()
public SimpleContainer getInventory()
```

From `workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java`:
```java
public boolean recoverControl(Player requester)
public boolean isWorking()
protected void rememberCurrentWorkAreaBinding()
@Nullable public UUID getBoundWorkAreaUUID()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the citizen role vocabulary and controller contracts</name>
  <files>src/main/java/com/talhanation/bannermod/citizen/CitizenRole.java, src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java, src/main/java/com/talhanation/bannermod/citizen/CitizenRoleContext.java</files>
  <action>Create the role/job seam per D-05 and D-07: one minimal controller contract and context object that can host role-specific recruit combat behavior or worker labor behavior later without adding a new deep subclass tree. Keep the contract narrow and wrapper-oriented per D-01 and D-04. Do not redesign UI, packets, or profession presentation in this plan per the deferred boundaries and D-10.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
required = [
    Path('src/main/java/com/talhanation/bannermod/citizen/CitizenRole.java'),
    Path('src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java'),
    Path('src/main/java/com/talhanation/bannermod/citizen/CitizenRoleContext.java'),
]
missing = [str(path) for path in required if not path.exists()]
if missing:
    raise SystemExit('Missing citizen role contract files: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>Downstream live-path plans have an explicit role/controller contract to implement against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add citizen accessors to recruit and worker wrappers without changing their entrypoints</name>
  <files>recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java, workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, src/test/java/com/talhanation/bannermod/citizen/CitizenRoleControllerTest.java</files>
  <behavior>
    - Test 1: recruit-wrapper delegation can expose shared citizen state and role metadata without changing class identity.
    - Test 2: worker-wrapper delegation can expose shared citizen state and role metadata without changing class identity.
    - Test 3: shared role-controller hooks can be invoked from both wrappers while packet and screen entrypoints remain untouched.
  </behavior>
  <action>Wire `AbstractRecruitEntity` and `AbstractWorkerEntity` to host or expose a `CitizenCore` accessor per D-01, D-02, and D-06. Keep the wrappers as the live classes per D-03 and D-04, and preserve packet/screen entrypoints per D-10. The worker side must continue to treat the recruit-owned pathfinding stack as canonical per D-08; do not fork a second navigation stack. Add focused unit coverage proving both wrappers can delegate through the same citizen role/controller contract.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.bannermod.citizen.CitizenRoleControllerTest --console=plain</automated>
  </verify>
  <done>Recruit and worker bases now expose the shared citizen seam while remaining compatibility wrappers around current runtime entrypoints.</done>
</task>

</tasks>

<verification>
- One citizen role/controller contract exists and is reusable by both recruit and worker wrappers.
- `AbstractRecruitEntity` and `AbstractWorkerEntity` both expose the citizen seam without changing entity ids, registries, or packet/screen entrypoints.
- The worker base still rides the recruit-owned navigation stack rather than inventing a separate pathfinding path.
</verification>

<success_criteria>
- Phase 22 now has a stable compatibility layer for narrow live-path adoption.
- Later recruit and worker conversions can stay focused on one path each instead of redoing shared delegation work.
</success_criteria>

<output>
After completion, create `.planning/phases/22-citizen-role-unification/22-citizen-role-unification-02-SUMMARY.md`
</output>
