---
phase: 22-citizen-role-unification
plan: 04
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
  - workers/src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java
  - src/test/java/com/talhanation/workers/CitizenWorkerBridgeTest.java
  - src/gametest/java/com/talhanation/bannermod/BannerModCitizenWorkerGameTests.java
autonomous: true
requirements:
  - CITIZEN-03
  - CITIZEN-04
must_haves:
  truths:
    - "At least one real worker path uses the citizen seam live while the worker wrapper class still exists."
    - "Worker recovery, work-area binding, and navigation handoff stay compatible with the recruit-owned pathfinding stack."
    - "Existing root GameTests remain the main safety net, with one focused worker-side citizen regression added for the new seam."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      provides: "First worker live adoption of citizen-owned shared state"
      contains: "CitizenCore"
    - path: "workers/src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java"
      provides: "Compatibility-safe worker binding/resume seam aligned to citizen state"
      contains: "WorkerBindingResume"
    - path: "src/test/java/com/talhanation/workers/CitizenWorkerBridgeTest.java"
      provides: "Targeted worker-side citizen regression coverage"
      contains: "@Test"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModCitizenWorkerGameTests.java"
      provides: "Root GameTest proof that the worker live path still works through the citizen seam"
      contains: "@GameTest"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      to: "src/main/java/com/talhanation/bannermod/citizen/CitizenRoleController.java"
      via: "worker recovery/binding delegation"
      pattern: "CitizenRoleController"
    - from: "workers/src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java"
      to: "workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java"
      via: "bound work-area resume data"
      pattern: "getBoundWorkAreaUUID|rememberCurrentWorkAreaBinding"
---

<objective>
Convert one controlled worker live path onto the citizen seam while preserving worker wrappers, work-area flows, and current runtime entrypoints.

Purpose: this proves the citizen model can serve real civilian runtime behavior without reopening the whole workers subsystem.
Output: one worker live path delegated through citizen state plus targeted unit and GameTest regressions.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-citizen-role-unification/22-CONTEXT.md
@.planning/phases/22-citizen-role-unification/22-02-PLAN.md
@src/gametest/java/com/talhanation/bannermod/BannerModSettlementLaborGameTests.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
@workers/src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java

<interfaces>
From `workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java`:
```java
public boolean recoverControl(Player requester)
public boolean isWorking()
protected void rememberCurrentWorkAreaBinding()
@Nullable public UUID getBoundWorkAreaUUID()
public void addAdditionalSaveData(CompoundTag nbt)
public void readAdditionalSaveData(CompoundTag nbt)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route one worker recovery/binding path through the citizen seam</name>
  <files>workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java, workers/src/main/java/com/talhanation/workers/entities/WorkerBindingResume.java, src/test/java/com/talhanation/workers/CitizenWorkerBridgeTest.java</files>
  <behavior>
    - Test 1: worker recovery and bound work-area resume data can hydrate from and persist through the citizen seam without changing legacy packet or save entrypoints.
    - Test 2: worker wrapper navigation handoff still rides the recruit-owned pathfinding stack after citizen delegation.
    - Test 3: worker wrapper class identity remains stable while the converted path moves onto citizen-owned state.
  </behavior>
  <action>Convert one controlled worker path per D-13 and D-14 by moving a narrow recovery/work-area-binding/common-flags slice in `AbstractWorkerEntity` and `WorkerBindingResume` onto the citizen seam. Preserve current packet and screen entrypoints per D-10, current worker ids/save identities per D-03, and the recruit-owned navigation stack per D-08. Keep labor AI, profession logic, and work-area semantics role-specific per D-07; this is an internal seam conversion, not a worker gameplay rewrite.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.CitizenWorkerBridgeTest --console=plain</automated>
  </verify>
  <done>The worker wrapper now uses citizen-owned shared state for one real path without changing external worker identity or entrypoints.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add worker-side citizen GameTest coverage and revalidate the root suite</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModCitizenWorkerGameTests.java</files>
  <behavior>
    - Test 1: a worker spawned in the root GameTest runtime can still recover control and preserve work-area binding behavior after citizen-seam hydration.
    - Test 2: the converted worker path remains compatible with the retained settlement-labor and ownership safety expectations.
  </behavior>
  <action>Add a focused root GameTest artifact per D-12 that composes existing settlement-labor and ownership-cycle setup patterns instead of creating a separate test subsystem. Keep the slice narrow to the converted worker path and end the plan by re-running the retained root GameTest gate so Phase 22 closes this live worker adoption with compatibility evidence.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage --console=plain</automated>
  </verify>
  <done>The retained root GameTest gate stays green and now includes one worker-side citizen regression slice.</done>
</task>

</tasks>

<verification>
- One live worker path delegates through the citizen seam while the worker wrapper remains in place.
- Worker recovery and work-area binding still behave through the retained recruit-owned navigation/runtime seams.
- The root GameTest suite remains the acceptance gate, now with an additive worker citizen regression.
</verification>

<success_criteria>
- Phase 22 proves the citizen seam can serve real worker runtime behavior without a broad workers rewrite.
- External worker identities, packet entrypoints, and screen entrypoints stay stable through the first live conversion.
</success_criteria>

<output>
After completion, create `.planning/phases/22-citizen-role-unification/22-citizen-role-unification-04-SUMMARY.md`
</output>
