# Phase 22 Deferred Items

- `./gradlew verifyGameTestStage` still has a pre-existing required failure in `hostileorunclaimedsettlementsnevercreateworkers` that is outside Phase 22 citizen-role scope.
