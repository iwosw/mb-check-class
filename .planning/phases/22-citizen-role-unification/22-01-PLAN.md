---
phase: 22-citizen-role-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java
  - src/main/java/com/talhanation/bannermod/citizen/CitizenStateSnapshot.java
  - src/main/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridge.java
  - src/test/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridgeTest.java
autonomous: true
requirements:
  - CITIZEN-01
must_haves:
  truths:
    - "A shared citizen-owned state contract exists before recruit and worker wrappers begin delegating to it."
    - "Legacy recruit and worker persistence can be read into one citizen snapshot without changing current save-facing keys or runtime ids."
    - "The new citizen seam is additive and compatibility-safe, not a one-shot class retirement."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java"
      provides: "Shared citizen-owned runtime state contract"
      exports: ["CitizenCore"]
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenStateSnapshot.java"
      provides: "Serializable common citizen state value object"
      exports: ["CitizenStateSnapshot"]
    - path: "src/main/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridge.java"
      provides: "Compatibility-safe read/write bridge between legacy NBT shapes and citizen state"
      exports: ["CitizenPersistenceBridge"]
    - path: "src/test/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridgeTest.java"
      provides: "Targeted automated regression coverage for citizen persistence migration"
      contains: "@Test"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridge.java"
      to: "src/main/java/com/talhanation/bannermod/citizen/CitizenStateSnapshot.java"
      via: "read/write conversion"
      pattern: "fromLegacy|toLegacy"
    - from: "src/test/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridgeTest.java"
      to: "src/main/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridge.java"
      via: "snapshot round-trip assertions"
      pattern: "CitizenPersistenceBridge"
---

<objective>
Define the citizen core and persistence contract before any live recruit or worker path begins delegating to it.

Purpose: this creates the compatibility-safe shared seam required by the rest of Phase 22.
Output: citizen core interfaces, a legacy-NBT bridge, and focused regression tests proving the citizen snapshot can absorb shared recruit/worker state.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-citizen-role-unification/22-CONTEXT.md
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java

<interfaces>
From `recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java`:
```java
public void addAdditionalSaveData(CompoundTag nbt)
public void readAdditionalSaveData(CompoundTag nbt)
public UUID getOwnerUUID()
public int getFollowState()
public void setOwnerUUID(Optional<UUID> id)
public void setFollowState(int state)
public void clearHoldPos()
public void clearMovePos()
public SimpleContainer getInventory()
```

From `workers/src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java`:
```java
public boolean recoverControl(Player requester)
public void addAdditionalSaveData(CompoundTag nbt)
public void readAdditionalSaveData(CompoundTag nbt)
public boolean isWorking()
protected void rememberCurrentWorkAreaBinding()
@Nullable public UUID getBoundWorkAreaUUID()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the citizen core contracts and shared snapshot types</name>
  <files>src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java, src/main/java/com/talhanation/bannermod/citizen/CitizenStateSnapshot.java</files>
  <action>Create the initial citizen-owned seam per D-01, D-02, and D-06: one shared contract that can hold owner UUID, team identity, follow/work state, inventory-facing hooks, navigation handoff markers, and common runtime flags without committing to a new entity base class yet. Keep the contract additive and wrapper-friendly per D-04. Do not introduce new entity ids, registries, or packet-facing DTOs per D-03 and D-11.</action>
  <verify>
    <automated>python3 - <<'PY'
from pathlib import Path
required = [
    Path('src/main/java/com/talhanation/bannermod/citizen/CitizenCore.java'),
    Path('src/main/java/com/talhanation/bannermod/citizen/CitizenStateSnapshot.java'),
]
missing = [str(path) for path in required if not path.exists()]
if missing:
    raise SystemExit('Missing citizen contract files: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The repository has one explicit citizen-owned contract and snapshot type that downstream wrapper adapters can code against directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the compatibility-safe citizen persistence bridge</name>
  <files>src/main/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridge.java, src/test/java/com/talhanation/bannermod/citizen/CitizenPersistenceBridgeTest.java</files>
  <behavior>
    - Test 1: legacy recruit-style ownership/follow/inventory keys read into one `CitizenStateSnapshot` without dropping values.
    - Test 2: legacy worker-style ownership/work-area binding/common flags read into the same snapshot shape without changing external keys.
    - Test 3: writing the snapshot back preserves the legacy key names required for compatibility-safe wrappers.
  </behavior>
  <action>Implement `CitizenPersistenceBridge` per D-09 and D-12 so recruit and worker wrappers can read legacy NBT into `CitizenStateSnapshot` and write compatible legacy-shaped tags back out while the phase stays incremental. Keep the bridge pure and brownfield-friendly: no world access, no entity registry changes, and no class-retirement work. Use targeted JUnit coverage as the first proof that the citizen seam can carry shared state before live entity paths adopt it.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.bannermod.citizen.CitizenPersistenceBridgeTest --console=plain</automated>
  </verify>
  <done>Shared citizen persistence is proven by automated tests before any live recruit or worker wrapper delegates to it.</done>
</task>

</tasks>

<verification>
- `CitizenCore`, `CitizenStateSnapshot`, and `CitizenPersistenceBridge` exist under one shared `bannermod.citizen` package.
- Targeted tests prove legacy recruit and worker persistence can flow through the citizen snapshot without changing external keys.
- No runtime id, packet entrypoint, or screen entrypoint changes are introduced in this foundation slice.
</verification>

<success_criteria>
- Downstream plans can consume one explicit citizen state contract instead of rediscovering shared fields in two inheritance trees.
- Legacy save-facing shapes remain readable and writable through the citizen seam.
</success_criteria>

<output>
After completion, create `.planning/phases/22-citizen-role-unification/22-citizen-role-unification-01-SUMMARY.md`
</output>
