---
phase: 14-formation-level-target-selection-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java
  - recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
  - recruits/src/test/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionControllerTest.java
autonomous: true
requirements:
  - TARGETSEL-01
must_haves:
  truths:
    - "Grouped or formation-linked recruits can share one combat target-selection result instead of each running a full nearby-enemy search every cycle."
    - "Shared target assignment drops or recomputes cleanly when the assigned enemy dies, disappears, or no longer passes existing recruit targeting rules."
    - "Built-in profiling exposes whether formation-level target selection is being reused or falling back to per-entity search."
  artifacts:
    - path: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      provides: "Shared formation/squad target-selection and assignment seam with resettable profiling snapshot"
      contains: "ProfilingSnapshot"
    - path: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      provides: "Live recruit target search routed through the formation-aware seam before legacy fallback"
      contains: "searchForTargets"
    - path: "recruits/src/test/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionControllerTest.java"
      provides: "Regression coverage for shared selection, invalidation, fallback, and profiling accounting"
      contains: "formation"
  key_links:
    - from: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      via: "searchForTargets delegates squad/formation assignment before local candidate scan"
      pattern: "FormationTargetSelectionController"
    - from: "recruits/src/test/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionControllerTest.java"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      via: "unit assertions lock profiling and invalidation semantics"
      pattern: "ProfilingSnapshot|fallback|invalidate"
---

<objective>
Replace the hottest per-entity recruit target-acquisition loop with one bounded formation/squad selection seam.

Purpose: TARGETSEL-01 turns repeated recruit-by-recruit target scans into shared combat intent without bundling pathfinding, damage, or broader AI rewrites.
Output: a formation-aware targeting controller, `AbstractRecruitEntity` wiring, and focused unit coverage for selection/fallback semantics.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-CONTEXT.md
@.planning/phases/11-large-battle-ai-profiling-baseline/11-PROFILING-BASELINE.md
@.planning/phases/12-global-pathfinding-control/12-global-pathfinding-control-01-SUMMARY.md
@.planning/phases/13-path-reuse-for-cohesion-movement/13-path-reuse-for-cohesion-movement-01-SUMMARY.md
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java

<interfaces>
From recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java:
```java
public void searchForTargets()
private void searchForTargetsAsync(ServerLevel serverLevel)
private void searchForTargetsSync(ServerLevel serverLevel)
public static void resetTargetSearchProfiling()
public static TargetSearchProfilingSnapshot targetSearchProfilingSnapshot()

public record TargetSearchProfilingSnapshot(
        long totalSearches,
        long asyncSearches,
        long syncSearches,
        long candidateEntitiesObserved,
        long targetsAssigned
) {}
```

From recruits/src/main/java/com/talhanation/recruits/pathfinding/GlobalPathfindingController.java:
```java
public static void resetProfiling()
public static ProfilingSnapshot profilingSnapshot()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the formation-level target-selection contract and lock it with unit tests</name>
  <files>recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java, recruits/src/test/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionControllerTest.java</files>
  <behavior>
    - Test 1: recruits from the same owner/group/formation cohort can reuse one selected living target instead of recomputing a fresh shared choice for every member on the same tick.
    - Test 2: dead, removed, stale, or cohort-incompatible assignments are invalidated and force a fresh selection or explicit fallback.
    - Test 3: profiling snapshots expose requests, shared assignments, reuse/handoff, invalidations, and fallback-to-local-search counts.
  </behavior>
  <action>Create `FormationTargetSelectionController` as the new explicit shared seam for TARGETSEL-01. Keep it utility-shaped and resettable like the Phase 11-13 profiling controllers; do not add a persistent manager or world save layer. Model the controller around lightweight request/assignment records that can be unit tested without spawning real entities. The shared-selection boundary must stay narrow: only recruits that share the same owner UUID plus the same non-null group UUID and are currently in a formation-capable combat state may reuse one selected target; ungrouped or non-formation recruits must fall back to the legacy local search path. The controller must track stable profiling counters named around formation selection requests, computations, assignments, invalidations, and local fallbacks so Plan 14-02 can surface them beside the existing Phase 11-13 counters.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest</automated>
  </verify>
  <done>The controller exposes one explicit contract for shared target selection, invalidation, and profiling, and unit tests prove the cohort/fallback rules without relying on live Minecraft entity setup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route recruit target acquisition through the formation-aware controller</name>
  <files>recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java, recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java, recruits/src/test/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionControllerTest.java</files>
  <behavior>
    - Test 1: grouped formation recruits can receive one shared assigned target while preserving the existing targeting predicate and nearest-candidate ordering rules.
    - Test 2: when a shared assignment is invalid, the recruit clears it and recomputes instead of retaining a dead or illegal target.
    - Test 3: recruits outside an eligible cohort keep the old per-entity search behavior and are counted as controller fallbacks, not silently grouped.
  </behavior>
  <action>Refactor `AbstractRecruitEntity.searchForTargets()`, `searchForTargetsAsync(...)`, and `searchForTargetsSync(...)` so the existing nearby-candidate scan becomes the fallback implementation rather than the default. Use the new controller to gate whether a recruit is eligible for shared squad/formation selection, compute or reuse one assignment for that cohort, and then set/clear the recruit target on the live entity. Preserve the current `targetingConditions` filter, the 40-block search box, async-vs-sync branching, and the existing rule that dead or removed targets are cleared. Do not move pathfinding, attack timing, follow-state transitions, or damage logic into this controller. If live-entity wiring exposes ambiguity about eligibility, prefer a local fallback search over broadening the cohort.</action>
  <verify>
    <automated>./gradlew compileJava test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest</automated>
  </verify>
  <done>`AbstractRecruitEntity` now enters one formation-aware target-selection seam first, shared assignments stay bounded to clear cohort rules, and non-eligible recruits still behave through the preserved local search path.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest`
- `./gradlew compileJava`
</verification>

<success_criteria>
- The runtime has one explicit formation/squad target-selection seam rather than only per-entity `searchForTargets()` scans.
- Shared target assignments are invalidated safely and fall back to local search when the cohort or target is no longer valid.
- Profiling counters exist for Plan 14-02 to compare shared selection activity against the Phase 11 baseline vocabulary.
</success_criteria>

<output>
After completion, create `.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-01-SUMMARY.md`
</output>
