---
phase: 14-formation-level-target-selection-rewrite
plan: 03
type: execute
wave: 3
depends_on:
  - 14-02
files_modified:
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
  - recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
  - recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java
autonomous: true
requirements:
  - TARGETSEL-02
gap_closure: true
must_haves:
  truths:
    - "A surviving formation cohort retargets onto a living enemy after its shared target dies."
    - "A formation cohort clears stale shared targets when no valid enemy replacement exists."
    - "The Phase 14 gap closure preserves the existing focus-fire and dense-battle targeting contract instead of widening into navigation or combat rewrites."
  artifacts:
    - path: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java"
      provides: "Focused GameTests for retarget and no-replacement target-loss behavior on a deterministic cohort setup"
      contains: "formationCohortRetargetsAfterSharedEnemyDies"
    - path: "recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      provides: "Battle helper seam for a narrow recovery/formation scenario that isolates target invalidation without dense-battle noise"
      contains: "spawnRecoveryPair"
    - path: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      provides: "Live recruit target-search wiring that invalidates dead shared targets and either recomputes or clears target state"
      contains: "searchForTargets"
    - path: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      provides: "Shared assignment invalidation/recompute contract with unchanged Phase 14 profiling vocabulary"
      contains: "ProfilingSnapshot"
  key_links:
    - from: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java"
      to: "recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      via: "Focused retarget/loss-of-target scenarios use deterministic cohort helpers instead of the noisier mixed-squad brownfield path"
      pattern: "spawnRecoveryPair|assignFormationCohort|setMutualTargets"
    - from: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      via: "searchForTargets delegates shared target reuse, invalidation, and recompute/clear behavior through the controller"
      pattern: "beginRuntimeSelection|completeRuntimeSelection"
---

<objective>
Close the two remaining Phase 14 local GameTest gaps without reopening unrelated reconnect authority work or widening the target-selection rewrite.

Purpose: TARGETSEL-02 is not fully verified until the retained retarget and loss-of-target cases pass on the real Phase 14 seam, but this gap closure must stay limited to those two failures only.
Output: a deterministic retarget/loss-of-target GameTest setup plus the smallest runtime fix needed for dead-target invalidation and no-replacement clearing.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-CONTEXT.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-VALIDATION.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-01-SUMMARY.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-02-SUMMARY.md
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java

<interfaces>
From recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java:
```java
public static BattleSquad spawnRecoveryPair(GameTestHelper helper, SquadAnchor anchor, UUID ownerId, String squadName)
public static void assignFormationCohort(List<? extends AbstractRecruitEntity> recruits, UUID groupId)
public static void setMutualTargets(List<? extends AbstractRecruitEntity> attackers, List<? extends AbstractRecruitEntity> defenders)
```

From recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java:
```java
public static Decision<LivingEntity> beginRuntimeSelection(RuntimeSelectionRequest request, Predicate<LivingEntity> targetValidator)
public static @Nullable LivingEntity completeRuntimeSelection(RuntimeSelectionRequest request, @Nullable LivingEntity selectedTarget)
public static ProfilingSnapshot profilingSnapshot()

public enum DecisionType {
    REUSED_SHARED_SELECTION,
    COMPUTE_SHARED_SELECTION,
    LOCAL_FALLBACK
}
```
 </interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the flaky retarget/loss-of-target setup with a deterministic cohort harness</name>
  <files>recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java</files>
  <action>Keep the existing Phase 14 focus-fire test intact, but harden the two failing cases called out in the 14-02 summary and STATE.md by moving them off the noisy mixed-squad brownfield setup onto the smallest deterministic cohort arrangement that still exercises the real formation-selection seam. Prefer the existing recovery-pair harness and helper patterns already used elsewhere in battle GameTests: explicitly register formation cohorts, stage aggro/hold intent, and control enemy death timing so the tests isolate only (a) replacement-target selection after one shared enemy dies and (b) target clearing when no enemy remains. Do not add a new world template, do not touch dense-battle scenario ids or profiling fields, and do not widen the scope into Phase 15 pathfinding budgeting or broader combat AI behavior.</action>
  <verify>
    <automated>./gradlew compileGameTestJava</automated>
  </verify>
  <done>The two Phase 14 gap tests run on a narrow deterministic harness whose setup noise is lower than the current mixed-squad battle path, while the existing focus-fire and dense-battle evidence contract stays unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix shared-target invalidation so cohorts retarget or clear correctly</name>
  <files>recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java, recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java, recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java</files>
  <behavior>
    - Test 1: after the shared target dies, the next surviving west cohort member computes one new living enemy and the rest of the cohort converges on that replacement.
    - Test 2: when the last enemy dies, surviving west recruits keep `null` target state instead of retaining or reusing a dead assignment.
    - Test 3: formation profiling still records invalidations/reuse with the existing Phase 14 counter names, and focus-fire behavior remains green.
  </behavior>
  <action>Diagnose the remaining failure at the real seam between `AbstractRecruitEntity.searchForTargets*()` and `FormationTargetSelectionController`. Apply the smallest fix that makes dead-target invalidation deterministic for shared cohorts: if the cached shared target is no longer valid, the first eligible recruit must invalidate and either publish one replacement target or leave the cohort unassigned when no candidates remain; later recruits on the same tick must observe that result rather than keeping stale entity references. Preserve the current owner+group cohort rule, utility-shaped controller state, and profiling vocabulary from Phase 14; do not add persistence, new managers, navigation changes, or broader combat-target heuristics.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest && ./gradlew compileGameTestJava verifyGameTestStage || (test -f build/reports/tests/test/index.html && python - <<'PY'
from pathlib import Path
paths = list(Path('build').rglob('*.xml')) + list(Path('build').rglob('*.txt'))
text = '\n'.join(p.read_text(errors='ignore') for p in paths)
phase_local = [
    'formationcohortretargetsaftersharedenemydies',
    'formationcohortclearsdeadtargetswhennoreplacementexists',
]
missing = [name for name in phase_local if name in text.lower()]
if missing:
    raise SystemExit('Phase-local failures still present: ' + ', '.join(missing))
if 'reconnectedownerregainsrecruitandworkerauthoritywithoutmanualrebinding' not in text.lower():
    raise SystemExit('Expected unrelated pre-existing reconnect failure was not found; inspect verifyGameTestStage output manually')
PY)</automated>
  </verify>
  <done>The two Phase 14 local failures are gone, shared target invalidation remains utility-shaped and cohort-bounded, and any remaining `verifyGameTestStage` failure is only the unrelated reconnect authority test already called out by the user.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest`
- `./gradlew compileGameTestJava`
- `./gradlew verifyGameTestStage` no longer reports `formationCohortRetargetsAfterSharedEnemyDies` or `formationCohortClearsDeadTargetsWhenNoReplacementExists`; the only tolerated remaining failure is the pre-existing reconnect authority case named in STATE.md and the user prompt.
</verification>

<success_criteria>
- The Phase 14 retarget GameTest proves a cohort can converge on a new living enemy after its shared target dies.
- The Phase 14 loss-of-target GameTest proves a cohort clears dead targets when no replacement exists.
- No new scenarios, profiling vocabularies, or broader AI rewrites are introduced beyond the minimum needed to close these two phase-local gaps.
</success_criteria>

<output>
After completion, create `.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-03-SUMMARY.md`
</output>
