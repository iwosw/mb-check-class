---
phase: 14-formation-level-target-selection-rewrite
plan: 04
type: execute
wave: 3
depends_on:
  - 14-03
files_modified:
  - recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
  - recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java
autonomous: true
requirements:
  - TARGETSEL-02
gap_closure: true
must_haves:
  truths:
    - "Dead or removed enemies cannot be republished as formation replacement targets during the Phase 14 recovery-field refresh path."
    - "The remaining optional Phase 14 retarget and loss-of-target GameTests turn green without widening the rewrite beyond target-selection correctness."
    - "Dense-battle profiling vocabulary, focus-fire behavior, and unrelated reconnect failures remain outside the scope of this follow-up except for regression verification."
  artifacts:
    - path: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      provides: "Live target-search filtering that excludes dead or removed candidates before a cohort republishes a shared assignment"
      contains: "searchForTargets"
    - path: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java"
      provides: "Focused brownfield recovery-field coverage for retarget and no-replacement behavior after the live candidate-filtering fix"
      contains: "formationCohortRetargetsAfterSharedEnemyDies"
  key_links:
    - from: "recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java"
      to: "recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java"
      via: "shared target invalidation stays controller-owned, while live candidate filtering now rejects dead/removed replacements before publication"
      pattern: "isValidSharedTarget|beginRuntimeSelection|completeRuntimeSelection"
    - from: "recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java"
      to: "recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java"
      via: "recovery-field cohort scenarios remain on the retained helper harness and verify the post-death refresh path only"
      pattern: "spawnRecoveryPair|assignFormationCohort|setMutualTargets"
---

<objective>
Close the final Phase 14 optional GameTest gap by fixing the live candidate-filtering seam that still lets dead enemies be reconsidered during recovery-field refresh.

Purpose: Plan 14-03 narrowed the bug to the real search path around `AbstractRecruitEntity`, so this follow-up should finish the retarget/loss-of-target closure with the smallest brownfield-safe change instead of reopening controller, navigation, or reconnect work.
Output: one narrow target-search fix plus focused verification that the optional retarget/loss-of-target checks no longer fail.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/debug/phase-14-optional-gametest-fails.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-CONTEXT.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-VALIDATION.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-02-SUMMARY.md
@.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-03-SUMMARY.md
@recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java
@recruits/src/gametest/java/com/talhanation/recruits/gametest/support/RecruitsBattleGameTestSupport.java

<interfaces>
From recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java:
```java
private void searchForTargetsSync(Level level)
private void searchForTargetsAsync(Level level)
private boolean isValidSharedTarget(@Nullable LivingEntity candidate)
```

From recruits/src/main/java/com/talhanation/recruits/entities/ai/FormationTargetSelectionController.java:
```java
public static Decision<LivingEntity> beginRuntimeSelection(RuntimeSelectionRequest request, Predicate<LivingEntity> targetValidator)
public static @Nullable LivingEntity completeRuntimeSelection(RuntimeSelectionRequest request, @Nullable LivingEntity selectedTarget)
```
 </interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exclude dead candidates from the shared replacement search path</name>
  <files>recruits/src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java</files>
  <behavior>
    - Test 1: when a shared target dies, the next target-search pass cannot choose that dead or removed entity as a replacement.
    - Test 2: if one valid enemy remains, the cohort republishes that living enemy and converges on it.
    - Test 3: if no valid enemy remains, the cohort publishes null and surviving recruits clear their target state.
  </behavior>
  <action>Apply the smallest fix inside both sync and async target-search paths so candidate filtering uses the same live-target validity rule as the shared formation seam instead of relying on `targetingConditions` alone. Prefer reusing `isValidSharedTarget(...)` or an equivalent explicit alive/removal guard before `chooseTargetFromCandidates(...)`. Keep the controller contract, profiling vocabulary, owner+group cohort rule, and broader combat heuristics unchanged.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest && ./gradlew compileGameTestJava</automated>
  </verify>
  <done>Dead or removed entities are no longer eligible replacement candidates during the Phase 14 shared refresh path.</done>
</task>

<task type="auto">
  <name>Task 2: Revalidate the optional recovery-field GameTests without widening scope</name>
  <files>recruits/src/gametest/java/com/talhanation/recruits/gametest/battle/FormationTargetSelectionGameTests.java</files>
  <action>Re-run the focused Phase 14 retarget and no-replacement GameTests on the retained recovery-field harness after the candidate-filtering fix. Only adjust the GameTest file if a tiny assertion-timing or helper-usage correction is still required to reflect the intended brownfield seam more truthfully; do not add a new template, new profiler scenario, or broader battle rewrite. Treat unrelated reconnect or packet failures as out of scope, but document them if the full root GameTest gate still reports them.</action>
  <verify>
    <automated>./gradlew verifyGameTestStage || (test -f build/reports/tests/test/index.html && python - <<'PY'
from pathlib import Path
paths = list(Path('build').rglob('*.xml')) + list(Path('build').rglob('*.txt'))
text = '\n'.join(p.read_text(errors='ignore') for p in paths).lower()
phase_local = [
    'formationcohortretargetsaftersharedenemydies',
    'formationcohortclearsdeadtargetswhennoreplacementexists',
]
missing = [name for name in phase_local if name in text]
if missing:
    raise SystemExit('Phase-local failures still present: ' + ', '.join(missing))
PY)</automated>
  </verify>
  <done>The optional Phase 14 retarget/loss-of-target failures are gone, or any remaining block is narrowed and documented without conflating it with unrelated brownfield failures.</done>
</task>

</tasks>

<verification>
- `./gradlew test --tests com.talhanation.recruits.entities.ai.FormationTargetSelectionControllerTest`
- `./gradlew compileGameTestJava`
- `./gradlew verifyGameTestStage` no longer reports `formationCohortRetargetsAfterSharedEnemyDies` or `formationCohortClearsDeadTargetsWhenNoReplacementExists`
</verification>

<success_criteria>
- Dead or removed enemies are excluded from the live replacement candidate set used by formation retargeting.
- The remaining optional Phase 14 retarget and loss-of-target GameTests turn green on the retained recovery-field harness.
- No new target-selection manager, persistence layer, pathfinding budget logic, or profiling vocabulary is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-formation-level-target-selection-rewrite/14-formation-level-target-selection-rewrite-04-SUMMARY.md`
</output>
