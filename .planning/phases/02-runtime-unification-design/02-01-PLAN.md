---
phase: 02-runtime-unification-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - recruits/src/main/resources/META-INF/mods.toml
  - MERGE_NOTES.md
  - .planning/phases/02-runtime-unification-design/02-runtime-identity-contract.md
autonomous: true
requirements:
  - BOOT-05
must_haves:
  truths:
    - "BannerMod is the explicit public identity of the merged runtime."
    - "Workers asset and translation ownership is documented as converging into the active `bannermod` namespace."
    - "Downstream phases have one active contract for release identity and namespace end-state."
  artifacts:
    - path: "recruits/src/main/resources/META-INF/mods.toml"
      provides: "Release-facing mod metadata"
      contains: "displayName=\"BannerMod\""
    - path: "MERGE_NOTES.md"
      provides: "Merge truth notes aligned to the new identity contract"
      contains: "BannerMod"
    - path: ".planning/phases/02-runtime-unification-design/02-runtime-identity-contract.md"
      provides: "Phase 2 identity and namespace target contract"
      contains: "bannermod namespace"
  key_links:
    - from: "recruits/src/main/resources/META-INF/mods.toml"
      to: "workers/src/main/java/com/talhanation/workers/WorkersRuntime.java"
      via: "shared active mod identity"
      pattern: "bannermod"
    - from: ".planning/phases/02-runtime-unification-design/02-runtime-identity-contract.md"
      to: "build.gradle"
      via: "resource namespace strategy"
      pattern: "assets/bannermod"
---

<objective>
Publish the BannerMod-first release identity and the full `bannermod` namespace end-state as active merge truth.

Purpose: Turn the already-partial runtime unification into an explicit contract so later implementation phases do not guess about public branding or namespace destination.
Output: One identity/namespace contract doc plus aligned active metadata and merge notes.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-runtime-unification-design/02-CONTEXT.md
@.planning/phases/02-runtime-unification-design/02-RESEARCH.md
@MERGE_NOTES.md
@build.gradle
@recruits/src/main/resources/META-INF/mods.toml
@workers/src/main/java/com/talhanation/workers/WorkersRuntime.java

<interfaces>
From workers/src/main/java/com/talhanation/workers/WorkersRuntime.java:
```java
public static final String MOD_ID = Main.MOD_ID;
public static final String LEGACY_MOD_ID = "workers";
public static final String ACTIVE_ASSET_NAMESPACE = "bannermod";
public static ResourceLocation mergedGuiTexture(String fileName);
public static ResourceLocation mergedStructureRoot();
```

From build.gradle:
```groovy
archivesBaseName = 'bannermod-1.20.1'
from('workers/src/main/resources/assets/workers/textures/gui') {
    into 'assets/bannermod/textures/gui/workers'
}
from('workers/src/main/resources/assets/workers/structures') {
    into 'assets/bannermod/structures/workers'
}
```
 </interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the runtime identity and namespace contract</name>
  <files>.planning/phases/02-runtime-unification-design/02-runtime-identity-contract.md</files>
  <action>Create `.planning/phases/02-runtime-unification-design/02-runtime-identity-contract.md` as the active contract for D-01 through D-04. State that the merged runtime is publicly `BannerMod` first, that any remaining Recruits-branded release metadata is transitional debt per D-02, that the end-state for Workers-owned GUI/structure/lang assets is full `bannermod` ownership per D-03, and that preserved `assets/workers/**` content is migration input rather than long-term namespace policy per D-04. Ground the contract in current code by citing `mods.toml`, `build.gradle` resource mirroring, and `WorkersRuntime.ACTIVE_ASSET_NAMESPACE`. Do not propose reviving a second live `workers` runtime identity.</action>
  <verify>
    <automated>./gradlew compileJava processResources</automated>
  </verify>
  <done>The phase has one explicit design contract describing the release identity and namespace destination in code-backed terms.</done>
</task>

<task type="auto">
  <name>Task 2: Align active metadata and merge notes to the BannerMod-first contract</name>
  <files>recruits/src/main/resources/META-INF/mods.toml, MERGE_NOTES.md</files>
  <action>Update active release-facing metadata and merge notes so they implement D-01 through D-04 without overstating runtime changes. In `mods.toml`, keep `modId="bannermod"` and `displayName="BannerMod"`, but refresh the description and any release-facing wording that still reads like standalone Recruits branding so it describes the merged BannerMod runtime truthfully. In `MERGE_NOTES.md`, replace or annotate outdated open questions/wording so the active note now points to the new identity contract, explicitly treats full `bannermod` asset/lang ownership as the target end-state, and keeps any surviving `workers:*` handling framed as migration-only. Do not change Java package names, registry ids, or build coordinates in this plan.</action>
  <verify>
    <automated>./gradlew test</automated>
  </verify>
  <done>Active metadata and merge notes now tell the same BannerMod-first story as the Phase 2 identity contract.</done>
</task>

</tasks>

<verification>
Run `./gradlew compileJava processResources test` from the root. Then confirm the new identity contract, `mods.toml`, and `MERGE_NOTES.md` all describe the same BannerMod-first identity and `bannermod` namespace destination.
</verification>

<success_criteria>
- `mods.toml` still ships `modId="bannermod"` and now uses merged-runtime wording
- The phase directory contains an explicit identity/namespace contract document
- `MERGE_NOTES.md` no longer treats the mixed asset/lang namespace as an acceptable end-state
- No task introduces a second active `workers` runtime identity
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-unification-design/02-runtime-unification-design-01-SUMMARY.md`
</output>
