---
phase: 05-stabilization-and-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java
  - workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java
  - src/test/java/com/talhanation/workers/BuildAreaUpdateAuthoringTest.java
autonomous: true
requirements:
  - STAB-02
must_haves:
  truths:
    - "Unauthorized players cannot use the merged build-area update packet to mutate worker build areas."
    - "Owner, same-team, and admin authoring paths still resolve as allowed."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java"
      provides: "Pure authoring decision seam for build-area updates"
      contains: "authorize"
    - path: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java"
      provides: "Authorized build-area packet handling"
      contains: "BuildAreaUpdateAuthoring.authorize"
    - path: "src/test/java/com/talhanation/workers/BuildAreaUpdateAuthoringTest.java"
      provides: "Root regression coverage for build-area authoring decisions"
      contains: "forbiddenAccessReturnsForbidden"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java"
      to: "workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java"
      via: "authorization guard before update()"
      pattern: "BuildAreaUpdateAuthoring\.authorize"
    - from: "workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java"
      to: "workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java"
      via: "translated denial message"
      pattern: "WorkAreaAuthoringRules\.getMessageKey"
---

<objective>
Harden merged build-area mutation so the packet follows the same authoring rules as the other worker edit flows.

Purpose: Close a high-risk stabilization gap without widening compatibility scope beyond the Phase 02 merged-runtime contract.
Output: One pure authorization seam, one guarded packet handler, and root regression coverage for the decision logic.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stabilization-and-cleanup/05-CONTEXT.md
@.planning/phases/02-runtime-unification-design/02-runtime-compatibility-contract.md
@workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java
@workers/src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java
@workers/src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java
@workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java
@workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java

<interfaces>
From workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java:
```java
public enum AccessLevel { OWNER, SAME_TEAM, ADMIN, FORBIDDEN }
public enum Decision { ALLOW, FORBIDDEN, AREA_NOT_FOUND, OUTSIDE_FACTION_CLAIM, OVERLAPPING, INVALID_REQUEST }
public static Decision modifyDecision(boolean areaExists, AccessLevel accessLevel)
public static boolean isAllowed(Decision decision)
public static String getMessageKey(Decision decision)
```

From workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java:
```java
public WorkAreaAuthoringRules.AccessLevel getAuthoringAccess(Player player)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a pure build-area authoring decision seam with root regression coverage</name>
  <files>workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java, src/test/java/com/talhanation/workers/BuildAreaUpdateAuthoringTest.java</files>
  <read_first>workers/src/main/java/com/talhanation/workers/network/WorkAreaAuthoringRules.java, workers/src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java, .planning/phases/05-stabilization-and-cleanup/05-CONTEXT.md, .planning/phases/02-runtime-unification-design/02-runtime-compatibility-contract.md</read_first>
  <behavior>
    - Test 1: `authorize(true, OWNER)`, `authorize(true, SAME_TEAM)`, and `authorize(true, ADMIN)` return `ALLOW`
    - Test 2: `authorize(true, FORBIDDEN)` returns `FORBIDDEN`
    - Test 3: `authorize(false, OWNER)` returns `AREA_NOT_FOUND`
  </behavior>
  <acceptance_criteria>
    - `workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java` contains `static WorkAreaAuthoringRules.Decision authorize`
    - `src/test/java/com/talhanation/workers/BuildAreaUpdateAuthoringTest.java` contains `forbiddenAccessReturnsForbidden`
    - `./gradlew test --tests "com.talhanation.workers.BuildAreaUpdateAuthoringTest"` exits 0
  </acceptance_criteria>
  <action>Create `BuildAreaUpdateAuthoring` as a tiny pure helper in `workers/src/main/java/com/talhanation/workers/network/` with one static method `authorize(boolean areaExists, WorkAreaAuthoringRules.AccessLevel accessLevel)` that returns the matching `WorkAreaAuthoringRules.Decision` by delegating to the existing rules instead of inventing new permission semantics. Add root JUnit coverage in `src/test/java/com/talhanation/workers/BuildAreaUpdateAuthoringTest.java` for owner, same-team, admin, forbidden, and area-missing cases. Do not introduce Mockito or any Forge runtime harness; keep this seam pure so the test stays in the fast root `./gradlew test` path.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.workers.BuildAreaUpdateAuthoringTest"</automated>
  </verify>
  <done>The merged workspace has a fast, root-level regression seam that expresses build-area authoring decisions in the same terms as the existing worker authoring rules.</done>
</task>

<task type="auto">
  <name>Task 2: Gate build-area packet mutation behind the shared authoring decision</name>
  <files>workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java</files>
  <read_first>workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java, workers/src/main/java/com/talhanation/workers/network/BuildAreaUpdateAuthoring.java, workers/src/main/java/com/talhanation/workers/network/MessageUpdateWorkArea.java, workers/src/main/java/com/talhanation/workers/network/MessageRotateWorkArea.java, workers/src/main/java/com/talhanation/workers/entities/workarea/AbstractWorkAreaEntity.java</read_first>
  <acceptance_criteria>
    - `workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java` contains `BuildAreaUpdateAuthoring.authorize`
    - `workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java` contains `WorkAreaAuthoringRules.getMessageKey`
    - `workers/src/main/java/com/talhanation/workers/network/MessageUpdateBuildArea.java` sends `AREA_NOT_FOUND` when the target build area is missing
    - `./gradlew test --tests "com.talhanation.workers.BuildAreaUpdateAuthoringTest"` exits 0
  </acceptance_criteria>
  <action>Update `MessageUpdateBuildArea.executeServerSide(...)` so it follows the same pattern as `MessageUpdateWorkArea` and `MessageRotateWorkArea`: resolve the target `BuildArea`, send `AREA_NOT_FOUND` when it is missing, call `BuildAreaUpdateAuthoring.authorize(true, buildArea.getAuthoringAccess(player))`, send the translated denial message from `WorkAreaAuthoringRules.getMessageKey(decision)` when access is forbidden, and call `update(buildArea)` only for allowed decisions. Keep the existing width/height/depth, structure NBT, start-build, and clear-planned-build mutations unchanged once authorization passes. Do not add broader standalone `workers` compatibility behavior or new packet payload fields.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.workers.BuildAreaUpdateAuthoringTest"</automated>
  </verify>
  <done>The merged build-area packet now uses the shared authoring rule boundary before mutating server-authoritative worker state.</done>
</task>

</tasks>

<verification>
Run the root JUnit seam and then inspect `MessageUpdateBuildArea.java` to confirm unauthorized requests are rejected before any build-area mutation occurs.
</verification>

<success_criteria>
- Build-area mutation decisions are expressed through one pure helper under the workers network package
- The packet denies missing-area and forbidden-author requests before calling `update(...)`
- Root regression coverage for the decision seam runs through `./gradlew test`
</success_criteria>

<output>
After completion, create `.planning/phases/05-stabilization-and-cleanup/05-stabilization-and-cleanup-02-SUMMARY.md`
</output>
