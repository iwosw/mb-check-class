---
phase: 05-stabilization-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
autonomous: true
requirements:
  - STAB-01
must_haves:
  truths:
    - "The root `./gradlew test` command executes retained Workers JUnit coverage from the merged workspace."
    - "Workers regression suites no longer depend on a hidden standalone test entrypoint."
  artifacts:
    - path: "build.gradle"
      provides: "Merged root test source-set wiring"
      contains: "workers/src/test/java"
  key_links:
    - from: "build.gradle"
      to: "workers/src/test/java"
      via: "root test sourceSets"
      pattern: "workers/src/test/java"
---

<objective>
Route retained Workers JUnit coverage through the active root verification pipeline.

Purpose: Lower stabilization risk by making `./gradlew test` exercise the existing Workers regression suites instead of leaving them outside the merged root baseline.
Output: Root Gradle test source-set wiring that includes the preserved Workers test tree.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-stabilization-and-cleanup/05-CONTEXT.md
@.planning/CODEBASE.md
@.planning/VERIFICATION.md
@build.gradle
@workers/src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java
@workers/src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the root test source set to include retained Workers suites</name>
  <files>build.gradle</files>
  <read_first>build.gradle, .planning/VERIFICATION.md, .planning/CODEBASE.md, .planning/phases/05-stabilization-and-cleanup/05-CONTEXT.md, workers/src/test/java/com/talhanation/workers/network/WorkAreaAuthoringRulesTest.java, workers/src/test/java/com/talhanation/workers/entities/WorkerBindingResumeTest.java</read_first>
  <acceptance_criteria>
    - `build.gradle` contains `java.srcDirs = ['src/test/java', 'recruits/src/test/java', 'workers/src/test/java']`
    - `build.gradle` still contains `java.srcDirs = ['src/main/java', 'recruits/src/main/java', 'workers/src/main/java']`
    - `build.gradle` still contains `java.srcDirs = ['src/gametest/java', 'recruits/src/gametest/java']`
    - `./gradlew test --tests "com.talhanation.workers.network.WorkAreaAuthoringRulesTest"` exits 0
    - `./gradlew test --tests "com.talhanation.workers.entities.WorkerBindingResumeTest"` exits 0
  </acceptance_criteria>
  <action>Update the root `test` source set in `build.gradle` so `java.srcDirs` includes exactly `src/test/java`, `recruits/src/test/java`, and `workers/src/test/java`. If you need an explicit workers test resources entry, add only `workers/src/test/resources` beside the existing root and recruits resource paths; do not create a second Workers-only Gradle task, restore a standalone Workers build, or change the root `main` or `gametest` source sets. Keep the active verification entrypoint as the repository-root `./gradlew test` command from the Phase 05 context and Phase 01 validation baseline.</action>
  <verify>
    <automated>./gradlew test --tests "com.talhanation.workers.network.WorkAreaAuthoringRulesTest" --tests "com.talhanation.workers.entities.WorkerBindingResumeTest"</automated>
  </verify>
  <done>The root merged test task executes retained Workers JUnit suites without leaving the active workspace.</done>
</task>

</tasks>

<verification>
Run the root test task against at least one retained Workers network suite and one retained Workers entity suite to confirm the merged workspace now owns that regression coverage.
</verification>

<success_criteria>
- `./gradlew test` can run preserved Workers JUnit classes from the root workspace
- No standalone Workers-only test task or second build entrypoint is introduced
- The merged root `main` and `gametest` source-set wiring stays unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/05-stabilization-and-cleanup/05-stabilization-and-cleanup-01-SUMMARY.md`
</output>
