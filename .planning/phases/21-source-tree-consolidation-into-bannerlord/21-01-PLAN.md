---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/talhanation/bannerlord/shared/**
  - src/main/java/com/talhanation/bannerlord/config/**
  - src/main/java/com/talhanation/bannermod/**
  - .planning/VERIFICATION.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Phase 21 starts with the root `com.talhanation.bannermod/**` seam classes because they are already imported across recruit and worker code and therefore have the lowest migration risk."
    - "The Java package move does not change the live runtime identity: `bannermod` mod id, shared asset namespace, and current config filenames must stay stable."
    - "Forwarding adapters are acceptable only as short-lived import bridges; this plan must not create duplicate long-lived implementations of the same shared seam."
  artifacts:
    - path: ".planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md"
      provides: "The canonical wave-1 move order and adapter policy for shared seam classes"
      contains: "Wave 1: move shared seams first"
    - path: "src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java"
      provides: "The current shared authority seam already used across the merged runtime"
      contains: "Relationship|Decision"
    - path: "src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java"
      provides: "The current shared logistics seam already bridging recruit and worker concepts"
      contains: "BuildProjectStatus|RecruitSupplyStatus"
    - path: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      provides: "The current claim-backed settlement seam that already bridges both families"
      contains: "resolveFactionStatus|resolveSettlementStatus"
    - path: "src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java"
      provides: "The config filename contract that must stay runtime-stable while packages move"
      contains: "bannermod-military.toml|bannermod-settlement.toml|bannermod-client.toml"
---

<objective>
Re-home the low-risk shared seam classes into `com.talhanation.bannerlord.shared/**` and `com.talhanation.bannerlord.config` while preserving runtime behavior and keeping any old-package forwarding surface as small and temporary as possible.

Purpose: this plan creates the new stable import base that later bootstrap, recruit, and worker moves depend on.
Output: moved shared seam classes, updated imports, and only the minimal adapters still needed for safe churn control.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
@src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move shared seam classes into `com.talhanation.bannerlord` shared/config packages</name>
  <files>src/main/java/com/talhanation/bannerlord/shared/**, src/main/java/com/talhanation/bannerlord/config/**, src/main/java/com/talhanation/bannermod/**</files>
  <action>Move the current root `com.talhanation.bannermod` seam classes that already bridge recruit and worker behavior into their canonical `com.talhanation.bannerlord` homes. Update imports across root, recruit, and worker code. Leave temporary forwarding adapters in the old `com.talhanation.bannermod` package only where they materially lower risk for the next wave; do not keep duplicate logic. Preserve the config filename and migration contract exactly.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
required = [
    Path('src/main/java/com/talhanation/bannerlord/shared'),
    Path('src/main/java/com/talhanation/bannerlord/config')
]
missing = [str(path) for path in required if not path.exists()]
if missing:
    raise SystemExit('Missing required bannerlord shared/config targets: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The merged runtime now has canonical Bannerlord shared/config homes that later Phase 21 slices can import directly.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh verification guidance for the new wave-1 package locations</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Update `VERIFICATION.md` only if the moved shared/config package locations change what Phase 21 execution needs to compile or target during focused validation. Keep the default repository-root validation baseline truthful.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/phases/21-source-tree-consolidation-into-bannerlord/21-01-PLAN.md').read_text()
required = ['com.talhanation.bannerlord.shared', 'com.talhanation.bannerlord.config', 'Forwarding adapters']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('Missing expected plan content: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The verification baseline still tells the truth after the wave-1 move.</done>
</task>

</tasks>

<verification>
- Shared seam classes now have canonical `com.talhanation.bannerlord` homes.
- Runtime-facing config filenames and migration behavior remain unchanged.
- Any remaining `com.talhanation.bannermod` classes are explicit short-lived adapters, not duplicate systems.
</verification>

<success_criteria>
- Later Phase 21 slices can import shared/config seams from `bannerlord` instead of the temporary `bannermod` package family.
- The move preserves runtime identity and keeps wave 2 smaller and safer.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-01-SUMMARY.md`
</output>
