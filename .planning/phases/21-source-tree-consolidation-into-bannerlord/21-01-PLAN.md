---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-02-SUMMARY.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-03-SUMMARY.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-04-SUMMARY.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-06-SUMMARY.md
  - .planning/ROADMAP.md
  - MERGE_NOTES.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "All in-flight working-tree edits made against `com.talhanation.bannerlord/**` are captured in git history before the revert runs (no silent loss)."
    - "All commits that materialized phase-21 work into `com.talhanation.bannerlord/**` are reverted via per-original-commit `git revert` commits with the shared preamble `revert(21): pivot bannerlord -> bannermod`."
    - "After revert, no `src/main/java/com/talhanation/bannerlord/**` package directories remain on disk."
    - "All five executed-plan SUMMARY files (21-02..21-06) are deleted; they will regenerate against the bannermod outcome."
  artifacts:
    - path: ".planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md"
      provides: "Locked pivot decisions D-01..D-17 governing this revert"
      contains: "Convergence namespace is `com.talhanation.bannermod"
  key_links:
    - from: "git history"
      to: "src/main/java/com/talhanation/bannerlord"
      via: "git revert commits"
      pattern: "revert\\(21"
---

<objective>
Reset Phase 21's executed state by capturing in-flight bannerlord edits, reverting all `bannerlord/*` move commits, and deleting the now-stale executed-plan SUMMARY files so the re-execution under the `bannermod` namespace can begin from a clean slate.

Purpose: per CONTEXT D-06..D-09, D-12, D-15, the wave-1 prerequisite for the pivot is a hard, auditable revert of the `bannerlord`-targeted work. The new plans 21-02..21-09 cannot be re-executed against a tree that still contains the `bannerlord` package family or stale summaries that misrepresent the executed state.
Output: clean working tree, contiguous per-original-commit revert commits in history, no `com.talhanation.bannerlord/**` directories on disk, and SUMMARY 02..06 files deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Triage in-flight working-tree edits and capture them in history before the revert</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-09, D-06)
    - Run `git status --short` and `git diff --stat` to enumerate currently uncommitted modified files (35 entries observed at planning time, including modified `bannerlord/**` files plus untracked `src/main/java/com/talhanation/bannerlord/compat/recruits/`).
  </read_first>
  <action>
    Run `git status --short` and `git diff --stat`. Inspect each modified file. Per CONTEXT D-09 the disposition options are: (a) commit them as `wip(21): capture in-flight bannerlord edits pre-pivot` so they enter history before the revert; (b) `git stash` and discard if obsolete; (c) re-apply selectively post-amendment. Default policy: choose option (a) for any change that touches `src/main/java/com/talhanation/bannerlord/**` or `src/main/java/com/talhanation/recruits/**` or `src/main/java/com/talhanation/workers/**` (these are the bannerlord-pivot churn). Leave `.planning/config.json` and the untracked `.planning/phases/21-*/21-CONTEXT.md`, `21-RESEARCH.md`, `21-VERIFICATION.md` UNTOUCHED — they are pivot artifacts the new plan set keeps. Stage with explicit paths only (`git add src/main/java/com/talhanation/bannerlord src/main/java/com/talhanation/recruits src/main/java/com/talhanation/workers`); do NOT use `git add -A` or `git add .`. Commit with the exact message `wip(21): capture in-flight bannerlord edits pre-pivot`.
  </action>
  <verify>
    <automated>bash -c "git diff --quiet -- src/main/java && git diff --cached --quiet -- src/main/java && git log -1 --pretty=%s | grep -Fxq 'wip(21): capture in-flight bannerlord edits pre-pivot'"</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short -- src/main/java/` returns empty (no modified or staged source files).
    - `git log -1 --pretty=%s` is exactly `wip(21): capture in-flight bannerlord edits pre-pivot`.
    - Untracked `21-CONTEXT.md`, `21-RESEARCH.md`, `21-VERIFICATION.md` in the phase directory remain untracked.
  </acceptance_criteria>
  <done>In-flight bannerlord-pivot edits are captured in one explicit WIP commit; source working tree is clean; pivot planning artifacts remain untracked for later handling.</done>
</task>

<task type="auto">
  <name>Task 2: Revert all bannerlord-targeted phase-21 commits (per CONTEXT D-07, D-08) plus the WIP commit</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-06, D-07, D-08)
    - Run `git log --oneline f1832af^..a792dc3` to confirm the exact revert range before executing.
  </read_first>
  <action>
    Per CONTEXT D-07, the revert range is from `f1832af feat(21-02): move bootstrap ownership into bannerlord packages` through `a792dc3 docs(21-06): complete shared seam ownership plan`, inclusive. Per D-08 the reverts must be one revert commit per original commit, not squashed (preserves bisectability). Per D-08 every revert commit message starts with the shared preamble `revert(21): pivot bannerlord -> bannermod -- `.

    **Derivation check (run FIRST, before any revert):** Assert that the typed SHA list matches the actual commit range in the repo at execution time (protects against rebased-since-context drift):
    ```
    actual=$(git log --pretty=%H f1832af^..a792dc3 | wc -l)
    [[ "$actual" -eq 19 ]] || { echo "ABORT: revert range f1832af^..a792dc3 has $actual commits, expected 19 (SHA list drift)"; exit 1; }
    ```

    Execute reverts in REVERSE chronological order (newest commit first) so each revert applies cleanly. Process this exact list in this order: `a792dc3 47d8562 264976b 4bfa8d7 288dce9 5c47295 da9ddc2 96c9563 c158fdc f9ecd0a 1ec78c4 78c66eb 99066ce eb53960 0da2cf8 1373218 a8415ed dc95147 f1832af`. For each SHA: run `git revert --no-commit <sha>`, then `git commit -m "revert(21): pivot bannerlord -> bannermod -- <original subject>"`. The original subject is the commit's `git log -1 --pretty=%s <sha>` output.

    If any revert fails with a merge conflict, STOP and report back with the failing SHA — do NOT use `--strategy=ours` or otherwise discard changes.

    **WIP-undo step (conditional on Task 1 outcome):** If Task 1 captured a WIP commit (`git log -1 --pretty=%s` equalled `wip(21): capture in-flight bannerlord edits pre-pivot` before Task 2 started), then AFTER the 19 reverts land, ALSO revert the WIP commit: `git revert --no-commit <wip-sha>`, then `git commit -m "revert(21): pivot bannerlord -> bannermod -- undo wip capture"`. If Task 1 reported "no in-flight bannerlord edits found" and did NOT create a WIP commit, SKIP this step. The expected final revert count is therefore EXACTLY 20 when a WIP commit existed, or EXACTLY 19 when it did not — it is NEVER > 20 and NEVER < 19.
  </action>
  <verify>
    <automated>bash -c "test ! -d src/main/java/com/talhanation/bannerlord && count=$(git log -25 --pretty=%s | grep -c '^revert(21): pivot bannerlord -> bannermod'); [[ \"$count\" -eq 19 || \"$count\" -eq 20 ]]"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannerlord` directory does NOT exist.
    - Pre-revert derivation check `git log --pretty=%H f1832af^..a792dc3 | wc -l` returned exactly 19 (SHA list not drifted).
    - `git log --pretty=%s | head -25 | grep -c '^revert(21): pivot bannerlord -> bannermod'` reports exactly 19 (if Task 1 found no WIP edits) OR exactly 20 (if Task 1 captured a WIP commit) — not a `>=` inequality.
    - `git status --short -- src/main/java/` returns empty.
    - `find src/main/java/com/talhanation/bannerlord -type f 2>/dev/null` returns nothing (path absent).
  </acceptance_criteria>
  <done>The full `bannerlord`-targeted commit range from `f1832af` through `a792dc3` plus (conditionally) the Task-1 WIP commit are reverted as 19 or 20 separate revert commits with the `revert(21): pivot bannerlord -> bannermod` preamble. The `src/main/java/com/talhanation/bannerlord/**` directory tree is gone.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the stale executed-plan SUMMARY files (D-15)</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decision D-15)
    - List existing summaries with `ls .planning/phases/21-source-tree-consolidation-into-bannerlord/*-SUMMARY.md`.
  </read_first>
  <action>
    Delete the five executed-plan SUMMARY files using `git rm`:
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-02-SUMMARY.md`
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-03-SUMMARY.md`
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-04-SUMMARY.md`
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md`
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-06-SUMMARY.md`
    Note: depending on the revert outcome of Task 2, the 02..06 SUMMARYs may have already been removed by reverting their `docs(21-XX)` commits. If `git rm` reports "did not match any files" for any path, that is acceptable — proceed and treat that file as already gone. Commit any actual deletions with the exact message `chore(21): delete stale bannerlord-era summaries pre-pivot`.
  </action>
  <verify>
    <automated>bash -c "for f in 02 03 04 05 06; do test ! -f \".planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-${f}-SUMMARY.md\" || exit 1; done"</automated>
  </verify>
  <acceptance_criteria>
    - None of the five executed-plan SUMMARY files exist on disk.
    - The 21-CONTEXT.md, 21-RESEARCH.md, 21-VERIFICATION.md files in the phase directory are NOT deleted by this task.
    - The 21-01..21-09 PLAN files are NOT deleted by this task.
  </acceptance_criteria>
  <done>The five stale `21-source-tree-consolidation-into-bannerlord-0N-SUMMARY.md` files (N=2..6) are removed. The phase directory now holds only PLAN files, CONTEXT/RESEARCH/VERIFICATION, and `deferred-items.md`.</done>
</task>

<task type="auto">
  <name>Task 4: Update ROADMAP.md §Phase 21 to post-revert truth AND append the Phase 21 Pivot section to MERGE_NOTES.md</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-02, D-06, D-15, D-17 — D-17 locks pivot rationale and revert range to 21-01)
    - .planning/ROADMAP.md (read the full §Phase 21 section so Edit replacements are precise)
    - MERGE_NOTES.md (read the full file — pivot section appends after the last existing entry)
  </read_first>
  <action>
    Two operations, both documentation-only, committed together:

    (A) **ROADMAP.md §Phase 21 post-revert reset.** Edit `.planning/ROADMAP.md`:
      - Rewrite the section heading from `## Phase 21: Source Tree Consolidation Into Bannerlord` to `## Phase 21: Source Tree Consolidation Into BannerMod`.
      - In the three bullets under the heading, replace every occurrence of `com.talhanation.bannerlord` with `com.talhanation.bannermod` and the phrasing `Into Bannerlord` with `Into BannerMod` where it appears in prose.
      - In the `Plans:` checklist, rewrite each `21-NN-PLAN.md` one-liner so `bannerlord` becomes `bannermod` in the description text. Reset EVERY plan checkbox from `[x]` or `[ ]` to `[ ]` (pivot invalidates all prior execution claims).
      - Rewrite the `**Plans:**` roll-up line to `**Plans:** 0/9 plans executed`.
      - Rewrite the trailing `Status:` paragraph to: `Status: In progress (0/9 after pivot from bannerlord to bannermod, 2026-04-15); the bannerlord-targeted work from f1832af through a792dc3 has been fully reverted per plan 21-01. Plans 21-02 through 21-09 will re-execute against the bannermod convergence namespace locked by 21-CONTEXT.md D-01/D-02.`

    (B) **MERGE_NOTES.md pivot append (owned by 21-01 per D-17).** Append to `MERGE_NOTES.md` a new top-level section:
    ```
    ## Phase 21 Pivot (2026-04-15)

    ### Why bannerlord was the wrong convergence namespace
    The wave-1 through wave-5 execution under `com.talhanation.bannerlord.**` was abandoned per CONTEXT D-01/D-02 after the team concluded (i) the bannerlord name collided with the sibling Bannerlord-inspired gameplay subsystem in planning-stage-only docs, creating ambiguity at every cross-reference, and (ii) `bannermod` already owned the active mod id, config filenames, and resource paths — converging into bannermod aligns the Java package tree with the runtime contract instead of forcing a second rename later.

    ### Revert range
    All nineteen commits from `f1832af feat(21-02): move bootstrap ownership into bannerlord packages` through `a792dc3 docs(21-06): complete shared seam ownership plan` inclusive are reverted as per-original-commit `revert(21): pivot bannerlord -> bannermod -- <subject>` commits (Task 2). The in-flight working-tree WIP capture (if any, from Task 1) is also reverted with the same preamble. Bisectability is preserved (no squash).

    ### Re-execution target
    Plans 21-02 through 21-09 re-execute against `com.talhanation.bannermod.**` (convergence namespace, CONTEXT D-01/D-02). Plan 21-02 is the only place the shared-package overlap note lives — this section does NOT duplicate it.
    ```
    Use plain ASCII only (no en-dashes; write `->` for the preamble arrow). Do not edit any pre-existing MERGE_NOTES section.

    Stage and commit both edits together with the exact message `docs(21-01): reset roadmap and record pivot rationale in merge notes`.
  </action>
  <verify>
    <automated>bash -c "
set -e
# ROADMAP truths
grep -q '^## Phase 21: Source Tree Consolidation Into BannerMod$' .planning/ROADMAP.md || { echo 'ROADMAP heading not reset to BannerMod'; exit 1; }
# No bannerlord references remain inside the §Phase 21 block
awk '/^## Phase 21:/,/^## Phase 22:|^## Appendix|^$/' .planning/ROADMAP.md | grep -n 'bannerlord' && { echo 'bannerlord reference still present in §Phase 21'; exit 1; } || true
# All nine plan checkboxes in §Phase 21 are [ ]
unchecked=$(awk '/^## Phase 21:/,/^## Phase 22:|^## Appendix/' .planning/ROADMAP.md | grep -c '^- \\[ \\] 21-0[1-9]-PLAN.md')
[[ \"\$unchecked\" -eq 9 ]] || { echo \"Expected 9 unchecked plan lines in §Phase 21, found \$unchecked\"; exit 1; }
# MERGE_NOTES pivot section present
grep -q '^## Phase 21 Pivot (2026-04-15)$' MERGE_NOTES.md || { echo 'MERGE_NOTES pivot section missing'; exit 1; }
grep -q '^### Why bannerlord was the wrong convergence namespace$' MERGE_NOTES.md || { echo 'MERGE_NOTES why-subsection missing'; exit 1; }
grep -q '^### Revert range$' MERGE_NOTES.md || { echo 'MERGE_NOTES revert-range subsection missing'; exit 1; }
# Commit subject check
git log -1 --pretty=%s | grep -Fxq 'docs(21-01): reset roadmap and record pivot rationale in merge notes' || { echo 'Task 4 commit missing'; exit 1; }
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` §Phase 21 heading reads exactly `## Phase 21: Source Tree Consolidation Into BannerMod`.
    - Inside the §Phase 21 block (from the heading to the next `## Phase` or `## Appendix`), no line contains the literal `bannerlord` (case-sensitive).
    - The §Phase 21 `Plans:` checklist has exactly nine lines matching `^- \[ \] 21-0[1-9]-PLAN.md` — every plan reset to unchecked.
    - `MERGE_NOTES.md` contains the top-level section `## Phase 21 Pivot (2026-04-15)` with the three subsections `### Why bannerlord was the wrong convergence namespace`, `### Revert range`, `### Re-execution target`.
    - `git log -1 --pretty=%s` equals `docs(21-01): reset roadmap and record pivot rationale in merge notes`.
  </acceptance_criteria>
  <done>ROADMAP §Phase 21 is truthful post-revert (BannerMod heading, bannermod prose, all nine plans unchecked, pivot status line), and MERGE_NOTES.md records the pivot rationale + revert range under a top-level `## Phase 21 Pivot (2026-04-15)` section owned by plan 21-01 per D-17. Plan 21-02 will later append a sibling `### Shared-package overlap` subsection; plan 21-09 will flip the nine checkboxes back to `[x]` at true closeout.</done>
</task>

</tasks>

<verification>
- `src/main/java/com/talhanation/bannerlord/**` no longer exists on disk.
- `git log` shows 19 or 20 `revert(21): pivot bannerlord -> bannermod` commits in the recent history (exact count depends on whether Task 1 captured a WIP commit).
- The five SUMMARY files for plans 02..06 are deleted and the deletion is committed.
- Source working tree is clean (`git status --short -- src/main/java/` is empty).
- `.planning/ROADMAP.md` §Phase 21 is reset to the BannerMod heading with all nine plan checkboxes unchecked.
- `MERGE_NOTES.md` contains the `## Phase 21 Pivot (2026-04-15)` section with why/revert-range/re-execution subsections.
</verification>

<success_criteria>
- Phase 21 is structurally reset to the pre-bannerlord-move state plus an auditable revert trail.
- Plans 21-02..21-09 can begin re-execution under the `bannermod` namespace from a clean tree.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-01-SUMMARY.md`
</output>
