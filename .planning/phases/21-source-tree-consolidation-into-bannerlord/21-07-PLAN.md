---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 07
type: execute
wave: 7
depends_on:
  - 21-06
files_modified:
  - src/main/java/com/talhanation/bannermod/entity/civilian/**
  - src/main/java/com/talhanation/bannermod/ai/civilian/**
  - src/main/java/com/talhanation/bannermod/persistence/civilian/**
  - src/main/java/com/talhanation/bannermod/client/civilian/**
  - src/main/java/com/talhanation/bannermod/inventory/civilian/**
  - src/main/java/com/talhanation/bannermod/items/civilian/**
  - src/main/java/com/talhanation/bannermod/settlement/civilian/**
  - src/main/resources/mixins.bannermod.json
  - workers/src/main/java/com/talhanation/workers/entities/**
  - workers/src/main/java/com/talhanation/workers/client/**
  - workers/src/main/java/com/talhanation/workers/world/**
  - workers/src/main/java/com/talhanation/workers/inventory/**
  - workers/src/main/java/com/talhanation/workers/items/**
  - workers/src/main/java/com/talhanation/workers/settlement/**
  - workers/src/main/resources/mixins.workers.json
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "`workers/entities/**` (47 files incl. `entities/ai/**`, `entities/ai/animals/**`, `entities/workarea/**`) moves to `bannermod.entity.civilian.**` and `bannermod.ai.civilian.**` (with workarea types as `bannermod.entity.civilian.workarea.**`)."
    - "`workers/client/**` (24) moves to `bannermod.client.civilian.**`."
    - "`workers/world/**` (8), `workers/inventory/**` (2), `workers/items/**` (1) move to `bannermod.persistence.civilian.**`, `bannermod.inventory.civilian.**`, `bannermod.items.civilian.**`."
    - "`workers/settlement/**` (2 files — `WorkerSettlementSpawnRules`, `WorkerSettlementSpawner`) merges into `bannermod.settlement.civilian.**` without clobbering the preserved `bannermod.settlement.*` subtree from 21-02."
    - "After this wave the `workers/` clone's `src/main/java/` contains only the `@Deprecated WorkersMain.java` shim. Mixin config `mixins.workers.json` is folded into `mixins.bannermod.json`."
---

<objective>
Mirror Wave 5+6 for the `workers/` clone: bulk-migrate the civilian subsystem (entities, AI, workarea, client, world, inventory, items, settlement rules) into `bannermod.*.civilian.**`. Empties the workers clone working tree down to the `WorkersMain.java` shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-06-SUMMARY.md
@MERGE_NOTES.md
@workers/src/main/java/com/talhanation/workers
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate workers entities + AI + workarea + client</name>
  <action>
    Bulk copy+rename:
    | Source (in `workers/src/main/java/com/talhanation/workers/`) | Target |
    |---|---|
    | `entities/ai/**` | `ai/civilian/**` |
    | `entities/workarea/**` | `entity/civilian/workarea/**` |
    | `entities/**` (minus `ai/`, `workarea/`) | `entity/civilian/**` |
    | `client/**` | `client/civilian/**` |

    Per-file: rewrite `package`, rewrite `import com.talhanation.workers.{entities,client}.` → corresponding `bannermod.*.civilian.` FQNs.

    Post-pass sed across the four target subtrees for cross-subsystem references (`workers.entities.` → `bannermod.entity.civilian.`, `workers.entities.ai.` → `bannermod.ai.civilian.`, `workers.entities.workarea.` → `bannermod.entity.civilian.workarea.`, `workers.client.` → `bannermod.client.civilian.`).

    `git rm` source inside workers clone; commit. `git add` + commit in outer.
  </action>
  <verify>
    <automated>bash -c "
set -e
[ \"\$(find src/main/java/com/talhanation/bannermod/entity/civilian -name '*.java' | wc -l)\" -ge 30 ]
[ \"\$(find src/main/java/com/talhanation/bannermod/ai/civilian -name '*.java' | wc -l)\" -ge 15 ]
test -d src/main/java/com/talhanation/bannermod/client/civilian
! ls workers/src/main/java/com/talhanation/workers/entities 2>/dev/null
! ls workers/src/main/java/com/talhanation/workers/client 2>/dev/null
"
    </automated>
  </verify>
  <done>Civilian entities, AI, workarea, client UI all migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate workers world/inventory/items/settlement + fold mixin config</name>
  <action>
    Bulk copy+rename:
    | Source | Target |
    |---|---|
    | `workers/world/**` | `bannermod/persistence/civilian/**` |
    | `workers/inventory/**` | `bannermod/inventory/civilian/**` |
    | `workers/items/**` | `bannermod/items/civilian/**` |
    | `workers/settlement/**` | `bannermod/settlement/civilian/**` |

    For the settlement merge: `workers/settlement/WorkerSettlementSpawnRules.java` and `WorkerSettlementSpawner.java` land as `bannermod.settlement.civilian.*` (new sibling package under the preserved `bannermod.settlement/` from 21-02). Document the two-subpackage structure in `MERGE_NOTES.md`.

    Fold `workers/src/main/resources/mixins.workers.json` into `src/main/resources/mixins.bannermod.json`: merge the `mixins`, `client`, and `server` arrays; rewrite any `com.talhanation.workers.mixin.*` references to the new `bannermod.mixin.*` FQNs (those mixin Java files, if any, were migrated in 21-05 via `recruits/mixin` if shared; if `workers/mixin` exists with its own files, migrate them here and record).

    Delete `workers/src/main/resources/mixins.workers.json` from the clone. Update outer `build.gradle` mixin declarations so only `mixins.bannermod.json` is referenced.

    Commit in clone + outer. Append Wave-7 entry to `MERGE_NOTES.md` with per-subtree file counts, mixin merge record, and confirmation that workers clone is now `{WorkersMain.java shim, network/}`.
  </action>
  <verify>
    <automated>bash -c "
set -e
test -d src/main/java/com/talhanation/bannermod/persistence/civilian
test -d src/main/java/com/talhanation/bannermod/settlement/civilian
test -f src/main/resources/mixins.bannermod.json
! test -f workers/src/main/resources/mixins.workers.json
! ls workers/src/main/java/com/talhanation/workers/world 2>/dev/null
! ls workers/src/main/java/com/talhanation/workers/settlement 2>/dev/null
"
    </automated>
  </verify>
  <done>Workers civilian subsystem fully migrated. Only `WorkersMain.java` shim + `network/` remain.</done>
</task>

</tasks>

<verification>
- Every non-network workers subtree migrated into `bannermod.*.civilian.**`.
- Mixin configs unified into `mixins.bannermod.json`.
- Clone working tree contains only `WorkersMain.java` shim + `network/` (for Wave 8).
</verification>

<success_criteria>
- Workers-side gameplay fully owned by `bannermod.**`.
- Settlement merge (preserved + new) is documented in `MERGE_NOTES.md`.
- Wave 8 can consolidate both networks against bannermod-owned types.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-SUMMARY.md`
</output>
