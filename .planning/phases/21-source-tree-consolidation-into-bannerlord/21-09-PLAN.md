---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 09
type: execute
wave: 9
depends_on:
  - 21-08
files_modified:
  - src/main/resources/META-INF/mods.toml
  - src/main/resources/mixins.bannermod.json
  - src/main/resources/assets/bannermod/**
  - src/main/resources/data/bannermod/**
  - src/main/resources/META-INF/accesstransformer.cfg
  - build.gradle
  - recruits/**
  - workers/**
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-03
  - SRCMOVE-04
must_haves:
  truths:
    - "`build.gradle` source sets contain only `src/main/java`, `src/test/java`, `src/gametest/java` (and matching resources). The `recruits/src/main/java`, `workers/src/main/java`, `recruits/src/main/resources`, `workers/src/main/resources` entries are removed."
    - "Resources are unified under `src/main/resources/{assets,data}/bannermod/**`. Any recruit/worker-specific resource under `recruits/src/main/resources/` or `workers/src/main/resources/` either moves to outer `src/main/resources/` (under `bannermod/` or kept folder name if referenced by ID) or is deleted. Asset resource-location IDs in code are rewritten `\"recruits:\"` → `\"bannermod:\"` and `\"workers:\"` → `\"bannermod:\"`."
    - "`src/main/resources/META-INF/mods.toml` declares only mod id `bannermod`; any remaining `recruits`/`workers`/`bannerlord` mod-id or entrypoint references are scrubbed."
    - "`./gradlew compileJava` succeeds from outer repo root with no reference to the embedded clone paths. Final compile-green gate per CONTEXT D-22."
    - "`recruits/` and `workers/` embedded clones are either (a) emptied of all source (only `Main.java`/`WorkersMain.java` shims and CLAUDE.md remain, with clone `.git/` preserved) OR (b) removed from the outer working tree entirely — decision recorded in `MERGE_NOTES.md`."
    - "`.planning/ROADMAP.md` §Phase 21 + `.planning/STATE.md` reflect the realized post-pivot structure with zero residual `bannerlord` references. `21-VERIFICATION.md` rewritten against the migrated tree."
---

<objective>
Close Phase 21: retire the embedded clone source sets from `build.gradle`, consolidate resources under `src/main/resources/{assets,data}/bannermod/`, scrub retired namespaces from `mods.toml` and mixin configs, run the compile-green gate, rewrite planning docs to reflect the final state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-08-SUMMARY.md
@.planning/ROADMAP.md
@.planning/STATE.md
@build.gradle
@src/main/resources/META-INF/mods.toml
@MERGE_NOTES.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate resources; retire clone source sets from build.gradle</name>
  <action>
    1. For each file under `recruits/src/main/resources/{assets,data}/bannermod/**` (already namespaced from earlier phases) and `workers/src/main/resources/{assets,data}/bannermod/**`: `git mv` or `cp + git rm` into outer `src/main/resources/{assets,data}/bannermod/**` preserving subpaths. Merge by path — if the same relative path exists in both and collides, prefer the file with a later mtime and document the picked side in `MERGE_NOTES.md`.
    2. For any files still under `recruits/src/main/resources/{assets,data}/recruits/**` or `workers/src/main/resources/{assets,data}/workers/**`: migrate to outer `src/main/resources/{assets,data}/bannermod/**`. Rewrite any in-file string `"recruits:` or `"workers:` or `minecraft:` references only where they name the mod's own resources (do not touch references to other mods).
    3. Run `grep -rln 'ResourceLocation("recruits"' src/main/java/ | xargs -r sed -i 's/ResourceLocation("recruits"/ResourceLocation("bannermod"/g'` and the same for `"workers"`. Handle the `new ResourceLocation(MOD_ID, ...)` pattern implicitly via the `MOD_ID = "bannermod"` constant (no code change needed).
    4. Edit `build.gradle`:
       - Line 50-ish: `java.srcDirs = ['src/main/java']` (drop `recruits/src/main/java`, `workers/src/main/java`).
       - Lines 51-57: `resources.srcDirs = ['src/main/resources']` (drop clone resource paths).
       - Lines 59-63: drop clone entries from `test`/`gametest` source sets too.
       - Line 244-256: drop `from('workers/src/main/resources')` copy rules.
       - Remove mixin refmap rules for `mixins.recruits.refmap.json` and `mixins.workers.refmap.json` if still present; add one for `mixins.bannermod.refmap.json`.
    5. Edit `src/main/resources/META-INF/mods.toml`: ensure exactly one `[[mods]]` entry with `modId="bannermod"`. Strip any `"recruits"`/`"workers"`/`"bannerlord"` mod-id or `entrypoint`/`mainClass` references.
    6. Decide clone retention (truth #5). Recommend: keep clone dirs as-is in outer working tree but not tracked by outer git — they retain their own git history for archaeological purposes. Document choice in `MERGE_NOTES.md`.
    7. Commit.
  </action>
  <verify>
    <automated>bash -c "
set -e
! grep -q 'recruits/src/main/java' build.gradle
! grep -q 'workers/src/main/java' build.gradle
! grep -q 'recruits/src/main/resources' build.gradle
! grep -q 'workers/src/main/resources' build.gradle
[ \"\$(grep -c '\"bannermod\"' src/main/resources/META-INF/mods.toml)\" -ge 1 ]
! grep -q '\"recruits\"' src/main/resources/META-INF/mods.toml
! grep -q '\"workers\"' src/main/resources/META-INF/mods.toml
! grep -q 'com\\.talhanation\\.bannerlord' src/main/resources/META-INF/mods.toml
test -d src/main/resources/assets/bannermod
"
    </automated>
  </verify>
  <done>Outer build.gradle + mods.toml own the single-entrypoint single-source-tree layout.</done>
</task>

<task type="auto">
  <name>Task 2: Run compile-green gate; rewrite planning docs; close phase</name>
  <action>
    1. Run `./gradlew compileJava` from outer repo root. If any unresolved-import errors remain, fix them via targeted `sed` across `src/main/java/com/talhanation/bannermod/**` (most will be stragglers pointing at `com.talhanation.{recruits,workers}.*` FQNs). Iterate until clean. Attach the final successful invocation to the SUMMARY.
    2. Run `./gradlew check` if the test tree is healthy post-migration. If individual tests fail for reasons traceable to the migration (e.g., test referencing a moved type with its old FQN), fix the test. If failures predate the migration, document as known-issues in `MERGE_NOTES.md` and proceed.
    3. Rewrite `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md` against the realized tree: one @Mod, shared channel, unified resources, no `bannerlord`/`recruits`/`workers` production source under outer `src/main/java/`, build green.
    4. Update `.planning/ROADMAP.md` §Phase 21: mark all 9 plans complete, strip `bannerlord` language, update the plan one-liners to match the realized subsystem-migration structure.
    5. Update `.planning/STATE.md` Current Position and plan progress markers.
    6. Append final Wave-9 entry to `MERGE_NOTES.md` summarizing what closed the phase, the compile-gate result, and the clone-retention decision.
    7. Commit.
  </action>
  <verify>
    <automated>bash -c "
set -e
./gradlew compileJava --quiet
! grep -rn 'com\\.talhanation\\.bannerlord' src/main/java/ src/main/resources/ build.gradle
[ \"\$(find src/main/java/com/talhanation -maxdepth 1 -type d | wc -l)\" -le 2 ]
"
    </automated>
  </verify>
  <done>Phase 21 closed: single-entrypoint single-source-tree single-mixin-config single-channel runtime under `com.talhanation.bannermod.**`, compile-green, docs updated.</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava` passes from outer repo root.
- `src/main/java/com/talhanation/` contains only `bannermod/` (no `bannerlord`, `recruits`, `workers` production subtrees).
- `mods.toml` declares only mod id `bannermod`.
- `build.gradle` source sets reference only outer paths.
- Planning docs reflect the final structure.
</verification>

<success_criteria>
- Full consolidation goal per CONTEXT D-01/D-02/D-18 achieved.
- Outer repo builds standalone.
- `bannermod.**` is the single namespace for the merged runtime.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-09-SUMMARY.md`
</output>
