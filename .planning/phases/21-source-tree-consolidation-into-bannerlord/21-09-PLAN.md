---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 09
type: execute
wave: 4
depends_on:
  - 21-06
  - 21-07
  - 21-08
files_modified:
  - src/main/java/com/talhanation/workers/**
  - src/main/java/com/talhanation/bannerlord/entity/civilian/**
  - src/main/java/com/talhanation/bannerlord/ai/civilian/**
  - src/main/java/com/talhanation/bannerlord/persistence/civilian/**
  - src/main/java/com/talhanation/bannerlord/client/civilian/**
  - src/main/java/com/talhanation/bannerlord/compat/workers/**
  - .planning/VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-04
gap_closure: true
must_haves:
  truths:
    - "The remaining `com.talhanation.workers/**` tree is reduced to documented compatibility-facing surfaces, not active civilian gameplay ownership."
    - "The retired root-only layout passes `compileJava`, `processResources`, `test`, and `verifyGameTestStage`."
    - "Phase 21 docs truthfully record the green closeout and the exact retained compatibility boundary."
  artifacts:
    - path: "src/main/java/com/talhanation/bannerlord/compat/workers/WorkersRuntime.java"
      provides: "Allowed worker compatibility seam"
      contains: "LEGACY_MOD_ID"
    - path: ".planning/VERIFICATION.md"
      provides: "Post-gap validation evidence"
      contains: "verifyGameTestStage"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 closeout truth"
      contains: "21-09-PLAN.md"
  key_links:
    - from: "src/main/java/com/talhanation/workers/**"
      to: "src/main/java/com/talhanation/bannerlord/**"
      via: "deprecated forwarders/adapters"
      pattern: "@Deprecated|com\\.talhanation\\.bannerlord"
    - from: ".planning/VERIFICATION.md"
      to: "./gradlew verifyGameTestStage"
      via: "recorded gate result"
      pattern: "verifyGameTestStage"
---

<objective>
Finish the source-tree consolidation by shrinking the remaining workers package family to compatibility-only surfaces and re-running the full Phase 21 validation gate.

Purpose: the phase is not done until both recruit and worker legacy package families stop owning live gameplay code and the retired root-only layout proves green end-to-end.
Output: worker compatibility is isolated under explicit adapters, all required Gradle gates pass, and roadmap/state/verification docs can honestly mark Phase 21 complete.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-04-SUMMARY.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md
@src/main/java/com/talhanation/bannerlord/compat/workers/WorkersRuntime.java
@src/main/java/com/talhanation/workers/WorkersRuntime.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote worker gameplay packages to adapter-only surfaces</name>
  <files>src/main/java/com/talhanation/workers/**, src/main/java/com/talhanation/bannerlord/entity/civilian/**, src/main/java/com/talhanation/bannerlord/ai/civilian/**, src/main/java/com/talhanation/bannerlord/persistence/civilian/**, src/main/java/com/talhanation/bannerlord/client/civilian/**, src/main/java/com/talhanation/bannerlord/compat/workers/**</files>
  <action>Convert any remaining worker gameplay owners into thin deprecated adapters that forward to the canonical bannerlord civilian/compat classes, or remove them where no runtime/save path still needs the old package. Preserve only the narrow worker compatibility seams called out in the Phase 20 move contract: legacy-id remaps, structure migration, runtime bootstrap helpers, registrars, and packet offset adapters. Everything else should be bannerlord-owned by the end of this task.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
bad = []
allowed_tokens = ('@Deprecated', 'compat', 'migration', 'WorkersRuntime', 'WorkersLegacyMappings', 'network', 'init', 'config')
for path in Path('src/main/java/com/talhanation/workers').rglob('*.java'):
    text = path.read_text()
    if 'package com.talhanation.workers' not in text:
        continue
    if not any(token in text for token in allowed_tokens):
        bad.append(str(path))
if bad:
    raise SystemExit('Workers files still look like active owners:\n' + '\n'.join(sorted(bad)[:80]))
PY</automated>
  </verify>
  <done>The workers package family no longer owns civilian gameplay classes; only documented compatibility-facing surfaces remain.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the full Phase 21 gate from the retired root-only layout</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Run the required acceptance gate in order from the repository root: `./gradlew compileJava`, `./gradlew processResources`, `./gradlew test`, and `./gradlew verifyGameTestStage`. If any command fails, fix only the worker-side ownership/adapter fallout uncovered by the new validation run before continuing. Record the final green evidence in `.planning/VERIFICATION.md` with the gap closed.</action>
  <verify>
    <automated>./gradlew compileJava processResources test verifyGameTestStage</automated>
  </verify>
  <done>The retired root-only layout is green across compile, resources, JUnit, and GameTest validation.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh roadmap, state, and merge notes with the honest Phase 21 closeout</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, MERGE_NOTES.md, .planning/VERIFICATION.md</files>
  <action>Once the full gate passes, update the active planning docs to mark Phase 21 complete, list the new gap-closure plans and summaries, and state exactly which compat surfaces still remain on purpose (`bannerlord.compat.workers`, bootstrap shims, and any explicitly retained forwarders). Remove wording that still describes recruit/workers package families as active owners.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
checks = {
    '.planning/ROADMAP.md': ['21-09-PLAN.md', 'Status: Complete'],
    '.planning/STATE.md': ['Phase 21', 'complete'],
    '.planning/VERIFICATION.md': ['verifyGameTestStage', 'BUILD SUCCESSFUL']
}
for file, required in checks.items():
    text = Path(file).read_text()
    missing = [item for item in required if item not in text]
    if missing:
        raise SystemExit(f'{file} missing expected closeout text: {missing}')
PY</automated>
  </verify>
  <done>Phase 21 planning and merge docs match the green post-retirement reality and the intentionally retained compatibility boundary.</done>
</task>

</tasks>

<verification>
- The worker package family is compatibility-only.
- All four required Gradle validation commands pass from the root-only layout.
- Planning docs and merge notes now close Phase 21 truthfully.
</verification>

<success_criteria>
- Verification truths 4 and 5 are fully closed.
- BannerMod is one active root-owned codebase with only explicit temporary compatibility wrappers remaining.
- Phase 22 can start from a genuinely consolidated bannerlord source tree.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-09-SUMMARY.md`
</output>
