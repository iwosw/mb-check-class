---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 09
type: execute
wave: 9
depends_on:
  - 21-08
files_modified:
  - src/main/java/com/talhanation/workers/**
  - src/main/java/com/talhanation/bannermod/compat/workers/**
  - src/test/java/**
  - src/gametest/java/**
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-03
  - SRCMOVE-04
must_haves:
  truths:
    - "Every surviving `.java` file under `src/main/java/com/talhanation/workers/**` is either (a) a `@Deprecated` forwarder whose javadoc `@deprecated` tag points at a canonical `com.talhanation.bannermod.*.civilian.*` or `com.talhanation.bannermod.compat.workers.*` home per D-11, or (b) one of the explicitly enumerated retained compat seams (bootstrap glue, legacy-id remap, structure NBT migration, packet offset registrar, event subscribers, i18n helpers) listed by path in `MERGE_NOTES.md` under `### Phase 21 worker compat surface`."
    - "No file under `src/main/java/com/talhanation/bannermod/**`, `src/test/java/**`, or `src/gametest/java/**` imports a non-deprecated `com.talhanation.workers.{entities,ai,world,client,inventory,items}.*` type for moved behavior; compat-regression tests carry the marker `// Phase 21: compat-surface regression — retains workers-package import by design`."
    - "The full Phase 21 validation gate passes from the root-only layout: `./gradlew compileJava`, `./gradlew processResources`, `./gradlew test`, and `./gradlew verifyGameTestStage` each exit `BUILD SUCCESSFUL`, with tail output of each run captured into the rewritten `21-VERIFICATION.md`."
    - "`.planning/ROADMAP.md`, `.planning/STATE.md`, `21-VERIFICATION.md` (rewritten from scratch per D-14), and `MERGE_NOTES.md` truthfully record the green Phase 21 closeout against canonical `com.talhanation.bannermod/**` ownership; zero prose anywhere in `.planning/**` contains the substring `com.talhanation.bannerlord` and the phase title drops `Into Bannerlord` in favor of `Into BannerMod` per D-13."
    - "Nothing under `com.talhanation.bannerlord.**` remains on disk (directory absent or empty)."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java"
      provides: "Narrow worker compat seam: legacy mod id, shared channel accessor, asset helpers"
      contains: "LEGACY_MOD_ID"
    - path: "src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java"
      provides: "MissingMappingsEvent bridge for legacy `workers:*` registry ids"
      contains: "MissingMappingsEvent"
    - path: ".planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md"
      provides: "Post-pivot verification report: all 5 truths VERIFIED, status verified, log tails embedded"
      contains: "verifyGameTestStage"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 marked Complete; heading reads `Into BannerMod`; all nine plan bullets checked"
      contains: "Into BannerMod"
    - path: "MERGE_NOTES.md"
      provides: "`### Phase 21 closeout` entry naming exact retained compat surfaces and cross-referencing recruit + worker compat-surface enumerations"
      contains: "Phase 21 closeout"
  key_links:
    - from: ".planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md"
      to: "./gradlew verifyGameTestStage"
      via: "captured BUILD SUCCESSFUL log tail embedded in the report"
      pattern: "verifyGameTestStage|BUILD SUCCESSFUL"
    - from: "src/main/java/com/talhanation/workers/**"
      to: "src/main/java/com/talhanation/bannermod/**"
      via: "@Deprecated forwarders + enumerated retained compat seams"
      pattern: "@Deprecated|com\\.talhanation\\.bannermod"
    - from: ".planning/ROADMAP.md"
      to: ".planning/phases/21-source-tree-consolidation-into-bannerlord/"
      via: "plan list + Complete status line"
      pattern: "21-09-PLAN.md"
---

<objective>
Close Phase 21: demote `com.talhanation.workers/**` to a documented compat surface per CONTEXT D-11, run the full validation gate green from the root-only layout, and rewrite the planning docs so ROADMAP/STATE/VERIFICATION/MERGE_NOTES truthfully reflect the post-pivot canonical `com.talhanation.bannermod/**` ownership with zero residual `bannerlord` language per D-02 and D-13.

Purpose: REQUIREMENTS.md SRCMOVE-04 requires worker-package files reduced to the narrow enumerated compat surface with canonical civilian ownership exclusively under `com.talhanation.bannermod.*.civilian/**` and narrow worker compat under `com.talhanation.bannermod.compat.workers/**`. CONTEXT D-14 requires rewriting `21-VERIFICATION.md` from scratch on re-execute. D-13 requires dropping "Into Bannerlord" from the phase title. CLAUDE.md preserves workers as a legacy subsystem — but only through the enumerated compat seams, not as an active civilian owner.

Output: workers tree is compat-only; full gate green with captured log tails; ROADMAP/STATE/VERIFICATION/MERGE_NOTES rewritten against the canonical bannermod namespace; Phase 21 marked Complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-PLAN.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-08-PLAN.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote worker-package files to @Deprecated forwarders or enumerated retained compat seams</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md — D-11 (demote `com.talhanation.workers/**` to `bannermod.compat.workers/**`), D-03 (preserve existing `com.talhanation.bannermod.{citizen,governance,settlement,logistics,authority,config}` subtrees — civilian work lands alongside them as `bannermod.*.civilian/**`).
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-PLAN.md — Wave 7 moved civilian gameplay to `com.talhanation.bannermod.{entity,ai,persistence,client}/civilian/**` and worker runtime helpers to `com.talhanation.bannermod.compat.workers/**`.
    - Run `ls -R src/main/java/com/talhanation/workers/` to enumerate every surviving file.
    - Run `grep -rLZ '@Deprecated\|@deprecated' src/main/java/com/talhanation/workers/entities src/main/java/com/talhanation/workers/ai src/main/java/com/talhanation/workers/world src/main/java/com/talhanation/workers/client src/main/java/com/talhanation/workers/inventory 2>/dev/null | xargs -0 -n1 echo` — lists demotion candidates.
    - src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java — confirm it owns `LEGACY_MOD_ID`, channel binding, and asset helpers (post-Wave 7 this is the canonical worker-compat home).
  </read_first>
  <action>
    For EVERY `.java` file still under `src/main/java/com/talhanation/workers/**`, assign exactly ONE disposition with a top-of-file comment `// Phase 21 disposition: <forwarder|retained-seam|deleted>`.

    **Disposition A — DELETE (via `git rm`):** The file was moved to `com.talhanation.bannermod.{entity,ai,persistence,client}.civilian` by Wave 7 and no retained caller imports the legacy worker path.
    - Verify with `grep -rln 'com\.talhanation\.workers\.<subpkg>\.<ClassName>' src/main/java src/test/java src/gametest/java` — zero hits outside the file itself → delete.
    - Typical candidates: concrete worker entities under `workers/entities/`, profession AI under `workers/ai/`, work-area persistence under `workers/world/`, worker UI screens under `workers/client/`.

    **Disposition B — DEMOTE to `@Deprecated` forwarder:** The file still has at least one retained caller.
    - Keep file in place, annotate type with `@Deprecated` plus javadoc:
      ```java
      /**
       * @deprecated Use {@link com.talhanation.bannermod.<canonical>.civilian.<Name>} instead.
       * Forwarder retained during Phase 21 consolidation (CONTEXT D-11). Scheduled for removal in a follow-up cleanup phase.
       */
      ```
    - Collapse all method bodies to single-line delegation to the canonical bannermod type.

    **Disposition C — RETAINED COMPAT SEAM (explicitly enumerated, no `@Deprecated` required):** The file is one of the narrow compatibility seams the runtime contract still needs. The ONLY allowed retained seams are EXACTLY:
    - `src/main/java/com/talhanation/workers/WorkersMain.java` — legacy bootstrap shim
    - `src/main/java/com/talhanation/workers/WorkersRuntime.java` — legacy mod id accessor, channel binding helper, asset helpers; delegates to `com.talhanation.bannermod.compat.workers.WorkersRuntime`
    - `src/main/java/com/talhanation/workers/WorkersSubsystem.java` — subsystem glue invoked from the canonical bootstrap
    - `src/main/java/com/talhanation/workers/MergedRuntimeCleanupPolicy.java` — merged-runtime cleanup policy bridge
    - Everything under `src/main/java/com/talhanation/workers/init/**` — `WorkersLifecycleRegistrar` preserves `WorkersLegacyMappings` registration
    - Everything under `src/main/java/com/talhanation/workers/network/**` — `WorkersNetworkRegistrar` preserves the packet offset contract
    - Everything under `src/main/java/com/talhanation/workers/config/**` — config helpers mirroring the legacy filename contract
    - `AttributeEvent.java`, `VillagerEvents.java`, `CommandEvents.java`, `UpdateChecker.java`, `Translatable.java` — Forge-bus event subscribers / i18n helpers
    - Anything under `workers/settlement/**` bridging the work-area claim contract

    Each Disposition-C file stays on disk; imports MUST point at `com.talhanation.bannermod.*` for moved symbols (zero `com.talhanation.bannerlord` references — CONTEXT D-02); each MUST appear by path in the MERGE_NOTES enumeration.

    Files NOT on the allowed-retained list and NOT movable to Disposition A go to Disposition B. No fourth disposition.

    After applying dispositions, append to `MERGE_NOTES.md` under the existing `## Phase 21 Pivot (2026-04-15)` heading. Add subsection with EXACT heading `### Phase 21 worker compat surface`. Body: flat bullet list of every surviving `src/main/java/com/talhanation/workers/**` file, grouped by disposition subheadings:
    ```
    #### Forwarders (Disposition B)
    - `src/main/java/com/talhanation/workers/<path>.java` — forwards to `com.talhanation.bannermod.<canonical>.civilian.<Name>`
    #### Retained bootstrap/event/migration seams (Disposition C)
    - `src/main/java/com/talhanation/workers/WorkersRuntime.java` — legacy mod id accessor + channel binding + asset helpers
    - `src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java` — registers WorkersLegacyMappings on the Forge bus
    ...
    ```

    Also retarget test/gametest imports — for every file under `src/test/java/**` or `src/gametest/java/**` matching `grep -rln 'import com\.talhanation\.workers\.\(entities\|ai\|world\|client\|inventory\)\.'`:
    - Rewrite `com.talhanation.workers.entities.<X>` → `com.talhanation.bannermod.entity.civilian.<X>`
    - Rewrite `com.talhanation.workers.ai.<Y>` → `com.talhanation.bannermod.ai.civilian.<Y>`
    - Rewrite `com.talhanation.workers.world.<Z>` → `com.talhanation.bannermod.persistence.civilian.<Z>`
    - Rewrite `com.talhanation.workers.client.<C>` → `com.talhanation.bannermod.client.civilian.<C>`
    - Exception: tests explicitly exercising a worker COMPAT adapter keep the legacy import AND carry the marker `// Phase 21: compat-surface regression — retains workers-package import by design`.
  </action>
  <verify>
    <automated>bash -c "set -e; violations=$(python3 - <<'PY'
from pathlib import Path
allowed = ('@Deprecated', '@deprecated', 'compat', 'migration', 'WorkersRuntime', 'WorkersLegacyMappings', 'WorkersSubsystem', 'WorkersMain', 'MergedRuntimeCleanupPolicy', 'WorkersLifecycleRegistrar', 'WorkersNetworkRegistrar', 'SubscribeEvent', 'UpdateChecker', 'Translatable', 'AttributeEvent', 'VillagerEvents', 'CommandEvents', 'Phase 21 disposition', 'LEGACY_MOD_ID')
bad = []
for path in Path('src/main/java/com/talhanation/workers').rglob('*.java'):
    text = path.read_text()
    if 'package com.talhanation.workers' not in text: continue
    if not any(t in text for t in allowed): bad.append(str(path))
    if 'com.talhanation.bannerlord' in text: bad.append(str(path) + ' (stale bannerlord import)')
if bad: print('\n'.join(sorted(bad)))
PY
); if [ -n \"$violations\" ]; then echo 'FAIL: workers files violate compat-only contract:'; echo \"$violations\"; exit 1; fi; \
grep -q '### Phase 21 worker compat surface' MERGE_NOTES.md || { echo 'FAIL: MERGE_NOTES missing worker compat surface enumeration'; exit 1; }; \
cross=$(grep -rln 'import com\\.talhanation\\.workers\\.\\(entities\\|ai\\|world\\|client\\|inventory\\|items\\)\\.' src/main/java/com/talhanation/bannermod src/test/java src/gametest/java 2>/dev/null | xargs -r grep -L '@Deprecated\\|Phase 21: compat-surface regression' 2>/dev/null); \
if [ -n \"$cross\" ]; then echo 'FAIL: callers still import non-deprecated worker gameplay:'; echo \"$cross\"; exit 1; fi; \
./gradlew compileJava compileTestJava"</automated>
  </verify>
  <acceptance_criteria>
    - Every surviving `.java` file under `src/main/java/com/talhanation/workers/**` contains at least one of the allowed tokens (`@Deprecated`, `@deprecated`, `compat`, `migration`, `WorkersRuntime`, `WorkersLegacyMappings`, `WorkersSubsystem`, `WorkersMain`, `MergedRuntimeCleanupPolicy`, `WorkersLifecycleRegistrar`, `WorkersNetworkRegistrar`, `SubscribeEvent`, `UpdateChecker`, `Translatable`, `AttributeEvent`, `VillagerEvents`, `CommandEvents`, `LEGACY_MOD_ID`, `Phase 21 disposition`).
    - Zero files under `src/main/java/com/talhanation/workers/**` contain the string `com.talhanation.bannerlord`.
    - Every surviving file path under `src/main/java/com/talhanation/workers/**` appears as a literal substring inside the `### Phase 21 worker compat surface` MERGE_NOTES subsection.
    - No file under `src/main/java/com/talhanation/bannermod/**`, `src/test/java/**`, or `src/gametest/java/**` imports a non-deprecated `com.talhanation.workers.{entities,ai,world,client,inventory,items}.*` type without either an `@Deprecated` annotation on its own type or the marker `Phase 21: compat-surface regression`.
    - `./gradlew compileJava compileTestJava` exits status 0 with `BUILD SUCCESSFUL`.
  </acceptance_criteria>
  <done>Worker package family is a documented compat surface per D-11. Canonical civilian ownership lives under `com.talhanation.bannermod.*.civilian/**` and narrow worker compat under `com.talhanation.bannermod.compat.workers/**`. Tests target canonical bannermod civilian classes.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: [BLOCKING] Run and capture the full Phase 21 validation gate green from the root-only layout</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md — any compile blocker referenced here MUST be resolved by this gate, not re-deferred.
    - build.gradle — confirm active source sets point only at `src/main/java`, `src/test/java`, `src/gametest/java`, and root resources.
    - Wave 7 / Wave 8 outputs — this task runs AFTER bannermod is canonical and workers/recruits are compat-only.
  </read_first>
  <action>
    **This is a blocking task.** Phase 21 cannot close without four green gate captures. Run these four commands in this exact order from the repository root and capture full output into the specified log files. Each command MUST end with `BUILD SUCCESSFUL`:

    ```bash
    ./gradlew compileJava 2>&1 | tee /tmp/21-09-compileJava.log
    ./gradlew processResources 2>&1 | tee /tmp/21-09-processResources.log
    ./gradlew test 2>&1 | tee /tmp/21-09-test.log
    ./gradlew verifyGameTestStage 2>&1 | tee /tmp/21-09-verifyGameTestStage.log
    ```

    After all four succeed, copy the tail (last ~60 lines including the `BUILD SUCCESSFUL` marker) of each log into permanent evidence files under the phase directory:
    ```bash
    mkdir -p .planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence
    for cmd in compileJava processResources test verifyGameTestStage; do
      tail -60 /tmp/21-09-$cmd.log > .planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence/$cmd.tail.log
    done
    ```
    Task 3 embeds these tails into the rewritten `21-VERIFICATION.md`.

    **If a gate command fails, fix scope is limited to:**
    - (a) Worker-side compat-adapter fallout (re-apply plan 21-09 Task 1 recipe; tighten a forwarder signature; retarget a stale import to `com.talhanation.bannermod.*`).
    - (b) Recruit-side compat-adapter fallout (re-apply plan 21-08 Task 1 recipe).
    - (c) Test-side import retargeting (plan 21-08 Task 2 recipe for recruits, this plan Task 1 for workers).
    - (d) Recruit↔bannermod bridge fallout not caught in Wave 7 (adjust the recruit-side adapter; do NOT edit canonical `com.talhanation.bannermod.*` gameplay logic to paper over a failing test).

    **OUT OF SCOPE:**
    - Editing `com.talhanation.bannermod.*` canonical gameplay logic to make a test pass (canonical class is authoritative; fix the test or the adapter instead).
    - Creating new files under `com.talhanation.bannerlord.**` (namespace retired per D-02).
    - Resolving brownfield `TODO` clusters (`CaptainEntity:183`, `VillagerNobleEntity:102`, `BannerModLogisticsService` empty returns) unless they directly block a gate command — those stay in their respective phases.

    **Final combined verification run** for the task-level `<verify>` block below (confirms end-to-end gate passage in one shot).
  </action>
  <verify>
    <automated>bash -c "set -e; ./gradlew compileJava processResources test verifyGameTestStage 2>&1 | tee /tmp/21-09-gate-final.log; tail -5 /tmp/21-09-gate-final.log | grep -q 'BUILD SUCCESSFUL' || { echo 'FAIL: Phase 21 gate did not end BUILD SUCCESSFUL'; exit 1; }; \
      for cmd in compileJava processResources test verifyGameTestStage; do \
        test -f .planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence/$cmd.tail.log || { echo \"FAIL: missing evidence capture for $cmd\"; exit 1; }; \
        grep -q 'BUILD SUCCESSFUL' .planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence/$cmd.tail.log || { echo \"FAIL: $cmd.tail.log does not contain BUILD SUCCESSFUL\"; exit 1; }; \
      done; \
      test ! -d src/main/java/com/talhanation/bannerlord || [ $(find src/main/java/com/talhanation/bannerlord -type f | wc -l) -eq 0 ] || { echo 'FAIL: com.talhanation.bannerlord still has files on disk'; exit 1; }"</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/21-09-compileJava.log`, `/tmp/21-09-processResources.log`, `/tmp/21-09-test.log`, `/tmp/21-09-verifyGameTestStage.log` all exist and each contains a `BUILD SUCCESSFUL` line.
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence/{compileJava,processResources,test,verifyGameTestStage}.tail.log` all exist and each contains `BUILD SUCCESSFUL`.
    - A combined `./gradlew compileJava processResources test verifyGameTestStage` run from the repository root exits status 0.
    - `test ! -d src/main/java/com/talhanation/bannerlord` OR the directory is empty — the `com.talhanation.bannerlord` namespace is absent on disk.
    - Requirements SRCMOVE-01..04 are all exercised by the gate: `compileJava` proves SRCMOVE-01 (canonical shared seam ownership) + SRCMOVE-02 (civilian/military re-home); `test` + `verifyGameTestStage` prove SRCMOVE-03 (recruit compat) + SRCMOVE-04 (worker compat) hold against live regression coverage.
  </acceptance_criteria>
  <done>Four gate commands captured green with permanent evidence files under `gate-evidence/`. Task 3 can embed the tails into the rewritten `21-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite ROADMAP, STATE, 21-VERIFICATION, and MERGE_NOTES for truthful post-pivot Phase 21 closeout</name>
  <read_first>
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/gate-evidence/{compileJava,processResources,test,verifyGameTestStage}.tail.log` — the four green captures from Task 2.
    - `.planning/ROADMAP.md` §Phase 21 — locate with `grep -n '## Phase 21' .planning/ROADMAP.md`; currently says "Into Bannerlord" (per old title) and a non-Complete status. Both must change per D-13.
    - `.planning/STATE.md` — locate Phase 21 entries with `grep -n 'Phase 21\|21-' .planning/STATE.md` and flip status to complete.
    - CONTEXT.md D-14 (rewrite/delete 21-VERIFICATION on re-execute — Wave 7 deleted the old file; this task writes a fresh one).
    - CLAUDE.md (workers preservation stance — the closeout prose must keep it intact: workers stays as a compat surface, not as an active civilian owner).
  </read_first>
  <action>
    Four sub-edits:

    **A. `.planning/ROADMAP.md` §Phase 21** — edit in place:
    1. Replace the heading `## Phase 21: Source Tree Consolidation Into Bannerlord` with `## Phase 21: Source Tree Consolidation Into BannerMod` (per CONTEXT D-13).
    2. Rewrite every description bullet that references `com.talhanation.bannerlord` / `src/main/java/com/talhanation/bannerlord/**` to reference `com.talhanation.bannermod` / `src/main/java/com/talhanation/bannermod/**`. Zero bannerlord mentions remain anywhere in §Phase 21.
    3. Update the `**Plans:**` count to `**Plans:** 9/9 plans complete`.
    4. Update every plan bullet 21-01..21-09 to checked `- [x]` with a bannermod-oriented one-liner:
       - `21-01-PLAN.md` — shared seam canonical ownership target bannermod
       - `21-02..21-06-PLAN.md` — structural moves (bootstrap/network/registry, recruit military/shared, worker civilian, source-root retirement, shared seam ownership) landed under bannerlord and pivoted to bannermod in Wave 7
       - `21-07-PLAN.md` — pivot bannerlord→bannermod + resolve recruit bridge + compileJava green
       - `21-08-PLAN.md` — demote `com.talhanation.recruits/**` to documented compat surfaces per D-10
       - `21-09-PLAN.md` — demote `com.talhanation.workers/**` per D-11, run full gate green, close Phase 21 truthfully
    5. Replace the Status line with: `Status: Complete (9/9 plans complete as of 2026-04-15); active Java ownership now lives exclusively under `com.talhanation.bannermod/**`, `com.talhanation.bannerlord.**` is fully retired per CONTEXT D-02, `recruits/**` and `workers/**` are reduced to documented compatibility surfaces enumerated in MERGE_NOTES.md, and the root-only validation gate (compileJava, processResources, test, verifyGameTestStage) exits BUILD SUCCESSFUL.`

    **B. `.planning/STATE.md`** — edit the current-phase marker and decisions block:
    1. If the active-phase pointer still says `Executing Phase 21`, advance it (next phase per ROADMAP — commonly Phase 23 if 22 is complete; double-check with `grep -n 'Status:' .planning/ROADMAP.md`).
    2. Update the `- Current focus:` bullet to drop any "bannerlord" language and state that Phase 21 is complete, active Java ownership lives under `com.talhanation.bannermod/**`, and recruit/worker packages are compat-only.
    3. Update the `- Pending major work:` bullet so it no longer mentions the `com.talhanation.bannerlord` namespace or retained recruit↔bannerlord type mismatches (those are resolved).
    4. Update the `- Workers status:` bullet — `worker gameplay ownership is rooted under bannerlord` must change to `worker gameplay ownership is rooted under bannermod`.
    5. Add a closeout decision entry under `## Decisions`: `[Phase 21-source-tree-consolidation-into-bannerlord]: Phase 21 complete; canonical Java ownership lives under com.talhanation.bannermod/**, com.talhanation.bannerlord.** is fully retired per CONTEXT D-02, recruits/** and workers/** are reduced to documented compatibility surfaces (see MERGE_NOTES "Phase 21 recruit compat surface" / "Phase 21 worker compat surface"), and ./gradlew compileJava processResources test verifyGameTestStage exits BUILD SUCCESSFUL from the root-only layout.`
    6. Strip ALL residual `com.talhanation.bannerlord` substrings from STATE.md — no existing decision or note may mention the retired namespace. Rewrite in-line (preserve the decision's intent) rather than deleting.

    **C. `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md`** — create from scratch (Wave 7 deleted the old one per D-14):
    1. Frontmatter: `phase: 21-source-tree-consolidation-into-bannerlord`, `verified: <current-ISO-8601>`, `status: verified`, `score: 5/5 truths verified`. No `gaps:` block.
    2. Phase Goal statement: "Consolidate active Java ownership into `src/main/java/com/talhanation/bannermod/**`; retire `com.talhanation.bannerlord.**` fully; reduce `com.talhanation.recruits/**` and `com.talhanation.workers/**` to documented compat surfaces; pass the root-only validation gate."
    3. Observable Truths table — all five rows `✓ VERIFIED`:
       - Truth 1: nested source roots retired (cite `build.gradle` source-set wiring).
       - Truth 2: canonical bannermod bootstrap/network/compat keeps the live `bannermod` mod id stable.
       - Truth 3: canonical `com.talhanation.bannermod.shared/**` and `com.talhanation.bannermod.config/**` own the shared seam + config taxonomy.
       - Truth 4: `com.talhanation.recruits/**` and `com.talhanation.workers/**` contain only `@Deprecated` forwarders or files enumerated in MERGE_NOTES.md under `### Phase 21 recruit compat surface` / `### Phase 21 worker compat surface`. `com.talhanation.bannerlord.**` is absent on disk.
       - Truth 5: quote the `BUILD SUCCESSFUL` tail from `gate-evidence/verifyGameTestStage.tail.log` (embed the last ~15 lines verbatim inside a fenced block).
    4. Required Artifacts table — every row `✓ VERIFIED` against real `com.talhanation.bannermod/**` paths (shared, bootstrap, network, registry, compat.workers, compat.recruits).
    5. Key Link Verification table — every row `✓ WIRED`.
    6. Behavioral Spot-Checks table — four rows, one per gate command, each `✓ PASS` with an excerpt from the corresponding `gate-evidence/*.tail.log`.
    7. Requirements Coverage table:
       - SRCMOVE-01 (canonical shared seam ownership under bannermod) — ✓ VERIFIED (plan 21-06 + Wave 7 pivot).
       - SRCMOVE-02 (canonical entity/ai/persistence/client re-home under bannermod) — ✓ VERIFIED (plans 21-03 + 21-04 + Wave 7 pivot).
       - SRCMOVE-03 (recruit compat surface documented) — ✓ VERIFIED (plan 21-08).
       - SRCMOVE-04 (worker compat surface enumerated narrow) — ✓ VERIFIED (plan 21-09).
    8. Closeout Summary: enumerate the exact retained compat surfaces by cross-reference to `MERGE_NOTES.md` (`### Phase 21 recruit compat surface`, `### Phase 21 worker compat surface`, the narrow `com.talhanation.bannermod.compat.workers/**` runtime helpers). Note that `com.talhanation.bannerlord.**` is fully retired per CONTEXT D-02.

    **D. `MERGE_NOTES.md`** — append:
    1. Under the existing `## Phase 21 Pivot (2026-04-15)` heading, add subsection `### Phase 21 closeout (2026-04-15)`.
    2. Body must contain:
       - One-line statement that `com.talhanation.bannerlord.**` is fully retired per CONTEXT D-02.
       - Four lines, one per gate command, each quoting one-line excerpts from `gate-evidence/{compileJava,processResources,test,verifyGameTestStage}.tail.log` including the `BUILD SUCCESSFUL` marker.
       - Cross-references (do NOT duplicate file lists — reference headings): "Retained recruit compat surface: see `### Phase 21 recruit compat surface`. Retained worker compat surface: see `### Phase 21 worker compat surface`. Narrow worker runtime compat home: `com.talhanation.bannermod.compat.workers/**`."
       - One-line statement that Phase 21 is closed and SRCMOVE-01..04 are satisfied.
  </action>
  <verify>
    <automated>bash -c "set -e; python3 - <<'PY'
from pathlib import Path
checks = {
    '.planning/ROADMAP.md': ['21-09-PLAN.md', 'Status: Complete', 'com.talhanation.bannermod', 'Into BannerMod', '9/9 plans complete'],
    '.planning/STATE.md': ['Phase 21', 'complete', 'com.talhanation.bannermod'],
    '.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md': ['verifyGameTestStage', 'BUILD SUCCESSFUL', 'com.talhanation.bannermod', 'status: verified', '5/5 truths verified', 'SRCMOVE-01', 'SRCMOVE-04'],
    'MERGE_NOTES.md': ['Phase 21 closeout', 'Phase 21 recruit compat surface', 'Phase 21 worker compat surface', 'BUILD SUCCESSFUL']
}
bad = []
for file, required in checks.items():
    text = Path(file).read_text()
    missing = [item for item in required if item not in text]
    if missing: bad.append(f'{file} missing {missing}')
forbidden = {
    '.planning/ROADMAP.md': ['Into Bannerlord', 'com.talhanation.bannerlord'],
    '.planning/STATE.md': ['com.talhanation.bannerlord'],
    '.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md': ['status: gaps_found', 'com.talhanation.bannerlord', '✗ FAILED']
}
for file, bad_strings in forbidden.items():
    text = Path(file).read_text()
    stale = [s for s in bad_strings if s in text]
    if stale: bad.append(f'{file} still contains stale text {stale}')
if bad: raise SystemExit('\n'.join(bad))
print('OK: all planning docs reflect truthful bannermod closeout')
PY
count=$(grep -rln 'com\\.talhanation\\.bannerlord' .planning 2>/dev/null | wc -l)
if [ \"$count\" -ne 0 ]; then echo \"FAIL: .planning still contains $count files with 'com.talhanation.bannerlord' references\"; grep -rln 'com\\.talhanation\\.bannerlord' .planning; exit 1; fi"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` §Phase 21 heading reads exactly `## Phase 21: Source Tree Consolidation Into BannerMod`; shows `**Plans:** 9/9 plans complete`; every bullet 21-01..21-09 checked `[x]`; the Status line contains both `Status: Complete` and `BUILD SUCCESSFUL`; zero substring `Into Bannerlord` or `com.talhanation.bannerlord` appears anywhere in the file.
    - `.planning/STATE.md` contains a line including both `Phase 21` and `complete` and zero occurrences of `com.talhanation.bannerlord`.
    - `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md` exists with frontmatter `status: verified` and `score: 5/5 truths verified`; contains `verifyGameTestStage`, `BUILD SUCCESSFUL`, `com.talhanation.bannermod`, and an evidence excerpt from `gate-evidence/verifyGameTestStage.tail.log`; contains zero occurrences of `status: gaps_found`, `✗ FAILED`, or `com.talhanation.bannerlord`.
    - `MERGE_NOTES.md` contains the literal headings `### Phase 21 closeout`, `### Phase 21 recruit compat surface`, and `### Phase 21 worker compat surface`, plus a `BUILD SUCCESSFUL` excerpt.
    - `grep -rln 'com\.talhanation\.bannerlord' .planning 2>/dev/null | wc -l` returns `0`.
  </acceptance_criteria>
  <done>Phase 21 planning docs truthfully record the green bannermod closeout: ROADMAP marks 9/9 complete with `Into BannerMod` title, STATE advances past Phase 21, VERIFICATION reports all five truths VERIFIED with gate-evidence tails embedded, MERGE_NOTES names the retained compat boundary by reference, and zero residual `bannerlord` prose remains in `.planning/**`.</done>
</task>

</tasks>

<verification>
- Worker package family matches the compat-only contract (D-11 satisfied).
- `./gradlew compileJava processResources test verifyGameTestStage` exits `BUILD SUCCESSFUL` from the repository root.
- ROADMAP/STATE/VERIFICATION/MERGE_NOTES record the Phase 21 closeout against canonical `com.talhanation.bannermod/**` ownership with zero residual `bannerlord` language.
- `com.talhanation.bannerlord.**` is absent on disk (D-02 satisfied).
- SRCMOVE-01..04 all closed with evidence in 21-VERIFICATION.md.
</verification>

<success_criteria>
- Phase 21 is Complete: 9/9 plans, full gate green, canonical ownership under `com.talhanation.bannermod/**`.
- Phase 22+ planners can trust that active Java ownership lives exclusively under `com.talhanation.bannermod/**`, that `com.talhanation.bannerlord.**` is retired, and that the retired root-only layout is gate-green.
- REQUIREMENTS.md SRCMOVE-01..04 are all satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-09-SUMMARY.md`
</output>
