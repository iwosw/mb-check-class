---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/main/java/com/talhanation/bannermod/shared/authority/BannerModAuthorityRules.java
  - src/main/java/com/talhanation/bannermod/shared/settlement/BannerModSettlementBinding.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModSupplyStatus.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModUpkeepProviders.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModLogisticsService.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModLogisticsRoute.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModCourierTask.java
  - src/main/java/com/talhanation/bannermod/shared/logistics/BannerModCombinedContainer.java
  - src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
  - src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
  - src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
  - src/main/java/com/talhanation/bannermod/logistics/BannerModUpkeepProviders.java
  - src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsService.java
  - src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsRoute.java
  - src/main/java/com/talhanation/bannermod/logistics/BannerModCourierTask.java
  - src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java
  - src/main/java/com/talhanation/recruits/**
  - src/main/java/com/talhanation/workers/**
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-01
must_haves:
  truths:
    - "Canonical shared seam ownership lives under `src/main/java/com/talhanation/bannermod/shared/**` (new sibling subtree per CONTEXT D-04)."
    - "All active recruit and worker callers import the new `com.talhanation.bannermod.shared.*` seams instead of the legacy `com.talhanation.bannermod.{authority,settlement,logistics}.*` packages."
    - "The pre-existing `com.talhanation.bannermod.{authority,settlement,logistics}` packages are reduced to deprecated thin forwarders into `bannermod.shared.*` (per CONTEXT D-05: do NOT dedup; record overlap in MERGE_NOTES.md)."
    - "Live runtime contract is unchanged: mod id stays `bannermod`, config filenames stay `bannermod-military.toml`/`bannermod-settlement.toml`/`bannermod-client.toml`, settlement binding stays claim-derived."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/shared/authority/BannerModAuthorityRules.java"
      provides: "Canonical authority decision seam under bannermod.shared"
      contains: "enum Relationship"
    - path: "src/main/java/com/talhanation/bannermod/shared/settlement/BannerModSettlementBinding.java"
      provides: "Canonical settlement-binding seam under bannermod.shared"
      contains: "record Binding"
    - path: "src/main/java/com/talhanation/bannermod/shared/logistics/BannerModLogisticsRoute.java"
      provides: "Canonical logistics route record with legacy accessor aliases preserved"
      contains: "minimumSourceStock|desiredDestinationStock"
    - path: "src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java"
      provides: "Canonical config helper (kept in existing bannermod.config package per D-03/D-05; overlap logged not deduped)"
      contains: "prepareConfigPath"
  key_links:
    - from: "src/main/java/com/talhanation/recruits/**"
      to: "src/main/java/com/talhanation/bannermod/shared/**"
      via: "direct imports"
      pattern: "import com\\.talhanation\\.bannermod\\.shared"
    - from: "src/main/java/com/talhanation/bannermod/{authority,settlement,logistics}/**"
      to: "src/main/java/com/talhanation/bannermod/shared/**"
      via: "deprecated forwarders"
      pattern: "@Deprecated"
---

<objective>
Re-execute the wave-1 shared-seam ownership move under the `bannermod` convergence namespace. Move the real authority/settlement/logistics implementations into `com.talhanation.bannermod.shared.{authority,settlement,logistics}` (a new sibling subtree per CONTEXT D-04), reduce the pre-existing `com.talhanation.bannermod.{authority,settlement,logistics}` packages to deprecated forwarders, and retarget all active recruit/worker callers.

Purpose: per CONTEXT D-01/D-02, the convergence namespace is `bannermod` and `bannerlord` is fully retired. The shared seams have to land first because every later move (entity, AI, persistence, client, compat) imports them. Per D-05 we do NOT delete or merge the existing `bannermod.{authority,settlement,logistics}` packages — overlap is logged in MERGE_NOTES.md and reconciled in a future phase.
Output: real `bannermod.shared.*` implementations, deprecated forwarders left in the legacy locations, retargeted callers, and a MERGE_NOTES.md entry recording the documented `shared` overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
@src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
@src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsRoute.java
@src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java`:
```java
public enum Relationship { OWNER, SAME_TEAM, ADMIN, FORBIDDEN }
public enum Decision { ALLOW, FORBIDDEN, TARGET_NOT_FOUND, OUTSIDE_FACTION_CLAIM, OVERLAPPING, INVALID_REQUEST }
public static Relationship resolveRelationship(boolean owner, boolean sameTeam, boolean admin)
public static Decision recoverControlDecision(boolean targetExists, Relationship relationship)
public static boolean isAllowed(Decision decision)
```

From `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java`:
```java
public enum Status { FRIENDLY_CLAIM, HOSTILE_CLAIM, UNCLAIMED, DEGRADED_MISMATCH }
public record Binding(Status status, @Nullable String settlementFactionId, @Nullable String claimFactionId) {}
public static Binding resolveFactionStatus(@Nullable RecruitsClaimManager claimManager, BlockPos pos, @Nullable String factionId)
public static Binding resolveSettlementStatus(@Nullable RecruitsClaim claim, ChunkPos chunkPos, @Nullable String settlementFactionId)
public static boolean allowsWorkAreaPlacement(@Nullable RecruitsClaimManager claimManager, BlockPos pos, @Nullable String factionId, boolean claimRestricted)
```

From `src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsRoute.java`:
```java
public record BannerModLogisticsRoute(UUID id, BlockPos source, BlockPos destination, int minSourceCount, int destinationThreshold, ItemFilter filter)
public int minimumSourceStock()         // legacy alias for UI compatibility
public int desiredDestinationStock()    // legacy alias for UI compatibility
```

From `src/main/java/com/talhanation/bannermod/config/BannerModConfigFiles.java`:
```java
public enum Surface { MILITARY, SETTLEMENT, CLIENT }
public static Path prepareConfigPath(Path configDir, Surface surface)
public static Path legacyPath(Path configDir, Surface surface)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical implementations under com.talhanation.bannermod.shared.{authority,settlement,logistics}</name>
  <precondition>
    FIRST LINE of this task MUST run: `test ! -d src/main/java/com/talhanation/bannerlord || { echo 'ABORT: src/main/java/com/talhanation/bannerlord still exists -- 21-01 revert did not complete'; exit 1; }`. This plan is wave-2 and depends on 21-01 having fully erased the bannerlord package tree. If the directory still exists, 21-01 has not landed cleanly and this plan must not run.
  </precondition>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-04, D-05)
    - src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java (current real implementation to be moved)
    - src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java (current real implementation; preserve mixed-claim overload accepting BOTH `com.talhanation.recruits.world.RecruitsClaim` AND any moved `bannermod.persistence.*` claim type)
    - src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsRoute.java (must keep `minimumSourceStock()` and `desiredDestinationStock()` accessor aliases for UI compatibility per StorageAreaScreen contract)
    - src/main/java/com/talhanation/bannermod/logistics/BannerModSupplyStatus.java
    - src/main/java/com/talhanation/bannermod/logistics/BannerModUpkeepProviders.java
    - src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsService.java
    - src/main/java/com/talhanation/bannermod/logistics/BannerModCourierTask.java
  </read_first>
  <action>
    Create EIGHT new files under `src/main/java/com/talhanation/bannermod/shared/**` by MOVING (not delegating) the implementations from the legacy `bannermod.{authority,settlement,logistics}` packages. Concrete file list and packages:

    1. `src/main/java/com/talhanation/bannermod/shared/authority/BannerModAuthorityRules.java` -- package `com.talhanation.bannermod.shared.authority`. Body identical to current `bannermod.authority.BannerModAuthorityRules` (preserve `enum Relationship { OWNER, SAME_TEAM, ADMIN, FORBIDDEN }`, `enum Decision`, `resolveRelationship`, `recoverControlDecision`, `isAllowed`).

    2. `src/main/java/com/talhanation/bannermod/shared/settlement/BannerModSettlementBinding.java` -- package `com.talhanation.bannermod.shared.settlement`. Preserve `enum Status { FRIENDLY_CLAIM, HOSTILE_CLAIM, UNCLAIMED, DEGRADED_MISMATCH }`, `record Binding`, both `resolveFactionStatus(...)` overloads, `resolveSettlementStatus(...)`, and `allowsWorkAreaPlacement(...)`. Preserve the dual-claim compatibility list overload that accepts `Iterable<com.talhanation.recruits.world.RecruitsClaim>` AND any future `Iterable<? extends Object>` form for moved claim types.

    3-8. Logistics: under `src/main/java/com/talhanation/bannermod/shared/logistics/` create `BannerModSupplyStatus.java`, `BannerModUpkeepProviders.java`, `BannerModLogisticsService.java`, `BannerModLogisticsRoute.java`, `BannerModCourierTask.java`, `BannerModCombinedContainer.java`. Each file uses package `com.talhanation.bannermod.shared.logistics`. For `BannerModLogisticsRoute`, preserve BOTH the canonical fields (`minSourceCount`, `destinationThreshold`) AND the legacy alias methods (`minimumSourceStock()`, `desiredDestinationStock()`) -- StorageAreaScreen depends on the alias names.

    Do NOT change any class names, enum members, method signatures, or filenames -- only the package declaration and import paths change. Config helpers (`com.talhanation.bannermod.config.BannerModConfigFiles`) STAY in the existing `bannermod.config` package per D-03/D-05 (already canonical bannermod ownership; overlap with proposed `bannermod.config` from old bannerlord move is moot now that the bannerlord move was reverted).
  </action>
  <verify>
    <automated>bash -c "for p in shared/authority/BannerModAuthorityRules.java shared/settlement/BannerModSettlementBinding.java shared/logistics/BannerModSupplyStatus.java shared/logistics/BannerModUpkeepProviders.java shared/logistics/BannerModLogisticsService.java shared/logistics/BannerModLogisticsRoute.java shared/logistics/BannerModCourierTask.java shared/logistics/BannerModCombinedContainer.java; do f=src/main/java/com/talhanation/bannermod/$p; test -f \"$f\" || { echo MISSING $f; exit 1; }; grep -q '^package com.talhanation.bannermod.shared' \"$f\" || { echo BAD PACKAGE in $f; exit 1; }; done"</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -d src/main/java/com/talhanation/bannerlord` (precondition): the bannerlord package tree is absent before this task runs.
    - All eight target files exist under `src/main/java/com/talhanation/bannermod/shared/**`.
    - Each file's `package` declaration starts with `com.talhanation.bannermod.shared`.
    - `BannerModSettlementBinding.java` contains both `enum Status` with all four values AND a `Binding` record AND a `resolveFactionStatus` method.
    - `BannerModLogisticsRoute.java` contains the strings `minimumSourceStock` AND `desiredDestinationStock` (legacy UI accessor aliases preserved).
    - `BannerModAuthorityRules.java` contains `enum Relationship` with members `OWNER`, `SAME_TEAM`, `ADMIN`, `FORBIDDEN`.
  </acceptance_criteria>
  <done>The canonical `bannermod.shared` subtree exists and owns the real authority/settlement/logistics behavior with all backward-compatible accessors preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce legacy bannermod.{authority,settlement,logistics} packages to deprecated forwarders + retarget all callers + log shared-overlap in MERGE_NOTES.md</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-05, D-11, D-17)
    - MERGE_NOTES.md (full file -- the shared-overlap entry appends after the existing "Future source-layout wording conflict" section; NOTE: pivot rationale and revert range are owned by 21-01 per D-17, NOT this task)
    - Run `grep -rln 'import com.talhanation.bannermod.authority.\|import com.talhanation.bannermod.settlement.\|import com.talhanation.bannermod.logistics.' src/main/java/ src/test/java/ src/gametest/java/ 2>/dev/null` to enumerate callers needing retargeting.
  </read_first>
  <action>
    Two operations:

    (A) Reduce each legacy file in `src/main/java/com/talhanation/bannermod/{authority,settlement,logistics}/*.java` to a deprecated thin forwarder. Each forwarder file keeps its original package declaration and class name but replaces its body with: (1) `@Deprecated` annotation on the type with javadoc `/** @deprecated Use {@link com.talhanation.bannermod.shared.<subpkg>.<Class>} instead. Forwarder retained for staged migration per Phase 21 D-05. */`; (2) for enums/records: re-export by extending or holding a static `forward()` to the canonical type if possible, else keep the enum literal-for-literal and document the mirror; (3) for utility classes with static methods: each public static method delegates to the corresponding `com.talhanation.bannermod.shared.<subpkg>.<Class>` method. Do NOT keep duplicate logic -- forwarders are mapping-only.

    (B) Retarget every caller found by the grep in `<read_first>`. Replace `import com.talhanation.bannermod.authority.BannerModAuthorityRules;` with `import com.talhanation.bannermod.shared.authority.BannerModAuthorityRules;`. Apply the same rename for `settlement.BannerModSettlementBinding`, `logistics.BannerModSupplyStatus`, `logistics.BannerModUpkeepProviders`, `logistics.BannerModLogisticsService`, `logistics.BannerModLogisticsRoute`, `logistics.BannerModCourierTask`, and `logistics.BannerModCombinedContainer`. Use `sed -i 's|import com.talhanation.bannermod.authority.BannerModAuthorityRules;|import com.talhanation.bannermod.shared.authority.BannerModAuthorityRules;|g'` family of commands across `src/main/java/com/talhanation/recruits`, `src/main/java/com/talhanation/workers`, `src/main/java/com/talhanation/bannermod/{citizen,governance}`, `src/test/java`, `src/gametest/java`. Verify no caller still imports the legacy paths.

    (C) Append to MERGE_NOTES.md — under the existing `## Phase 21 Pivot (2026-04-15)` section created by plan 21-01 (per D-17) — a subsection documenting ONLY the shared-overlap specific to this plan: the documented overlap between the new `bannermod.shared.*` subtree and the legacy `bannermod.{authority,settlement,logistics}` packages, explicitly noting that per D-05 the overlap is NOT being deduplicated in Phase 21 and reconciliation is deferred to a follow-up phase. Do NOT re-document pivot rationale or the revert range in this task — 21-01 owns those entries. Use plain ASCII (no en-dash; use `->` instead).
  </action>
  <verify>
    <automated>bash -c "bad=$(grep -rln 'import com\\.talhanation\\.bannermod\\.\\(authority\\|settlement\\|logistics\\)\\.' src/main/java/com/talhanation/recruits src/main/java/com/talhanation/workers src/main/java/com/talhanation/bannermod/citizen src/main/java/com/talhanation/bannermod/governance src/test/java src/gametest/java 2>/dev/null | grep -v '^src/main/java/com/talhanation/bannermod/\\(authority\\|settlement\\|logistics\\)/'); if [ -n \"$bad\" ]; then echo \"Legacy imports remain:\"; echo \"$bad\"; exit 1; fi; grep -q 'Phase 21 Pivot' MERGE_NOTES.md || { echo 'MERGE_NOTES pivot section missing (should have been created by 21-01)'; exit 1; }; grep -q 'bannermod.shared' MERGE_NOTES.md || { echo 'MERGE_NOTES shared-overlap entry missing'; exit 1; }; for f in src/main/java/com/talhanation/bannermod/authority/BannerModAuthorityRules.java src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java src/main/java/com/talhanation/bannermod/logistics/BannerModLogisticsRoute.java; do grep -q '@Deprecated' \"$f\" || { echo \"Missing @Deprecated in $f\"; exit 1; }; done"</automated>
  </verify>
  <acceptance_criteria>
    - No file under `src/main/java/com/talhanation/recruits`, `src/main/java/com/talhanation/workers`, `src/main/java/com/talhanation/bannermod/citizen`, `src/main/java/com/talhanation/bannermod/governance`, `src/test/java`, or `src/gametest/java` imports `com.talhanation.bannermod.authority.*`, `com.talhanation.bannermod.settlement.*`, or `com.talhanation.bannermod.logistics.*`.
    - Each legacy file in `src/main/java/com/talhanation/bannermod/{authority,settlement,logistics}/` contains `@Deprecated`.
    - `MERGE_NOTES.md` contains the section `## Phase 21 Pivot (2026-04-15)` (authored by 21-01) AND a shared-overlap subsection referencing `bannermod.shared` authored by this task.
    - `./gradlew compileJava` for the shared seams compiles (legacy forwarders + new shared canonicals coexist).
  </acceptance_criteria>
  <done>Legacy `bannermod.{authority,settlement,logistics}` packages are deprecated forwarders only; all active callers point at `bannermod.shared.*`; the documented `shared` overlap is recorded in MERGE_NOTES.md under the pivot section 21-01 created.</done>
</task>

</tasks>

<verification>
- `src/main/java/com/talhanation/bannermod/shared/{authority,settlement,logistics}/**` exists with real implementations.
- Legacy `bannermod.{authority,settlement,logistics}` files are `@Deprecated` forwarders only.
- No active caller imports the legacy seam packages directly.
- MERGE_NOTES.md records the pivot rationale (authored by 21-01) and the deferred shared-overlap reconciliation per D-05/D-17 (authored by this plan).
</verification>

<success_criteria>
- Wave-1 ownership truth from VERIFICATION.md is restored under the bannermod namespace.
- Later plans 21-03..21-09 can import shared seams from `bannermod.shared.*` directly.
- Live runtime contract (mod id, config filenames, settlement vocabulary) is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-02-SUMMARY.md`
</output>
