---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - build.gradle
  - src/main/java/com/talhanation/bannerlord/bootstrap/**
  - src/main/java/com/talhanation/bannerlord/network/**
  - src/main/java/com/talhanation/bannerlord/registry/**
  - recruits/src/main/java/com/talhanation/recruits/Main.java
  - recruits/src/main/java/com/talhanation/recruits/init/**
  - workers/src/main/java/com/talhanation/workers/network/**
  - workers/src/main/java/com/talhanation/workers/init/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "This plan must preserve one active `@Mod` entrypoint, one `bannermod` mod id, and one shared `SimpleChannel`; there must never be a parallel bootstrap during the move."
    - "Worker packet ordering and offset behavior are compatibility-sensitive and must remain stable unless all dependent registrations are updated in the same verified slice."
    - "Registry ids and missing-mapping behavior stay runtime-stable even while registry holders and lifecycle wiring move to `bannerlord` packages."
  artifacts:
    - path: "recruits/src/main/java/com/talhanation/recruits/Main.java"
      provides: "The current runtime composition root and shared channel owner"
      contains: "@Mod(Main.MOD_ID)|SIMPLE_CHANNEL|workersSubsystem"
    - path: "recruits/src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java"
      provides: "The current recruit-led lifecycle/config registration bridge"
      contains: "registerRuntimeListeners|BannerModConfigFiles"
    - path: "workers/src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java"
      provides: "The current worker packet catalog and ordering boundary"
      contains: "MESSAGE_TYPES|registerAll|messageCount"
    - path: "workers/src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java"
      provides: "The current worker-side lifecycle and registration seam"
      contains: "registerCommon|WorkersLegacyMappings"
---

<objective>
Move bootstrap, shared networking, and registry/lifecycle composition into explicit `com.talhanation.bannerlord.bootstrap`, `.network`, and `.registry` package families without changing the live runtime contract.

Purpose: this plan relocates the recruit-led composition root and the shared registration surfaces so later entity and worker moves no longer depend on legacy package anchors.
Output: the one runtime entrypoint and its packet/registry composition now live under `bannerlord` package ownership.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@build.gradle
@.planning/VERIFICATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-01-PLAN.md
@recruits/src/main/java/com/talhanation/recruits/Main.java
@recruits/src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java
@workers/src/main/java/com/talhanation/workers/WorkersSubsystem.java
@workers/src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java
@workers/src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home the runtime composition root, lifecycle wiring, and packet registration structure</name>
  <files>build.gradle, src/main/java/com/talhanation/bannerlord/bootstrap/**, src/main/java/com/talhanation/bannerlord/network/**, src/main/java/com/talhanation/bannerlord/registry/**</files>
  <action>Move the active Forge entrypoint, lifecycle registration, shared channel ownership, recruit packet registration, worker packet registration bridge, and registry composition into `bannerlord` packages. Keep the runtime mod id at `bannermod`, preserve the worker packet offset contract, and avoid creating any second bootstrap. Update `build.gradle` only as needed to point the live code at the new locations while Phase 21 continues.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
targets = [
    'src/main/java/com/talhanation/bannerlord/bootstrap',
    'src/main/java/com/talhanation/bannerlord/network',
    'src/main/java/com/talhanation/bannerlord/registry'
]
missing = [target for target in targets if not Path(target).exists()]
if missing:
    raise SystemExit('Missing required bannerlord bootstrap/network/registry targets: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The merged runtime composition root and registration surfaces now live under `bannerlord` package ownership while the runtime contract remains unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Keep validation and docs aligned with the moved bootstrap surfaces</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Update any verification references that still imply the only authoritative package home for bootstrap/network/registry code is under `recruits/` or `workers/`. Keep the root-task validation entrypoints the same unless the code requires a stronger gate.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/phases/21-source-tree-consolidation-into-bannerlord/21-02-PLAN.md').read_text()
required = ['bannermod', 'SimpleChannel', 'worker packet offset contract']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('Missing expected plan guarantees: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The root verification/docs still describe the true bootstrap and packet-registration ownership after wave 2.</done>
</task>

</tasks>

<verification>
- There is still one runtime entrypoint and one shared packet channel.
- Registry ids and worker packet ordering remain stable.
- Bootstrap no longer depends on legacy package ownership to represent the merged runtime.
</verification>

<success_criteria>
- The Phase 21 move no longer treats recruit `Main` and split registrars as permanent physical anchors.
- Later entity and worker moves can target `bannerlord` package ownership without bootstrap confusion.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-02-SUMMARY.md`
</output>
