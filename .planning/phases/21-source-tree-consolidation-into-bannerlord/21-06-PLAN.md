---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 06
type: execute
wave: 6
depends_on:
  - 21-05
files_modified:
  - src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java
  - src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java
  - src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java
  - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java
  - src/main/java/com/talhanation/recruits/RecruitEvents.java
  - src/main/java/com/talhanation/recruits/FactionEvents.java
  - src/main/java/com/talhanation/recruits/ClaimEvents.java
  - src/main/java/com/talhanation/recruits/CommandEvents.java
  - src/main/java/com/talhanation/recruits/inventory/RecruitSimpleContainer.java
  - src/main/java/com/talhanation/recruits/inventory/RecruitInventoryMenu.java
  - src/main/java/com/talhanation/recruits/inventory/DebugInvMenu.java
  - src/main/java/com/talhanation/recruits/inventory/AssassinLeaderMenu.java
  - src/main/java/com/talhanation/recruits/inventory/PatrolLeaderContainer.java
  - src/main/java/com/talhanation/recruits/inventory/RecruitHireMenu.java
  - src/main/java/com/talhanation/recruits/compat/Corpse.java
  - src/main/java/com/talhanation/recruits/util/AttackUtil.java
  - .planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "The moved `bannermod.entity.{shared,military}`, `bannermod.ai.*`, and `bannermod.persistence.military.*` foundations compile cleanly against the retained recruit-only adapter helpers (`RecruitSimpleContainer`, `RecruitInventoryMenu`, `DebugInvMenu`, `AssassinLeaderMenu`, `PatrolLeaderContainer`, `Corpse`, `AttackUtil`)."
    - "Governor assignment, inventory open/save, corpse spawning, and faction/team add-remove accept bannermod-owned recruit entities without type mismatches."
    - "`./gradlew compileJava` is green from the root-only layout for the first time since the 21-04/21-05 moves landed under `bannermod`."
    - "The fix lives behind one narrow `com.talhanation.bannermod.compat.recruits` bridge, not by re-owning recruit gameplay."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java"
      provides: "Type bridge between moved bannermod entities and retained recruit-only adapter APIs"
      contains: "toLegacyRecruit|toGovernorTarget"
    - path: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java"
      provides: "Bannermod inventory base wired through compat bridge"
      contains: "createInventory"
    - path: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java"
      provides: "Bannermod recruit base free of direct recruit-type mismatches"
      contains: "openInventory"
    - path: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java"
      provides: "Governor service accepting bannermod recruit entities"
      contains: "assignGovernor"
  key_links:
    - from: "src/main/java/com/talhanation/recruits/RecruitEvents.java"
      to: "src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java"
      via: "assignGovernor(claim, player, recruit)"
      pattern: "assignGovernor\\(claim, player"
    - from: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java"
      to: "src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java"
      via: "compat bridge calls"
      pattern: "RecruitsEntityCompat"
---

<objective>
Close the compile blocker caused by the moved `bannermod.entity.*` / `bannermod.ai.*` / `bannermod.persistence.military.*` foundations still speaking recruit-only concrete types and signatures at their call sites.

Purpose: per 21-VERIFICATION.md truth 5 and `deferred-items.md` "Plan 21-04 carryover", `./gradlew compileJava` fails on recruit↔bannermod type/signature mismatches between the moved bannermod bases and the retained recruit-only adapter APIs (`RecruitSimpleContainer` constructor, `Corpse.spawnCorpse`, `FactionEvents.addRecruitToTeam`, `BannerModGovernorService.assignGovernor`, etc.). Phase 21 cannot progress to recruit-package demotion (21-07) or worker-package demotion (21-08) until the root build compiles from the retired root-only layout. Per CONTEXT D-01/D-04 the fix must bridge through `bannermod.compat.recruits`, NOT by re-owning recruit gameplay.
Output: one narrow `com.talhanation.bannermod.compat.recruits.RecruitsEntityCompat` bridge plus patched governor, inventory, menu, corpse, and faction call sites. `compileJava` is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-04-PLAN.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-05-PLAN.md
@src/main/java/com/talhanation/recruits/RecruitEvents.java
@src/main/java/com/talhanation/recruits/FactionEvents.java
@src/main/java/com/talhanation/recruits/inventory/RecruitSimpleContainer.java
@src/main/java/com/talhanation/recruits/compat/Corpse.java

<interfaces>
Retained recruit-only adapter signatures the moved bannermod foundations must satisfy:

From `src/main/java/com/talhanation/recruits/inventory/RecruitSimpleContainer.java`:
```java
public RecruitSimpleContainer(int inventorySize, AbstractInventoryEntity abstractInventoryEntity)
```

From `src/main/java/com/talhanation/recruits/compat/Corpse.java`:
```java
public static void spawnCorpse(AbstractInventoryEntity recruit)
```

From `src/main/java/com/talhanation/recruits/FactionEvents.java`:
```java
public static void addRecruitToTeam(AbstractRecruitEntity recruit, Team team, ServerLevel level)
public static void removeRecruitFromTeam(AbstractRecruitEntity recruit, Team team, ServerLevel level)
```

From `src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java`:
```java
public OperationResult assignGovernor(RecruitsClaim claim, ServerPlayer player, AbstractRecruitEntity recruit)
public record RecruitGovernorTarget(UUID recruitUuid, @Nullable UUID ownerUuid, @Nullable String teamId)
```

Moved canonical bases (from plan 21-04):
```java
package com.talhanation.bannermod.entity.shared;
public abstract class AbstractRecruitEntity extends AbstractInventoryEntity { ... }
public abstract class AbstractInventoryEntity extends PathfinderMob { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce one narrow bannermod.compat.recruits bridge for the moved recruit-entity bases</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-04)
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md (the "Plan 21-04 carryover" entry enumerating carryover mismatches)
    - src/main/java/com/talhanation/recruits/RecruitEvents.java (the `assignGovernor(claim, player, recruit)` call site currently passing a bannermod recruit into a signature written for the retained recruit type)
    - src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java (current governor-assignment signature; must accept `com.talhanation.bannermod.entity.shared.AbstractRecruitEntity` directly OR via the compat bridge)
    - Run `./gradlew compileJava 2>&1 | head -80` to enumerate the exact cross-package errors before designing the bridge.
  </read_first>
  <action>
    Create `src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java` with package `com.talhanation.bannermod.compat.recruits`. Annotate the class `@Deprecated` with javadoc `/** @deprecated Narrow bannermod->recruits type bridge for retained adapter APIs during Phase 21 consolidation. Remove when plan 21-07 demotes the recruit gameplay packages. */`.

    Provide these public static helpers (exact names required by `<verify>`):

    1. `public static com.talhanation.bannermod.entity.shared.AbstractRecruitEntity toLegacyRecruit(com.talhanation.bannermod.entity.shared.AbstractRecruitEntity recruit)` — identity return on the moved base. Exists so retained recruit-menu/inventory/corpse call sites can route through the bridge name instead of a raw cast, keeping the intent traceable.
    2. `public static com.talhanation.bannermod.entity.shared.AbstractInventoryEntity toLegacyInventoryHolder(com.talhanation.bannermod.entity.shared.AbstractInventoryEntity entity)` — identity return on the moved inventory base; used by `RecruitSimpleContainer(int, AbstractInventoryEntity)` and `Corpse.spawnCorpse(AbstractInventoryEntity)` call sites after Task 2 widens those signatures to the bannermod base.
    3. `public static com.talhanation.bannermod.governance.BannerModGovernorService.RecruitGovernorTarget toGovernorTarget(com.talhanation.bannermod.entity.shared.AbstractRecruitEntity recruit)` — constructs a `RecruitGovernorTarget(recruit.getUUID(), recruit.getOwnerUUID(), teamName)` record where `teamName` is derived from `recruit.getTeam()` (null-safe). Used by `RecruitEvents` so `BannerModGovernorService.assignGovernor(...)` no longer needs to accept a raw entity.

    Update `src/main/java/com/talhanation/bannermod/governance/BannerModGovernorService.java`: add an overload `public OperationResult assignGovernor(RecruitsClaim claim, ServerPlayer player, RecruitGovernorTarget target)` that does the full assignment work against the record, and re-express the existing entity-accepting overload as `assignGovernor(claim, player, RecruitsEntityCompat.toGovernorTarget(recruit))`. Do NOT remove the entity overload yet — retained callers still use it.

    Update `src/main/java/com/talhanation/recruits/RecruitEvents.java`: change the call-site to import `com.talhanation.bannermod.compat.recruits.RecruitsEntityCompat` and pass `RecruitsEntityCompat.toGovernorTarget(recruit)` into the governor service instead of the raw recruit entity.
  </action>
  <verify>
    <automated>bash -c "test -f src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q '^package com.talhanation.bannermod.compat.recruits;' src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q 'class RecruitsEntityCompat' src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q 'toLegacyRecruit' src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q 'toGovernorTarget' src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q 'toLegacyInventoryHolder' src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java && grep -q 'RecruitsEntityCompat' src/main/java/com/talhanation/recruits/RecruitEvents.java"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannermod/compat/recruits/RecruitsEntityCompat.java` exists with package `com.talhanation.bannermod.compat.recruits`.
    - File contains the identifier strings `class RecruitsEntityCompat`, `toLegacyRecruit`, `toLegacyInventoryHolder`, and `toGovernorTarget`.
    - `RecruitEvents.java` imports `com.talhanation.bannermod.compat.recruits.RecruitsEntityCompat` and calls `RecruitsEntityCompat.toGovernorTarget(...)` on the governor assignment path.
    - `BannerModGovernorService.java` contains both an `assignGovernor(..., RecruitGovernorTarget target)` overload and the legacy entity-accepting overload that delegates to it.
    - The new bridge file carries `@Deprecated` on the type declaration.
  </acceptance_criteria>
  <done>One narrow bannermod->recruits compat bridge exists, governor flow routes through it, and the moved bannermod recruit bases no longer reach recruit-only concrete types directly from `RecruitEvents`.</done>
</task>

<task type="auto">
  <name>Task 2: Widen retained recruit-only adapter signatures to the bannermod bases until `./gradlew compileJava` is green</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md (behavioral spot-checks table listing compile failures in `RecruitEvents`, `bannerlord.entity.shared.AbstractInventoryEntity`, `bannerlord.entity.shared.AbstractRecruitEntity` — after the 21-01 revert and 21-04/21-05 re-execute, the same mismatch pattern now applies to the `bannermod` bases)
    - src/main/java/com/talhanation/recruits/inventory/RecruitSimpleContainer.java (constructor still typed on the retained `com.talhanation.recruits.entities.AbstractInventoryEntity` — must be widened to `com.talhanation.bannermod.entity.shared.AbstractInventoryEntity`)
    - src/main/java/com/talhanation/recruits/inventory/RecruitInventoryMenu.java, DebugInvMenu.java, AssassinLeaderMenu.java, PatrolLeaderContainer.java, RecruitHireMenu.java (menu constructors accepting recruit-only concrete types)
    - src/main/java/com/talhanation/recruits/compat/Corpse.java (`spawnCorpse(AbstractInventoryEntity)` parameter type)
    - src/main/java/com/talhanation/recruits/FactionEvents.java (`addRecruitToTeam` / `removeRecruitFromTeam` parameter type)
    - src/main/java/com/talhanation/recruits/util/AttackUtil.java and src/main/java/com/talhanation/recruits/ClaimEvents.java / CommandEvents.java (any remaining recruit-only parameter types on helpers consumed by the moved bases)
    - Run `./gradlew compileJava 2>&1 | tail -120` to enumerate remaining compile errors before editing each file.
  </read_first>
  <action>
    **Brownfield note:** This task is the FIRST touch of the retained recruit-only adapter files (`RecruitSimpleContainer`, `RecruitInventoryMenu`, `DebugInvMenu`, `AssassinLeaderMenu`, `PatrolLeaderContainer`, `RecruitHireMenu`, `Corpse`, `AttackUtil`, `ClaimEvents`, `CommandEvents`, `FactionEvents`) AFTER the 21-01 revert has erased the prior bannerlord-target widenings. The working-tree state at the start of this task is the pre-pivot recruit-only typing (e.g., `RecruitSimpleContainer(int, com.talhanation.recruits.entities.AbstractInventoryEntity)`), NOT a re-edit of already-correct bannermod typing. Treat every parameter-type change below as a fresh edit from recruit-only to bannermod — any appearance of `com.talhanation.bannerlord.*` in these files indicates an incomplete 21-01 revert and must abort this task (use the wave-2/3 `test ! -d src/main/java/com/talhanation/bannerlord` precondition pattern if in doubt).

    For each retained recruit-only adapter file enumerated above, widen the parameter type from the legacy recruit-only concrete class to the moved bannermod base. Exact mappings (update both the parameter type AND the import statement):

    - `com.talhanation.recruits.entities.AbstractInventoryEntity` -> `com.talhanation.bannermod.entity.shared.AbstractInventoryEntity` in: `RecruitSimpleContainer`, `Corpse.spawnCorpse`, any inventory menu constructors.
    - `com.talhanation.recruits.entities.AbstractRecruitEntity` -> `com.talhanation.bannermod.entity.shared.AbstractRecruitEntity` in: `FactionEvents.addRecruitToTeam`, `FactionEvents.removeRecruitFromTeam`, `AttackUtil` helpers, `ClaimEvents`, `CommandEvents`, all menu constructors.
    - Concrete military entities (`MessengerEntity`, `ScoutEntity`, `CaptainEntity`, `VillagerNobleEntity`, `AbstractLeaderEntity`, `IRangedRecruit`, `IWeapon`) consumed by retained call sites: update imports to `com.talhanation.bannermod.entity.military.*` or `com.talhanation.bannermod.entity.shared.*` per the plan 21-04 destination mapping.

    Where a retained call site needs to construct a container/menu from a bannermod entity, route through `RecruitsEntityCompat.toLegacyInventoryHolder(entity)` / `toLegacyRecruit(entity)` to document the bridge rather than doing a raw widening at the call site. Do not move method bodies; only parameter/import types change.

    If a retained file cannot be widened without restructuring its public API (e.g., a mixin target or a save-data adapter with a fixed shape), DO NOT break it — instead document it in `.planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md` under a new heading `Plan 21-06 carryover` listing the exact file, method, and reason, and leave the bridge in place to satisfy that specific call site.

    Stop only when `./gradlew compileJava` exits 0 from the repository root.
  </action>
  <verify>
    <automated>./gradlew compileJava</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew compileJava` exits with status 0 and prints `BUILD SUCCESSFUL`.
    - `grep -n 'com.talhanation.bannermod.entity.shared.AbstractInventoryEntity' src/main/java/com/talhanation/recruits/inventory/RecruitSimpleContainer.java` returns at least one match (the widened constructor parameter type).
    - `grep -n 'com.talhanation.bannermod.entity.shared.AbstractInventoryEntity' src/main/java/com/talhanation/recruits/compat/Corpse.java` returns at least one match.
    - `grep -n 'com.talhanation.bannermod.entity.shared.AbstractRecruitEntity' src/main/java/com/talhanation/recruits/FactionEvents.java` returns at least one match.
    - If any carryover was deferred, `.planning/phases/21-source-tree-consolidation-into-bannerlord/deferred-items.md` contains a new heading `Plan 21-06 carryover` enumerating the exact files/methods skipped and why.
  </acceptance_criteria>
  <done>The retained recruit-only adapter surface now accepts bannermod-owned recruit/inventory bases. `./gradlew compileJava` is green from the retired root-only layout for the first time since the pivot. Any residual mismatches are documented as 21-07/21-08 scope in `deferred-items.md`.</done>
</task>

</tasks>

<verification>
- `./gradlew compileJava` succeeds from the repository root.
- No compile error output mentions `bannermod.entity.shared.AbstractInventoryEntity`, `bannermod.entity.shared.AbstractRecruitEntity`, or `BannerModGovernorService.assignGovernor(...)`.
- The fix lives behind the narrow `com.talhanation.bannermod.compat.recruits.RecruitsEntityCompat` bridge, not by re-owning recruit gameplay back into `com.talhanation.recruits.entities`.
- `deferred-items.md` truthfully logs any call sites that could not be widened in this slice.
</verification>

<success_criteria>
- Root Java compilation is restored under the bannermod-pivot tree.
- Bannermod-owned recruit/inventory foundations can consume retained brownfield helpers through a single documented bridge.
- Plans 21-07 and 21-08 have a stable compile baseline to demote the recruit and worker package families against.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-06-SUMMARY.md`
</output>
