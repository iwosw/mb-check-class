---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 06
type: execute
wave: 6
depends_on:
  - 21-05
files_modified:
  - src/main/java/com/talhanation/bannermod/client/military/**
  - src/main/java/com/talhanation/bannermod/inventory/military/**
  - src/main/java/com/talhanation/bannermod/persistence/military/**
  - src/main/java/com/talhanation/bannermod/items/military/**
  - recruits/src/main/java/com/talhanation/recruits/client/**
  - recruits/src/main/java/com/talhanation/recruits/inventory/**
  - recruits/src/main/java/com/talhanation/recruits/world/**
  - recruits/src/main/java/com/talhanation/recruits/items/**
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "`recruits/client/**` (107 files, incl. `client/api`, `client/events`, `client/gui/**`, `client/models`, `client/render/**`) moves to `com.talhanation.bannermod.client.military.**` preserving the subpackage shape."
    - "`recruits/inventory/**` (11 files) moves to `bannermod.inventory.military.**`."
    - "`recruits/world/**` (22 files — SavedData/manager classes for factions, claims, groups, diplomacy, routes, hire trades) moves to `bannermod.persistence.military.**`."
    - "`recruits/items/**` (2 files) moves to `bannermod.items.military.**`."
    - "After this wave the `recruits/` clone's `src/main/java/` contains only the `@Deprecated Main.java` shim (from 21-03) — every other file has been migrated."
---

<objective>
Migrate the remaining recruits-side subsystems — client UI, inventory menus, world managers / SavedData, items — from the `recruits/` clone into `bannermod.{client.military,inventory.military,persistence.military,items.military}`. Empties the recruits clone working tree down to the deprecated `Main.java` shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-05-SUMMARY.md
@MERGE_NOTES.md
@recruits/src/main/java/com/talhanation/recruits/client
@recruits/src/main/java/com/talhanation/recruits/world
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-migrate recruits/client/** into bannermod.client.military.**</name>
  <action>
    Bulk copy `recruits/src/main/java/com/talhanation/recruits/client/**` → `src/main/java/com/talhanation/bannermod/client/military/**` preserving subpaths (`api/`, `events/`, `gui/**`, `models/`, `render/**`). For each copied file:
    1. Rewrite `package com.talhanation.recruits.client.` → `package com.talhanation.bannermod.client.military.`
    2. Rewrite all `import com.talhanation.recruits.client.` → `import com.talhanation.bannermod.client.military.`
    3. Rewrite already-migrated FQNs carried forward from prior waves (`recruits.entities.` → `bannermod.entity.military.`, `recruits.pathfinding.` → `bannermod.ai.pathfinding.`, `recruits.events.` → `bannermod.events.`, etc.) via a single compound `sed` pass.
    4. `git rm` source files inside the clone.
    5. Update `BannerModMain.clientSetup()` to reference the new FQNs for `KeyEvents`, `ClientPlayerEvents`, `ClaimOverlayManager`, `CommandCategoryManager` + command-category types.
    Commit in clone (`refactor(21-06): remove migrated client subsystem`) and outer (`feat(21-06): migrate recruits client into bannermod`).
  </action>
  <verify>
    <automated>bash -c "
set -e
[ \"\$(find src/main/java/com/talhanation/bannermod/client/military -name '*.java' | wc -l)\" -ge 90 ]
! ls recruits/src/main/java/com/talhanation/recruits/client 2>/dev/null
"
    </automated>
  </verify>
  <done>All recruits client UI code is under `bannermod.client.military.**`.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate inventory, world managers, items</name>
  <action>
    Same bulk pattern:
    | Source | Target |
    |---|---|
    | `recruits/inventory/**` | `bannermod/inventory/military/**` |
    | `recruits/world/**` | `bannermod/persistence/military/**` |
    | `recruits/items/**` | `bannermod/items/military/**` |

    Post-pass sed across the three target subtrees to fix cross-migrated FQN references. `git rm` originals from clone; commit.

    Append Wave-6 entry to `MERGE_NOTES.md` listing file counts per subtree, the unified sed command used, and the remaining clone working-tree inventory (expected: only `Main.java` shim).
  </action>
  <verify>
    <automated>bash -c "
set -e
test -d src/main/java/com/talhanation/bannermod/inventory/military
test -d src/main/java/com/talhanation/bannermod/persistence/military
[ \"\$(find src/main/java/com/talhanation/bannermod/persistence/military -name '*.java' | wc -l)\" -ge 15 ]
! ls recruits/src/main/java/com/talhanation/recruits/inventory 2>/dev/null
! ls recruits/src/main/java/com/talhanation/recruits/world 2>/dev/null
! ls recruits/src/main/java/com/talhanation/recruits/items 2>/dev/null
# Remaining recruits clone working tree should be the Main.java shim plus whatever network/ still has (network moves in 21-08).
"
    </automated>
  </verify>
  <done>Recruits non-network subsystems fully migrated. Only `Main.java` shim + `network/` remain in the clone.</done>
</task>

</tasks>

<verification>
- `bannermod.client.military.**`, `bannermod.inventory.military.**`, `bannermod.persistence.military.**`, `bannermod.items.military.**` contain the full migrated source.
- `recruits/src/main/java/com/talhanation/recruits/` contains only `Main.java` (deprecated shim) and `network/` (deferred to Wave 8).
</verification>

<success_criteria>
- Recruits-side UI, persistence, inventory, items fully owned by `bannermod.**`.
- Clone tree is nearly empty; only network + shim remain.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-06-SUMMARY.md`
</output>
