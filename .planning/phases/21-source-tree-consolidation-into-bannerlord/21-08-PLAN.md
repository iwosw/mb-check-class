---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 08
type: execute
wave: 3
depends_on:
  - 21-07
files_modified:
  - src/main/java/com/talhanation/recruits/entities/**
  - src/main/java/com/talhanation/recruits/ai/**
  - src/main/java/com/talhanation/recruits/pathfinding/**
  - src/main/java/com/talhanation/recruits/client/**
  - src/main/java/com/talhanation/recruits/world/**
  - src/main/java/com/talhanation/recruits/util/**
  - src/main/java/com/talhanation/recruits/network/**
  - src/main/java/com/talhanation/bannerlord/entity/**
  - src/main/java/com/talhanation/bannerlord/ai/**
  - src/main/java/com/talhanation/bannerlord/persistence/**
  - src/main/java/com/talhanation/bannerlord/client/**
  - src/test/java/**
  - src/gametest/java/**
autonomous: true
requirements:
  - SRCMOVE-03
gap_closure: true
must_haves:
  truths:
    - "The remaining `com.talhanation.recruits/**` tree is reduced to explicit compatibility surfaces instead of owning live military/shared behavior."
    - "Tests and GameTests import bannerlord-owned military/shared classes for moved code paths rather than the old recruit packages."
    - "A grep over `src/main/java/com/talhanation/recruits/**` only leaves documented adapters, bootstrap shims, or brownfield compatibility seams."
  artifacts:
    - path: "src/main/java/com/talhanation/recruits/Main.java"
      provides: "Allowed compatibility bootstrap shim"
      contains: "deprecated"
    - path: "src/main/java/com/talhanation/bannerlord/entity/shared/AbstractRecruitEntity.java"
      provides: "Canonical recruit base"
      contains: "package com.talhanation.bannerlord.entity.shared"
    - path: "src/test/java"
      provides: "Retargeted regression coverage"
      contains: "com.talhanation.bannerlord"
  key_links:
    - from: "src/test/java/**"
      to: "src/main/java/com/talhanation/bannerlord/**"
      via: "imports"
      pattern: "import com\\.talhanation\\.bannerlord"
    - from: "src/main/java/com/talhanation/recruits/**"
      to: "src/main/java/com/talhanation/bannerlord/**"
      via: "deprecated forwarders/adapters"
      pattern: "@Deprecated|com\\.talhanation\\.bannerlord"
---

<objective>
Shrink the copied root `com.talhanation.recruits/**` tree down to a true compatibility layer now that the bannerlord military/shared code compiles again.

Purpose: verification truth 4 cannot pass while the old recruit package family still owns the moved entity, AI, persistence, and client behavior inside the active root source tree.
Output: bannerlord packages become the only live military/shared owners, while recruit packages retain only documented shims/adapters still needed for transition safety.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-03-SUMMARY.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md
@src/main/java/com/talhanation/bannerlord/entity/shared/AbstractRecruitEntity.java
@src/main/java/com/talhanation/recruits/Main.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote recruit gameplay packages to thin adapters or remove duplicated owners</name>
  <files>src/main/java/com/talhanation/recruits/entities/**, src/main/java/com/talhanation/recruits/ai/**, src/main/java/com/talhanation/recruits/pathfinding/**, src/main/java/com/talhanation/recruits/client/**, src/main/java/com/talhanation/recruits/world/**, src/main/java/com/talhanation/recruits/util/**, src/main/java/com/talhanation/recruits/network/**, src/main/java/com/talhanation/bannerlord/entity/**, src/main/java/com/talhanation/bannerlord/ai/**, src/main/java/com/talhanation/bannerlord/persistence/**, src/main/java/com/talhanation/bannerlord/client/**</files>
  <action>Delete or convert duplicated recruit-owned implementations anywhere Phase 21 already established a canonical bannerlord home. Keep only the narrowly allowed recruit compatibility surfaces from the Phase 20/21 move contract (bootstrap shim, adapter helpers, explicitly retained migration seams). Each retained recruit file should be a deprecated forwarder/adapter into `com.talhanation.bannerlord/**`, not a second owner. Do not leave bannerlord code importing recruit gameplay packages after this cleanup.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
bad = []
allowed_tokens = ('@Deprecated', 'compat', 'migration', 'bootstrap', 'Main')
for path in Path('src/main/java/com/talhanation/recruits').rglob('*.java'):
    text = path.read_text()
    if 'package com.talhanation.recruits' not in text:
        continue
    if not any(token in text for token in allowed_tokens):
        bad.append(str(path))
if bad:
    raise SystemExit('Recruit files still look like active owners:\n' + '\n'.join(sorted(bad)[:80]))
PY</automated>
  </verify>
  <done>The recruit package family no longer owns moved gameplay code; it only exposes documented compatibility wrappers where transition safety still needs them.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget retained tests and GameTests to bannerlord-owned military/shared packages</name>
  <files>src/test/java/**, src/gametest/java/**</files>
  <action>Update retained JUnit and GameTest imports/fixtures so validation targets the bannerlord-owned military/shared classes created in waves 2-4. Keep the existing scenario ids and brownfield harness structure, but stop proving ownership through the old recruit package names except where a test is explicitly checking compatibility adapters.</action>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
bad = []
for root in ['src/test/java', 'src/gametest/java']:
    for path in Path(root).rglob('*.java'):
        text = path.read_text()
        if 'import com.talhanation.recruits.entities.' in text or 'import com.talhanation.recruits.pathfinding.' in text:
            bad.append(str(path))
if bad:
    raise SystemExit('Tests still import recruit owners:\n' + '\n'.join(sorted(bad)[:80]))
PY</automated>
  </verify>
  <done>Regression coverage now treats bannerlord packages as the canonical owners for the moved recruit systems.</done>
</task>

</tasks>

<verification>
- The root recruit package tree is adapter-only for moved military/shared code.
- Tests and GameTests target bannerlord owners for recruit systems.
- No active source file still depends on recruit packages as the canonical home for moved military/shared behavior.
</verification>

<success_criteria>
- Verification truth 4 is closed for the recruit side of the tree.
- Bannerlord military/shared packages are the sole active owners for moved recruit behavior.
- The remaining recruit package surface is explicit, narrow, and documented.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-08-SUMMARY.md`
</output>
