---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 08
type: execute
wave: 8
depends_on:
  - 21-07
files_modified:
  - src/main/java/com/talhanation/recruits/**
  - src/main/java/com/talhanation/bannermod/compat/recruits/**
  - src/test/java/**
  - src/gametest/java/**
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-03
must_haves:
  truths:
    - "Every surviving `.java` file under `src/main/java/com/talhanation/recruits/**` is either (a) a `@Deprecated` forwarder whose javadoc `@deprecated` tag points at its canonical `com.talhanation.bannermod.*` home per D-10, or (b) a documented retained compat/event/adapter/mixin seam enumerated by path in `MERGE_NOTES.md` under `### Phase 21 recruit compat surface`."
    - "No file under `src/main/java/com/talhanation/bannermod/**`, `src/main/java/com/talhanation/workers/**`, `src/test/java/**`, or `src/gametest/java/**` imports a non-deprecated `com.talhanation.recruits.{entities,ai,pathfinding,world,client}.*` type for moved behavior; compat-regression tests that intentionally exercise a forwarder carry the marker `// Phase 21: compat-surface regression — retains recruit-package import by design`."
    - "Retained tests and GameTests import `com.talhanation.bannermod.*` canonical classes for moved recruit gameplay paths."
    - "`./gradlew compileJava compileTestJava` remains green after demotion."
  artifacts:
    - path: "src/main/java/com/talhanation/recruits/Main.java"
      provides: "Deprecated bootstrap shim forwarding to com.talhanation.bannermod.bootstrap (already demoted in earlier waves; re-verified here)"
      contains: "@Deprecated"
    - path: "src/main/java/com/talhanation/recruits/RecruitEvents.java"
      provides: "Retained Forge-bus event subscriber seam documented in MERGE_NOTES.md"
      contains: "com.talhanation.bannermod"
    - path: "MERGE_NOTES.md"
      provides: "Enumerated recruit compat surface listing every retained recruit-package file by path + one-line reason"
      contains: "Phase 21 recruit compat surface"
  key_links:
    - from: "src/test/java/**"
      to: "src/main/java/com/talhanation/bannermod/**"
      via: "imports for moved military/shared gameplay"
      pattern: "import com\\.talhanation\\.bannermod"
    - from: "src/main/java/com/talhanation/recruits/**"
      to: "src/main/java/com/talhanation/bannermod/**"
      via: "@Deprecated javadoc links and delegating method bodies"
      pattern: "@Deprecated|com\\.talhanation\\.bannermod"
---

<objective>
Now that Wave 7 relocated the canonical military/shared gameplay into `com.talhanation.bannermod.*`, shrink the remaining `com.talhanation.recruits/**` tree down to a documented compatibility surface per CONTEXT D-10: every retained recruit file is either a `@Deprecated` forwarder pointing at its bannermod home, or an enumerated retained seam listed by path in `MERGE_NOTES.md`.

Purpose: REQUIREMENTS.md SRCMOVE-03 requires recruit-package files to be reduced to documented compatibility surfaces — no recruit-package file may still own live military/shared gameplay behavior. CLAUDE.md preserves `recruits` as "the runtime base" so the package itself stays on disk, but ownership of moved behavior must sit canonically under `bannermod.*`.

Output: every surviving recruit-package `.java` file matches exactly one of three dispositions (delete, `@Deprecated` forwarder, or retained seam enumerated in MERGE_NOTES.md). Tests/GameTests targeting moved behavior import bannermod-owned canonical classes. Compat-regression tests are explicitly labeled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-PLAN.md
@src/main/java/com/talhanation/recruits/Main.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Demote recruit-package files to @Deprecated forwarders or enumerated retained seams</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md — D-10 (demote recruit-package to documented compat shims under `com.talhanation.bannermod.compat.recruits/**`), D-03 (do not touch preserved bannermod subtrees).
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-PLAN.md — Wave 7 moved canonical recruit-gameplay types to `com.talhanation.bannermod.{entity,ai,pathfinding,persistence,client}/**`; this plan assumes that move is complete and compileJava is green.
    - Run `ls -R src/main/java/com/talhanation/recruits/` to enumerate every surviving file.
    - Run `grep -rLZ '@Deprecated\|@deprecated' src/main/java/com/talhanation/recruits/entities src/main/java/com/talhanation/recruits/ai src/main/java/com/talhanation/recruits/pathfinding src/main/java/com/talhanation/recruits/world src/main/java/com/talhanation/recruits/client 2>/dev/null | xargs -0 -n1 echo` — lists non-deprecated recruit files that are demotion candidates.
    - src/main/java/com/talhanation/recruits/Main.java — reference style for the forwarder pattern (already demoted to `@Deprecated` in an earlier wave).
    - src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java — confirms the canonical home exists post-Wave 7; forwarders delegate here.
  </read_first>
  <action>
    For EVERY `.java` file that still exists under `src/main/java/com/talhanation/recruits/**`, assign exactly ONE of three dispositions and record it with a top-of-file comment `// Phase 21 disposition: <forwarder|retained-seam|deleted>` (deletion recorded in the MERGE_NOTES enumeration rather than in-file).

    **Disposition A — DELETE (via `git rm`):** The file's type was relocated to `com.talhanation.bannermod.{entity,ai,pathfinding,persistence,client}` by Wave 7 AND no retained caller imports the legacy recruit path.
    - Verify zero external dependency with: `grep -rln 'com\.talhanation\.recruits\.<subpkg>\.<ClassName>' src/main/java src/test/java src/gametest/java` — zero hits outside the file itself → delete.
    - Typical candidates: pure gameplay classes under `recruits/entities/`, `recruits/ai/`, `recruits/pathfinding/`, `recruits/world/` that Wave 7 moved.

    **Disposition B — DEMOTE to `@Deprecated` forwarder:** The file is still imported by at least one retained caller (a retained menu adapter, mixin target, save-data migrator, Forge-bus subscriber).
    - Keep the file in place.
    - Annotate the top-level type with `@Deprecated` and javadoc block EXACTLY:
      ```java
      /**
       * @deprecated Use {@link com.talhanation.bannermod.<canonical.fq.path>.<Name>} instead.
       * Forwarder retained during Phase 21 consolidation (see CONTEXT D-10). Scheduled for removal in a follow-up cleanup phase.
       */
      ```
    - Replace every static method and instance method body with a single-line delegation to the canonical bannermod type. Example:
      ```java
      public static Foo bar(int x) { return com.talhanation.bannermod.entity.shared.CanonicalHome.bar(x); }
      ```
    - Do NOT keep duplicate logic. If the canonical home does not expose the needed method, add the method on the canonical side (prefer extending the canonical contract over keeping live logic in the forwarder).

    **Disposition C — RETAINED EVENT/ADAPTER/MIXIN SEAM (explicitly enumerated):** The file is a narrow compat surface the runtime contract still needs. The ALLOWED retained types are:
    - `Main.java` — bootstrap shim (already demoted)
    - `RecruitEvents.java`, `FactionEvents.java`, `ClaimEvents.java`, `CommandEvents.java`, `AssassinEvents.java`, `DamageEvent.java`, `DebugEvents.java`, `PillagerEvents.java`, `VillagerEvents.java`, `AttributeEvent.java` — Forge-bus `@SubscribeEvent` owners
    - `UpdateChecker.java` — legacy update-check seam
    - Anything under `recruits/mixin/` — registered mixin targets
    - Anything under `recruits/migration/` — save-data migration adapters
    - Anything under `recruits/inventory/` — retained menu adapter seams
    - Anything under `recruits/compat/` — cross-mod compat bridges (e.g., `Corpse.java`, `IWeapon.java`, `SmallShips.java`)
    - `recruits/util/AttackUtil.java` — if still imported by retained callers
    - `recruits/config/**` — legacy config filename seams
    - `recruits/items/**` — legacy item registrations still anchored to the recruit namespace
    - `recruits/network/**` — message wrappers still registered through the shared channel
    - Each Disposition-C file stays on disk; its imports MUST point at `com.talhanation.bannermod.*` for every moved symbol (no `com.talhanation.bannerlord.*` — that namespace is retired per CONTEXT D-02); it MUST appear by path in the MERGE_NOTES enumeration below.

    After applying dispositions, append to `MERGE_NOTES.md` under the existing `## Phase 21 Pivot (2026-04-15)` heading. Add a new subsection with the EXACT heading `### Phase 21 recruit compat surface`. Body: a flat bullet list enumerating every file that survives under `src/main/java/com/talhanation/recruits/**` after this task, grouped by disposition subheadings:
    ```
    #### Forwarders (Disposition B)
    - `src/main/java/com/talhanation/recruits/<path>.java` — forwards to `com.talhanation.bannermod.<canonical>.<Name>`
    #### Retained event/adapter/mixin seams (Disposition C)
    - `src/main/java/com/talhanation/recruits/RecruitEvents.java` — Forge `@SubscribeEvent` bus owner for recruit damage/command/claim/faction wiring
    ...
    ```
    Each bullet gives the repo-relative path and a one-line reason it stays.

    Rule: no caller outside `src/main/java/com/talhanation/recruits/**` may import a non-deprecated recruit type after this task. Verify with the grep in the verify block.
  </action>
  <verify>
    <automated>bash -c "set -e; python3 - <<'PY'
import re, sys
from pathlib import Path
merge_notes = Path('MERGE_NOTES.md').read_text()
m = re.search(r'^### Phase 21 recruit compat surface$(.*?)(?=^### |^## |\\Z)', merge_notes, flags=re.MULTILINE | re.DOTALL)
if not m:
    print('FAIL: MERGE_NOTES.md missing \"### Phase 21 recruit compat surface\" subsection'); sys.exit(1)
enumeration = m.group(1)
allowed_tokens = ('@Deprecated', '@deprecated', 'compat', 'migration', 'bootstrap', 'Main', 'SubscribeEvent', 'Events', 'Mixin', 'UpdateChecker', 'Translatable', 'Phase 21 disposition')
missing_token, missing_enum = [], []
for path in sorted(Path('src/main/java/com/talhanation/recruits').rglob('*.java')):
    text = path.read_text()
    if 'package com.talhanation.recruits' not in text: continue
    if not any(tok in text for tok in allowed_tokens): missing_token.append(str(path))
    if str(path) not in enumeration: missing_enum.append(str(path))
fail = False
if missing_token:
    print('FAIL: recruit files without allowed-token signal:'); [print('  ' + p) for p in missing_token]; fail = True
if missing_enum:
    print('FAIL: recruit files not enumerated in MERGE_NOTES:'); [print('  ' + p) for p in missing_enum]; fail = True
if fail: sys.exit(1)
PY
stale=$(grep -rln 'com\\.talhanation\\.bannerlord' src/main/java/com/talhanation/recruits 2>/dev/null || true)
if [ -n \"$stale\" ]; then echo 'FAIL: recruit files still reference retired bannerlord namespace:'; echo \"$stale\"; exit 1; fi
./gradlew compileJava"</automated>
  </verify>
  <acceptance_criteria>
    - Every surviving `.java` file under `src/main/java/com/talhanation/recruits/**` contains at least one allowed signal token (`@Deprecated`, `@deprecated`, `compat`, `migration`, `bootstrap`, `Main`, `SubscribeEvent`, `Events`, `Mixin`, `UpdateChecker`, `Translatable`, `Phase 21 disposition`).
    - Every surviving file path under `src/main/java/com/talhanation/recruits/**` appears as a literal substring inside the `### Phase 21 recruit compat surface` subsection of `MERGE_NOTES.md`.
    - Zero files under `src/main/java/com/talhanation/recruits/**` contain the string `com.talhanation.bannerlord` (CONTEXT D-02: bannerlord namespace is fully retired).
    - `MERGE_NOTES.md` contains the literal heading `### Phase 21 recruit compat surface`.
    - `./gradlew compileJava` exits status 0 with `BUILD SUCCESSFUL`.
  </acceptance_criteria>
  <done>The recruit package family is reduced to a documented compatibility surface: `@Deprecated` forwarders for moved types plus explicitly enumerated retained event/adapter/mixin seams. Canonical ownership for moved recruit gameplay lives under `com.talhanation.bannermod.*`.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget retained tests and GameTests to bannermod-owned military/shared classes</name>
  <read_first>
    - Run `grep -rln 'import com\\.talhanation\\.recruits\\.\\(entities\\|ai\\|pathfinding\\|world\\|client\\)\\.' src/test/java src/gametest/java` — enumerate every test/gametest file still importing a legacy recruit gameplay type.
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-PLAN.md — Wave 7 destination mapping (bannerlord→bannermod; recruit→bannermod still goes through the same canonical sub-packages: `entity/{shared,military}`, `ai/{military,pathfinding}`, `persistence/military`, `client/{shared,military}`).
  </read_first>
  <action>
    For every file the grep prints, rewrite imports using these exact literal substitutions (verify per-file that the destination package matches — `entity/shared` vs `entity/military` split per the canonical bannermod layout established in Wave 7):

    - `import com.talhanation.recruits.entities.AbstractRecruitEntity;` → `import com.talhanation.bannermod.entity.shared.AbstractRecruitEntity;`
    - `import com.talhanation.recruits.entities.AbstractInventoryEntity;` → `import com.talhanation.bannermod.entity.shared.AbstractInventoryEntity;`
    - `import com.talhanation.recruits.entities.AbstractLeaderEntity;` → `import com.talhanation.bannermod.entity.shared.AbstractLeaderEntity;`
    - `import com.talhanation.recruits.entities.<ConcreteMilitaryEntity>;` → `import com.talhanation.bannermod.entity.military.<ConcreteMilitaryEntity>;` (e.g., `MessengerEntity`, `ScoutEntity`, `CaptainEntity`, `VillagerNobleEntity`)
    - `import com.talhanation.recruits.pathfinding.GlobalPathfindingController;` → `import com.talhanation.bannermod.ai.pathfinding.GlobalPathfindingController;`
    - `import com.talhanation.recruits.world.Recruits<Class>;` → `import com.talhanation.bannermod.persistence.military.Recruits<Class>;` for `RecruitsClaim`, `RecruitsClaimManager`, `RecruitsPatrolSpawn`, `RecruitsPlayerInfo`, `RecruitsTreatyManager`.
    - Client widget imports per the Wave 7 destination (`bannermod.client.shared.*` for cross-cutting, `bannermod.client.military.*` for military-only UI).

    Also sweep for any `com.talhanation.bannerlord.*` imports left behind by test-file edits — rewrite those to `com.talhanation.bannermod.*` with the same `sed` substitution Wave 7 used. CONTEXT D-02 requires zero `bannerlord` references anywhere.

    **Exception:** a test explicitly exercising a recruit COMPAT adapter (verifying `RecruitSimpleContainer` accepts the widened parameter, or that a `@Deprecated` forwarder still delegates correctly) is ALLOWED to import the recruit type. Those tests MUST carry the marker comment `// Phase 21: compat-surface regression — retains recruit-package import by design` immediately above the relevant import block.

    After rewrites, run `./gradlew compileTestJava` to confirm test sources compile. Do NOT run `test` or `verifyGameTestStage` — that is plan 21-09's scope.
  </action>
  <verify>
    <automated>bash -c "set -e; \
      bad=$(grep -rln 'import com\\.talhanation\\.recruits\\.\\(entities\\|ai\\|pathfinding\\|world\\.Recruits\\)\\.' src/test/java src/gametest/java 2>/dev/null | xargs -r grep -L 'Phase 21: compat-surface regression' 2>/dev/null); \
      if [ -n \"$bad\" ]; then echo 'FAIL: tests still import recruit gameplay without compat marker:'; echo \"$bad\"; exit 1; fi; \
      stale=$(grep -rln 'com\\.talhanation\\.bannerlord' src/test/java src/gametest/java 2>/dev/null || true); \
      if [ -n \"$stale\" ]; then echo 'FAIL: tests still reference retired bannerlord namespace:'; echo \"$stale\"; exit 1; fi; \
      cnt_bm=$(grep -rl 'import com\\.talhanation\\.bannermod\\.\\(entity\\|ai\\|persistence\\.military\\)\\.' src/test/java src/gametest/java 2>/dev/null | wc -l); \
      if [ \"$cnt_bm\" -lt 2 ]; then echo 'FAIL: expected at least 2 test/gametest files importing bannermod canonical classes'; exit 1; fi; \
      ./gradlew compileTestJava"</automated>
  </verify>
  <acceptance_criteria>
    - No file under `src/test/java/**` or `src/gametest/java/**` imports `com.talhanation.recruits.entities.*`, `com.talhanation.recruits.ai.*`, `com.talhanation.recruits.pathfinding.*`, or `com.talhanation.recruits.world.Recruits*` UNLESS the file also contains the marker string `Phase 21: compat-surface regression`.
    - At least one file under `src/test/java/**` AND at least one file under `src/gametest/java/**` imports a `com.talhanation.bannermod.entity.*`, `com.talhanation.bannermod.ai.*`, or `com.talhanation.bannermod.persistence.military.*` symbol.
    - Zero files under `src/test/java/**` or `src/gametest/java/**` contain the string `com.talhanation.bannerlord`.
    - `./gradlew compileTestJava` exits status 0.
  </acceptance_criteria>
  <done>Regression coverage targets canonical `com.talhanation.bannermod.*` classes for all moved recruit gameplay paths. Compat-regression tests are explicitly labeled and allowed to stay on recruit imports.</done>
</task>

</tasks>

<verification>
- Every surviving file under `src/main/java/com/talhanation/recruits/**` is a documented compat surface (deprecated forwarder or enumerated retained seam).
- `MERGE_NOTES.md` enumerates the recruit compat surface under `### Phase 21 recruit compat surface`.
- Tests and GameTests target bannermod-owned canonical classes; compat-regression tests are marked.
- Zero residual `com.talhanation.bannerlord` references anywhere under `src/**` (CONTEXT D-02 continues to hold).
- `./gradlew compileJava compileTestJava` is green.
</verification>

<success_criteria>
- SRCMOVE-03 is structurally closed: no recruit-package file still owns live military/shared gameplay behavior.
- Plan 21-09 can demote the workers package family against a known-green recruit baseline and run the full validation gate.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-08-SUMMARY.md`
</output>
