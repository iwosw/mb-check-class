---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 08
type: execute
wave: 8
depends_on:
  - 21-07
files_modified:
  - src/main/java/com/talhanation/bannermod/network/messages/military/**
  - src/main/java/com/talhanation/bannermod/network/messages/civilian/**
  - src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
  - src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
  - recruits/src/main/java/com/talhanation/recruits/network/**
  - workers/src/main/java/com/talhanation/workers/network/**
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "All 109 message classes under `recruits/network/**` migrate to `bannermod.network.messages.military.**`; all 24 under `workers/network/**` migrate to `bannermod.network.messages.civilian.**`."
    - "`BannerModNetworkBootstrap.createSharedChannel()` is rewritten to register both message families against the single `SimpleChannel` using the offset contract (military at `[0..N)`, civilian at `[N..N+M)`). Order within each family is preserved from the legacy `messages[]` arrays in the original Mains."
    - "`workerPacketOffset()` returns the compile-time military message count."
    - "`BannerModMain` imports the two message packages and passes them to `BannerModNetworkBootstrap.createSharedChannel()`. The legacy clone `SIMPLE_CHANNEL` forwarders still exist (set in 21-03) and now resolve via `BannerModMain.SIMPLE_CHANNEL`."
    - "After this wave, neither clone's `src/main/java/` contains a `network/` subtree. Only the `@Deprecated` shim `Main.java`/`WorkersMain.java` remains."
---

<objective>
Consolidate the two legacy network surfaces (`recruits/network/` + `workers/network/`) into a single `bannermod.network.messages.{military,civilian}.**` tree and finalize the shared `SimpleChannel` + offset contract. This is the wave that makes the unified runtime composition from 21-03 actually correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-07-SUMMARY.md
@MERGE_NOTES.md
@recruits/src/main/java/com/talhanation/recruits/network
@workers/src/main/java/com/talhanation/workers/network
@src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk-migrate recruits and workers network packages</name>
  <action>
    For each `*.java` under `recruits/src/main/java/com/talhanation/recruits/network/`: copy to `src/main/java/com/talhanation/bannermod/network/messages/military/{same basename}.java`; rewrite `package` + `import com.talhanation.recruits.network.` → `com.talhanation.bannermod.network.messages.military.`.

    For each `*.java` under `workers/src/main/java/com/talhanation/workers/network/`: copy to `.../civilian/{same basename}.java`; rewrite `package` + `import com.talhanation.workers.network.` → `com.talhanation.bannermod.network.messages.civilian.`.

    Post-pass sed across both target subtrees: rewrite any already-migrated FQNs from earlier waves (`recruits.entities.` → `bannermod.entity.military.`, `recruits.world.` → `bannermod.persistence.military.`, `workers.entities.` → `bannermod.entity.civilian.`, etc.).

    `git rm` both `network/` subtrees inside the two clones; commit there.
    `git add` the new subtrees in outer repo; commit.
  </action>
  <verify>
    <automated>bash -c "
set -e
[ \"\$(find src/main/java/com/talhanation/bannermod/network/messages/military -name '*.java' | wc -l)\" -ge 100 ]
[ \"\$(find src/main/java/com/talhanation/bannermod/network/messages/civilian -name '*.java' | wc -l)\" -ge 20 ]
! ls recruits/src/main/java/com/talhanation/recruits/network 2>/dev/null
! ls workers/src/main/java/com/talhanation/workers/network 2>/dev/null
"
    </automated>
  </verify>
  <done>Both message families live under `bannermod.network.messages.{military,civilian}.**`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite BannerModNetworkBootstrap with canonical shared-channel + offset contract</name>
  <action>
    Edit `src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java`:
    1. Import both `bannermod.network.messages.military.*` and `bannermod.network.messages.civilian.*`.
    2. Define `MILITARY_MESSAGES` as a `Class<?>[]` in the order originally in `recruits.Main.setup()` (preserve ordering exactly — copy-paste the array body verbatim from what was captured pre-migration).
    3. Define `CIVILIAN_MESSAGES` as a `Class<?>[]` in the order originally in `workers.WorkersMain.setup()`.
    4. `createSharedChannel()` registers `MILITARY_MESSAGES[i]` at index `i`, then `CIVILIAN_MESSAGES[j]` at index `MILITARY_MESSAGES.length + j`.
    5. `workerPacketOffset()` returns `MILITARY_MESSAGES.length`.
    6. `BannerModMain.setup(FMLCommonSetupEvent)` calls `SIMPLE_CHANNEL = BannerModNetworkBootstrap.createSharedChannel();` — nothing else registers against the channel.

    Append Wave-8 entry to `MERGE_NOTES.md`: include the final `MILITARY_MESSAGES` count + `CIVILIAN_MESSAGES` count + the `workerPacketOffset()` constant.

    Commit.
  </action>
  <verify>
    <automated>bash -c "
set -e
grep -q 'MILITARY_MESSAGES' src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
grep -q 'CIVILIAN_MESSAGES' src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
grep -q 'workerPacketOffset' src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
grep -q 'createSharedChannel' src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
"
    </automated>
  </verify>
  <done>Single `SimpleChannel` serves both message families with preserved ordering and documented offset.</done>
</task>

</tasks>

<verification>
- Both message families under `bannermod.network.messages.{military,civilian}.**`.
- `BannerModNetworkBootstrap` owns the full canonical registration body.
- Clones contain only the deprecated `Main.java`/`WorkersMain.java` shims — no other source.
</verification>

<success_criteria>
- Single shared channel with deterministic packet IDs identical to the pre-split catalog.
- Must-have #4 from 21-03 ("worker packet IDs at offset = recruit catalog size") finally provable.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-08-SUMMARY.md`
</output>
