---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - src/main/java/com/talhanation/bannermod/events/**
  - src/main/java/com/talhanation/bannermod/commands/**
  - src/main/java/com/talhanation/bannermod/config/**
  - src/main/java/com/talhanation/bannermod/util/**
  - src/main/java/com/talhanation/bannermod/Translatable.java
  - src/main/java/com/talhanation/bannermod/UpdateChecker.java
  - recruits/src/main/java/com/talhanation/recruits/*.java
  - recruits/src/main/java/com/talhanation/recruits/commands/**
  - recruits/src/main/java/com/talhanation/recruits/config/**
  - workers/src/main/java/com/talhanation/workers/*.java
  - workers/src/main/java/com/talhanation/workers/config/**
  - MERGE_NOTES.md
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "All top-level event classes (`RecruitEvents`, `FactionEvents`, `ClaimEvents`, `CommandEvents`, `VillagerEvents`, `PillagerEvents`, `AssassinEvents`, `DamageEvent`, `DebugEvents`, `AttributeEvent`) from both clones move to `com.talhanation.bannermod.events.*`."
    - "`recruits/commands/**` moves to `bannermod.commands.military.*`; `workers/commands/**` (if any) moves to `bannermod.commands.civilian.*`."
    - "`recruits/config/{RecruitsClientConfig,RecruitsServerConfig}` and `workers/config/{WorkersServerConfig,WorkersModConfig}` move to `bannermod.config.*` alongside the preserved existing `config/` subtree (no clobber)."
    - "`Translatable`, `UpdateChecker`, and small top-level utilities move into `bannermod.{Translatable,UpdateChecker}` (outer root of bannermod pkg) or `bannermod.util.*`."
    - "Clone working trees no longer contain migrated event/command/config source. Only `Main.java`/`WorkersMain.java` shims and not-yet-migrated subsystems remain in each clone."
---

<objective>
Move event handlers, commands, top-level utilities, and config schemas from both clones into the outer `bannermod.*` tree. Preserves the existing `bannermod.config.*` subtree — added configs land as sibling files, overlaps documented in `MERGE_NOTES.md` per CONTEXT D-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-03-SUMMARY.md
@MERGE_NOTES.md
@recruits/src/main/java/com/talhanation/recruits
@workers/src/main/java/com/talhanation/workers
@src/main/java/com/talhanation/bannermod/config
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate events + top-level utilities from both clones</name>
  <action>
    1. For each `*.java` at top of `recruits/src/main/java/com/talhanation/recruits/` that is an event class (`RecruitEvents`, `FactionEvents`, `ClaimEvents`, `CommandEvents`, `VillagerEvents`, `PillagerEvents`, `AssassinEvents`, `DamageEvent`, `DebugEvents`, `AttributeEvent`): copy to `src/main/java/com/talhanation/bannermod/events/{same-name}.java`. Rewrite first line `package com.talhanation.recruits;` → `package com.talhanation.bannermod.events;`. Rewrite internal imports: `com.talhanation.recruits.` → `com.talhanation.bannermod.` for any path now owned in outer repo (events, bootstrap, registry, network, shared).
    2. Same for worker event classes (`VillagerEvents`, `CommandEvents`, `AttributeEvent`). If a class name collides with an already-copied recruit version, rename worker version to `Workers{Name}` (`WorkersVillagerEvents`, `WorkersCommandEvents`, `WorkersAttributeEvent`).
    3. Move `recruits/.../UpdateChecker.java` → `bannermod.UpdateChecker`; `workers/.../UpdateChecker.java` → `bannermod.WorkersUpdateChecker`. Move `workers/.../Translatable.java` → `bannermod.Translatable`.
    4. `git rm` the originals in each clone (commit inside clones). `git add` the new files in outer repo.
    5. Rewrite `BannerModMain`'s `MinecraftForge.EVENT_BUS.register(new RecruitEvents())` etc. to reference the new `com.talhanation.bannermod.events.RecruitEvents` FQN.
    6. Run `sed -i` across all already-migrated outer-repo files to rewrite any `import com.talhanation.recruits.{RecruitEvents,...};` → `import com.talhanation.bannermod.events.*;`.
  </action>
  <verify>
    <automated>bash -c "
set -e
test -d src/main/java/com/talhanation/bannermod/events
[ \"\$(find src/main/java/com/talhanation/bannermod/events -name '*.java' | wc -l)\" -ge 10 ]
! ls recruits/src/main/java/com/talhanation/recruits/{RecruitEvents,FactionEvents,ClaimEvents,CommandEvents,VillagerEvents,PillagerEvents,AssassinEvents,DamageEvent,DebugEvents,AttributeEvent}.java 2>/dev/null | head -1
! ls workers/src/main/java/com/talhanation/workers/{VillagerEvents,CommandEvents,AttributeEvent}.java 2>/dev/null | head -1
"
    </automated>
  </verify>
  <done>All event classes and top-level utilities live under `bannermod.events.*` / `bannermod.{Translatable,UpdateChecker,WorkersUpdateChecker}`.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate commands and config schemas</name>
  <action>
    1. Copy `recruits/src/main/java/com/talhanation/recruits/commands/*.java` → `src/main/java/com/talhanation/bannermod/commands/military/*.java`. Rewrite package + imports. Same for `workers/.../commands/*` if present → `bannermod.commands.civilian.*`.
    2. Copy `recruits/src/main/java/com/talhanation/recruits/config/*.java` → `src/main/java/com/talhanation/bannermod/config/{same-name}.java`. Check for collision with existing `bannermod.config.BannerModConfigFiles` — preserve the existing file, document overlap. Rewrite package + imports in copies.
    3. Copy `workers/src/main/java/com/talhanation/workers/config/*.java` → `src/main/java/com/talhanation/bannermod/config/{same-name}.java`. Rewrite package + imports.
    4. `git rm` originals in both clones; `git add` + commit in outer repo.
    5. Update `BannerModMain` to register the migrated config schemas at the new FQN.
    6. Append Wave-4 entry to `MERGE_NOTES.md` listing every file moved and any naming collision resolved.
  </action>
  <verify>
    <automated>bash -c "
set -e
test -d src/main/java/com/talhanation/bannermod/commands
[ \"\$(find src/main/java/com/talhanation/bannermod/config -name '*.java' | wc -l)\" -ge 3 ]
! ls recruits/src/main/java/com/talhanation/recruits/commands/*.java 2>/dev/null | head -1
! ls recruits/src/main/java/com/talhanation/recruits/config/*.java 2>/dev/null | head -1
! ls workers/src/main/java/com/talhanation/workers/config/*.java 2>/dev/null | head -1
"
    </automated>
  </verify>
  <done>Commands and config schemas land in outer `bannermod.*` without clobbering preserved files.</done>
</task>

</tasks>

<verification>
- Every migrated file has a rewritten `package` statement matching its new location.
- Both clones lose their top-level events, commands, and config source files from the working tree (and git).
- `MERGE_NOTES.md` has a Wave-4 entry.
- Compile state: still expected to be red per D-22.
</verification>

<success_criteria>
- `bannermod.events.*`, `bannermod.commands.*`, and augmented `bannermod.config.*` exist with all migrated content.
- Bannerod runtime (BannerModMain) references the new FQNs.
- Clone trees shrunk by the migrated subsystems.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-04-SUMMARY.md`
</output>
