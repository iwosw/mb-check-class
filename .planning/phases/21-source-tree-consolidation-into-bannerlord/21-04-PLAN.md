---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - src/main/java/com/talhanation/bannermod/entity/shared/**
  - src/main/java/com/talhanation/bannermod/entity/military/**
  - src/main/java/com/talhanation/bannermod/ai/pathfinding/**
  - src/main/java/com/talhanation/bannermod/ai/military/**
  - src/main/java/com/talhanation/bannermod/persistence/military/**
  - src/main/java/com/talhanation/bannermod/client/shared/**
  - src/main/java/com/talhanation/bannermod/client/military/**
  - src/main/java/com/talhanation/recruits/entities/**
  - src/main/java/com/talhanation/recruits/pathfinding/**
  - src/main/java/com/talhanation/recruits/client/**
  - src/main/java/com/talhanation/recruits/world/**
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "Recruit-owned controlling foundations -- entity bases, military entities, pathfinding, military AI, recruit persistence/save vocabulary, recruit client widgets -- physically live under `src/main/java/com/talhanation/bannermod/{entity,ai,persistence,client}/{shared,military}/**`."
    - "Phase 11-19 performance/correctness seams (GlobalPathfindingController, formation target selection, AI LOD, async pathfinding) are MOVED, not rewritten -- behavior preserved class-for-class."
    - "Worker code may import the new bannermod-owned military/shared bases temporarily; this plan does not yet move worker civilian packages (that is 21-05)."
    - "Serialization keys (NBT tag names, save-data names) and registry ids are unchanged."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java"
      provides: "Canonical recruit base under bannermod"
      contains: "package com.talhanation.bannermod.entity.shared"
    - path: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java"
      provides: "Canonical inventory base under bannermod"
      contains: "package com.talhanation.bannermod.entity.shared"
    - path: "src/main/java/com/talhanation/bannermod/ai/pathfinding/GlobalPathfindingController.java"
      provides: "Phase 12 controller, moved verbatim into bannermod"
      contains: "GlobalPathfindingController"
  key_links:
    - from: "src/main/java/com/talhanation/recruits/entities/**"
      to: "src/main/java/com/talhanation/bannermod/entity/**"
      via: "subclass / forwarder"
      pattern: "extends com\\.talhanation\\.bannermod\\.entity"
---

<objective>
Move the recruit-owned controlling systems into `com.talhanation.bannermod.{entity,ai,persistence,client}` packages so worker civilian code (plan 21-05) can build on a bannermod-owned foundation.

Purpose: per CONTEXT D-04 the expected new sibling packages under bannermod include `entity`, `ai`, `persistence`, `client`. The recruit foundations (entity bases, pathfinding, military AI, persistence vocabulary, client widgets) are imported by worker code, so they must move FIRST.
Output: military/shared entity, pathfinding, AI, persistence, and client foundations live under `bannermod` ownership with behavior preserved. Recruit packages remain on disk for now -- plan 21-08 demotes them to compat shims.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/19-large-battle-performance-validation/19-VALIDATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-03-PLAN.md
@src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java
@src/main/java/com/talhanation/recruits/entities/AbstractInventoryEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move recruit entity bases, military entities, AI/pathfinding, military persistence, and military client widgets into bannermod packages</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-04)
    - .planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md (worker dependency map showing which recruit bases workers import)
    - Run `ls src/main/java/com/talhanation/recruits/entities/` to enumerate the entity files needing moves (e.g., `AbstractRecruitEntity.java`, `AbstractInventoryEntity.java`, `AbstractLeaderEntity.java`, `MessengerEntity.java`, `ScoutEntity.java`, `CaptainEntity.java`, `VillagerNobleEntity.java`, plus `IRangedRecruit` / `IWeapon` interfaces).
    - Run `ls src/main/java/com/talhanation/recruits/pathfinding/` for the Phase 12 controller and reuse code.
    - Run `ls src/main/java/com/talhanation/recruits/client/` for shared client widgets.
    - Run `ls src/main/java/com/talhanation/recruits/world/` for `RecruitsClaim`, `RecruitsClaimManager`, `RecruitsPatrolSpawn`, `RecruitsPlayerInfo`, `RecruitsTreatyManager`.
  </read_first>
  <action>
    Apply package-rename moves with explicit destinations. For EACH file enumerated by the grep/ls in `<read_first>`, perform `git mv` to the new path, then update the `package` declaration in the moved file:

    - `src/main/java/com/talhanation/recruits/entities/AbstractRecruitEntity.java` -> `src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java` with package `com.talhanation.bannermod.entity.shared`.
    - `src/main/java/com/talhanation/recruits/entities/AbstractInventoryEntity.java` -> `src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java` (package `com.talhanation.bannermod.entity.shared`).
    - `src/main/java/com/talhanation/recruits/entities/AbstractLeaderEntity.java`, `IRangedRecruit.java`, `IWeapon.java` -> `src/main/java/com/talhanation/bannermod/entity/shared/`.
    - All concrete military entity files (e.g., `MessengerEntity`, `ScoutEntity`, `CaptainEntity`, `VillagerNobleEntity`, plus all `*Recruit*Entity` military variants) -> `src/main/java/com/talhanation/bannermod/entity/military/<Name>.java` with package `com.talhanation.bannermod.entity.military`.
    - All `src/main/java/com/talhanation/recruits/pathfinding/*` -> `src/main/java/com/talhanation/bannermod/ai/pathfinding/` (especially `GlobalPathfindingController.java`).
    - Recruit AI controllers under `src/main/java/com/talhanation/recruits/ai/` -> `src/main/java/com/talhanation/bannermod/ai/military/`.
    - `src/main/java/com/talhanation/recruits/world/Recruits*.java` -> `src/main/java/com/talhanation/bannermod/persistence/military/`.
    - Recruit client widgets (`src/main/java/com/talhanation/recruits/client/gui/*`, `client/render/*`, etc. -- shared widgets that worker UI imports) -> `src/main/java/com/talhanation/bannermod/client/shared/` for cross-cutting widgets, or `bannermod/client/military/` for military-specific UI.

    Use `git mv <old> <new>` (preserves history). Then update the `package` line in each moved file. Use `sed -i 's|^package com\\.talhanation\\.recruits\\.entities;$|package com.talhanation.bannermod.entity.shared;|' <file>` style commands; verify each file's package declaration matches the destination directory.

    Update ALL imports across `src/main/java/`, `src/test/java/`, `src/gametest/java/` to use the new package paths. Use grep + sed to enumerate and rewrite. Pattern examples:
    `import com.talhanation.recruits.entities.AbstractRecruitEntity;` -> `import com.talhanation.bannermod.entity.shared.AbstractRecruitEntity;`
    `import com.talhanation.recruits.pathfinding.GlobalPathfindingController;` -> `import com.talhanation.bannermod.ai.pathfinding.GlobalPathfindingController;`

    Do NOT change behavior. Each moved class keeps identical method signatures, field names, NBT tag strings, and registry id strings.
  </action>
  <verify>
    <automated>bash -c "test -f src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java && grep -q '^package com.talhanation.bannermod.entity.shared;' src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java && test -f src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java && test -f src/main/java/com/talhanation/bannermod/ai/pathfinding/GlobalPathfindingController.java && grep -q '^package com.talhanation.bannermod.ai.pathfinding;' src/main/java/com/talhanation/bannermod/ai/pathfinding/GlobalPathfindingController.java && test -d src/main/java/com/talhanation/bannermod/persistence/military"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java` exists with package `com.talhanation.bannermod.entity.shared`.
    - `src/main/java/com/talhanation/bannermod/entity/shared/AbstractInventoryEntity.java` exists with the same package.
    - `src/main/java/com/talhanation/bannermod/ai/pathfinding/GlobalPathfindingController.java` exists with package `com.talhanation.bannermod.ai.pathfinding`.
    - `src/main/java/com/talhanation/bannermod/persistence/military/` contains at least one of `RecruitsClaim.java`, `RecruitsClaimManager.java`, or similar moved persistence types.
    - `git status --short -- src/main/java/com/talhanation/recruits/entities/` shows the original entity files as deleted (renamed via `git mv`).
  </acceptance_criteria>
  <done>The recruit-controlled foundations physically live under bannermod packages with preserved behavior, names, and serialization keys.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget root tests, GameTests, and remaining recruit/worker callers to the new bannermod imports</name>
  <read_first>
    - Run `grep -rln 'com\\.talhanation\\.recruits\\.entities\\.\\|com\\.talhanation\\.recruits\\.pathfinding\\.\\|com\\.talhanation\\.recruits\\.world\\.Recruits\\|com\\.talhanation\\.recruits\\.client\\.' src/main/java src/test/java src/gametest/java` to enumerate every remaining caller of the moved packages.
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-VERIFICATION.md (gap context for what `compileJava` previously failed on)
  </read_first>
  <action>
    For every file printed by the grep above, rewrite its imports to the new bannermod package paths (mapping table from Task 1). Use `sed -i` per file. Patterns:
    - `s|com\\.talhanation\\.recruits\\.entities\\.|com.talhanation.bannermod.entity.shared.|g` for base classes; for concrete military entities target `bannermod.entity.military.` (do this per-class, not as a blanket sed, since the destination differs by file).
    - `s|com\\.talhanation\\.recruits\\.pathfinding\\.|com.talhanation.bannermod.ai.pathfinding.|g`.
    - `s|com\\.talhanation\\.recruits\\.world\\.Recruits|com.talhanation.bannermod.persistence.military.Recruits|g` (only for moved persistence types).
    - `s|com\\.talhanation\\.recruits\\.client\\.gui\\.|com.talhanation.bannermod.client.shared.|g` for cross-cutting widgets (verify per file before applying).

    Note: callers that still need to interoperate via the recruit-only adapter signatures will be addressed in plan 21-07. This task only rewrites import statements; do NOT change call signatures here.

    After rewrites, run `./gradlew compileJava` once. Compilation may still fail on recruit-only API mismatches (e.g., `RecruitSimpleContainer` constructor expecting old `AbstractInventoryEntity`) -- these are scope of plan 21-07 and are acceptable here. Document any new compile-error categories in `deferred-items.md`.
  </action>
  <verify>
    <automated>bash -c "remaining=$(grep -rln 'import com\\.talhanation\\.recruits\\.entities\\.\\|import com\\.talhanation\\.recruits\\.pathfinding\\.' src/main/java src/test/java src/gametest/java 2>/dev/null | wc -l); echo \"Remaining legacy entity/pathfinding imports: $remaining\"; [ \"$remaining\" -le 5 ]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rln 'import com.talhanation.recruits.entities.' src/main/java src/test/java src/gametest/java` returns at most 5 files (residual adapters/menus addressed in plan 21-07).
    - `grep -rln 'import com.talhanation.recruits.pathfinding.' src/main/java src/test/java src/gametest/java` returns at most 5 files.
    - At least one root test file under `src/test/java/` and one GameTest file under `src/gametest/java/` import a `com.talhanation.bannermod.entity.*` or `com.talhanation.bannermod.ai.pathfinding.*` symbol.
    - `deferred-items.md` is updated with a new entry titled `Plan 21-04 carryover` listing any remaining recruit-only signature mismatches that plan 21-07 must fix.
  </acceptance_criteria>
  <done>Active callers and tests now import bannermod-owned military/shared classes for moved code paths; residual recruit-only signature mismatches are documented as 21-07 scope.</done>
</task>

</tasks>

<verification>
- Recruit-owned controlling foundations live under `bannermod.{entity,ai,persistence,client}/{shared,military}/**`.
- The retained pathfinding, AI, client, and persistence behavior is preserved (no logic changes).
- Active callers and tests target the new bannermod package paths.
- Any residual recruit-only signature mismatches are documented in `deferred-items.md` for 21-07.
</verification>

<success_criteria>
- Plan 21-05 (worker civilian move) can import `bannermod.entity.shared.*`, `bannermod.ai.*`, and `bannermod.client.shared.*` directly.
- The legacy recruit packages still hold concrete UI menus and event helpers; demotion happens in plan 21-08.
- Phase 11-19 performance seams remain class-for-class identical.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-04-SUMMARY.md`
</output>
