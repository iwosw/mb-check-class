---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 04
type: execute
wave: 4
depends_on:
  - 21-03
files_modified:
  - src/main/java/com/talhanation/bannerlord/entity/civilian/**
  - src/main/java/com/talhanation/bannerlord/ai/civilian/**
  - src/main/java/com/talhanation/bannerlord/persistence/civilian/**
  - src/main/java/com/talhanation/bannerlord/client/civilian/**
  - src/main/java/com/talhanation/bannerlord/compat/workers/**
  - workers/src/main/java/com/talhanation/workers/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "Worker civilian packages move only after their imported bases already live under `bannerlord`; this plan should consume that new foundation instead of recreating it."
    - "The worker move must preserve known compatibility-critical paths: legacy `workers:*` remaps, structure/build-area NBT migration, and shared-channel packet offset behavior."
    - "Temporary adapters may remain around client imports or compatibility helpers, but worker gameplay ownership should now be physically rooted under `src/main/java/com/talhanation/bannerlord/**`."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/WorkersRuntime.java"
      provides: "The active worker runtime compatibility boundary"
      contains: "LEGACY_MOD_ID|migrateLegacyId|migrateStructureNbt"
    - path: "workers/src/main/java/com/talhanation/workers/WorkersLegacyMappings.java"
      provides: "The missing-mapping bridge that must survive the move"
      contains: "MissingMappingsEvent|mapping.remap"
    - path: "workers/src/main/java/com/talhanation/workers/client/**"
      provides: "The worker UI surfaces that still depend on recruit client/shared widgets"
      contains: "Screen|Menu|Renderer"
---

<objective>
Move worker civilian entities, AI, persistence, and client flows onto the new `bannerlord` physical ownership tree while keeping only the narrow compatibility layer alive.

Purpose: this plan finishes the real code move for the worker subsystem after the foundations are already relocated.
Output: worker gameplay ownership now lives under `bannerlord`, with any retained adapters isolated to explicit compatibility packages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/VERIFICATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-03-PLAN.md
@workers/src/main/java/com/talhanation/workers/WorkersRuntime.java
@workers/src/main/java/com/talhanation/workers/WorkersLegacyMappings.java
@workers/src/main/java/com/talhanation/workers/entities/**
@workers/src/main/java/com/talhanation/workers/world/**
@workers/src/main/java/com/talhanation/workers/client/**
@workers/src/main/java/com/talhanation/workers/network/**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home worker civilian code and isolate the compatibility layer</name>
  <files>src/main/java/com/talhanation/bannerlord/entity/civilian/**, src/main/java/com/talhanation/bannerlord/ai/civilian/**, src/main/java/com/talhanation/bannerlord/persistence/civilian/**, src/main/java/com/talhanation/bannerlord/client/civilian/**, src/main/java/com/talhanation/bannerlord/compat/workers/**</files>
  <action>Move worker entities, profession AI, work-area/world serialization, and worker client flows into `bannerlord` civilian packages. Re-home `WorkersRuntime`-style migration helpers and missing-mapping bridges into `com.talhanation.bannerlord.compat.workers` so the remaining compatibility policy is explicit and narrow. Remove temporary adapters that are no longer needed after worker imports settle.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
targets = [
    'src/main/java/com/talhanation/bannerlord/entity/civilian',
    'src/main/java/com/talhanation/bannerlord/ai/civilian',
    'src/main/java/com/talhanation/bannerlord/persistence/civilian',
    'src/main/java/com/talhanation/bannerlord/client/civilian',
    'src/main/java/com/talhanation/bannerlord/compat/workers'
]
missing = [target for target in targets if not Path(target).exists()]
if missing:
    raise SystemExit('Missing required wave-4 bannerlord targets: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>Worker gameplay code is physically consolidated under `bannerlord`, and the remaining legacy handling is visibly isolated to the compatibility layer.</done>
</task>

<task type="auto">
  <name>Task 2: Keep worker regression coverage and runtime validation aligned with the move</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Refresh `VERIFICATION.md` if the moved worker package locations or compatibility helpers change which root tests or GameTests Phase 21 must run for confidence. Preserve coverage for worker runtime identity, mining/build mechanics, and known legacy migration helpers.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/phases/21-source-tree-consolidation-into-bannerlord/21-04-PLAN.md').read_text()
required = ['compat.workers', 'legacy `workers:*` remaps', 'worker runtime identity']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('Missing expected wave-4 compatibility content: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The verification baseline still covers the worker-specific seams that matter after physical consolidation.</done>
</task>

</tasks>

<verification>
- Worker gameplay ownership now lives under `bannerlord` civilian packages.
- Compatibility-critical migration helpers remain explicit under `compat.workers`.
- The worker move closes the physical split without widening runtime promises.
</verification>

<success_criteria>
- The `workers/` tree no longer owns active gameplay systems except any intentionally retained short-lived adapters.
- Legacy-save and packet-order compatibility surfaces remain intact while the physical move completes.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-04-SUMMARY.md`
</output>
