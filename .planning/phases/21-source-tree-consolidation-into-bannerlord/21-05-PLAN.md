---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 05
type: execute
wave: 5
depends_on:
  - 21-04
files_modified:
  - src/main/java/com/talhanation/bannermod/entity/civilian/**
  - src/main/java/com/talhanation/bannermod/ai/civilian/**
  - src/main/java/com/talhanation/bannermod/persistence/civilian/**
  - src/main/java/com/talhanation/bannermod/client/civilian/**
  - src/main/java/com/talhanation/bannermod/compat/workers/**
  - src/main/java/com/talhanation/workers/entities/**
  - src/main/java/com/talhanation/workers/ai/**
  - src/main/java/com/talhanation/workers/world/**
  - src/main/java/com/talhanation/workers/client/**
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "Worker civilian gameplay code -- entities, profession AI, work-area persistence, and client flows -- physically lives under `src/main/java/com/talhanation/bannermod/{entity,ai,persistence,client}/civilian/**`."
    - "Narrow worker compatibility helpers (legacy `workers:*` id remap, structure NBT migration, runtime bootstrap helpers) live under `src/main/java/com/talhanation/bannermod/compat/workers/**`."
    - "Worker civilian classes import `com.talhanation.bannermod.entity.shared.*` and `com.talhanation.bannermod.ai.pathfinding.*` (the bannermod foundations from plan 21-04), not legacy `recruits/*` packages."
    - "Legacy registry id strings, NBT tags, packet IDs, and the worker packet offset contract are unchanged."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/entity/civilian/AbstractWorkerEntity.java"
      provides: "Canonical worker base under bannermod"
      contains: "package com.talhanation.bannermod.entity.civilian"
    - path: "src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java"
      provides: "Narrow worker compat seam (legacy id, channel, asset helpers)"
      contains: "LEGACY_MOD_ID"
    - path: "src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java"
      provides: "Missing-mapping bridge for legacy workers:* registry ids"
      contains: "MissingMappingsEvent"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/entity/civilian/**"
      to: "src/main/java/com/talhanation/bannermod/entity/shared/AbstractRecruitEntity.java"
      via: "extends / imports"
      pattern: "extends .*AbstractRecruitEntity|com\\.talhanation\\.bannermod\\.entity\\.shared"
    - from: "src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java"
      to: "Forge MissingMappingsEvent bus"
      via: "registered listener"
      pattern: "MissingMappingsEvent|@SubscribeEvent"
---

<objective>
Move worker civilian entities, AI, persistence, and client flows into `com.talhanation.bannermod.{entity,ai,persistence,client}.civilian` packages, and isolate the narrow worker compatibility surface under `com.talhanation.bannermod.compat.workers`.

Purpose: per CONTEXT D-04 the convergence namespace covers civilian gameplay too. The worker civilian move depends on plan 21-04 (which placed `bannermod.entity.shared.AbstractRecruitEntity`, pathfinding, and shared client widgets under bannermod). The narrow `compat.workers` package preserves only the legacy id remap, structure migration, and bootstrap helpers the runtime still needs.
Output: worker gameplay ownership physically lives under `bannermod.*.civilian/**` and `bannermod.compat.workers/**`. Legacy `workers/**` source paths remain on disk awaiting plan 21-09 demotion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@MERGE_NOTES.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-04-PLAN.md
@src/main/java/com/talhanation/workers/WorkersRuntime.java
@src/main/java/com/talhanation/workers/WorkersLegacyMappings.java
@src/main/java/com/talhanation/workers/entities/AbstractWorkerEntity.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move worker civilian entities, profession AI, work-area persistence, and client flows into bannermod.*.civilian packages</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-04)
    - Run `ls src/main/java/com/talhanation/workers/entities/` to enumerate worker entity files (e.g., `AbstractWorkerEntity.java`, `MinerEntity.java`, `BuilderEntity.java`, `FarmerEntity.java`, `MerchantEntity.java`, etc.).
    - Run `ls src/main/java/com/talhanation/workers/ai/` for profession AI controllers/goals.
    - Run `ls src/main/java/com/talhanation/workers/world/` for work-area / build-area / mining-area persistence (e.g., `WorkArea.java`, `BuildArea.java`, `MiningArea.java`).
    - Run `ls src/main/java/com/talhanation/workers/client/` for worker UI screens, menus, renderers.
  </read_first>
  <action>
    For EACH worker civilian file, perform `git mv` and `package` rewrite using the destination mapping below:
    - `src/main/java/com/talhanation/workers/entities/<X>.java` -> `src/main/java/com/talhanation/bannermod/entity/civilian/<X>.java` with package `com.talhanation.bannermod.entity.civilian`.
    - `src/main/java/com/talhanation/workers/ai/<Y>.java` -> `src/main/java/com/talhanation/bannermod/ai/civilian/<Y>.java` with package `com.talhanation.bannermod.ai.civilian`.
    - `src/main/java/com/talhanation/workers/world/<Z>.java` -> `src/main/java/com/talhanation/bannermod/persistence/civilian/<Z>.java` with package `com.talhanation.bannermod.persistence.civilian`.
    - `src/main/java/com/talhanation/workers/client/<C>.java` -> `src/main/java/com/talhanation/bannermod/client/civilian/<C>.java` with package `com.talhanation.bannermod.client.civilian`.

    EXCEPTIONS (do NOT move in this task -- they are the narrow compat layer addressed in Task 2):
    - `WorkersRuntime.java`
    - `WorkersLegacyMappings.java`
    - `WorkersSubsystem.java` (if present)
    - `workers/network/WorkersNetworkRegistrar.java` (already addressed in plan 21-03 as compat seam)
    - `workers/init/WorkersLifecycleRegistrar.java` (already addressed in plan 21-03)

    Update `package` declaration in each moved file using `sed -i 's|^package com\\.talhanation\\.workers\\.<oldsubpkg>;$|package com.talhanation.bannermod.<newsubpkg>;|' <file>`. Then enumerate and rewrite all importing callers in `src/main/java/`, `src/test/java/`, `src/gametest/java/` with sed: `s|com\\.talhanation\\.workers\\.entities\\.|com.talhanation.bannermod.entity.civilian.|g`, etc.

    Worker entity classes that previously extended legacy `recruits.entities.AbstractWorkerBase` or similar: re-target their `extends` clause to import `com.talhanation.bannermod.entity.shared.AbstractRecruitEntity` (the canonical base from plan 21-04) or another bannermod-owned base. Do NOT rewrite behavior.

    Preserve: NBT tag strings, registry id strings (still `bannermod:` prefix per existing runtime contract), packet field names, save-data keys.
  </action>
  <verify>
    <automated>bash -c "test -d src/main/java/com/talhanation/bannermod/entity/civilian && test -d src/main/java/com/talhanation/bannermod/ai/civilian && test -d src/main/java/com/talhanation/bannermod/persistence/civilian && test -d src/main/java/com/talhanation/bannermod/client/civilian && [ \"\$(find src/main/java/com/talhanation/bannermod/entity/civilian -name '*.java' | wc -l)\" -ge 1 ] && find src/main/java/com/talhanation/bannermod/entity/civilian -name '*.java' | head -1 | xargs grep -q '^package com.talhanation.bannermod.entity.civilian;' && ! grep -rn 'com\\.talhanation\\.workers\\.\\(entities\\|ai\\|world\\|client\\)\\.' src/main/java src/test/java src/gametest/java 2>/dev/null | grep -v '^src/main/java/com/talhanation/bannermod/compat/workers/' | grep -v '^src/main/java/com/talhanation/workers/'"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannermod/entity/civilian/`, `.../ai/civilian/`, `.../persistence/civilian/`, `.../client/civilian/` all exist and contain at least one `.java` file each.
    - Every moved file's `package` declaration matches its new bannermod location (grep `^package com\.talhanation\.bannermod\.(entity|ai|persistence|client)\.civilian;`).
    - No file under `src/main/java/com/talhanation/bannermod/**`, `src/test/java/**`, or `src/gametest/java/**` imports `com.talhanation.workers.{entities,ai,world,client}.*` (the `workers/**` package-local references inside the legacy tree are fine — plan 21-09 demotes those).
    - Worker entity classes that previously extended a recruits-namespace base now extend or import `com.talhanation.bannermod.entity.shared.*`.
    - NBT tag strings, registry ids (`bannermod:` prefix), and packet field names are byte-identical to pre-move state (spot-check: `grep -rE 'bannermod:[a-z_]+' src/main/java/com/talhanation/bannermod/entity/civilian | wc -l` is > 0 and matches the pre-move count).
  </acceptance_criteria>
  <done>Worker civilian entities, profession AI, work-area persistence, and client flows are physically relocated under `bannermod.{entity,ai,persistence,client}.civilian/**`, with package declarations and imports rewritten. Legacy `workers/{entities,ai,world,client}` source trees no longer hold active gameplay code (only the narrow compat files remain, handled by Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Move narrow worker compat seam (WorkersRuntime, WorkersLegacyMappings, WorkersSubsystem) into bannermod.compat.workers</name>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-04, D-05 — narrow compat isolation)
    - src/main/java/com/talhanation/workers/WorkersRuntime.java (current legacy-id / channel / asset helper holder; preserve `LEGACY_MOD_ID` constant and every public static helper signature)
    - src/main/java/com/talhanation/workers/WorkersLegacyMappings.java (Forge `MissingMappingsEvent` listener that remaps legacy `workers:*` registry ids; preserve the `@SubscribeEvent` signature and the id-remap table)
    - `ls src/main/java/com/talhanation/workers/` to confirm whether `WorkersSubsystem.java` exists at the workers root (if present, it is the third compat file — otherwise skip).
  </read_first>
  <action>
    Perform targeted `git mv` for the three narrow compat files:

    1. `git mv src/main/java/com/talhanation/workers/WorkersRuntime.java src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java` and rewrite its `package` declaration from `package com.talhanation.workers;` to `package com.talhanation.bannermod.compat.workers;`. Preserve `public static final String LEGACY_MOD_ID = "workers";` verbatim (do NOT rewrite to `bannermod` — the entire point of this constant is to preserve the retired legacy id for missing-mappings lookup).

    2. `git mv src/main/java/com/talhanation/workers/WorkersLegacyMappings.java src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java` and rewrite `package com.talhanation.workers;` to `package com.talhanation.bannermod.compat.workers;`. Preserve the `@SubscribeEvent` method signature on `MissingMappingsEvent` and the id-remap table byte-for-byte.

    3. If `src/main/java/com/talhanation/workers/WorkersSubsystem.java` exists: `git mv` it to `src/main/java/com/talhanation/bannermod/compat/workers/WorkersSubsystem.java` and rewrite its `package` declaration the same way. If it does not exist, skip without error.

    Retarget every caller: `grep -rln 'com\.talhanation\.workers\.WorkersRuntime\|com\.talhanation\.workers\.WorkersLegacyMappings\|com\.talhanation\.workers\.WorkersSubsystem' src/main/java src/test/java src/gametest/java` — rewrite each occurrence to `com.talhanation.bannermod.compat.workers.<class>` with `sed -i`. Two key callers to double-check: `bannermod.bootstrap.BannerModMain` (plan 21-03) must import the new `com.talhanation.bannermod.compat.workers.WorkersSubsystem` (if present) for the `public static WorkersSubsystem workersSubsystem;` field; `bannermod.compat.workers.WorkersLegacyMappings` must be registered onto Forge's event bus by `BannerModMain`'s constructor (preserve the subscription).

    Preserve: `LEGACY_MOD_ID = "workers"` literal, the `MissingMappingsEvent` subscription, any resource-location namespaces the compat layer rewrites against.
  </action>
  <verify>
    <automated>bash -c "
set -e
test -f src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java
test -f src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java
grep -q '^package com\\.talhanation\\.bannermod\\.compat\\.workers;' src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java
grep -q '^package com\\.talhanation\\.bannermod\\.compat\\.workers;' src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java
grep -q 'LEGACY_MOD_ID = \"workers\"' src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java
grep -q 'MissingMappingsEvent' src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java
test ! -f src/main/java/com/talhanation/workers/WorkersRuntime.java
test ! -f src/main/java/com/talhanation/workers/WorkersLegacyMappings.java
! grep -rn 'com\\.talhanation\\.workers\\.WorkersRuntime\\|com\\.talhanation\\.workers\\.WorkersLegacyMappings\\|com\\.talhanation\\.workers\\.WorkersSubsystem' src/main/java src/test/java src/gametest/java
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannermod/compat/workers/WorkersRuntime.java` exists with `package com.talhanation.bannermod.compat.workers;` and contains `LEGACY_MOD_ID = "workers"` (literal preserved).
    - `src/main/java/com/talhanation/bannermod/compat/workers/WorkersLegacyMappings.java` exists with the same package and contains a `MissingMappingsEvent` reference.
    - The original files at `src/main/java/com/talhanation/workers/WorkersRuntime.java` and `src/main/java/com/talhanation/workers/WorkersLegacyMappings.java` are gone (`test ! -f`).
    - If `WorkersSubsystem.java` existed at the workers root, it is now at `src/main/java/com/talhanation/bannermod/compat/workers/WorkersSubsystem.java` with the rewritten package.
    - No file under `src/main/java/**`, `src/test/java/**`, or `src/gametest/java/**` references the old FQCNs `com.talhanation.workers.WorkersRuntime`, `com.talhanation.workers.WorkersLegacyMappings`, or `com.talhanation.workers.WorkersSubsystem`.
  </acceptance_criteria>
  <done>The narrow worker compat seam (`WorkersRuntime`, `WorkersLegacyMappings`, optionally `WorkersSubsystem`) is isolated under `bannermod.compat.workers/**` with package rewrites, and every caller imports the new FQCNs. The `LEGACY_MOD_ID = "workers"` constant and `MissingMappingsEvent` subscription are preserved so legacy saves keep loading.</done>
</task>

</tasks>

<verification>
- `src/main/java/com/talhanation/bannermod/{entity,ai,persistence,client}/civilian/**` directories exist and hold the moved worker civilian code.
- `src/main/java/com/talhanation/bannermod/compat/workers/{WorkersRuntime,WorkersLegacyMappings}.java` exist with rewritten packages and preserved legacy contracts.
- No `src/main/java/com/talhanation/workers/{entities,ai,world,client}/**` files are imported from active (non-legacy) callers.
- `grep -rE 'bannermod:[a-z_]+' src/main/java/com/talhanation/bannermod/entity/civilian` returns the same set of registry ids as the pre-move worker entities (no id drift).
</verification>

<success_criteria>
- Worker civilian gameplay ownership physically lives under `com.talhanation.bannermod.**` per CONTEXT D-04.
- Narrow worker compat (legacy id remap + missing-mappings + runtime helpers) is isolated under `bannermod.compat.workers/**` per CONTEXT D-05.
- Plan 21-09 can later demote the residual `workers/**` tree to compat shims or delete it without disturbing the active bannermod gameplay code.
- Live runtime contract (mod id, NBT keys, registry ids, packet offset) is unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md`
</output>