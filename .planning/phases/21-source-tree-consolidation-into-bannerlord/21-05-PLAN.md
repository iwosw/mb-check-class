---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 05
type: execute
wave: 5
depends_on:
  - 21-04
files_modified:
  - build.gradle
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/VERIFICATION.md
  - MERGE_NOTES.md
  - recruits/src/main/java/**
  - workers/src/main/java/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "Source-root retirement can happen only after the active Java ownership really lives under `src/main/java/com/talhanation/bannerlord/**` and the required validation passes from the repository root."
    - "Retirement does not mean compatibility is gone: the narrow `compat.workers` migration boundary may remain if save/runtime evidence still requires it."
    - "Root planning docs, verification docs, and merge notes must tell the post-move truth immediately after the source-root change so later phases do not inherit stale ownership assumptions."
  artifacts:
    - path: ".planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md"
      provides: "The retirement preconditions that must be satisfied before legacy source roots are removed"
      contains: "source-root retirement preconditions"
    - path: ".planning/VERIFICATION.md"
      provides: "The required repository-root validation baseline for accepting the move"
      contains: "compileJava|processResources|test|verifyGameTestStage"
---

<objective>
Retire the legacy Java source roots and refresh the root planning/verification truth once the Phase 21 move is complete.

Purpose: this plan closes the physical consolidation by removing obsolete source-root wiring, documenting the final compatibility layer, and proving the merged runtime still validates from the root.
Output: `build.gradle` no longer composes active Java code from `recruits/` or `workers/`, and the root planning context records the new physical ownership truth.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@build.gradle
@MERGE_NOTES.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/VERIFICATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy source-root ownership from the active build</name>
  <files>build.gradle, recruits/src/main/java/**, workers/src/main/java/**</files>
  <action>After waves 1 through 4 finish, update `build.gradle` so the active Java ownership no longer depends on `recruits/src/main/java` or `workers/src/main/java`. Remove or archive obsolete Java sources only when the moved `bannerlord` tree already owns their active behavior. Keep any explicitly retained compatibility adapters in their documented location.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('build.gradle').read_text()
for forbidden in ['recruits/src/main/java', 'workers/src/main/java']:
    if forbidden in text:
        raise SystemExit('Legacy Java source root still active in build.gradle: ' + forbidden)
PY</automated>
  </verify>
  <done>The root build now reflects one physical Java ownership tree for the merged runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh roadmap, state, verification, and merge notes to record the post-consolidation truth</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/VERIFICATION.md, MERGE_NOTES.md</files>
  <action>Update the active planning docs and merge notes so they say Phase 21 is complete only when the move, retirement, and validation are actually done. Record any compatibility adapters that intentionally remain, and remove stale wording that still treats `recruits/` or `workers/` Java trees as active owners.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
state = Path('.planning/STATE.md').read_text()
roadmap = Path('.planning/ROADMAP.md').read_text()
required = [
    '21-source-tree-consolidation-into-bannerlord',
    'Phase 21',
    'bannerlord'
]
for name, text in [('STATE', state), ('ROADMAP', roadmap)]:
    missing = [item for item in required if item not in text]
    if missing:
        raise SystemExit(f'Missing expected Phase 21 handoff content in {name}: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The root planning context now tells the truth about the consolidated source tree and any retained compatibility boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Run the full root validation gate required for source-root retirement</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Run the required root validation after the source-root retirement: `./gradlew compileJava`, `./gradlew processResources`, `./gradlew test`, and `./gradlew verifyGameTestStage` unless the final execution summary documents why the GameTest gate is intentionally deferred. Record the actual acceptance outcome in the summary and keep `VERIFICATION.md` aligned with that gate.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/phases/21-source-tree-consolidation-into-bannerlord/21-05-PLAN.md').read_text()
required = ['./gradlew compileJava', './gradlew processResources', './gradlew test', './gradlew verifyGameTestStage']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('Missing required Phase 21 validation commands: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>Phase 21 only closes when the post-retirement root validation evidence is explicit and green.</done>
</task>

</tasks>

<verification>
- `build.gradle` no longer composes active Java ownership from the legacy source roots.
- Root docs describe the new physical ownership and any intentionally retained compatibility adapters truthfully.
- Full root validation is green after source-root retirement, or any deferred gate is explicitly justified in the summary.
</verification>

<success_criteria>
- BannerMod is one physical Java codebase under `src/main/java/com/talhanation/bannerlord/**`.
- The repository keeps only the compatibility surface it still needs for real runtime/save migration.
- Later phases can build on the consolidated tree instead of the old split-source layout.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-05-SUMMARY.md`
</output>
