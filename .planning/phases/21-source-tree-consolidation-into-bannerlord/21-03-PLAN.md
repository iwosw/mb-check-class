---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
  - src/main/java/com/talhanation/bannermod/bootstrap/BannerModLifecycle.java
  - src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
  - src/main/java/com/talhanation/bannermod/registry/**
  - src/main/resources/META-INF/mods.toml
  - MERGE_NOTES.md
  - recruits/src/main/java/com/talhanation/recruits/Main.java
  - recruits/src/main/java/com/talhanation/recruits/init/**
  - workers/src/main/java/com/talhanation/workers/WorkersMain.java
  - workers/src/main/java/com/talhanation/workers/WorkersRuntime.java
  - workers/src/main/java/com/talhanation/workers/WorkersSubsystem.java
  - workers/src/main/java/com/talhanation/workers/MergedRuntimeCleanupPolicy.java
  - workers/src/main/java/com/talhanation/workers/init/**
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "Exactly one `@Mod` annotation exists under the outer repo's `src/main/java/`, on `com.talhanation.bannermod.bootstrap.BannerModMain` with mod id `bannermod`."
    - "The recruits and workers message sets are registered against a single shared `SimpleChannel` owned by `com.talhanation.bannermod.network.BannerModNetworkBootstrap`. Workers messages register at offset = recruit catalog size."
    - "`recruits/.../Main.java` and `workers/.../WorkersMain.java` are demoted: `@Mod` annotation removed, class marked `@Deprecated`, bodies reduced to forwarders onto `BannerModMain` statics."
    - "Registry holders `ModBlocks`, `ModItems`, `ModEntityTypes`, `ModPois`, `ModProfessions`, `ModScreens`, `ModMenuTypes`, `ModShortcuts`, `ModSounds` are copied from both clones into `com.talhanation.bannermod.registry.{military,civilian}.*` with `package` and internal `import` lines rewritten. Source files are deleted in the clones."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java"
      provides: "Unified @Mod entrypoint (recruit + worker setup merged)"
      contains: "@Mod(\"bannermod\")"
    - path: "src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java"
      provides: "Single shared SimpleChannel + packet offset contract"
      contains: "workerPacketOffset|createSharedChannel"
---

<objective>
Physically move the runtime composition root from the embedded `recruits/` and `workers/` clones into `com.talhanation.bannermod.{bootstrap,network,registry}` in the outer repo. Unify both `@Mod` entrypoints into a single `BannerModMain` with mod id `bannermod`. Leave clone `Main`/`WorkersMain` as deprecated forwarders so later waves can keep referring to them transiently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@MERGE_NOTES.md
@recruits/src/main/java/com/talhanation/recruits/Main.java
@workers/src/main/java/com/talhanation/workers/WorkersMain.java
@workers/src/main/java/com/talhanation/workers/WorkersRuntime.java
@workers/src/main/java/com/talhanation/workers/WorkersSubsystem.java
@build.gradle
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge recruits.Main + workers.WorkersMain into BannerModMain; extract shared channel + registry holders</name>
  <read_first>
    - recruits/src/main/java/com/talhanation/recruits/Main.java (MOD_ID="recruits", registers ~100 recruit messages on its own SimpleChannel, wires lifecycle listeners and deferred registers)
    - workers/src/main/java/com/talhanation/workers/WorkersMain.java (MOD_ID="workers", registers ~33 worker messages on its own SimpleChannel)
    - workers/src/main/java/com/talhanation/workers/WorkersRuntime.java, WorkersSubsystem.java, MergedRuntimeCleanupPolicy.java (Phase-20 runtime-unification glue)
  </read_first>
  <action>
    1. Create `src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java` annotated `@Mod("bannermod")`. Constructor wires Forge `IEventBus` to lifecycle methods that union the bodies of `recruits.Main.setup()` and `workers.WorkersMain.setup()`. Static fields: `MOD_ID = "bannermod"`, `SIMPLE_CHANNEL` (single), `workersSubsystem`, `LOGGER`, plus the compat booleans (`isMusketModLoaded`, `isSmallShipsLoaded`, etc.). Client setup merges both `clientSetup()` bodies. Creative-tab hook unions both `addCreativeTabs()` bodies.
    2. Create `src/main/java/com/talhanation/bannermod/bootstrap/BannerModLifecycle.java` hosting `@SubscribeEvent` lifecycle methods extracted from both legacy `Main`s (`onRegisterCommands` etc.).
    3. Create `src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java` with `public static SimpleChannel createSharedChannel()` and `public static int workerPacketOffset()`. `createSharedChannel` registers recruit packets at indices `[0..N)` and worker packets at `[N..N+M)`. Preserves order from the two legacy `messages[]` arrays verbatim.
    4. Copy the Phase-20 runtime glue to outer repo with new package: `workers.WorkersRuntime` → `bannermod.bootstrap.WorkersRuntime`, `workers.WorkersSubsystem` → `bannermod.bootstrap.WorkersSubsystem`, `workers.MergedRuntimeCleanupPolicy` → `bannermod.bootstrap.MergedRuntimeCleanupPolicy`. Rewrite `package` and internal same-repo imports via `sed`.
    5. Copy registry holders: `recruits/init/Mod{Blocks,Items,EntityTypes,Pois,Professions,Screens,Shortcuts,Sounds}.java` → `src/main/java/com/talhanation/bannermod/registry/military/*.java`; `workers/init/Mod{Blocks,Items,EntityTypes,MenuTypes,Pois,Professions,Shortcuts,Sounds}.java` → `src/main/java/com/talhanation/bannermod/registry/civilian/*.java`. In each copied file: rewrite `package` and rewrite internal `import com.talhanation.{recruits|workers}.init.` self-references; preserve DeferredRegister `MOD_ID` references as `BannerModMain.MOD_ID`. The type imports for entities/blocks/items remain pointing at `com.talhanation.{recruits,workers}.*` for now — those get fixed as later waves move the referenced types.
    6. Rewrite `BannerModMain`'s constructor to call `BannerModNetworkBootstrap.createSharedChannel()` and to register the DeferredRegisters from `bannermod.registry.{military,civilian}.*` (not from the clones).
    7. Edit `src/main/resources/META-INF/mods.toml`: the `[[mods]] modId` entry becomes `modId="bannermod"` if not already. Remove any lingering `"recruits"` or `"workers"` mod-id entries (they become sub-components of bannermod). Retain dependency entries for corelib/forge.
    8. Commit in outer repo with scope `21-03`.
  </action>
  <verify>
    <automated>bash -c "
set -e
test -f src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
grep -q '@Mod(\"bannermod\")' src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
test -f src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
grep -q 'workerPacketOffset' src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
test -d src/main/java/com/talhanation/bannermod/registry/military
test -d src/main/java/com/talhanation/bannermod/registry/civilian
[ \"\$(grep -rln '@Mod(' src/main/java/ | wc -l)\" = '1' ]
"</automated>
  </verify>
  <acceptance_criteria>
    - `BannerModMain` exists with `@Mod("bannermod")` and unified setup body.
    - `BannerModNetworkBootstrap` exposes `createSharedChannel()` and `workerPacketOffset()`.
    - Registry holders exist under `bannermod.registry.{military,civilian}/` with rewritten package statements.
    - Exactly one `@Mod(` in outer `src/main/java/`.
    - No `@Mod(` reference surviving in the clones except on legacy `Main`/`WorkersMain` (removed in Task 2).
  </acceptance_criteria>
  <done>Outer repo owns the runtime composition root; clones still hold legacy `Main`s unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Demote clone entrypoints to @Deprecated forwarders; delete migrated init/ from clones</name>
  <read_first>
    - Task 1 outputs under `src/main/java/com/talhanation/bannermod/{bootstrap,network,registry}/`
  </read_first>
  <action>
    1. Edit `recruits/src/main/java/com/talhanation/recruits/Main.java`: remove `@Mod`; add `@Deprecated`; preserve `MOD_ID = "recruits"` as a compat constant referencing `BannerModMain.MOD_ID`; convert `SIMPLE_CHANNEL` to a static field initialized from `BannerModMain.SIMPLE_CHANNEL`. Remove the entire `setup()`/`clientSetup()` bodies — they're in `BannerModMain` now. Keep only the static forwarder fields and empty constructor. Commit in the clone's git.
    2. Edit `workers/src/main/java/com/talhanation/workers/WorkersMain.java`: same treatment. Remove `@Mod`, add `@Deprecated`, shrink to forwarder statics onto `BannerModMain`. Commit in the clone's git.
    3. In each clone, `git rm` the migrated `init/Mod*.java` files (all 8 in recruits/init, all 8 in workers/init). Commit in clone's git.
    4. In the outer repo, run `sed -i` across all tracked `.java` under `src/main/java/com/talhanation/bannermod/` to rewrite any surviving FQN references `com.talhanation.recruits.init.` → `com.talhanation.bannermod.registry.military.` and `com.talhanation.workers.init.` → `com.talhanation.bannermod.registry.civilian.`. Commit in outer repo.
    5. Append an entry to `MERGE_NOTES.md` under the Phase 21 Pivot section: "Wave 3 — composition-root migration", listing what moved, what stayed as forwarders, and the `sed` invocations used.
  </action>
  <verify>
    <automated>bash -c "
set -e
! grep -q '@Mod(' recruits/src/main/java/com/talhanation/recruits/Main.java
! grep -q '@Mod(' workers/src/main/java/com/talhanation/workers/WorkersMain.java
grep -q '@Deprecated' recruits/src/main/java/com/talhanation/recruits/Main.java
grep -q '@Deprecated' workers/src/main/java/com/talhanation/workers/WorkersMain.java
! ls recruits/src/main/java/com/talhanation/recruits/init/Mod*.java 2>/dev/null
! ls workers/src/main/java/com/talhanation/workers/init/Mod*.java 2>/dev/null
! grep -rn 'com\\.talhanation\\.recruits\\.init\\.' src/main/java/com/talhanation/bannermod/
! grep -rn 'com\\.talhanation\\.workers\\.init\\.' src/main/java/com/talhanation/bannermod/
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Clone `Main`/`WorkersMain` files are `@Deprecated` forwarders without `@Mod`.
    - Clone `init/Mod*` files are deleted from disk and git-removed inside the clones.
    - No FQN references to `com.talhanation.{recruits,workers}.init.*` remain inside outer `bannermod/`.
    - `MERGE_NOTES.md` records the Wave-3 migration.
  </acceptance_criteria>
  <done>Composition-root migration is complete. Waves 4–9 can proceed against the new canonical bootstrap/network/registry.</done>
</task>

</tasks>

<verification>
- Outer `bannermod.bootstrap.BannerModMain` is the single `@Mod` entrypoint.
- Outer `bannermod.network.BannerModNetworkBootstrap` owns the shared `SimpleChannel` + offset contract.
- Outer `bannermod.registry.{military,civilian}.*` holds every former `init/Mod*` registry holder.
- Clone `Main`/`WorkersMain` are deprecated forwarders; clone `init/` is empty.
- Compile state: outer repo will still have unresolved imports (entity/message types still in clones) — deferred to later waves per D-22.
</verification>

<success_criteria>
- Bannermod-owned runtime composition (bootstrap/network/registry).
- Unified `@Mod` entrypoint.
- Waves 4–9 have a stable target namespace to import from.
</success_criteria>

<output>
Create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-03-SUMMARY.md`
</output>
