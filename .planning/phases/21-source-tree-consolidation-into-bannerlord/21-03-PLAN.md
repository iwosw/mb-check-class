---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/main/java/com/talhanation/bannerlord/entity/military/**
  - src/main/java/com/talhanation/bannerlord/entity/shared/**
  - src/main/java/com/talhanation/bannerlord/ai/pathfinding/**
  - src/main/java/com/talhanation/bannerlord/ai/military/**
  - src/main/java/com/talhanation/bannerlord/persistence/military/**
  - src/main/java/com/talhanation/bannerlord/client/shared/**
  - src/main/java/com/talhanation/bannerlord/client/military/**
  - recruits/src/main/java/com/talhanation/recruits/**
autonomous: true
requirements: []
must_haves:
  truths:
    - "Wave 3 exists to move the controlling seams that worker code already imports today: entity bases, navigation, military AI, persistence vocabulary, and shared client widgets."
    - "This plan must preserve the Phase 11-19 performance and correctness seams rather than folding optimization rewrites into the package move."
    - "Worker code may temporarily import relocated `bannerlord` military/shared classes, but this plan must not move worker civilian packages ahead of those foundations."
  artifacts:
    - path: ".planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md"
      provides: "The explicit blocker list showing worker dependency on recruit entity/pathfinding/client bases"
      contains: "Worker entities cannot move cleanly before shared entity base"
    - path: ".planning/phases/19-large-battle-performance-validation/19-VALIDATION.md"
      provides: "The retained performance-validation baseline that must remain truthful through the move"
      contains: "verifyGameTestStage|GlobalPathfindingControllerTest"
---

<objective>
Move the recruit-owned controlling systems into their `bannerlord` package destinations before worker civilian code follows them.

Purpose: this plan re-homes the base classes and systems that currently make workers recruit-dependent, which is the prerequisite for the civilian move in wave 4.
Output: military/shared entity, pathfinding, AI, persistence, and client foundations live under `bannerlord` ownership with behavior preserved.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/VERIFICATION.md
@.planning/phases/19-large-battle-performance-validation/19-VALIDATION.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@recruits/src/main/java/com/talhanation/recruits/entities/**
@recruits/src/main/java/com/talhanation/recruits/pathfinding/**
@recruits/src/main/java/com/talhanation/recruits/client/**
@recruits/src/main/java/com/talhanation/recruits/world/**
@workers/src/main/java/com/talhanation/workers/entities/**
@workers/src/main/java/com/talhanation/workers/client/**
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-home recruit-controlled foundations into `bannerlord` military/shared packages</name>
  <files>src/main/java/com/talhanation/bannerlord/entity/**, src/main/java/com/talhanation/bannerlord/ai/**, src/main/java/com/talhanation/bannerlord/persistence/**, src/main/java/com/talhanation/bannerlord/client/**</files>
  <action>Move recruit entity bases and military entities, pathfinding, military AI, recruit persistence/save vocabulary, and the recruit client/widget surfaces that worker UI depends on. Update imports across the repository, but keep the move focused on physical ownership, not behavioral rewrites. Preserve profiling seams, GameTest assumptions, and serialization contracts.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
targets = [
    'src/main/java/com/talhanation/bannerlord/entity',
    'src/main/java/com/talhanation/bannerlord/ai',
    'src/main/java/com/talhanation/bannerlord/persistence',
    'src/main/java/com/talhanation/bannerlord/client'
]
missing = [target for target in targets if not Path(target).exists()]
if missing:
    raise SystemExit('Missing required wave-3 bannerlord targets: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The controlling military/shared foundations no longer live only in the legacy recruit source tree.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run the correctness and performance-sensitive validation surfaces that this move can disturb</name>
  <files>.planning/VERIFICATION.md</files>
  <action>Keep `VERIFICATION.md` truthful about the minimum Phase 21 validation for wave 3. At minimum, preserve root compile/test validation; if pathfinding, persistence, or client ownership changes widen risk enough, add the relevant GameTest or targeted unit-test expectations explicitly.</action>
  <verify>
    <automated>python - <<'PY'
from pathlib import Path
text = Path('.planning/phases/21-source-tree-consolidation-into-bannerlord/21-03-PLAN.md').read_text()
required = ['performance', 'pathfinding', 'serialization contracts']
missing = [item for item in required if item not in text]
if missing:
    raise SystemExit('Missing expected wave-3 safeguards: ' + ', '.join(missing))
PY</automated>
  </verify>
  <done>The active verification baseline still matches the risk profile after the controlling-systems move.</done>
</task>

</tasks>

<verification>
- Worker imports can now target relocated `bannerlord` military/shared foundations.
- The package move preserves retained pathfinding, AI, client, and persistence behavior.
- Validation still covers the performance-sensitive seams already established in earlier phases.
</verification>

<success_criteria>
- Wave 4 no longer needs to drag recruit-owned foundations along with worker packages.
- The legacy recruit tree is no longer the only physical owner of the systems that govern the merged runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-03-SUMMARY.md`
</output>
