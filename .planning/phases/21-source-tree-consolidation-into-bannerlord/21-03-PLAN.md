---
phase: 21-source-tree-consolidation-into-bannerlord
plan: 03
type: execute
wave: 3
depends_on:
  - 21-02
files_modified:
  - src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
  - src/main/java/com/talhanation/bannermod/bootstrap/BannerModLifecycleRegistrar.java
  - src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java
  - src/main/java/com/talhanation/bannermod/registry/**
  - src/main/java/com/talhanation/recruits/Main.java
  - src/main/java/com/talhanation/recruits/init/**
  - src/main/java/com/talhanation/workers/network/**
  - src/main/java/com/talhanation/workers/init/**
  - src/main/resources/META-INF/mods.toml
  - src/main/resources/**/*.mixins.json
  - build.gradle
autonomous: true
requirements:
  - SRCMOVE-02
must_haves:
  truths:
    - "There is exactly one active `@Mod` entrypoint, and it lives at `com.talhanation.bannermod.bootstrap.BannerModMain` with mod id `bannermod`."
    - "The shared `SimpleChannel` is created exactly once, by `com.talhanation.bannermod.network.BannerModNetworkBootstrap`."
    - "Worker packet ordering is preserved: worker packet IDs are assigned at offset = recruit packet catalog size (no gap, no overlap)."
    - "Registry holders, lifecycle listeners, and missing-mapping handlers move into `com.talhanation.bannermod.{bootstrap,registry,network}` packages without changing registration order or registry id strings."
    - "`com.talhanation.recruits.Main` becomes a deprecated compatibility shim (no `@Mod` annotation) that forwards static accessors to `BannerModMain`."
  artifacts:
    - path: "src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java"
      provides: "Canonical @Mod entrypoint and runtime composition root"
      contains: "@Mod(\"bannermod\")"
    - path: "src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java"
      provides: "Canonical SimpleChannel owner with worker offset wiring"
      contains: "SimpleChannel|workerPacketOffset"
    - path: "src/main/java/com/talhanation/recruits/Main.java"
      provides: "Deprecated compatibility shim only"
      contains: "@Deprecated"
  key_links:
    - from: "src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java"
      to: "src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java"
      via: "createSharedChannel() called from setup"
      pattern: "BannerModNetworkBootstrap"
    - from: "src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java"
      to: "src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java"
      via: "workerPacketOffset() consumed by worker packet registrar"
      pattern: "workerPacketOffset"
---

<objective>
Re-home the runtime composition root, lifecycle wiring, shared networking, and registry holders into `com.talhanation.bannermod.{bootstrap,network,registry}` packages without changing the live runtime contract.

Purpose: per CONTEXT D-01/D-04 the convergence namespace is `bannermod`, so the bootstrap that was previously moved to `bannerlord.bootstrap` (and reverted in 21-01) must land at `bannermod.bootstrap` instead. Plans 21-04..21-09 import these new package families.
Output: one `bannermod.bootstrap.BannerModMain` with `@Mod("bannermod")`, one `bannermod.network.BannerModNetworkBootstrap` owning the shared `SimpleChannel`, registry composition under `bannermod.registry`, and `recruits.Main` reduced to a deprecated forwarder shim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@MERGE_NOTES.md
@build.gradle
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-TARGET-ARCHITECTURE.md
@.planning/phases/20-runtime-audit-and-bannerlord-target-architecture/20-OWNERSHIP-MATRIX.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md
@.planning/phases/21-source-tree-consolidation-into-bannerlord/21-02-PLAN.md
@src/main/java/com/talhanation/recruits/Main.java
@src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java
@src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java
@src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java

<interfaces>
From `src/main/java/com/talhanation/recruits/Main.java` (current entrypoint):
```java
@Mod(Main.MOD_ID)
public class Main {
  public static final String MOD_ID = "bannermod";
  public static SimpleChannel SIMPLE_CHANNEL;
  public static WorkersSubsystem workersSubsystem;
  // setup(), client setup, lifecycle hooks
}
```

From `src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java`:
```java
public static final List<MessageType<?>> MESSAGE_TYPES;
public static int messageCount();
public static void registerAll(SimpleChannel channel, int recruitOffset);
```

From `src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java`:
```java
public static void registerCommon(IEventBus bus);
public static void registerRuntimeListeners(IEventBus forgeBus);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical bannermod bootstrap, network, and registry composition</name>
  <precondition>
    FIRST LINE of this task MUST run: `test ! -d src/main/java/com/talhanation/bannerlord || { echo 'ABORT: src/main/java/com/talhanation/bannerlord still exists -- 21-01 revert did not complete'; exit 1; }`. This plan is wave-3 and depends on 21-01 having fully erased the bannerlord package tree. If the directory still exists, 21-01 has not landed cleanly and this plan must not run.
  </precondition>
  <read_first>
    - .planning/phases/21-source-tree-consolidation-into-bannerlord/21-CONTEXT.md (decisions D-01, D-04)
    - src/main/java/com/talhanation/recruits/Main.java (current entrypoint -- preserve every public static field including `MOD_ID`, `SIMPLE_CHANNEL`, `workersSubsystem`, plus all `@SubscribeEvent` lifecycle methods)
    - src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java (preserve `registerRuntimeListeners` signature; preserve `BannerModConfigFiles` config-file resolution)
    - src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java (preserve packet offset contract: workers register at `recruitOffset = recruitMessageCount`)
    - src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java (preserve missing-mapping handler registration)
  </read_first>
  <action>
    Create the following new files; each must use the explicit package declaration shown:

    1. `src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java` -- package `com.talhanation.bannermod.bootstrap`. Annotated `@Mod("bannermod")`. Contains `public static final String MOD_ID = "bannermod";`, `public static SimpleChannel SIMPLE_CHANNEL;`, `public static WorkersSubsystem workersSubsystem;`. Constructor wires the Forge `IEventBus` to lifecycle methods identical to the current `Main`. The `setup(FMLCommonSetupEvent)` method calls `BannerModNetworkBootstrap.createSharedChannel()` and assigns the result to `SIMPLE_CHANNEL`.

    2. `src/main/java/com/talhanation/bannermod/bootstrap/BannerModLifecycleRegistrar.java` -- package `com.talhanation.bannermod.bootstrap`. Identical body to the current `recruits.init.ModLifecycleRegistrar` class but with the new package. Imports must use `com.talhanation.bannermod.config.BannerModConfigFiles` (already canonical bannermod ownership).

    3. `src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java` -- package `com.talhanation.bannermod.network`. Provides `public static SimpleChannel createSharedChannel()`, `public static int workerPacketOffset()`, and the recruit packet registration list. Method `workerPacketOffset()` returns the recruit packet catalog size (preserving the existing offset contract). Calls `WorkersNetworkRegistrar.registerAll(channel, workerPacketOffset())` to maintain worker packet ordering.

    4. `src/main/java/com/talhanation/bannermod/registry/BannerModRegistries.java` (and additional registry holder files as needed -- enumerate by reading existing `recruits.init.*` registry holders). Each preserves the existing registry id strings and registration order. Use the existing `com.talhanation.bannermod` registry namespace (mod id stays `bannermod`).

    Do NOT delete the legacy `recruits.Main` or `recruits.init.*` files in this task -- Task 2 demotes them to shims.
  </action>
  <verify>
    <automated>bash -c "test -f src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java && grep -q '@Mod(\"bannermod\")' src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java && grep -q 'package com.talhanation.bannermod.bootstrap;' src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java && test -f src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java && grep -q 'workerPacketOffset' src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java && test -d src/main/java/com/talhanation/bannermod/registry"</automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java` exists with `@Mod("bannermod")` and package `com.talhanation.bannermod.bootstrap`.
    - `src/main/java/com/talhanation/bannermod/network/BannerModNetworkBootstrap.java` exists, exports `createSharedChannel()`, and references `workerPacketOffset`.
    - `src/main/java/com/talhanation/bannermod/registry/` directory exists with at least one registry holder file using `package com.talhanation.bannermod.registry;`.
    - The new `BannerModMain` constructor calls into `BannerModLifecycleRegistrar.registerRuntimeListeners(...)` (string match `BannerModLifecycleRegistrar`).
  </acceptance_criteria>
  <done>The canonical bannermod bootstrap/network/registry families exist and own runtime composition.</done>
</task>

<task type="auto">
  <name>Task 2: Demote recruits.Main and recruits.init/workers.init/workers.network legacy entries to deprecated shims</name>
  <read_first>
    - src/main/java/com/talhanation/recruits/Main.java (current entrypoint)
    - src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java
    - src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java (must KEEP `WorkersLegacyMappings` registration -- it is part of the narrow allowed compat per CONTEXT)
  </read_first>
  <action>
    (A) Edit `src/main/java/com/talhanation/recruits/Main.java`: REMOVE the `@Mod` annotation. Mark the class `@Deprecated`. Replace static field declarations with delegating accessors that read from `com.talhanation.bannermod.bootstrap.BannerModMain` (e.g., `public static SimpleChannel SIMPLE_CHANNEL = BannerModMain.SIMPLE_CHANNEL;` becomes a getter or static initializer that mirrors the bannermod field). Per CONTEXT D-21 (carried forward as Phase 20 decision) `recruits.Main` stays as a compatibility shim only.

    (B) Edit `src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java`: mark `@Deprecated`, body becomes a forwarder calling `com.talhanation.bannermod.bootstrap.BannerModLifecycleRegistrar.registerRuntimeListeners(bus)` etc.

    (C) Edit `src/main/java/com/talhanation/workers/network/WorkersNetworkRegistrar.java` and `src/main/java/com/talhanation/workers/init/WorkersLifecycleRegistrar.java`: KEEP these in place (they are the narrow worker compatibility seam per CONTEXT). Update their internal imports/calls to use `com.talhanation.bannermod.network.BannerModNetworkBootstrap` instead of `recruits.Main.SIMPLE_CHANNEL`. Preserve the worker packet offset call site.

    (D) **Retarget resource-level mod-loading references to the new `@Mod` owner.** This is a hard retarget, not a soft "verify".
      1. Grep `src/main/resources/META-INF/mods.toml` and all `src/main/resources/**/*.mixins.json` files for any class reference that still points at the retired namespace or at the old `recruits.Main` entrypoint:
         - `grep -rn 'com\.talhanation\.recruits\.Main' src/main/resources` — any hit must be rewritten to `com.talhanation.bannermod.bootstrap.BannerModMain`.
         - `grep -rn 'com\.talhanation\.bannerlord' src/main/resources` — any hit must be rewritten to the corresponding `com.talhanation.bannermod.*` package (specifically, any `com.talhanation.bannerlord.bootstrap.*` becomes `com.talhanation.bannermod.bootstrap.*`; any `com.talhanation.bannerlord.network.*` becomes `com.talhanation.bannermod.network.*`; etc.).
      2. For `mods.toml` specifically, verify (and add if absent) that the `[[mods]] modId="bannermod"` entry is consistent with the new entrypoint, and that any `modLoader`/`entrypoint`/`mainClass` key (if present in this project's mods.toml) points at `com.talhanation.bannermod.bootstrap.BannerModMain`.
      3. For every Mixin config `*.mixins.json`, rewrite `package`, `mixins[]`, `client[]`, and `server[]` entries so that any class under a retired `com.talhanation.bannerlord.*` or deprecated `com.talhanation.recruits.Main`-adjacent path instead references the `com.talhanation.bannermod.*` equivalent. Do NOT widen or narrow the mixin surface — only rewrite the namespace.
      4. `build.gradle` itself typically does not need to change (Forge picks up `@Mod` automatically from annotation processing), but IF `build.gradle` contains any explicit reference to `com.talhanation.recruits.Main` or `com.talhanation.bannerlord.*` for run configs, IDE run tasks, or Mixin service discovery, rewrite those to `com.talhanation.bannermod.bootstrap.BannerModMain` / `com.talhanation.bannermod.*`.
  </action>
  <verify>
    <automated>bash -c "
set -e
grep -L '@Mod' src/main/java/com/talhanation/recruits/Main.java | grep -q .
grep -q '@Deprecated' src/main/java/com/talhanation/recruits/Main.java
grep -q '@Deprecated' src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java
[ \"\$(grep -rln '@Mod(' src/main/java/ | wc -l)\" = '1' ]
grep -q '@Mod' src/main/java/com/talhanation/bannermod/bootstrap/BannerModMain.java
# Resource-level retargets (sub-step D):
! grep -rn 'com\\.talhanation\\.bannerlord' src/main/resources
! grep -rn 'com\\.talhanation\\.recruits\\.Main' src/main/resources
# build.gradle is clean of retired namespaces:
! grep -n 'com\\.talhanation\\.bannerlord' build.gradle
! grep -n 'com\\.talhanation\\.recruits\\.Main' build.gradle
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `src/main/java/com/talhanation/recruits/Main.java` does NOT contain `@Mod(`.
    - `src/main/java/com/talhanation/recruits/Main.java` contains `@Deprecated`.
    - `src/main/java/com/talhanation/recruits/init/ModLifecycleRegistrar.java` contains `@Deprecated`.
    - Exactly ONE file under `src/main/java/` contains `@Mod(`, and that file is `BannerModMain.java`.
    - `WorkersNetworkRegistrar.java` and `WorkersLifecycleRegistrar.java` still exist (narrow compat seam preserved) and reference `com.talhanation.bannermod.network.BannerModNetworkBootstrap`.
    - `grep -rn 'com\.talhanation\.bannerlord' src/main/resources` returns 0 hits (retired namespace fully scrubbed from resources).
    - `grep -rn 'com\.talhanation\.recruits\.Main' src/main/resources` returns 0 hits (old entrypoint not referenced from `mods.toml` or any `*.mixins.json`).
    - Any class-reference in `src/main/resources/META-INF/mods.toml` or `src/main/resources/**/*.mixins.json` pointing at a mod entrypoint resolves to `com.talhanation.bannermod.bootstrap.BannerModMain` (verified: `grep -rn 'com\.talhanation\.bannermod\.bootstrap\.BannerModMain' src/main/resources` returns >= 1 hit if the project's mods.toml or any mixins.json names an entrypoint explicitly, otherwise 0 is acceptable — the annotation-only path is also valid).
    - `build.gradle` does not reference `com.talhanation.bannerlord` or `com.talhanation.recruits.Main`.
  </acceptance_criteria>
  <done>`recruits.Main` and `recruits.init.*` are deprecated forwarders. The single live `@Mod` entrypoint is `BannerModMain`. Worker packet offset wiring still works through the narrow compat layer.</done>
</task>

</tasks>

<verification>
- Exactly one `@Mod` annotation under `src/main/java/`, on `BannerModMain`.
- Shared `SimpleChannel` is created exactly once by `BannerModNetworkBootstrap.createSharedChannel()`.
- Worker packet offset wiring still resolves through `BannerModNetworkBootstrap.workerPacketOffset()`.
- `recruits.Main`, `recruits.init.ModLifecycleRegistrar` are deprecated compat shims.
</verification>

<success_criteria>
- One bannermod-owned bootstrap/network/registry composition.
- Live mod id, packet ordering, registry id strings unchanged.
- Plans 21-04..21-09 can target bannermod-owned bootstrap without recreating it.
</success_criteria>

<output>
After completion, create `.planning/phases/21-source-tree-consolidation-into-bannerlord/21-source-tree-consolidation-into-bannerlord-03-SUMMARY.md`
</output>
