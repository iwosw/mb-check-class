---
phase: 30-worker-birth-and-claim-based-settlement-spawn
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - workers/src/main/java/com/talhanation/workers/VillagerEvents.java
  - workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java
  - workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawner.java
  - workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java
autonomous: true
requirements:
  - WBSP-01
  - WBSP-02
must_haves:
  truths:
    - "Eligible villager birth or maturation inside a friendly settlement claim can create a worker through one server-side spawn path."
    - "Autonomous settlement worker spawning occurs only for friendly claims and respects quota/cooldown limits."
    - "Settlement-born workers inherit claim/faction authority defaults instead of spawning as ownerless or hostile entities."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawner.java"
      provides: "Runtime entity creation and ownership/team seeding for settlement-born workers"
      exports: ["WorkerSettlementSpawner"]
    - path: "workers/src/main/java/com/talhanation/workers/VillagerEvents.java"
      provides: "Birth and claim-based settlement spawn event wiring"
      contains: "WorkerSettlementSpawn"
    - path: "workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java"
      provides: "Runtime toggles and cadence values consumed by event wiring"
      contains: "Worker settlement"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/VillagerEvents.java"
      to: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java"
      via: "birth/spawn decision evaluation"
      pattern: "WorkerSettlementSpawnRules"
    - from: "workers/src/main/java/com/talhanation/workers/VillagerEvents.java"
      to: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawner.java"
      via: "runtime entity creation"
      pattern: "WorkerSettlementSpawner"
    - from: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawner.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "friendly-claim gate and claim leader seeding"
      pattern: "resolveSettlementStatus|resolveFactionStatus"
---

<objective>
Wire Phase 30 into the live runtime so worker birth and settlement spawning actually happen through claim-aware server hooks.

Purpose: Convert the new rules contract into one runtime path that respects existing claim/faction authority and avoids ad hoc village-only spawning.
Output: Claim-aware villager birth handling, bounded autonomous settlement worker spawning, and one reusable runtime spawner helper.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-worker-birth-and-claim-based-settlement-spawn-01-SUMMARY.md
@.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-binding-contract-02-SUMMARY.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-settlement-faction-enforcement-validation-02-SUMMARY.md
@workers/src/main/java/com/talhanation/workers/VillagerEvents.java
@workers/src/main/java/com/talhanation/workers/entities/FarmerEntity.java
@workers/src/main/java/com/talhanation/workers/entities/MinerEntity.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java
@recruits/src/main/java/com/talhanation/recruits/VillagerEvents.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java`:
```java
public static Binding resolveSettlementStatus(@Nullable RecruitsClaimManager claimManager, BlockPos pos, @Nullable String settlementFactionId)
public static boolean allowsSettlementOperation(Binding binding)
```

From `workers/src/main/java/com/talhanation/workers/entities/FarmerEntity.java` and `MinerEntity.java`:
```java
public SpawnGroupData finalizeSpawn(ServerLevelAccessor world, DifficultyInstance difficultyInstance, MobSpawnType reason,
        @Nullable SpawnGroupData data, @Nullable CompoundTag nbt)

public void initSpawn()
```

From `recruits/src/main/java/com/talhanation/recruits/VillagerEvents.java`:
```java
private static void createRecruit(Villager villager, EntityType<? extends AbstractRecruitEntity> recruitType)
```

Executor note: mirror the recruit-side villager replacement pattern, but keep worker spawning behind the Phase 30 claim-aware rules seam and existing worker `initSpawn()` behavior.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one runtime spawner for settlement-born workers</name>
  <files>workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawner.java, workers/src/main/java/com/talhanation/workers/VillagerEvents.java</files>
  <action>Per WBSP-01, create `WorkerSettlementSpawner` as the only runtime entity-construction seam for Phase 30. It should take the rule decision from plan 30-01, create the chosen worker entity via existing worker registries, call the worker's normal spawn/init path, preserve villager name/inventory when birth conversion is replacing a villager, and seed owner/team defaults from the friendly claim's faction leader so downstream work-area permissions remain compatible with the existing authority model. Reuse `BannerModSettlementBinding` for the friendly-claim gate; do not bypass it with raw scoreboard checks.</action>
  <verify>
    <automated>./gradlew compileJava --console=plain</automated>
  </verify>
  <done>`WorkerSettlementSpawner` is the single place where settlement-born workers are created, claim/faction defaults are assigned, and existing worker initialization is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire villager birth and claim-based settlement spawn into server events</name>
  <files>workers/src/main/java/com/talhanation/workers/VillagerEvents.java, workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java, workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java</files>
  <action>Per WBSP-02, extend `VillagerEvents` so Phase 30 has two bounded runtime flows: (1) villager birth or maturation inside a friendly claim can convert into a worker through `WorkerSettlementSpawner` when the rules seam allows it, and (2) autonomous settlement worker spawning runs only for friendly claims, only on the configured cadence, and only while the per-claim worker cap remains under budget. Use a deterministic profession picker (config-backed whitelist and stable rotation/seed) instead of ad hoc randomness, and keep hostile or unclaimed settlements as hard denials. Avoid a new persistent settlement manager; keep the implementation helper-shaped and claim-derived per the existing Phase 09/10 decisions.</action>
  <verify>
    <automated>./gradlew compileJava test --tests com.talhanation.workers.WorkerSettlementSpawnRulesTest --console=plain</automated>
  </verify>
  <done>Live worker birth and settlement spawning both route through claim-aware rules, respect quota/cooldown settings, and never create workers in hostile or unclaimed territory.</done>
</task>

</tasks>

<verification>
- Run `./gradlew compileJava test --tests com.talhanation.workers.WorkerSettlementSpawnRulesTest --console=plain`.
- Confirm `VillagerEvents` delegates decisions to `WorkerSettlementSpawnRules` and entity creation to `WorkerSettlementSpawner` rather than re-embedding the logic inline.
</verification>

<success_criteria>
- Friendly-claim worker birth and autonomous settlement spawn both exist in runtime code.
- Settlement-born workers inherit stable owner/team defaults derived from the claim's faction leader.
- Hostile/unclaimed chunks and cap/cooldown violations are denied before entity creation.
</success_criteria>

<output>
After completion, create `.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-worker-birth-and-claim-based-settlement-spawn-02-SUMMARY.md`
</output>
