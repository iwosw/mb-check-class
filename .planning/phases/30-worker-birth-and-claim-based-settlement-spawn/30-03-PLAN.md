---
phase: 30-worker-birth-and-claim-based-settlement-spawn
plan: 03
type: execute
wave: 3
depends_on:
  - 30-02
files_modified:
  - src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
  - src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
  - src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java
  - workers/src/main/java/com/talhanation/workers/VillagerEvents.java
autonomous: true
requirements:
  - WBSP-03
must_haves:
  truths:
    - "Automated tests prove friendly-claim worker birth creates a correctly owned settlement worker."
    - "Automated tests prove claim-based settlement spawning is bounded and does not fire in hostile or unclaimed territory."
    - "Phase 30 verification uses the real claim/faction runtime seams instead of mocked settlement logic."
  artifacts:
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java"
      provides: "Root GameTests for worker birth and settlement spawn"
      contains: "@GameTest"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java"
      provides: "Reusable villager/home/meeting-point spawn helpers"
      contains: "spawn"
    - path: "src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java"
      provides: "Reusable claim/faction setup and leader ownership helpers"
      contains: "seedClaim"
  key_links:
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java"
      to: "workers/src/main/java/com/talhanation/workers/VillagerEvents.java"
      via: "live runtime birth/spawn path"
      pattern: "VillagerEvents"
    - from: "src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java"
      to: "src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java"
      via: "asserted settlement status"
      pattern: "FRIENDLY_CLAIM|HOSTILE_CLAIM|UNCLAIMED"
---

<objective>
Prove the new worker birth and claim-based settlement spawn flows through executable root validation.

Purpose: Phase 30 is not complete until the runtime behavior is covered by the same GameTest discipline used for earlier claim/settlement work.
Output: Reusable GameTest helpers and a dedicated root GameTest class for worker birth plus settlement spawn scenarios.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-worker-birth-and-claim-based-settlement-spawn-02-SUMMARY.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-settlement-faction-enforcement-validation-01-SUMMARY.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-settlement-faction-enforcement-validation-02-SUMMARY.md
@src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java
@src/gametest/java/com/talhanation/bannermod/BannerModSettlementFactionEnforcementGameTests.java
@workers/src/main/java/com/talhanation/workers/VillagerEvents.java

<interfaces>
From `src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java`:
```java
public static <T extends Entity> T spawnEntity(GameTestHelper helper, EntityType<T> entityType, BlockPos relativePos)
public static FarmerEntity spawnOwnedFarmer(GameTestHelper helper, Player player, BlockPos relativePos)
```

From `src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java`:
```java
public static Player createFakeServerPlayer(ServerLevel level, UUID playerId, String name)
public static RecruitsClaim seedClaim(ServerLevel level, BlockPos pos, String factionId, UUID leaderId, String leaderName)
public static PlayerTeam joinTeam(ServerLevel level, String teamName, Entity... members)
```

Executor note: add helper methods instead of duplicating setup inside each GameTest; keep validation on the live claim manager and live worker spawn hooks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GameTest support for claim-backed villager birth scenarios</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModGameTestSupport.java, src/gametest/java/com/talhanation/bannermod/BannerModDedicatedServerGameTestSupport.java</files>
  <action>Per WBSP-03 and the established Phase 10 helper pattern, add only the helper seams needed to seed Phase 30 scenarios: fake villagers with stable home/meeting/job-site memories, friendly claim leader ownership/team setup, and a deterministic way to trigger the Phase 30 worker birth or settlement spawn path without waiting on uncontrolled world randomness. Keep the helpers additive and reusable for later settlement-governance/logistics phases; do not build a second settlement test subsystem.</action>
  <verify>
    <automated>./gradlew compileGameTestJava --console=plain</automated>
  </verify>
  <done>GameTest support can now stand up claim-backed villager birth and settlement spawn scenarios through reusable helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add root GameTests for worker birth, bounded claim spawn, and hostile denial</name>
  <files>src/gametest/java/com/talhanation/bannermod/BannerModWorkerBirthAndSettlementSpawnGameTests.java, workers/src/main/java/com/talhanation/workers/VillagerEvents.java</files>
  <action>Implement a dedicated `BannerModWorkerBirthAndSettlementSpawnGameTests` class that proves three live behaviors: (1) a villager birth or maturation event inside a friendly claim produces one worker whose owner/team derive from the claim leader, (2) autonomous settlement spawning can create one bounded worker inside a friendly claim and then stops until the configured cooldown window expires, and (3) hostile or unclaimed settlements never create workers. If the runtime path needs a minimal direct-call seam for deterministic testing, add that seam inside `VillagerEvents` rather than mocking the outcome in the test.</action>
  <verify>
    <automated>./gradlew compileGameTestJava verifyGameTestStage --console=plain</automated>
  </verify>
  <done>Root GameTests cover friendly worker birth, bounded claim-based settlement spawn, and hostile/unclaimed denial through the live runtime seam.</done>
</task>

</tasks>

<verification>
- Run `./gradlew compileGameTestJava verifyGameTestStage --console=plain`.
- Confirm the new GameTest class asserts both settlement-binding status and worker ownership/team outcomes, not just raw entity counts.
</verification>

<success_criteria>
- `BannerModWorkerBirthAndSettlementSpawnGameTests` exists and covers the Phase 30 friendly and denial cases.
- GameTest helpers reuse the existing claim/faction setup path instead of bespoke mocks.
- `verifyGameTestStage` remains green with the new Phase 30 coverage included.
</success_criteria>

<output>
After completion, create `.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-worker-birth-and-claim-based-settlement-spawn-03-SUMMARY.md`
</output>
