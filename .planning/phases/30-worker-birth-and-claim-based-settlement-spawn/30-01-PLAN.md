---
phase: 30-worker-birth-and-claim-based-settlement-spawn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java
  - workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java
  - src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java
autonomous: true
requirements:
  - WBSP-01
must_haves:
  truths:
    - "Settlement worker birth and spawn eligibility can be evaluated from one explicit claim-aware rules seam."
    - "Friendly-claim, hostile-claim, unclaimed, and quota/cooldown outcomes are testable without world-side randomness."
    - "Worker birth/spawn defaults are configurable from the settlement config surface instead of hardcoded inside event handlers."
  artifacts:
    - path: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java"
      provides: "Pure claim-aware birth and settlement spawn decisions"
      exports: ["WorkerSettlementSpawnRules"]
    - path: "workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java"
      provides: "Phase 30 config toggles and spawn/birth defaults"
      contains: "Worker birth and claim settlement spawn settings"
    - path: "src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java"
      provides: "Automated coverage for friendly/hostile/unclaimed/quota decisions"
      contains: "@Test"
  key_links:
    - from: "workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java"
      to: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java"
      via: "default rule inputs"
      pattern: "WorkerSettlementSpawnRules"
    - from: "src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java"
      to: "workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java"
      via: "direct rule evaluation"
      pattern: "WorkerSettlementSpawnRules"
---

<objective>
Define the claim-aware worker birth and settlement-spawn contract before any runtime hooks change.

Purpose: Keep Phase 30 grounded in one testable rules seam instead of scattering claim, quota, and cooldown decisions through `VillagerEvents`.
Output: A pure `WorkerSettlementSpawnRules` helper, explicit settlement config inputs, and focused JUnit coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/CODEBASE.md
@.planning/phases/09-settlement-faction-binding-contract/09-settlement-faction-binding-contract-02-SUMMARY.md
@.planning/phases/10-settlement-faction-enforcement-validation/10-settlement-faction-enforcement-validation-01-SUMMARY.md
@workers/src/main/java/com/talhanation/workers/VillagerEvents.java
@workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java
@src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java

<interfaces>
From `src/main/java/com/talhanation/bannermod/settlement/BannerModSettlementBinding.java`:
```java
public enum Status {
    FRIENDLY_CLAIM,
    HOSTILE_CLAIM,
    UNCLAIMED,
    DEGRADED_MISMATCH
}

public record Binding(Status status, @Nullable String settlementFactionId, @Nullable String claimFactionId) {
    public boolean isFriendly() {
        return status == Status.FRIENDLY_CLAIM;
    }
}
```

From `workers/src/main/java/com/talhanation/workers/VillagerEvents.java`:
```java
public void onServerStarting(ServerStartingEvent event)
public void onPlayerJoinWorld(EntityJoinLevelEvent event)
public void onAnimalJoinWorld(EntityJoinLevelEvent event)
```

Executor note: the new rules seam should stay pure and world-agnostic. Runtime hooks consume it later.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the pure worker settlement spawn rules seam</name>
  <files>workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java, src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java</files>
  <behavior>
    - Test 1: Friendly claim with birth enabled, quota available, and cooldown clear returns an allow decision for worker birth.
    - Test 2: Hostile or unclaimed settlement context returns denial for both worker birth and autonomous settlement spawn.
    - Test 3: Quota exhaustion or active cooldown returns denial without requiring world-side randomness.
  </behavior>
  <action>Per WBSP-01 and the Phase 09/10 settlement-binding pattern, add a pure `WorkerSettlementSpawnRules` helper under `workers/.../settlement/` that accepts explicit inputs (binding status, villager count, current worker count, cooldown state, feature toggle, and allowed profession pool) and returns deterministic birth/spawn decisions. Keep the helper stateless and claim-derived; do not introduce a settlement manager, server-level lookups, or direct config reads inside the rule methods.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.WorkerSettlementSpawnRulesTest --console=plain</automated>
  </verify>
  <done>The new helper exposes deterministic allow/deny outcomes for worker birth and claim-based settlement spawn, and focused tests cover friendly, hostile, unclaimed, cooldown, and quota cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit Phase 30 settlement spawn config inputs</name>
  <files>workers/src/main/java/com/talhanation/workers/config/WorkersServerConfig.java, workers/src/main/java/com/talhanation/workers/settlement/WorkerSettlementSpawnRules.java, src/test/java/com/talhanation/workers/WorkerSettlementSpawnRulesTest.java</files>
  <behavior>
    - Test 1: Default config-derived inputs enable friendly-claim worker birth and bounded settlement spawning.
    - Test 2: Disabling the birth or spawn toggle forces a deny decision even when claim/quota inputs are otherwise valid.
  </behavior>
  <action>Extend `WorkersServerConfig` with the minimum explicit knobs Phase 30 needs: worker-birth enablement, claim-based settlement-spawn enablement, minimum villager count, per-claim worker cap, cooldown/day cadence, and allowed profession pool seed. Thread these values into `WorkerSettlementSpawnRules` through plain parameters or a small immutable config view so tests stay fast and the runtime layer later consumes one stable contract. Avoid hiding new behavior behind unrelated legacy cost settings.</action>
  <verify>
    <automated>./gradlew test --tests com.talhanation.workers.WorkerSettlementSpawnRulesTest --console=plain</automated>
  </verify>
  <done>Phase 30 behavior is backed by explicit settlement config fields, and the rules seam consumes those values without static, untestable branching.</done>
</task>

</tasks>

<verification>
- Re-run `./gradlew test --tests com.talhanation.workers.WorkerSettlementSpawnRulesTest --console=plain` after both tasks.
- Confirm the new helper remains pure: no `ServerLevel`, `EntityJoinLevelEvent`, or `ClaimEvents` imports inside `WorkerSettlementSpawnRules`.
</verification>

<success_criteria>
- `WorkerSettlementSpawnRules` exists and is the only new decision seam for Phase 30 birth/spawn eligibility.
- `WorkersServerConfig` exposes explicit Phase 30 toggles and bounds.
- Targeted JUnit coverage proves friendly allow, hostile/unclaimed denial, and quota/cooldown denial behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/30-worker-birth-and-claim-based-settlement-spawn/30-worker-birth-and-claim-based-settlement-spawn-01-SUMMARY.md`
</output>
